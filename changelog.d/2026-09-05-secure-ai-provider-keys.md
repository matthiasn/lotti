### Security
- **AI provider API keys are now protected by platform secure storage.** Existing
  plaintext keys are migrated on first use, while encrypted sync continues to
  keep provider configurations working across devices.
