### Changed

- **The AI summary card keeps the summary and its proposals; the agent
  plumbing moved behind *Open agent internals*.** *Update now*, the
  next-update schedule, the automatic-updates switch and the model and
  provider the agent runs on are now in the internals panel, which the agent's
  name in the card header opens. On task and project detail the card speaks up
  about freshness only when the summary is out of date, and then it offers
  *Update now* beside the warning.
