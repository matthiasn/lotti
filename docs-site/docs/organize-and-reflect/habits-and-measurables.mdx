---
title: Habits & measurables
description: Track recurring behavior and numeric observations without confusing the two.
---

# Habits & measurables

A habit records whether a recurring behavior succeeded, failed, or should be
skipped. A measurable records a numeric observation. They can support one
another without being the same thing: “inspect habitat seals” is a habit,
while “sardines consumed” and “habitat temperature” are measurable values.

<ManualScreenshot
  caseId="habits/today"
  alt="Penguin Ops habits dashboard showing habitat checks, penguin roll call, sardine inventory, completion state, streaks, and recent history"
/>

The habits page separates what is due now, scheduled for later, and already
completed. Use the filters to focus on one of those groups or return to the full
list. The recent-history grid and streak summaries make patterns visible
without turning a single missed day into the whole story.

## Create a habit

1. Open **Settings → Habits**.
2. Add a habit and give it a specific title.
3. Assign a category when that helps filtering and recognition.
4. Configure its schedule and start date.
5. Save it.

When completing a habit, choose the state that preserves what happened:

- **Success** — you completed the intended behavior.
- **Missed** — the behavior was possible but did not happen.
- **Skip** — it was not applicable or not reasonably possible.

<ManualScreenshot
  caseId="habits/record"
  alt="Habit completion form for inspecting the penguin habitat seals, with Success, Skip, and Missed outcomes plus date and notes"
/>

Do not use success merely to remove an uncomfortable red state; honest history
is more useful than a perfect chart.

## Create a measurable data type

Open **Settings → Measurable data types**, then add a name, optional description,
unit, and aggregation behavior. Aggregation determines whether a dashboard
shows individual values or combines observations over a day.

For example, configure **Sardines consumed** with the unit `fish` and daily-sum
aggregation. Each meal or delivery can add an observation, while the dashboard
combines them into one daily total.
