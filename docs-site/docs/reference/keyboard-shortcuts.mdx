---
title: Work with keyboard shortcuts
description: Find and use Lotti's desktop command bindings.
---

# Work with keyboard shortcuts

Desktop Lotti exposes a searchable catalog of app-wide and contextual
commands. Open **Settings → Keyboard shortcuts** to see each command with the
binding resolved for your platform.

<ManualScreenshot
  caseId="keyboard-shortcuts/catalog"
  alt="Lotti Keyboard shortcuts settings showing desktop commands grouped by purpose with their current key combinations"
  caption="Primary means Command on macOS and Control on Windows or Linux; the catalog renders the correct symbols for the current platform."
/>

## Open shortcut help quickly

Press **F1** or **Primary + ?** to open the shortcut reference without leaving
your current work. **Primary** means Command on macOS and Control on Windows or
Linux.

Frequently useful app-wide commands include:

| Action | Shortcut |
| --- | --- |
| Open the command palette | Primary + K |
| Create a text entry | Primary + N |
| Create a task | Primary + T |
| Open Tasks, Daily OS, Projects, or Habits | Primary + 1 / 2 / 3 / 4 |
| Open Dashboards, Journal, Events, or Settings | Primary + 5 / 6 / 7 / 8 |

Individual pages add contextual commands such as save, refresh, search, or
create. Their availability can depend on what is currently focused or whether
there are unsaved changes.

## Run a command without leaving your work

Press **Primary + K** to open the command palette over the current page. It
combines app-wide actions—creating entries, opening tasks, navigating, and
zooming—with commands supplied by the active page.

<ManualScreenshot
  caseId="keyboard-shortcuts/palette"
  alt="The Lotti command palette open over Keyboard shortcuts, listing available creation, navigation, view, and editing commands"
  caption="Only commands that are available in the current context appear. The Mobile view demonstrates the same desktop palette in a narrow window; keyboard commands are a desktop feature."
/>

Start typing to narrow the list by action name, group, or key combination. Use
the up and down arrows to move the highlight, **Enter** to run it, and
**Escape** to close the palette. Because the palette is contextual, a command
such as **Save** only appears when the focused editor can currently save.

## Search by action or key

<ManualScreenshot
  caseId="keyboard-shortcuts/search"
  alt="Lotti keyboard shortcut catalog filtered with a search query"
  caption="Search the catalog instead of memorizing it. Results update as you type."
/>

Use the search field when you know either the action or part of its key
combination. The in-app catalog is the authority if a binding changes in a
later version of Lotti.
