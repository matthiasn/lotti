diff --git a/build_tool/lib/src/environment.dart b/build_tool/lib/src/environment.dart
index 1111111..2222222 100644
--- a/build_tool/lib/src/environment.dart
+++ b/build_tool/lib/src/environment.dart
@@
-/// Allow downloads unless explicitly disabled.
-bool get allowDownloads => Platform.environment['CARGOKIT_ALLOW_DOWNLOADS'] != 'false';
+/// Default to offline unless explicitly enabled.
+bool get allowDownloads => Platform.environment['CARGOKIT_ALLOW_DOWNLOADS'] == 'true';

diff --git a/build_tool/lib/src/rustup.dart b/build_tool/lib/src/rustup.dart
index 3333333..4444444 100644
--- a/build_tool/lib/src/rustup.dart
+++ b/build_tool/lib/src/rustup.dart
@@
-Future<void> ensureRustup({required bool allowDownloads}) async {
-  final rustup = await _findRustup();
-  if (rustup != null) {
-    return;
-  }
-  if (!allowDownloads) {
-    throw const RustupMissingException();
-  }
-  await _installRustup();
-}
+Future<void> ensureRustup({required bool allowDownloads}) async {
+  final rustup = await _findRustup();
+  if (rustup != null) return;
+  // Flatpak: rely on preinstalled SDK toolchain when downloads are disallowed.
+  if (!allowDownloads) return;
+  await _installRustup();
+}

diff --git a/build_tool/lib/src/artifacts_provider.dart b/build_tool/lib/src/artifacts_provider.dart
index 5555555..6666666 100644
--- a/build_tool/lib/src/artifacts_provider.dart
+++ b/build_tool/lib/src/artifacts_provider.dart
@@
-  Future<void> _getPrecompiledArtifacts() async {
-    final artifacts = await _get(Application.buildTarget.targetTriple);
-    final entries = artifacts.entries.toList();
-    for (final entry in entries) {
-      await _downloadEntry(entry.key, entry.value);
-    }
-  }
+  Future<void> _getPrecompiledArtifacts() async {
+    // Flatpak: skip precompiled downloads when offline.
+    if (!allowDownloads) {
+      _logger.info('Skipping precompiled artifacts (offline)');
+      return;
+    }
+    final artifacts = await _get(Application.buildTarget.targetTriple);
+    final entries = artifacts.entries.toList();
+    for (final entry in entries) {
+      await _downloadEntry(entry.key, entry.value);
+    }
+  }
