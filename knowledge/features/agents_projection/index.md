# Projection kernel

A pure, deterministic fold over an event *set* — proving that projected state is the same regardless of arrival order or branching.

* [Overview](overview.md) - the runtime model.
