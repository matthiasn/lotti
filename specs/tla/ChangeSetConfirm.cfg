\* Two concurrent confirmers, recoverable faults, no crash.
SPECIFICATION Spec
CONSTANTS
    Callers = {c1, c2}
    MaxAttempts = 2
    MaxCrashes = 0
    Faults = {"dispatchFails", "hookThrows"}
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK AtMostOnceApply RejectedMeansNotApplied ConfirmedMeansApplied
