\* Two crashes anywhere, no lease (a device with no sync host fires in its
\* first pass), and one backward clock step inside the day.
SPECIFICATION Spec
CONSTANTS
    MaxCrashes = 2
    MaxSkews = 1
    MaxJobs = 5
    Leased = FALSE
    DigestOwnsRecovery = TRUE
    ProbeSeesLimbo = TRUE
    WindowFromDayStart = TRUE
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK AtMostOneDigest NoInferenceAfterBriefing InferencesBounded
PROPERTIES EventuallyBriefed
