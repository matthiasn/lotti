\* Two devices, two items: every interleaving of local writes, sync in any
\* order, the orchestrator's and the dispatcher's Phase A runs, commits,
\* escalations and the lease-elected report.
SPECIFICATION Spec
CONSTANTS
    N = 2
    K = 2
    Hidden = 0
    Lock = "agent"
    Validate = "rederive"
    Escalate = "contradicted"
    Restart = "recompute"
    ArmAt = "commit"
    OnSynced = "ignore"
    MaxTick = 9
    MaxCrashes = 0
    MaxDeaths = 0
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK Converged Complete ReportCurrent EscalationDurable
