\* Device 2 never sees item 2 (a private entry it hides, or a time zone
\* that moves it to another day): the devices' views never agree, and they
\* must still stop writing.
SPECIFICATION Spec
CONSTANTS
    N = 2
    K = 2
    Hidden = 2
    Lock = "agent"
    Validate = "rederive"
    Escalate = "contradicted"
    Restart = "recompute"
    ArmAt = "commit"
    OnSynced = "ignore"
    MaxTick = 12
    MaxCrashes = 0
    MaxDeaths = 0
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK Bounded
