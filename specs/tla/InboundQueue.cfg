\* One room, three timeline events (one on the server before the first
\* start), a crash or a stop, one fault among a failed live enqueue, an
\* incomplete walk and a worker throw, one retry, two resurrection passes and
\* one gap-recovery walk.
SPECIFICATION Spec
CONSTANTS
    N = 3
    InitTip = 1
    EncInit = {}
    MaxDowns = 1
    FaultBudget = 1
    Faults = {"enqueue", "walk", "worker"}
    MaxRetries = 1
    MaxResurrections = 2
    GapRecovery = TRUE
    ClaimOnStart = TRUE
    ClaimOnGap = TRUE
    ClaimOnWalk = TRUE
    CheckpointForward = TRUE
    FailedEnqueueLowersFloor = TRUE
    WorkerSurvivesErrors = TRUE
    GuardedResurrect = TRUE
    SliceRace = FALSE
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK NoSilentLoss HeldIsLeased
PROPERTIES MarkerMonotone AppliedIsFinal
