\* Baseline: no faults, no crashes, some reservations without a payload id.
\* Every property holds.
SPECIFICATION Spec
CONSTANTS
    Peers = {p1}
    Entities = {e1, e2}
    MaxCounter = 3
    MaxCrashes = 0
    FaultBudget = 0
    Faults = {}
    IntentChoices = {TRUE, FALSE}
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK NoFalseBurn ReceivedIsReal BoundRowsHavePayload
PROPERTIES BurnedIsTerminal EventuallyDelivered NoStuckRequest
