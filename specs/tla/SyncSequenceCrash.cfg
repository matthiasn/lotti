\* One originator crash at any point, every reservation naming its payload.
\* Every property holds: nothing committed is lost, no request stays open.
SPECIFICATION Spec
CONSTANTS
    Peers = {p1}
    Entities = {e1, e2}
    MaxCounter = 3
    MaxCrashes = 1
    FaultBudget = 0
    Faults = {}
    IntentChoices = {TRUE}
    MisnamedReservations = FALSE
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK NoFalseBurn ReceivedIsReal BoundRowsHavePayload
PROPERTIES BurnedIsTerminal EventuallyDelivered NoStuckRequest
