\* Goal specs: two devices, two revisions each, every row delivered in any
\* order.
SPECIFICATION Spec
CONSTANTS
    Kind = "goal"
    EditsPerDevice = 2
    Rollback = FALSE
    SupersedeAll = TRUE
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK Converged HeadResolves SettlesAfterCleanEdit
