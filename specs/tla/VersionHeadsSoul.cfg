\* Soul documents and agent templates: two devices, two edits each, any of
\* them a rollback, every row delivered in any order.
SPECIFICATION Spec
CONSTANTS
    Kind = "soul"
    EditsPerDevice = 2
    Rollback = TRUE
    SupersedeAll = TRUE
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK Converged HeadResolves SettlesAfterCleanEdit
