\* One crash and one abort, at any point. Liveness across a restart.
SPECIFICATION Spec
CONSTANTS
    Agents = {a1, a2}
    MaxTriggers = 4
    MaxCrashes = 1
    MaxAborts = 1
CHECK_DEADLOCK FALSE
INVARIANTS TypeOK SingleFlight HungOnlyWhenDetached
PROPERTIES NoLostWake
