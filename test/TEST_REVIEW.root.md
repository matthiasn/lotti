# Test Review: Root-level test/ files

Scope: Files directly under `test/` that are real test files (not helpers or infrastructure), plus their lib counterparts.

Files reviewed:
- `test/vector_clock_glados_test.dart` (191 lines) — lib: `lib/features/sync/vector_clock.dart` (154 lines)
- `test/vector_clock_test.dart` (65 lines) — same lib file
- `test/get_it_test.dart` (257 lines) — lib: `lib/get_it.dart` (757 lines)

Not in scope here (covered elsewhere or infrastructure): `test/flutter_test_config.dart`, `test/widget_test_utils.dart`, `test/test_helper.dart`.

Generated by automated test-quality review (static analysis).

## File / scope table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/features/sync/vector_clock.dart` | 154 | Yes (both static and Glados) | Merge and serialization contracts are covered |
| `test/vector_clock_glados_test.dart` | 191 | n/a | Glados tags and algebraic merge properties are present |
| `test/vector_clock_test.dart` | 65 | n/a | Core clock behavior has deterministic examples |
| `lib/get_it.dart` | 757 | Partial | `registerSingletons` remains integration-level orchestration |
| `test/get_it_test.dart` | 257 | n/a | Mock setup is proportionate; test quality is good |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/get_it.dart` was 856 lines and mixed registration orchestration, helper utilities, maintenance logic, and scope management. **RESOLVED:** maintenance and helper logic moved to `get_it_maintenance.dart` and `get_it_helpers.dart` part files, obsolete testing shims were removed, and `get_it.dart` is now 757 lines.
- [x] **[LOW]** `test/get_it_test.dart` was 455 lines. **RESOLVED:** it remains the correct mirror for the part-based `get_it.dart` library and is now 257 lines after implementation-coupled shim tests were removed.

---

## Test quality improvements

- [x] **[HIGH]** `test/vector_clock_glados_test.dart` — **all four `Glados2` calls are missing `tags: 'glados'`**. This is required by the project convention and by CI pipeline design (the Glados CI lane is separate). Without the tag these property tests run in the standard `very_good test --exclude-tags glados` shard, slowing fast feedback for everyone. Add `tags: 'glados'` to all four `Glados2(...).test(...)` calls.
  **RESOLVED:** all VectorClock properties are tagged `glados` and run in the dedicated Glados CI lane.
- [x] **[MED]** `test/get_it_test.dart:249-293` — `'marks done and skips when all data sources are empty'` and `'marks done when journal, links, and agent data are empty'` are nearly identical tests (sequential-log-count≥150 vs ==0, but same end-state). Consider parameterizing or merging to avoid duplication.
  **RESOLVED:** merged into one parameterized test (`'marks done and skips when all data sources are empty'`) — the surviving test already covers the populated (150) count; the duplicate empty (0) case added no distinct behavior since the end-state assertion is identical.
- [x] **[LOW]** `test/vector_clock_test.dart:5-7` — node IDs are generated with `Uuid().v1()` at module load time. While not `DateTime.now()`, UUIDs are time-based and non-deterministic. Use fixed string literals instead for fully deterministic tests.
  **RESOLVED:** replaced with `const String nodeId1='node-1'` (etc.); dropped the `uuid` import. VectorClock treats them as opaque keys, so the clock literals are now `const` too.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `VectorClock.merge(vc1, vc2)` — has clear algebraic invariants:
  - Commutativity: `merge(a, b) == merge(b, a)` (uses `max` per node; order-independent).
  - Idempotency: `merge(a, a) == a`.
  - Result dominance: for each node `k`, `result.get(k) >= a.get(k)` and `result.get(k) >= b.get(k)`.
  **RESOLVED:** `vector_clock_glados_test.dart` covers commutativity, idempotency, result dominance, and null operands.
- [x] **[HIGH]** `VectorClock.mergeUniqueClocks(Iterable<VectorClock?>)` — properties:
  - Returns `null` iff all inputs are `null`.
  - Result contains no duplicates (`result.toSet().length == result.length`).
  - Every non-null input element appears in the result.
  **RESOLVED:** static coverage verifies all-null input, de-duplication, null filtering, and preservation of distinct clocks.
- [x] **[MED]** `VectorClock.fromJson` / `toJson` round-trip: `fromJson(vc.toJson()).vclock == vc.vclock`. Straightforward property over `any.vc`.
  **RESOLVED:** added a `Glados<VectorClock>(any.vc3)` property (tagged `glados`) in `vector_clock_glados_test.dart` asserting `fromJson(toJson(vc))` equals `vc` (Equatable) and its `vclock` map deep-equals.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `VectorClock.merge` and `VectorClock.mergeUniqueClocks` are **completely untested** in both `vector_clock_test.dart` and `vector_clock_glados_test.dart`. These are non-trivial merge functions with null-safety logic.
  **RESOLVED:** algebraic Glados properties cover `merge`, while focused examples cover `mergeUniqueClocks` null handling and de-duplication.
- [x] **[MED]** `VectorClock.fromJson` and `toJson` are untested.
  **RESOLVED:** covered by the new round-trip Glados property above (`fromJson(toJson(vc))` over `any.vc3`).
- [x] **[MED]** `lib/get_it.dart:172` — `registerSingletons()` (the main production bootstrap function) has **no direct unit-test coverage**. Testing it fully requires integration infrastructure because it instantiates roughly 30 database and platform services. **ASSESSED:** the former public testing shim duplicated private bootstrap helpers without production callers and was removed; app startup and integration tests remain the appropriate coverage level for this orchestration.
- [x] **[LOW]** The former `safeLogForTesting` shim duplicated a private logging helper solely to expose it to tests. **RESOLVED:** the unused shim and its implementation-coupled tests were removed.

---

## Test execution speed opportunities

- [x] **[LOW]** All tests in `get_it_test.dart` and `vector_clock_test.dart` are synchronous unit tests. No speed issues.
  **RESOLVED (no change):** confirmed — both files are synchronous; no real time/timers introduced by the changes above.
- [x] **[LOW]** `vector_clock_glados_test.dart` runs four Glados2 properties without an `ExploreConfig`. This uses the default `numRuns` (100), which is fine, but once the `tags: 'glados'` fix is applied these will move to the dedicated Glados CI lane as intended.
  **RESOLVED (no change):** the `tags: 'glados'` fix is already applied (HIGH item above), so all properties — including the new round-trip — run in the dedicated Glados lane at the default `numRuns`.

---

## Summary

- No active critical finding remains: VectorClock properties are tagged for the Glados lane, and merge behavior now has algebraic and edge-case coverage.
- `lib/get_it.dart` at 757 lines is the largest file in this review scope; its main `registerSingletons` orchestration remains intentionally integration-level.
- `test/get_it_test.dart` has good quality and structure for what it covers; the primary gap is the untested lib surface area, not weak assertions.
