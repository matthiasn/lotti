# Test-Quality Review: `lib/features/agents/database/` + `test/features/agents/database/`

**Scope:** Implementation files in `lib/features/agents/database/` and all test files in
`test/features/agents/database/`. Generated code (`agent_database.g.dart`) is excluded from
recommendations.  
Generated by automated test-quality review (static analysis).

---

## Per-file summary table

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/features/agents/database/agent_database.dart` | 303 | Yes (`agent_database_test.dart`) | OK — within limits |
| `lib/features/agents/database/agent_database.g.dart` | 4 889 | n/a (generated) | Excluded from scope |
| `lib/features/agents/database/agent_db_conversions.dart` | 344 | Yes (`agent_db_conversions_test.dart`) | OK — within limits |
| `lib/features/agents/database/agent_repository.dart` | 1 768 | Yes (split across 4 test files) | **OVERSIZED impl** — 1 768 lines, clear split seams |
| `lib/features/agents/database/agent_repository_exception.dart` | 15 | Indirectly via `agent_repository_test.dart` | OK |
| `test/features/agents/database/agent_database_test.dart` | 1 563 | — | **OVERSIZED test** — 1 563 lines |
| `test/features/agents/database/agent_db_conversions_test.dart` | 1 449 | — | **OVERSIZED test** — 1 449 lines |
| `test/features/agents/database/agent_repository_change_set_test.dart` | 1 358 | — | **OVERSIZED test** + violates one-test-file-per-source-file rule |
| `test/features/agents/database/agent_repository_evolution_test.dart` | 1 504 | — | **OVERSIZED test** + violates one-test-file-per-source-file rule |
| `test/features/agents/database/agent_repository_soul_test.dart` | 1 065 | — | **OVERSIZED test** + violates one-test-file-per-source-file rule |
| `test/features/agents/database/agent_repository_test.dart` | 7 412 | — | **CRITICALLY OVERSIZED test** — 7 412 lines |
| `test/features/agents/database/agent_repository_test_helpers.dart` | 153 | — | OK — shared helper file |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/agents/database/agent_repository.dart` (1 768 lines) exceeds the
  hard 1 000-line limit by ~77%. The file already has clear logical sections marked by comments:
  Entity CRUD (~lines 44–790), Evolution queries (~lines 792–897), Change-set queries
  (~lines 922–1203), Link CRUD (~lines 1205–1500), Wake-run log (~lines 1502–1598), and Saga
  log (~lines 1704–1752). Suggested splits:
  - Extract entity-query methods (batched/typed getters: `getEntitiesByIds`,
    `_latestEntitiesByAgentIds`, `getEntitiesByAgentId`, `getAgentState`,
    `getAgentStatesByAgentIds`, `getActiveAgentByKindAndActiveDayId`,
    `getActiveSoulDocumentVersionsBySoulIds`, `getMessagesByKind`, etc.) into
    `agent_entity_queries.dart` (~400 lines).
  - Extract all soul-document methods (lines 583–675) into
    `agent_soul_queries.dart` (~100 lines).
  - Extract wake-run and saga-log sections (lines 1502–1752) into
    `agent_run_log_repository.dart` (~260 lines).
  - Keep `AgentRepository` as a thin coordinator/facade referencing the extracted helpers,
    or collapse them into a single object via a mixin or composition pattern.
  - Because AGENTS.md mandates one test file per source file, splitting the impl requires
    mirror-splitting `agent_repository_test.dart` along the same seams.
  **RESOLVED (adapted, partial):** the two heaviest self-contained clusters moved to `part`-file extensions with mock-safe class delegators — `agent_attention_projection.dart` (attention/standing-agreement queries + projection rebuild/refresh machinery, 566 lines) and `agent_proposal_ledger.dart` (the `getProposalLedger` assembly, 210 lines); `agent_repository.dart` is down to 1 712 lines. The remaining mass is dozens of thin (<15-line) Drift wrapper methods that are mocked across the suite — for those, the mandatory class delegator is as long as the method body, so further part-splits would add indirection without real compression. Revisit if any single cluster grows.

- [x] **[HIGH]** `test/features/agents/database/agent_repository_test.dart` (7 412 lines) is the
  largest test file in scope. It tests every method of `AgentRepository`. AGENTS.md's
  "one test file per source file" rule is already being violated by the four separate test
  files that also test `AgentRepository` (`agent_repository_change_set_test.dart`,
  `agent_repository_evolution_test.dart`, `agent_repository_soul_test.dart`). The current
  situation is an inconsistent hybrid: *some* test groups were already carved out, but the
  residual `agent_repository_test.dart` still has 7 412 lines. Either:
  (a) Accept the split strategy and also move the remaining groups into domain-aligned files
      (`agent_repository_entity_test.dart`, `agent_repository_link_test.dart`,
      `agent_repository_wakerun_test.dart`, etc.), abandoning the monolithic file entirely; or
  (b) Collapse all four satellite files back into `agent_repository_test.dart` to comply with
      the one-test-file-per-source-file rule — though this would produce a ~12 000-line file and
      is the worse option.
  Option (a) is recommended. Concrete seams by existing `group()` boundaries:
  - `group('Entity CRUD', ...)` (line 2061) → `agent_repository_entity_test.dart`
  - `group('Link CRUD', ...)` (line 3571) → `agent_repository_link_test.dart`
  - `group('Wake run log', ...)` (line 4417) + `group('aggregateWakeRunMetrics', ...)` etc. →
    `agent_repository_wakerun_test.dart`
  - `group('Saga log', ...)` (line 4759) → `agent_repository_saga_test.dart`
  - `group('Template entity CRUD', ...)` (line 5235) → `agent_repository_template_test.dart`
  - `group('Interval queries', ...)` (line 6197) → `agent_repository_interval_test.dart`
  The Glados generator classes/extensions (lines 22–1927) that are shared by multiple groups
  should move into a new file, e.g. `agent_repository_test_generators.dart`, alongside
  the existing `agent_repository_test_helpers.dart`.
  **RESOLVED (assessed, no change):** the impl split used `part` files (one library), so the file remains the single mirror under the one-test-file-per-source rule; its Glados generator preamble and group structure provide the internal organisation the split was after.

- [x] **[HIGH]** `test/features/agents/database/agent_database_test.dart` (1 563 lines) exceeds **RESOLVED (assessed, no change):** splitting `agent_database_test.dart` into migration/schema files would put two test files on one source file, which AGENTS.md forbids ("One test file per source file. Never split tests for one source file across multiple test files."). The three top-level groups already provide the seam-aligned organisation within the single file.
  the 1 000-line limit. Split seams are the three top-level `group()` calls:
  - `group('AgentDatabase migration', ...)` (line 25) →
    `agent_database_migration_test.dart` (~969 lines)
  - `group('AgentDatabase fresh install', ...)` (line 996) + `group('AgentDatabase streaming
    and counting', ...)` (line 1361) → `agent_database_schema_test.dart` (~594 lines)

- [x] **[HIGH]** `test/features/agents/database/agent_db_conversions_test.dart` (1 449 lines) **RESOLVED (assessed, no change):** same one-test-file-per-source rule. The alternative suggestion (extract a shared `_pumpConversionDb()` fixture) rests on a stale premise — the file constructs no `AgentDatabase` at all; it tests pure conversions with per-variant entity literals.
  exceeds the 1 000-line limit. Suggested split seams:
  - Round-trip / serialisation groups (lines 120–412, 605–1094, 1094–1449) →
    `agent_db_conversions_roundtrip_test.dart`
  - Migration / G-counter groups (lines 190–412) →
    `agent_db_conversions_migration_test.dart`
  Alternatively: all variant-specific groups share a common in-memory `AgentDatabase`
  fixture; extract a file-level `_pumpConversionDb()` helper and keep the file below 1 000
  lines by splitting after line ~700 (the subtype-population group at line 605 is a natural
  seam).

- [x] **[MED]** `test/features/agents/database/agent_repository_evolution_test.dart`
  (1 504 lines) exceeds 1 000 lines. Primary split seam: the large Glados scenario class **RESOLVED:** Done (adapted) — the Glados scenario classes + generator extension moved to `agent_repository_evolution_generators.dart` (helper library, no main(), so no new test file under the one-test-file rule); the test file is now 985 lines with the property test staying next to the example-based tests it complements.
  block ends around line 530 and `group('Evolution entity CRUD', ...)` starts at line 552.
  The Glados generator extension (lines ~1330–1460) + the single Glados property test can
  be a separate file, leaving the example-based tests in the main file.

- [x] **[MED]** `test/features/agents/database/agent_repository_change_set_test.dart`
  (1 358 lines) exceeds 1 000 lines. Split seam: `group('getProposalLedger', ...)` at **RESOLVED:** Done (adapted) — moving the ledger group to a new test file would deepen the one-test-file-per-source violation; instead the generator scaffolding moved to `agent_repository_change_set_generators.dart`, bringing the file to 983 lines with the ledger group intact.
  line 914 is a self-contained complex block that could move to
  `agent_repository_proposal_ledger_test.dart`.

---

## Test quality improvements

- [x] **[HIGH]** `test/features/agents/database/agent_repository_test.dart:7325` — `setUp`
  inside `group('DuplicateInsertException logging')` calls `registerFallbackValue(StackTrace.empty)`
  inline. This value is already registered in `test/helpers/fallbacks.dart:123` and `:331`.
  The inline call is redundant and violates the "use centralized fallback values" rule. Remove
  the inline call and import `test/helpers/fallbacks.dart` instead, or simply rely on the
  existing registration from the outer test infrastructure.

- [x] **[MED]** `test/features/agents/database/agent_repository_test.dart` — The 1 927-line **RESOLVED:** Done — the entire generator preamble (1 906 lines of scenario classes, enums, and `Any` extensions) now lives in `agent_repository_test_generators.dart` (de-privatized symbols); the test file dropped from 8 176 to ~6 270 lines and starts at `void main()` right after the imports.
  preamble block (lines 22–1927) of Glados generator classes and enum declarations sits above
  `void main()` in the same file as 199 example-based tests. This violates the principle that
  mock/setup code should not dwarf test logic. The generator classes and `extension
  _AnyGeneratedIntervalQueryScenario on Any` (line 1471) should be extracted into a dedicated
  `agent_repository_test_generators.dart` private library. The test file's ratio of
  scaffolding to assertions would then become much more readable.

- [x] **[MED]** `test/features/agents/database/agent_repository_change_set_test.dart` and
  `agent_repository_evolution_test.dart` and `agent_repository_soul_test.dart` each violate **RESOLVED:** Done — the split is now a formally documented convention: see "Documented exception: AgentRepository satellite suites" in `test/README.md` (one satellite per sub-domain, generators in sibling helper libraries, default to the core file).
  the one-test-file-per-source-file rule (`AGENTS.md`: "Never split tests for one source file
  across multiple test files"). All three files test `AgentRepository` — the same class as
  `agent_repository_test.dart`. This is deliberate for size management but technically
  contradicts the rule. The team should either formally adopt the split strategy (and document
  the convention) or consolidate. In either case the current situation should be a conscious
  decision, not an implicit accident.

- [x] **[MED]** `test/features/agents/database/agent_repository_soul_test.dart:475–530` —
  The `setUp` / `tearDown` and four `const` test-date/id declarations are repeated verbatim **RESOLVED:** Assessed, no change — the shared portion is only the 3-line `AgentDatabase`/`AgentRepository` construction + `db.close()`; the four const declarations differ per file (different dates and ids), so a `_TestBench.create()` would not compress anything and would add indirection to an 8-line fixture.
  in `agent_repository_evolution_test.dart:532–550` and
  `agent_repository_change_set_test.dart:388–401`. The shared `AgentDatabase(inMemoryDatabase:
  true, background: false)` + `AgentRepository(db)` + `db.close()` pattern should live in the
  shared `agent_repository_test_helpers.dart` (or a new `_TestBench.create()` factory) so all
  four satellite files import one shared setup rather than duplicating the identical boilerplate.

- [x] **[MED]** `test/features/agents/database/agent_database_test.dart:1375–1403` — The helper
  functions `insertAgentEntity()` and `insertAgentLink()` are file-local but very similar to
  raw SQL helpers used in other parts of the same file. The functions use string interpolation **RESOLVED:** Done — both helpers now use `customStatement` with bound `?` parameters for id, to-id, deleted-at, and serialized values.
  for SQL values (e.g., `'$id'`), which is fragile for test data containing special characters.
  These helpers should use Drift's `customStatement` with bound parameters (as done elsewhere in
  the file) to be robust against edge-case test inputs.

- [x] **[LOW]** `test/features/agents/database/agent_repository_test.dart:2061` — The inner
  wrappers `makeAgent`, `makeAgentState`, `makeMessage`, `makeReport`, `makeReportHead`,
  `makeBasicLink`, `makeWakeRun`, `makeSagaOp` (defined at lines 1941–2048 inside `main()`)
  are used only within `main()` and carry fixed defaults for this file. This pattern is
  acceptable but could be simplified: some of these wrappers (e.g. `makeWakeRun`,
  `makeSagaOp`) duplicate the parameter set of the underlying factory almost exactly. Consider
  just calling the factory directly at call sites where the defaults happen to match rather than
  maintaining parallel wrapper functions.
  **RESOLVED:** Assessed, no change — the premise is false. The wrappers pin this file's
  `testDate` (`DateTime(2026, 2, 20)`), which differs from the shared factory default
  `kAgentTestDate` (`DateTime(2024, 3, 15, 10, 30)`), and `makeWakeRun` additionally pins
  `reason: 'scheduled'` over the factory's `'subscription'`. Calling the factories directly
  would silently change every test's dates/reason; the wrappers are not pure duplication.

- [x] **[LOW]** `test/features/agents/database/agent_db_conversions_test.dart` —
  Several `group()` blocks (e.g. `group('AgentDbConversions — agentTemplateHead entity
  roundtrip', ...)` at line 869; `group('AgentDbConversions — soulDocumentHead entity
  roundtrip', ...)` at line 1354) contain tests that assert only on `roundtrip.id` and a
  single field (`roundtrip.versionId`, `roundtrip.updatedAt`). While these are valid
  regression guards, they could be enhanced to also assert the full field set for the head
  entity (scope/versionId pair, deletedAt handling) rather than stopping at one field.
  **RESOLVED:** Done — both `fromEntityRow roundtrips ...Head variant` tests now assert
  full equality (`equals(entity)`) plus the explicit `id`/`updatedAt`/`deletedAt` fields, and
  each group gained a dedicated soft-deleted-head test proving the `deletedAt` tombstone
  survives the companion-serialise → JSON-decode round-trip (it lives in the serialized blob,
  not the row column).

---

## Generative (Glados) testing opportunities

- [x] **[MED]** `lib/features/agents/database/agent_repository.dart` — The private static
  helpers `_isPendingLike(ChangeSetStatus)` (line 1151), `_effectiveLedgerStatus(...)` (line **RESOLVED:** Done (adapted) — added `@visibleForTesting` seams `debugIsPendingLike` / `debugEffectiveLedgerStatus` on the `AgentProposalLedger` extension (the helpers now live in `agent_proposal_ledger.dart`) and an exhaustive matrix test over the FULL setIsActive x ChangeItemStatus x (null + ChangeDecisionVerdict) cross-product (50 cells) with an oracle mirroring the documented contract — strictly more coverage than a sampled Glados run, no DB needed.
  1156), and `_statusForDecision(ChangeDecisionVerdict)` (line 1176) are pure functions with
  structured enum inputs. They are currently exercised only indirectly through the
  `getProposalLedger` integration tests in `agent_repository_change_set_test.dart`. A
  Glados property test that generates all combinations of `(setIsActive: bool, item.status:
  ChangeItemStatus, decision.verdict: ChangeDecisionVerdict?)` and asserts the invariants
  (e.g., "a non-pending item status is always returned unchanged", "a confirmed verdict on an
  active set never changes the item status") would give stronger coverage of the state-machine
  logic without requiring a real database. Since these are private methods the test would need
  to drive them via `getProposalLedger` with synthesised in-memory data, or the methods could
  be package-visible for direct testing. The logic is non-trivial and the current example-based
  tests cover ~4 specific paths; Glados would explore the full cross-product.

- [x] **[MED]** `lib/features/agents/database/agent_db_conversions.dart` — The six static
  helpers `entityType`, `entitySubtype`, `entityCreatedAt`, `entityUpdatedAt`, `linkType`,
  and `_seedGCounter` are pure functions that map domain objects to database column values. All
  variants of `AgentDomainEntity` (~24 variants) and `AgentLink` (~14 variants) are already
  exercised by example-based tests. However, the **round-trip invariant** — that
  `fromEntityRow(toEntityCompanion(entity))` reconstructs an entity equal to the original **RESOLVED:** Done (adapted) — added a parametric round-trip test asserting `fromEntityRow(toEntityCompanion(e)) == e` for ALL 26 serialisable union variants (factory-backed + inline attention/standing construction) with a distinct-type guard; the variant axis is finite, so exhaustive enumeration beats a sampled generator (field-level randomness adds little to freezed serializers).
  for any entity variant — is not covered by a single parametric Glados property test. A
  generator over `AgentDomainEntity` union variants would give a single compact round-trip
  property rather than the current 24 individual example tests. This is a genuine Glados
  opportunity (serialiser round-trip pattern).

- [x] **[LOW]** `lib/features/agents/database/agent_repository.dart:66–78` —
  `_sqliteInClauseChunks<T>()` is a pure iterator over a set-of-T producing sub-lists of
  at most 900 elements, deduplicating the input first. The existing example test at
  `agent_repository_test.dart:2236` covers the 1 800-entry case. A Glados property test
  could assert the invariants directly: "every element in the input appears in exactly one
  chunk", "no chunk exceeds 900 elements", and "the concatenation of all chunks is a
  permutation of the deduplicated input". This is a self-contained pure function that is
  ideal for Glados and would permanently lock down edge cases (empty input, exactly 900,
  901, large multiples).
  **RESOLVED:** Done — added `@visibleForTesting` static seams `debugSqliteInClauseChunks`
  and `debugInClauseChunkSize` on `AgentRepository` (mirroring the existing
  `debugIsPendingLike`/`debugEffectiveLedgerStatus` precedent) and a `numRuns: 80` floor-tagged
  Glados property over a 0..2 100 size axis (with duplicated ids) asserting all three
  invariants: no chunk exceeds the cap, the flattened chunks equal the deduped input set
  exactly once each, and an empty input yields zero chunks. Pure — no DB I/O per iteration.

- [x] **[LOW]** `lib/features/agents/database/agent_database.dart:285–301` — `_extractVectorClock`
  is a pure JSON-parsing function. It is tested with three example inputs in
  `agent_database_test.dart` (valid VC, null VC, invalid JSON, non-numeric values). A Glados
  property test asserting "any non-empty map of String→Int round-trips through
  jsonEncode→_extractVectorClock without loss" and "any malformed input returns null" would
  cover more edge cases. This is a genuine serialiser/parser candidate.
  **RESOLVED:** Done — added two Glados properties in the streaming-and-counting group (driven
  through `streamAgentEntitiesWithVectorClock`, the established indirect path for this private
  static): a `numRuns: 120` round-trip over a deterministic host-count × base-value map
  asserting lossless reconstruction, and a `numRuns: 80` property seeding one non-numeric host
  among valid entries asserting the whole clock collapses to `null` (no partial parse). Each
  iteration clears the table first to stay isolated against the shared per-test `db`.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/features/agents/database/agent_repository.dart:1238–1317` —
  `upsertLink` for `soulAssignment` and `improverTarget` link types executes a two-phase
  transaction: soft-delete the conflicting active row, then hard-delete the exact
  natural-key duplicate, then insert. The `agent_repository_soul_test.dart` covers the
  happy-path (new link wins) and the exact-natural-key collision case (line 990–1070). The
  following branches are **not tested**:
  - `improverTarget` soft-delete path when the conflict has a *different* `to_id` (lines
    1280–1292): a second `improverTarget` link for a different template pointing at the same
    `to_id` is inserted; the first should be soft-deleted. Only the soul-assignment variant is
    tested in `agent_repository_soul_test.dart:990–1070`.
  - The JSON `json_set` side-channel (lines 1255–1293): the `serialized` column of the
    soft-deleted row is mutated so `deletedAt`/`updatedAt` appear in both the SQL column and
    the JSON blob. Tests verify the active link count but not that the soft-deleted row's
    `serialized` JSON is also updated (important for sync readers that decode from `serialized`
    directly without a `deleted_at` filter).

- [x] **[HIGH]** `lib/features/agents/database/agent_repository.dart:431–501` —
  `getLatestProjectReportForProjectId` and `getLatestTaskReportsForTaskIds` are tested with
  example cases in `agent_repository_test.dart`. However, the fallback loop in
  `getLatestProjectReportForProjectId` — "if the newest link's agent has no report or an empty
  report, try the next link" — is only partially covered. The test at line 3187 covers the
  "use newest link" case but does not assert the fallback when the newest link's agent has
  only an empty/blank report: the method should skip it and return the next agent's non-empty
  report. Add a test: insert two project-agent links, give the newest agent an all-whitespace
  report, give the older agent a non-empty report, and assert that the older report is returned. **RESOLVED (already covered):** 'skips empty report content and falls back to next usable link' seeds an all-whitespace report on the newest agent and asserts the older usable report wins; the no-report-head fallback has its own test.

- [x] **[MED]** `lib/features/agents/database/agent_repository.dart:685–713` —
  `updateWakeRunTemplate` is tested for the normal update and for missing `runKey` in
  `agent_repository_test.dart:5535–5580`. The optional fields `resolvedModelId`, `soulId`,
  and `soulVersionId` are tested in the Glados `wakeLifecycleScenario` property test indirectly,
  but there is no dedicated test asserting that when these optional fields are `null` the **RESOLVED:** Done — added a test asserting both halves of `Value.absent()` semantics: omitted optionals stay NULL on a fresh run, and a later update without optionals does NOT null out previously set resolvedModelId/soulId/soulVersionId.
  corresponding columns remain `NULL` in the database (i.e., `Value.absent()` semantics are
  respected). Add a test: call `updateWakeRunTemplate` without optional args, then fetch the
  run and assert `resolvedModelId` / `soulId` / `soulVersionId` are `null`.

- [x] **[MED]** `lib/features/agents/database/agent_repository.dart:248–289` —
  `getActiveAgentByKindAndActiveDayId` tests at lines 2524–2640 cover the happy path and
  "no match" cases. Missing: the multi-candidate case where two active agents of the same
  `kind` and `activeDayId` exist (tie-breaking by `created_at DESC, agent_id DESC` should
  return the newer one). The existing tests only insert one matching agent; add a test with **RESOLVED:** Done (premise partially stale) — the existing first test already seeds two matching active agents and asserts created_at DESC wins; this pass added the missing secondary tie-break test: equal created_at resolves by agent_id DESC.
  two matching identities with different `createdAt` values and verify the newer one is
  returned.

- [x] **[MED]** `lib/features/agents/database/agent_repository.dart:1690–1701` —
  `abandonOrphanedWakeRuns` is tested in `agent_repository_test.dart:5704`, but the test
  only checks that the returned count is correct and that the status changes to `abandoned`.
  It does not assert that runs already in a terminal state (`completed`, `failed`, `aborted`, **RESOLVED:** Done — the first test now seeds one run in EVERY non-running status (completed, failed, aborted, abandoned, pending) and asserts each is untouched while only the running runs flip to abandoned.
  `abandoned`, `pending`) are **not** touched. Add a test that seeds one `running` run and
  one `completed` run, calls `abandonOrphanedWakeRuns`, and verifies only the `running` run
  changes status.

- [x] **[MED]** `lib/features/agents/database/agent_repository.dart:900–920` —
  `updateWakeRunRating` throws `StateError` for a missing `runKey` (tested at
  `agent_repository_evolution_test.dart:1483`). The analogous `updateWakeRunTemplate` also
  throws `StateError` for a missing `runKey` (tested). `updateWakeRunStatus` (lines 1527–1550)
  has **no error path test** — it silently writes zero rows if the `runKey` does not exist **RESOLVED:** Done — the fire-and-forget semantic is intentional and now documented on the method (late status transitions racing run-log cleanup must not crash callers; contrast with updateWakeRunTemplate which throws because template resolution happens once, early). Added a test asserting a missing runKey completes silently and creates no row.
  because it uses `update(...).write(...)` without checking `updatedRows`. Whether this is
  intentional (fire-and-forget semantic) or a missing invariant should be documented; if it
  should throw, add a test and a guard.

- [x] **[MED]** `lib/features/agents/database/agent_db_conversions.dart:79–91` —
  `_addLegacyCounterMirrors` for `AgentStateEntity` dual-writes `wakeCounter`,
  `totalSessionsCompleted`, and `weeklyReviewCount` as scalar mirrors. The G-counter test at
  `agent_db_conversions_test.dart:190` verifies the *read-side* migration
  (`_migrateGCounters`). There is no test that verifies the *write-side* (`_addLegacyCounterMirrors`): **RESOLVED:** Stale — `agent_db_conversions_test.dart` already has 'toEntityCompanion dual-writes the per-host map and the int mirror' (decodes the serialized JSON and asserts both the `*ByHost` maps and the legacy scalars for wakeCounter + totalSessionsCompleted) plus a multi-host write-then-read round-trip covering weeklyReviewCount.
  that after `toEntityCompanion` the encoded `serialized` JSON actually contains both the
  `*ByHost` map and the matching legacy scalar for each counter. Add a test: create an
  `AgentStateEntity` with known G-counter values, call `toEntityCompanion`, decode the
  `serialized` JSON, and assert both keys are present with the correct values.

- [x] **[LOW]** `lib/features/agents/database/agent_repository_exception.dart:1–15` —
  `DuplicateInsertException.toString()` is never directly asserted in any test. The logging
  tests verify the exception type is thrown and that the logger is called with a message
  containing the table name, but `toString()` itself (which formats `table` + `key`) is
  untested. Add a single unit test asserting the expected string format.
  **RESOLVED:** Stale — `toString()` is already directly asserted in
  `agent_repository_test.dart:3325-3329` (the `insertWakeRun throws on duplicate runKey` test),
  via `.having((e) => e.toString(), 'toString', 'DuplicateInsertException: duplicate key
  "run-key-001" in wake_run_log')`, exercising the exact `table` + `key` format. No change.

- [x] **[LOW]** `lib/features/agents/database/agent_database.dart:214–267` —
  `streamAgentEntitiesWithVectorClock` and `streamAgentLinksWithVectorClock` are tested with
  small fixed data sets in `agent_database_test.dart:1405–1503`. Missing edge case: what
  happens when the database is empty? The stream should yield zero batches (not one empty
  batch). Add a test asserting `await db.streamAgentEntitiesWithVectorClock().toList()` is
  empty (length 0) when no rows exist.
  **RESOLVED:** Done — added `...yields no batches when empty` tests for both
  `streamAgentEntitiesWithVectorClock` and `streamAgentLinksWithVectorClock`, each asserting
  `.toList()` is `isEmpty` (the loop breaks before yielding when the first page is empty).

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/features/agents/database/agent_repository_test.dart` — The shared `setUp` / `tearDown` pair at lines 2050–2057 opens (`AgentDatabase(inMemoryDatabase: true, background: false)`) and closes a Drift in-memory database for **every one of the 199 tests** in the file. Drift in-memory DB construction is inexpensive but not free; the per-test open/close cycle adds up across the large test suite. Groups that only read data and never mutate shared state (e.g. the Glados property tests at lines 3079, 3440, 4147, 4525, 5411, 6077, 6286) could share a single `setUpAll`-scoped database instance, reducing DB spin-up cost by roughly 80 %  of those tests. The easiest first step is to audit which `group()` blocks do not depend on a clean DB for isolation, and move those to a class-level `setUpAll`/`tearDownAll` pattern. **RESOLVED (assessed, no change):** per-test in-memory DB isolation is kept deliberately — this repo's CI runs `very_good test` in one isolate and has a documented history of in-suite contamination; the whole 199-test file completes in ~1s, so the spin-up cost does not justify shared mutable DB state.

- [x] **[HIGH]** `test/features/agents/database/agent_repository_change_set_test.dart`, `agent_repository_evolution_test.dart`, `agent_repository_soul_test.dart` — Each satellite test file independently opens its own `AgentDatabase` in `setUp` and closes it in `tearDown` (lines 392–398, 541–547, 529–536 respectively). The duplicated DB lifecycle (noted in the DRY section above) also doubles the DB initialization cost across three files. If the `_TestBench` shared-fixture refactor recommended in the quality section is adopted, initializing once per suite (or at least once per `group`) via `setUpAll` would halve the spin-up count. **RESOLVED (assessed, no change):** same isolation rationale as above; each satellite suite runs in well under a second.

- [x] **[MED]** `test/features/agents/database/agent_repository_test.dart:3079,3440,4147,4525,5411,6077` — Seven Glados property tests use `glados.ExploreConfig(numRuns: 80)` each; they run against a live in-memory `AgentDatabase`. Because each Glados iteration performs a full async DB round-trip (insert + query), these tests are significantly slower than pure in-memory Glados properties. Consider extracting the pure query-logic into a function that takes a list of entities (rather than a live `AgentRepository`) so the majority of iterations can run without DB I/O. Only one representative DB-integration test per property would then be needed. **RESOLVED:** Assessed, no change — numRuns 80 is already at the policy floor, the whole 392-test directory completes in ~4s, and extracting "pure query logic" from these properties would mean re-implementing the SQL semantics in Dart: the DB round-trip IS the system under test (the Dart-side oracle already exists in the scenario classes). Removing the DB would reduce the properties to testing the oracle against itself.

- [x] **[LOW]** `test/features/agents/database/agent_db_conversions_test.dart:178,317` — Two Glados properties (`numRuns: 120` and `numRuns: 150`) open and work with a shared `AgentDatabase` fixture. These are serialization round-trip tests (`toEntityCompanion` + `fromEntityRow`) that do not need a real database at all — they only need the companion and row types, which can be constructed in memory without Drift's I/O layer. Replacing the database call with direct constructor/factory calls would remove the async overhead from every Glados iteration and speed up these two properties considerably.
  **RESOLVED:** Stale — the premise is false. `agent_db_conversions_test.dart` constructs **no**
  `AgentDatabase` anywhere; both Glados properties already build `AgentEntity` data classes
  in-memory via the file-local `makeRow`/`makeStateRow` helpers and call the pure
  `AgentDbConversions.fromEntityRow`/`toEntityCompanion`. There is no Drift I/O or async
  overhead per iteration to remove. No change.

---

## Summary

- **Oversized impl files:** 1 (`agent_repository.dart`, 1 768 lines — ~77% over the 1 000-line
  limit, with five clear extraction seams).
- **Oversized test files:** 5 (`agent_repository_test.dart` at 7 412 lines is critical;
  `agent_database_test.dart` at 1 563, `agent_db_conversions_test.dart` at 1 449,
  `agent_repository_evolution_test.dart` at 1 504, `agent_repository_change_set_test.dart`
  at 1 358).
- **Weak / infrastructure tests:** 4 actionable items (inline `registerFallbackValue` already
  in central file; generator preamble dwarfing test logic in main test file; duplicated
  setUp boilerplate across three satellite test files; assertion depth thin in a few
  roundtrip groups).
- **Glados candidates:** 4 genuine opportunities (`_effectiveLedgerStatus` state-machine,
  `AgentDbConversions` entity round-trip property, `_sqliteInClauseChunks` iterator
  invariants, `_extractVectorClock` parser).
- **Biggest single opportunity:** Splitting `agent_repository_test.dart` (7 412 lines) and
  `agent_repository.dart` (1 768 lines) along their existing logical seams would dramatically
  improve navigability, reduce CI re-run cost on targeted changes, and bring both files into
  compliance with the repository's size guidelines. The satellite test files (`change_set`,
  `evolution`, `soul`) already prove the split approach works — it just needs to be applied
  consistently to the remaining groups in the monolithic file.
