# Test-Quality Review: agents/ui — root-level files; the evolution/ and small subdirs are covered in separate reports

**Scope**: Root-level files only — `lib/features/agents/ui/*.dart` (30 impl) and
`test/features/agents/ui/*.dart` (27 test files). Subdirectories `evolution/`,
`ai_summary_card/`, `instances/`, `listing/`, `pending_wakes/`, `souls/`,
`templates/`, `widgets/` are excluded.

Generated by automated test-quality review (static analysis)

---

## Per-file table

| File | Impl Lines | Test File | Test Lines | Top issue |
|---|---|---|---|---|
| `token_stats_tab.dart` | 1212 | `token_stats_tab_test.dart` | 946 | **OVERSIZED impl** — 14 private classes; split by section |
| `agent_template_detail_page.dart` | 831 | `agent_template_detail_page_test.dart` | 2391 | **Both oversized**; test 2391 lines, 38 repeated `getAgentsForTemplate` stubs |
| `agent_soul_detail_page.dart` | 771 | `agent_soul_detail_page_test.dart` | 1602 | Both oversized; 1 raw `ProviderContainer`+`MaterialApp` bypass |
| `agent_activity_log.dart` | 594 | `agent_activity_log_test.dart` | 1095 | Test oversized; many `pumpAndSettle` chains |
| `sidebar_wake_queue.dart` | 561 | `sidebar_wake_queue_test.dart` | 1185 | Test oversized; 33 `pumpAndSettle` calls |
| `ai_summary_card.dart` | 456 | *(none at root level)* | — | **No root-level test** (part-files tested in subdir) |
| `change_set_summary_card.dart` | 445 | `change_set_summary_card_test.dart` | 488 | OK |
| `agent_settings_page.dart` | 401 | `agent_settings_page_test.dart` | 1313 | Test oversized; 57 `pumpAndSettle` calls |
| `agent_internals_body.dart` | 400 | `agent_internals_body_test.dart` | 263 | OK |
| `agent_token_usage_section.dart` | 350 | `agent_token_usage_section_test.dart` | 392 | OK |
| `wake_activity_chart.dart` | 287 | `wake_activity_chart_test.dart` | 216 | OK |
| `agent_creation_modal.dart` | 281 | `agent_creation_modal_test.dart` | 872 | OK |
| `agent_conversation_log.dart` | 281 | `agent_conversation_log_test.dart` | 432 | OK |
| `agent_internals_panel.dart` | 268 | `agent_internals_panel_test.dart` | 270 | OK |
| `agent_controls.dart` | 239 | `agent_controls_test.dart` | 670 | OK |
| `template_token_usage_section.dart` | 225 | `template_token_usage_section_test.dart` | 434 | OK |
| `profile_selector.dart` | 199 | `profile_selector_test.dart` | 193 | OK |
| `soul_selector.dart` | 180 | `soul_selector_test.dart` | 214 | OK |
| `agent_report_section.dart` | 173 | `agent_report_section_test.dart` | 313 | OK |
| `template_selector.dart` | 172 | `template_selector_test.dart` | 198 | OK |
| `wake_countdown_state.dart` | 141 | `wake_countdown_state_test.dart` | 404 | Good; uses real `Timer.periodic`, checked via `clock` |
| `time_entry_tile.dart` | 139 | `time_entry_tile_test.dart` | 170 | OK |
| `agent_detail_page.dart` | 133 | `agent_detail_page_test.dart` | 766 | OK |
| `ritual_pending_indicator.dart` | 94 | `ritual_pending_indicator_test.dart` | 129 | OK |
| `report_content_parser.dart` | 74 | `report_content_parser_test.dart` | 420 | Has Glados — good |
| `agent_badge_widgets.dart` | 63 | *(none)* | — | **No test file** — pure presentational widget |
| `agent_nav_helpers.dart` | 56 | `agent_nav_helpers_test.dart` | 255 | OK |
| `agent_palette.dart` | 54 | *(none)* | — | **No test file** — pure constants/function |
| `agent_date_format.dart` | 16 | `agent_date_format_test.dart` | 150 | Has Glados — good |
| `agent_instances_list.dart` | 15 | `agent_instances_list_test.dart` | 1083 | Thin shell; test covers `AgentInstancesPage` proxy correctly |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/agents/ui/token_stats_tab.dart:1` — 1212 lines containing 14 **RESOLVED:** split into three part files — `token_stats_tab_daily_section.dart` (daily section + range selector + comparison widgets, 307 ln), `token_stats_tab_chart.dart` (interactive chart + bars + detail + legend, 519 ln), `token_stats_tab_per_model_section.dart` (115 ln); the tab shell keeps 278 lines. Privates and the single mirror test untouched; all 26 tests pass.
  private classes (`_DailyUsageSection`, `_DayRangeSelector`, `_ComparisonSummary`,
  `_ComparisonMetrics`, `_InteractiveWeeklyChart`, `_DayLabelsRow`, `_DayBar`,
  `_SelectedDayDetail`, `_TokenBreakdownRow`, `_MiniMetric`, `_ChartLegend`,
  `_PerModelChartsSection`, `_ModelChartCard`, `_SourceBreakdownSection`,
  `_SourceListTile`, `_AverageDashedLinePainter`). Natural split seams:
  - `token_stats_tab_daily_section.dart` — lines 67–484 (daily section + day-range
    selector + comparison widgets + interactive chart + day labels + day bar)
  - `token_stats_tab_per_model_section.dart` — lines 891–1003 (per-model section +
    model chart card)
  - `token_stats_tab_source_breakdown_section.dart` — lines 1005–1152 (breakdown
    section + source tile)
  - `token_stats_tab_helpers.dart` — lines 1154–1212 (painters + pure helpers)
  - Mirror-split tests accordingly from `token_stats_tab_test.dart`.

- [x] **[HIGH]** `test/features/agents/ui/agent_template_detail_page_test.dart:1` — 2391 lines,
  49 test cases across 4 groups. The "reseed" tests at lines 1257–1427 manually
  build a `ProviderContainer` + `UncontrolledProviderScope` + raw `MaterialApp`
  instead of using `makeTestableWidgetNoScroll`. Logical split:
  - `agent_template_detail_page_create_test.dart` — create-mode group (~280 lines)
  - `agent_template_detail_page_edit_test.dart` — edit-mode group (~690 lines)
  - `agent_template_detail_page_nav_test.dart` — back-navigation group (~130 lines)
  - `agent_template_detail_page_soul_test.dart` — soul-assignment group (~280 lines)
  Note: AGENTS.md mandates one test file per source file; discuss with team whether
  a logical group-based split or keeping the single file with better helpers is
  preferred.
  **RESOLVED (adapted):** kept the single mirror per the one-test-file-per-source rule; the raw `ProviderContainer` + `UncontrolledProviderScope` + `MaterialApp` harness flagged in the reseed tests has since been replaced with the central `makeTestableWidgetWithContainer` helper, so the better-helpers half is in place.

- [x] **[HIGH]** `lib/features/agents/ui/agent_soul_detail_page.dart:1` — 771 lines with **RESOLVED:** the Info-tab cluster (`_InfoTabContent`, version history + tiles, evolution history, assigned templates) moved to an `agent_soul_detail_info_tab.dart` part file — page now 466 lines; all 38 tests pass.
  complex tabbed form + version history. Same split pattern as the template detail
  page applies (settings-tab widget, info-tab widget, version history section).

- [x] **[MED]** `test/features/agents/ui/agent_soul_detail_page_test.dart:1` — 1602 lines,
  38 test cases. One `ProviderContainer`+`MaterialApp` bypass at line 1547 for the **RESOLVED:** Stale — the reseed scenario already uses the harness helper `makeTestableWidgetWithContainer` (returns both widget and container); no manual ProviderContainer/MaterialApp boilerplate remains.
  "reseed" scenario. The `buildEditSubject` helper is the right abstraction; extract
  a `_buildEditSubjectWithContainer` factory that returns both widget and container to
  remove the manual boilerplate.

- [x] **[MED]** `test/features/agents/ui/agent_settings_page_test.dart:1` — 1313 lines for
  401-line impl. The `buildOverrides` helper at line 87 is good, but most tests
  bottom out in full-page `pumpWidget`+`pumpAndSettle` — a shared `setUp`/ **RESOLVED:** Assessed, no change — a pre-built shared widget cannot work for widget tests: every test parameterizes `buildSubject(...)` differently and `pumpWidget` must run per test; a setUpAll-shared tree would also be in-suite-contamination-prone under the very_good single-isolate runner. `buildSubject` already reduces each call site to one line.
  `setUpAll` with a pre-built widget could shrink this significantly.

- [x] **[MED]** `test/features/agents/ui/sidebar_wake_queue_test.dart:1` — 1185 lines.
  A shared `buildSubject` helper exists but each test still calls `pumpAndSettle`
  2-3 times. Extract `_pumpAndReady(tester, records)` to collapse the repetition. **RESOLVED:** Stale — the file contains ZERO `pumpAndSettle` calls; the prevailing pattern is already bounded `pump() + pump(300ms)` with comments explaining why settling would hang on spinners.

- [x] **[LOW]** `test/features/agents/ui/agent_activity_log_test.dart:1` — 1095 lines for
  594-line impl. The `buildSubject` factory is good; the size is driven by
  comprehensive coverage rather than waste.
  **RESOLVED:** Confirmed non-actionable — the file has 40 `testWidgets` cases and a
  shared `buildSubject` factory (used 21x); the line count tracks coverage breadth,
  not duplication. No change.

---

## Test quality improvements

- [x] **[HIGH]** `test/features/agents/ui/agent_template_detail_page_test.dart:288`
  `getAgentsForTemplate` stub (`when(() => mockTemplateService.getAgentsForTemplate(any())).thenAnswer((_) async => [])`)
  is copy-pasted verbatim in 38 of the 49 test bodies. Extract a
  `_stubGetAgentsEmpty(mockTemplateService)` helper and call it from a shared
  `setUp()` block — or add it as the default `buildEditSubject` behavior — to
  eliminate ~140 lines of identical boilerplate.

- [x] **[HIGH]** `test/features/agents/ui/agent_template_detail_page_test.dart:1309`
  and `:1393` — two "reseed" tests use manually constructed `ProviderContainer` +
  `UncontrolledProviderScope` + raw `MaterialApp` with `localizationsDelegates`,
  bypassing `makeTestableWidgetNoScroll`. This duplicates the framework setup and
  is error-prone. Add a `makeTestableWidgetWithContainer` helper to
  `test/widget_test_utils.dart` that returns both the widget and the container, or
  move the invalidation step into a `ProviderScope`-level helper. Lines 1309–1345
  and 1393–1427 are the main offenders.

- [x] **[HIGH]** `test/features/agents/ui/agent_soul_detail_page_test.dart:1547` — same
  raw `ProviderContainer` + `MaterialApp` bypass for the "reseeds directives" test
  (~50 lines). Same fix as above.

- [x] **[MED]** `test/features/agents/ui/agent_template_detail_page_test.dart:161` —
  `'shows create title and empty form'` test only asserts on the presence of label
  text (`findsOneWidget`). While labels are useful, the test body would be
  meaningless if the form layout broke silently. Pair it with an assertion that **RESOLVED:** Done — the test now asserts `nameField.controller?.text` is empty in addition to the label.
  the name `TextField` is empty (value `''`) so the "empty form" claim is
  actually verified. Line 173-176 has the existing assertions.

- [x] **[MED]** `test/features/agents/ui/agent_template_detail_page_test.dart:862` —
  `'shows loading indicator when template is loading'` asserts
  `find.byType(CircularProgressIndicator)` but no meaningful page state. The
  test is correct but weak — it should also assert that no form content is shown **RESOLVED:** Done — the loading test now also asserts `findsNothing` for TextField and the display-name label while the spinner shows.
  while loading.

- [x] **[MED]** `test/features/agents/ui/agent_settings_page_test.dart` — large mock
  override block in `buildOverrides` (lines 87–155) is repeated as `buildSubject`
  calls across 34 test cases. Tests that don't exercise pending-wakes or
  token-stats providers still instantiate all 10+ overrides. Introduce a **RESOLVED:** Assessed, no change — factually unworkable: the page keeps ALL tab bodies mounted in an IndexedStack (there is a dedicated test for exactly that), so every pump instantiates every tab's providers; a minimal override set would leave unmocked providers erroring rather than saving work.
  `_minimalBuildOverrides()` helper for structural/tab-navigation tests, reserving
  the full override set for the handful of tests that actually check those sections.

- [x] **[MED]** `test/features/agents/ui/token_stats_tab_test.dart:206` — the
  `'shows source breakdown with items'` test asserts `find.text('80 %')` / 
  `find.text('20 %')` but the `Duration(minutes: 15)` for the Summary Agent never **RESOLVED:** Done — the breakdown test now asserts both _formatDuration branches via textContaining: '2h 7m' (hours+minutes) and '15m' (minutes-only).
  has its formatted output (`'15m'`) verified — only the 1h 30m case at line 338
  is checked. Add a test for the minutes-only branch of `_formatDuration` (line
  1208 of the impl) to cover the `hours == 0` path.

- [x] **[LOW]** `test/features/agents/ui/token_stats_tab_test.dart:803` — the
  `'non-today bar with full by-time portion renders rounded top'` test sets
  `tester.view.physicalSize` to 1200×2400 (a very large surface) and asserts that
  tapping a bar shows `'10K'`. This is a good coverage test but the surface-size
  manipulation is fragile; prefer a `Scaffold` with a fixed height parent or a
  `BoxConstraints.tight` wrapper for chart tests that require reliable bar
  hit-testing.
  **RESOLVED:** Done — replaced the global `tester.view.physicalSize`/`devicePixelRatio`
  mutation (and its `addTearDown(tester.view.reset)`) with a scoped
  `MediaQueryData(size: Size(1200, 2400))` passed through a new `mediaQueryData`
  parameter on `_buildSubject` (file-level `_largeChartMediaQueryData` const). Same
  logical surface, but tree-scoped so it cannot leak across tests. Applied to all
  three chart hit-testing tests.

- [x] **[LOW]** `test/features/agents/ui/agent_nav_helpers_test.dart` — the
  `agentBackButton` function is mentioned in the impl but has no standalone
  test coverage outside of the integration-style template and soul detail page
  tests. Consider a minimal `testWidgets` confirming the back button widget is
  a `GestureDetector`/`IconButton` and its tap invokes the correct route logic.
  **RESOLVED:** Stale — `agent_nav_helpers_test.dart` already has a dedicated
  `group('agentBackButton', ...)` with three meaningful tests: it asserts the
  localized tooltip + `Icons.chevron_left` at size 30, that an `onPressed` override
  is invoked (and `beamBack` is not), and that the default `onPressed` delegates to
  `navigateBackFromAgent` (verifying `mockNav.beamBack()` is called). No change.

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** `lib/features/agents/ui/report_content_parser.dart` — already has
  excellent Glados coverage in `report_content_parser_test.dart`. No additional
  Glados candidates.
  **RESOLVED:** Confirmed — `report_content_parser_test.dart` has three `Glados(...)`
  property tests (heading/bold/paragraph generators, `numRuns: 160`, `tags: 'glados'`).
  No additional candidates. No change.

- [x] **[LOW]** `lib/features/agents/ui/agent_date_format.dart` — already has
  Glados coverage in `agent_date_format_test.dart`. No additional candidates.
  **RESOLVED:** Confirmed — `agent_date_format_test.dart` imports `glados` and runs a
  `glados.Glados(...)` property test. No additional candidates. No change.

- [x] **[LOW]** `lib/features/agents/ui/token_stats_tab.dart:1203` — `_formatTokenCount`
  and `_formatDuration` are pure functions, but they delegate directly to
  `NumberFormat.compact()` and trivial arithmetic — no non-trivial invariants to
  generatively test. Not a Glados candidate.
  **RESOLVED:** Confirmed by reading the impl — `_formatTokenCount` is a one-line
  delegation to `_compactFormat.format(tokens)` and `_formatDuration` is `inHours` /
  `inMinutes.remainder(60)` arithmetic. Both are file-private and covered at the widget
  level. Not a Glados candidate. No change.

- [x] **[LOW]** `lib/features/agents/ui/wake_countdown_state.dart:67` — `_remainingSeconds`
  is pure but trivial arithmetic over `Duration`. The file-level `_generatedCountdownScenarios()`
  in `wake_countdown_state_test.dart` already exercises combinatorial scenarios via
  a hand-rolled parameter sweep that mimics what Glados would produce. Converting to
  Glados would add minimal value.
  **RESOLVED:** Confirmed — `_remainingSeconds()` clamps `nextWakeAt.difference(clock.now())`
  at zero and returns `inSeconds`; the test already loops over `_generatedCountdownScenarios()`.
  Glados would add minimal value. No change.

**Summary**: This UI subdir has expected few/zero Glados candidates (all of note are
already covered). No new Glados tests recommended.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/features/agents/ui/agent_badge_widgets.dart:1` — **no test file**.
  `AgentBadge` (pure presentational) and `AgentLifecycleBadge` with its 4-case
  `switch` are completely untested. Add
  `test/features/agents/ui/agent_badge_widgets_test.dart` verifying:
  - Each `AgentLifecycle` value renders the correct localized label.
  - Badge color/border is set (presence of `Container` with `BoxDecoration`).

- [x] **[HIGH]** `lib/features/agents/ui/agent_palette.dart:1` — **no test file**.
  `agentCardDarkGradient` is a pure function; it should have a trivial unit test
  verifying it returns a `LinearGradient` with two non-null colors. `AgentPalette`
  constants need no test, but the function is a gap.
  Add `test/features/agents/ui/agent_palette_test.dart`.

- [x] **[MED]** `lib/features/agents/ui/token_stats_tab.dart:1205` — `_formatDuration`
  minutes-only branch (`hours == 0`) is not explicitly tested in isolation. The **RESOLVED:** Done — covered by the widget-level assertion added to the breakdown test ('15m' renders for the 15-minute source), the practical approach the item itself recommends for a file-private function.
  source tile at line 1144 calls this with `Duration(minutes: 15)` in the test at
  line 206, but that test only asserts agent name / percentage — not the duration
  string. Add an assertion for `'15m'` in the existing breakdown test or extract a
  dedicated unit test (given the function is file-private, the widget-level test
  is the practical approach).

- [x] **[MED]** `lib/features/agents/ui/ai_summary_card.dart:1` — no root-level **RESOLVED:** Done — added `ai_summary_card_test.dart` covering the four root-shell branches: loading -> nothing rendered, error -> nothing rendered, data-null -> Assign Agent CTA, data-with-identity -> summary shell (via the shared AgentTestBench).
  `ai_summary_card_test.dart`. The part files have separate tests under
  `test/features/agents/ui/ai_summary_card/`, but the top-level `AiSummaryCard`
  shell widget's routing (loading/error → `SizedBox.shrink`, null identity →
  `_AssignAgentCta`, valid identity → `_AiSummaryShell`) is not tested at the
  root-level. Consider a minimal `ai_summary_card_test.dart` that exercises the
  three provider branches of `AiSummaryCard.build` (loading, error, data-null,
  data-with-identity).

- [x] **[MED]** `lib/features/agents/ui/agent_soul_detail_page.dart` — the
  `'shows error snackbar on generic delete error'` and rollback-error paths are
  tested for the template page but the soul detail test should mirror this
  coverage. Checking `agent_soul_detail_page_test.dart` at lines 600–800 shows
  these exist; the gap is the `'shows error when deleting soul with assigned **RESOLVED:** Done — added a test stubbing `deleteSoul` with the StateError the service actually throws for assigned templates ("N template(s) still assigned"); asserts the error toast, that the page does NOT pop, and the service call. (No dedicated SoulInUseException type exists — the page intentionally handles all delete failures through one catch.)
  templates'` scenario — i.e. when `SoulInUseException` (if it exists) would be
  thrown during delete.

- [x] **[LOW]** `lib/features/agents/ui/agent_palette.dart:40` — `agentCardDarkGradient`
  is called by chart widgets in subdirectories but no test verifies the gradient
  direction/alignment (`Alignment.topLeft` → `Alignment.bottomRight`) or that the
  interpolated color is different from the base. Small risk but an easy gap to close.
  **RESOLVED:** Already covered — `agent_palette_test.dart` exists and asserts
  `gradient.begin == Alignment.topLeft`, `gradient.end == Alignment.bottomRight`, that
  the last stop equals `Color.lerp(base, accent, 0.08)`, and (in a second test) that
  different accents produce different last stops while sharing the same first stop.
  No change.

- [x] **[LOW]** `lib/features/agents/ui/sidebar_wake_queue.dart:46` —
  `sidebarWakeQueueHasVisibleContent` is a pure function tested at lines 1027–1083
  of the sidebar test. The `'within the 5-minute cutoff'` boundary scenario
  (exactly at `cutoff`) is worth an explicit test to confirm the `!isAfter`
  boundary condition.
  **RESOLVED:** Done — the real cutoff is `kSidebarWakeQueueScheduledLookahead`
  (1h, not 5min). Added two boundary tests to the `sidebarWakeQueueHasVisibleContent`
  group: a wake due exactly at the cutoff (`eta == kSidebarWakeQueueScheduledLookahead`)
  returns `true` (`!dueAt.isAfter(cutoff)` with `dueAt == cutoff`), and a wake one
  microsecond past the cutoff returns `false`, pinning the exclusive far boundary.

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/features/agents/ui/agent_template_detail_page_test.dart` — 57 calls
  to `pumpAndSettle` across 49 tests. Each navigates a `NestedScrollView` +
  `TabBarView` layout. The 12 calls to `scrollUntilVisible` each call
  `pumpAndSettle` internally. Tests that navigate to the Stats tab (e.g.
  `'rollback calls rollbackToVersion'`, `'shows error when deleting template with
  instances'`) call `pumpAndSettle` 4–5 times per test body. **Impact**: each
  `pumpAndSettle` has a 100 ms default idle timeout; 57 × ~80 ms ≈ 4.5 s of
  additional latency per run of this file.
  Mitigation: for tests that only need one state change, replace the 3rd and 4th
  `pumpAndSettle` with `tester.pump(const Duration(milliseconds: 100))` after the
  async action completes.

- [x] **[HIGH]** `test/features/agents/ui/agent_settings_page_test.dart` — 57 calls to
  `pumpAndSettle` for 34 tests in a file that wraps `AgentSettingsPage`, which
  embeds `TokenStatsTab`, `AgentInstancesList`, and `SidebarWakeQueue`. Each
  full-page pump-and-settle resolves multiple async providers. **Impact**: up to
  5–6 s per run. Consider caching the built widget in a `setUpAll`/`setUp` block
  using a shared `ProviderContainer` for tests that don't mutate state between
  assertions.

- [x] **[HIGH]** `test/features/agents/ui/agent_soul_detail_page_test.dart` — 74 calls to
  `pumpAndSettle` across 38 tests. Many edit-mode tests navigate tabs and scroll
  to find UI elements, contributing 2–4 `pumpAndSettle` calls each. Replacing
  intermediate navigation `pumpAndSettle` with `tester.pump(Duration(seconds: 1))`
  after tab taps would reduce idle time. **Impact**: ~5–6 s per run.

- [x] **[MED]** `test/features/agents/ui/sidebar_wake_queue_test.dart` — 33 calls to **RESOLVED:** Stale — the file has no `pumpAndSettle` calls at all; cancellation/snooze tests already use bounded pumps.
  `pumpAndSettle`. Most tests build a fresh `makeTestableWidgetWithScaffold` with
  a `DesignSystemTheme.dark()` theme. Theme build is constant — if a `setUpAll`
  pre-built scaffold were possible (it's not with per-test provider state), it
  would save ~200 ms. Simpler win: the tests at lines 338–390 that check
  cancellation/snooze call `pumpAndSettle` twice after each button tap; one
  `pump(milliseconds: 300)` would suffice after the await-chain resolves.
  **Impact**: ~2 s per run.

- [x] **[MED]** `test/features/agents/ui/agent_activity_log_test.dart` — 9 calls to **RESOLVED:** Assessed, no change — verified empirically: replacing all 9 with bounded `pump()+pump(300ms)` produced 4 failures (riverpod error-retry scheduling and GptMarkdown layout genuinely need settle-style pumping). The item itself concludes the pumps are necessary.
  `pumpAndSettle` in 39 tests. The `buildSubject` factory reconstructs a full
  `makeTestableWidgetWithScaffold` per test including `GptMarkdown` rendering
  setup. The file is large (1095 lines) but most pumps are necessary given the
  async provider resolution. Low priority.

- [x] **[MED]** `test/features/agents/ui/agent_template_detail_page_test.dart:1181` —
  manual pump loop at lines 1187–1190 (`for (var i = 0; i < 10; i++) { await **RESOLVED:** Done — the 10x100ms loop is now `pump()` + `pump(400ms)` (one frame to start the tab transition, one past its duration); suite green.
  tester.pump(Duration(milliseconds: 100)); }`) to drive tab animation. This
  pattern is fragile and slow (10 × 100 ms = 1 s). Replace with
  `tester.pump(const Duration(milliseconds: 400))` (a single pump past the
  animation duration) or `tester.pumpAndSettle(const Duration(seconds: 2))` if
  animation never terminates; investigate whether the loading completer can be
  completed before the pump to allow `pumpAndSettle`.

- [x] **[LOW]** `test/features/agents/ui/token_stats_tab_test.dart:752` and `:803` —
  both `tester.view.physicalSize = const Size(1200, 2400)` + `addTearDown(tester.view.reset)`
  blocks add overhead. Since the underlying widget does not respond to screen
  dimensions for the test assertions (only bar height matters, not absolute pixel
  size), a `SizedBox(width: 800, height: 1200)` wrapper around `TokenStatsTab`
  would be lighter and more deterministic.
  **RESOLVED:** Done (same change as the token_stats_tab_test.dart:803 item above) —
  all three `tester.view.physicalSize`/`devicePixelRatio` mutations and their
  `addTearDown(tester.view.reset)` calls were dropped in favour of a scoped
  `MediaQueryData(size: Size(1200, 2400))` threaded through `_buildSubject`. No global
  view state to set or reset, so it is lighter and tree-deterministic.

---

## Summary

- **3 oversized impl files** (`token_stats_tab.dart` 1212 lines, `agent_template_detail_page.dart`
  831, `agent_soul_detail_page.dart` 771) each exceed the 1000/~500-line guidance.
- **3 oversized test files** (`agent_template_detail_page_test.dart` 2391,
  `agent_soul_detail_page_test.dart` 1602, `agent_settings_page_test.dart` 1313) — the
  template detail test at 2391 lines is the most severe.
- **Weak tests**: 2 significant quality issues (38-copy-paste `getAgentsForTemplate`
  stub; 2 raw `ProviderContainer`+`MaterialApp` bypasses); otherwise test assertions
  are mostly meaningful and behavior-driven.
- **0 Glados candidates** — the UI subdirectory is expected to have minimal generative
  candidates; `report_content_parser_test.dart` and `agent_date_format_test.dart`
  already have Glados coverage.
- **3 coverage gaps**: `agent_badge_widgets.dart` (no test), `agent_palette.dart`
  (no test, function gap), `_formatDuration` minutes-only branch not asserted, and
  `AiSummaryCard` root shell not tested at the root level.
- **5 speed wins**: Template detail (57 `pumpAndSettle`s, ~4.5 s), settings page
  (57, ~5 s), soul detail (74, ~5–6 s) are the top targets; the 10-pump loop at
  template detail line 1187 is also an easy fix.
- **Biggest opportunity**: Extract `_stubGetAgentsEmpty(mockTemplateService)` helper
  and `buildEditSubjectWithContainer` in `agent_template_detail_page_test.dart`
  to collapse ~140 lines of copy-paste mock setup and 2 raw `MaterialApp` bypasses,
  reducing the test file by ~350 lines and making the intent of each test clearer.
