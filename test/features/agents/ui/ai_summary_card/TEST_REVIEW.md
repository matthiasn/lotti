# TEST_REVIEW — `test/features/agents/ui/ai_summary_card/`

Scope: impl files under `lib/features/agents/ui/ai_summary_card/` (4 Dart files) + `lib/features/agents/ui/ai_summary_card.dart` (parent); test files in this subdir (6 test files + 1 shared bench).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../ai_summary_card/proposals_section_part.dart` | 880 | Yes — `proposals_test.dart` | **Oversized**: 880 lines, candidate for split |
| `lib/.../ai_summary_card/tldr_section_part.dart` | 466 | Yes — `tldr_section_part_test.dart` | Healthy size; well covered |
| `lib/.../ai_summary_card/assign_agent_cta_part.dart` | 104 | Yes — `assign_agent_cta_part_test.dart` | Good coverage |
| `lib/.../ai_summary_card/proposal_kind_part.dart` | 122 | Indirect via `proposals_test.dart` | No dedicated test file; mirror test missing |
| `lib/../ai_summary_card.dart` (parent) | — | Split across 5 test files | Covered well by `gating_and_tldr_test`, `lifecycle_test` |
| `test/.../assign_agent_cta_part_test.dart` | 364 | ✓ | Healthy; mock-setup heavier than test bodies in the success-path test |
| `test/.../gating_and_tldr_test.dart` | 205 | ✓ | Good quality, two inline overrides duplicated from `test_bench.dart` |
| `test/.../lifecycle_test.dart` | 276 | ✓ | Good; `_buildShell` helper centralises repeated overrides |
| `test/.../proposals_test.dart` | **1,251** | ✓ | **Oversized test file**; 1,251 lines |
| `test/.../tldr_section_part_test.dart` | 287 | ✓ | Healthy; `_narrowScope` helper is good DRY practice |
| `test/.../wake_test.dart` | 128 | ✓ | Good; `AgentTestBench` keeps setup DRY |
| `test/.../test_bench.dart` | 173 | Shared helper | Well-structured; no improvements needed |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/agents/ui/ai_summary_card/proposals_section_part.dart` — 880 lines, the largest impl file in scope. Natural seams exist: `_ProposalRow` / `_ProposalRowState` (lines 285–698, the swipe + wiggle + busy logic) is functionally independent of the stateless section-header widgets (`_ProposalsSection`, `_PendingPill`, `_ConfirmAllButton`, `_EmptyProposalsRow`, `_HistoryToggle`) and the pure chip helpers (`_KindChip`, `_RowActions`, `_SquareIconButton`, `_ResolvedTag`). Splitting into at least `_proposal_row_part.dart` + `_proposal_chip_part.dart` (or similar) would bring each file under ~400 lines and make the test file easier to map back to source. **RESOLVED:** split along the suggested seams into `proposal_row_part.dart` (418 lines, `_ProposalRow`/`_ProposalRowState`) and `proposal_row_widgets_part.dart` (183 lines, `_KindChip`/`_RowActions`/`_SquareIconButton`/`_ResolvedTag`), keeping the stateless section widgets in `proposals_section_part.dart` (284 lines); all three remain parts of `ai_summary_card.dart`.
- [x] **[HIGH]** `test/features/agents/ui/ai_summary_card/proposals_test.dart` — 1,251 lines. The file spans six independent `group()` blocks (Proposals, Error & cancel paths, History, Proposal kind chips, Row error branches, Swipe affordances, Busy spinner). Each group maps to a different part of the impl. Mirror-split the test file to match any impl split described above. **RESOLVED:** mirror-split to match the impl split — `proposal_row_part_test.dart` (row error branches, swipe affordances, row busy spinner; 249 lines) and `proposal_row_widgets_part_test.dart` (proposal kind chips + token selectors; 150 lines); `proposals_test.dart` keeps the section/error-cancel/history/confirm-all groups (925 lines). All 64 directory tests pass.
- [x] **[LOW]** `lib/features/agents/ui/ai_summary_card/proposal_kind_part.dart` — 122 lines, no standalone test file. Covered indirectly through `proposals_test.dart` chip group but lacks a `proposal_kind_part_test.dart` mirror.
  **RESOLVED:** all symbols in `proposal_kind_part.dart` (`_ProposalKind`, `_resolveKind`, `_kindMeta`) are `private` `part of` the `ai_summary_card` library and have no public surface, so a standalone `proposal_kind_part_test.dart` could not reach them — they can only be exercised through the rendered chip. The dedicated home for that logic is `proposal_row_widgets_part_test.dart` (the file that renders `_KindChip` from `_kindMeta`), which now mirror-tests the full `_resolveKind`→`_kindMeta` dispatch table (every known tool name) plus the `default→update` fallback added under the line-52 item. A separate mirror file would only re-host the same widget tests.

---

## Test quality improvements

- [x] **[MED]** `gating_and_tldr_test.dart` lines 129–162 (`tapping the agent name…` test) and lines 163–203 (`Open agent internals pill…` test): both inline the full 6-override provider list rather than delegating to `AgentTestBench` or `_buildShell`. The overrides are identical to those used in `lifecycle_test.dart`'s `_buildShell`. Extract a shared helper or use `AgentTestBench` with an `agentIdentityProvider` parameter to avoid the duplication.
  **RESOLVED:** added a `provideAgentIdentity` flag to `AgentTestBench` that, when set, also overrides `agentIdentityProvider` (the only extra provider the pushed `AgentInternalsPanel` reads). Both navigation tests now call `AgentTestBench(report: …, provideAgentIdentity: true).build()`, dropping ~14 inline override lines each; the unused `RiverpodWidgetTestBench`/state-provider imports were removed from the file.
- [x] **[LOW]** `proposals_test.dart` line 212–283 (running-agent refresh test) and lines 286–342 (disposal test): both build their own full `RiverpodWidgetTestBench` override list inline instead of using `AgentTestBench`. The two tests share ~12 identical override lines. A variant of `AgentTestBench` that accepts a `StreamController<bool>` for `agentIsRunningProvider` would shrink each test body by ~15 lines and keep the setup DRY.
  **RESOLVED:** added optional `isRunningOverride` (`Stream<bool> Function(Ref, String)`) and `suggestionListOverride` (`FutureOr<UnifiedSuggestionList> Function(Ref, String)`) parameters to `AgentTestBench` that replace the default static `agentIsRunningProvider` / `unifiedSuggestionListProvider` overrides. Both tests now drive their `StreamController<bool>` and reactive suggestion list through the bench, dropping the inlined config/taskAgent/report/template/state overrides (the bench's defaults already match). The remaining inline `RiverpodWidgetTestBench` in this file (the task-switch test) is genuinely bespoke and left as-is.
- [x] **[LOW]** `assign_agent_cta_part_test.dart` success-path test (lines 250–362): the mock setup block (templates service, task-agent service, inference profile controller, dynamic identity closure) is ~50 lines before the first user action at line 315. Consider extracting a `_successPathHarness(...)` helper for the repeated setup so the test body reads as action + assertion.
  **RESOLVED:** extracted a `_SuccessPathHarness` class with a `create({required categoryId})` factory that wires the category-scoped template service, the task-agent service, the fake inference-profile controller and the task; it exposes `templateService`/`taskAgentService`/`task` for verification, a `build()` that returns the configured `RiverpodWidgetTestBench`, and `attachAgent()` to flip `taskAgentProvider` from null to a real identity after creation. The test body is now setup-line → tap → assert.

---

## Generative (Glados) testing opportunities

No Glados candidates are identified for this subdir. The logic under test is predominantly widget rendering and stateful interaction (swipe gestures, async confirmation flows, provider subscription lifecycles). These are UI/widget tests where Glados is explicitly a poor fit per `test/README.md`. The one pure-logic candidate — `_cleanText` (the kind-label prefix stripping regex, lines 686–697 of `proposals_section_part.dart`) — is a private method exercised via widget tests; it is simple enough that the existing concrete examples cover it adequately.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `proposal_kind_part.dart` has no dedicated mirror test. The `_resolveKind` / `_kindMeta` dispatch table is exercised only indirectly through the chip loop in `proposals_test.dart`. An unrecognised tool name falling through to the fallback `_ProposalKind.update` case is not covered by any explicit test.
  **RESOLVED:** added an `'an unrecognised tool name falls through to the Update kind chip'` test to `proposal_row_widgets_part_test.dart` that pumps a proposal with a made-up tool name (`totally_unknown_future_tool`) and asserts the rendered chip reads "Update" and carries the Update token color + surface — exactly the `default → _ProposalKind.update → _kindMeta(update)` path. The known-tool dispatch arms were already mirror-tested in the same file's parameterized chip group.
- [x] **[MED]** Compact-width (< 600px) proposal row: the narrow-viewport branch (where `_RowActions` is replaced by `SizedBox.shrink()`) is exercised only through the swipe-intent-label tests with `reducedMotionDesktop`. A compact-width confirm/reject interaction path (confirm via swipe on a `<600px` viewport) is not directly tested.
  **RESOLVED:** added a `'AiSummaryCard – compact-width proposal row'` group to `proposal_row_part_test.dart` with two tests at a `420×800` (animations-disabled) viewport: one asserts both action icons are absent (`_RowActions`→`SizedBox.shrink`) and a right swipe still dispatches `confirmItem` + `notify`; the other verifies a left swipe dispatches `rejectItem` + `notify`. This is the first direct coverage of confirm/reject through swipe on the narrow layout.
- [x] **[LOW]** `_ProposalRow.fromLedger` with `ChangeItemStatus.retracted`: the `lineThrough` / `dimmed` path triggers for both `rejected` and `retracted` statuses, but only `rejected` is tested in `proposals_test.dart:836`. The `retracted` arm is dead code in tests.
  **RESOLVED:** added a `'retracted entries also render dimmed with strikethrough'` test to the History group in `proposals_test.dart` — a `retracted` ledger entry renders the "Dismissed" tag, a `lineThrough` body, and the dimmed `Opacity(0.45)` wrapper, covering the `_resolvedStatus == retracted` half of the `lineThrough`/`dimmed` condition.
- [x] **[LOW]** `_wiggleController` forward path: the wiggle-hint animation (non-reduce-motion, non-first-frame) is never tested because `AgentTestBench` uses real animations and `pumpAndSettle` completes them. The test at line 1033 (`reduce-motion skips scheduling`) tests only the early-return branch. Testing that the wiggle runs on a standard viewport would require a bounded `tester.pump(const Duration(milliseconds: 1600))` instead of `pumpAndSettle`.
  **RESOLVED:** added `'the first pending row runs the wiggle hint on a standard viewport'` to the swipe-affordances group in `proposal_row_part_test.dart`. With the default (animations-enabled) bench it reads the row's `AnimatedContainer.transform` translation x directly via bounded pumps: 0 at rest, fires the 350ms start Timer, advances ~150ms into the first easeInOutSine leg and asserts the offset is in `(0, 14]` (proving `_wiggleController.forward` is driving the peek), then runs the full 1600ms sequence and asserts it settles back to 0. No `pumpAndSettle` is used.

---

## Test execution speed opportunities

- [x] **[MED]** `proposals_test.dart` uses `pumpAndSettle` in ~46 locations. Most are appropriate (waiting for async provider resolution), but several immediately follow a `tester.tap()` that triggers only a sync state update (e.g., lines 172–178 `Confirm all batches…`, lines 444–449 `tap-reject dispatches…`). Replace those single-action settlements with a bounded `tester.pump()` + `tester.pump()` pair to avoid the 10s fallback timeout risk if an animation ever fails to settle.
  **RESOLVED:** already addressed by the HIGH split — the current `proposals_test.dart` contains zero `pumpAndSettle` calls (grep confirms). Every action site uses the bounded `tester.pump()` + `tester.pump(const Duration(milliseconds: 300))` pair the item recommends, including the `Confirm all batches…` and `tap-reject…` tests. No further change needed.
- [x] **[LOW]** `assign_agent_cta_part_test.dart` uses `setUpTestGetIt` (involving `getIt` and additional service registrations) on every test in the file. Because there are only 4 tests and they all need the same GetIt state, the overhead is unavoidable; no structural change is needed here.
  **RESOLVED:** confirmed by reading the file — all four CTA tests render the full `AiSummaryCard` tree, which (through `_createTaskAgent`'s `entryControllerProvider` read and the journal/editor stack) requires the GetIt singletons registered in `setUpTestGetIt`'s `additionalSetup`. The shared per-test `setUp` is the correct pattern; no change.
- [x] **[LOW]** `tldr_section_part_test.dart` lines 87–90: the spinner test manually pumps 5 × 16 ms frames to resolve async providers. This is the correct pattern (avoids `pumpAndSettle` with an indefinitely-animating `CircularProgressIndicator`). No change needed; flagged as reference-good practice for other files in scope.
  **RESOLVED:** confirmed by reading the test — it pumps 5×16 ms bounded frames (not `pumpAndSettle`) to resolve the `taskAgent`/`report` providers, then asserts the `CircularProgressIndicator` is shown and the refresh icon is hidden while running. This is the recommended pattern for indefinitely-animating spinners; no change.

---

## Summary

- `proposals_section_part.dart` (880 lines) is the only oversized impl file; it has a clear split seam around `_ProposalRow`.
- `proposals_test.dart` (1,251 lines) is the only oversized test file and mirrors the impl's mixed responsibilities.
- `proposal_kind_part.dart` has no dedicated mirror test file — the `_resolveKind` fallback and `retracted` status paths are untested.
- All test files use centralized mocks and `AgentTestBench`/`makeTestableWidget` infrastructure; no inline mock class definitions found.
- No fake-time violations (`Future.delayed`, `sleep`) were found.
- No Glados candidates: all logic under test is widget/provider interaction, not pure functions.
- 3 speed improvements: replace some `pumpAndSettle` calls after sync taps with bounded `pump()` pairs.
