# TEST_REVIEW — `test/features/agents/ui/instances/`

Scope: impl files under `lib/features/agents/ui/instances/` (2 Dart files); test files in this subdir (2 test files).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../instances/agent_instances_page.dart` | 393 | Yes — `agent_instances_page_test.dart` | Good size; several widget branches not directly tested |
| `lib/.../instances/instance_view_model.dart` | 216 | Yes — `instance_view_model_provider_test.dart` | Well covered by Glados + unit tests |
| `test/.../instances/agent_instances_page_test.dart` | 198 | ✓ | Only 2 scenarios; several page behaviors untested |
| `test/.../instances/instance_view_model_provider_test.dart` | 302 | ✓ | Strong: Glados + unit tests |

---

## File size / split opportunities

- [ ] **[LOW]** All impl and test files are under 400 lines. No split is needed.

---

## Test quality improvements

- [ ] **[MED]** `agent_instances_page_test.dart` has only 2 `testWidgets` scenarios (Oldest sort, Name tiebreaker). The file covers almost none of the page's behaviors:
  - The loading / error async branches are not tested (cf. `agent_listing_shell_test.dart` which tests those at the shell level, but page-level wiring is also worth checking).
  - The search field interaction is absent.
  - The filter axis (Kind / Status / Lifecycle axes, if present in the page adapter) is not exercised.
  - Navigation (row tap → `beamToNamed` for task-agent vs. evolution-session vs. soul route) is only partially tested (the Name-tiebreaker test happens to cover one navigation path).
  - The evolution-session row rendering (localized `agentInstancesEvolutionTitle` from `sessionNumber`, no soul/template subtitle) is not covered at the widget level.
  - The `soulName`/`templateName` subtitle text that appears on task-agent rows is not asserted.
- [ ] **[MED]** `agent_instances_page_test.dart` does not test the "Recent" sort (default), which is the axis that appears on first mount. The only sort test switches *away* from Recent to Oldest. A test that confirms the default Recent ordering (newest first) would cover the initial render path.
- [ ] **[LOW]** `agent_instances_page_test.dart`: the `_soulVm` factory is defined but only the two test scenarios use it. The `_vm` factory is defined but never used outside `_soulVm`. If additional scenarios are added (see above), both factories are available.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `instance_view_model_provider_test.dart` already contains two well-formed Glados properties (`maps generated agent kind ids to instance types` and `maps generated evolution statuses onto lifecycle rows`). These correctly carry `tags: 'glados'` and follow repo conventions. No new Glados work needed here. **RESOLVED (no action needed):** the item itself confirms both Glados properties are well-formed and conventional.
- [ ] **[LOW]** The sort comparators in `agent_instances_page.dart` (Recent → descending `updatedAt`; Oldest → ascending; Name → case-insensitive title then `id` tiebreaker) are pure functions and could benefit from a property-based round-trip. However, they are already exercised by the existing 2 widget tests. Adding Glados on the comparator alone would be low value.

---

## Coverage / missing-behavior gaps

- [ ] **[HIGH]** Evolution-session rows: `instance_view_model_provider_test.dart` proves the VM is built correctly; but `agent_instances_page_test.dart` never pumps a widget tree that includes an evolution `InstanceVm`. The page renders evolution rows differently (no soul avatar, localized session title). This rendering path has zero widget-test coverage.
- [ ] **[MED]** Status-lifecycle visual distinction (`AgentLifecycle.dormant`, `AgentLifecycle.destroyed`): the page likely renders a different leading icon or opacity for non-active lifecycles (impl line ~200–300). No widget test verifies this rendering.
- [ ] **[MED]** Navigation to non-task-agent routes: task-agents beam to `instances/{id}`, but evolution sessions and other types may have different route targets. Only the task-agent beam is tested.
- [ ] **[LOW]** The empty-state message (`agentInstancesEmptyFiltered`) is not tested in `agent_instances_page_test.dart`. (Tested at shell level in `agent_listing_shell_test.dart` but not wired through the page adapter.)

---

## Test execution speed opportunities

- [ ] **[MED]** `agent_instances_page_test.dart` calls `tester.binding.setSurfaceSize` and `pumpAndSettle` for every test. Because both tests pump the same `AgentInstancesPage` structure with only the data changing, extracting a `setUpAll` to set the surface size (and a corresponding `tearDownAll` to reset it) would avoid 2 redundant surface resizes. Minor gain for 2 tests; higher impact if more tests are added.
- [ ] **[LOW]** The `selectSort` helper calls `pumpAndSettle` twice (open popover, select item). This is appropriate since popover animations must settle before the item is tappable. No change needed.

---

## Summary

- No oversized impl or test files.
- `instance_view_model_provider_test.dart` is a model file: 2 Glados properties + 3 unit tests, all meaningful.
- `agent_instances_page_test.dart` is thin (only 2 widget scenarios): evolution-session row rendering, search, default sort, and full navigation coverage are absent — 4 high/med behavior gaps.
- No fake-time violations or inline mock definitions found.
- No new Glados candidates (existing ones are already in place).
- 1 speed improvement: share `setSurfaceSize` across tests.
