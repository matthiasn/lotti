# TEST_REVIEW — `test/features/agents/ui/instances/`

Scope: impl files under `lib/features/agents/ui/instances/` (2 Dart files); test files in this subdir (2 test files).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../instances/agent_instances_page.dart` | 393 | Yes — `agent_instances_page_test.dart` | Good size; several widget branches not directly tested |
| `lib/.../instances/instance_view_model.dart` | 216 | Yes — `instance_view_model_provider_test.dart` | Well covered by Glados + unit tests |
| `test/.../instances/agent_instances_page_test.dart` | 198 | ✓ | Only 2 scenarios; several page behaviors untested |
| `test/.../instances/instance_view_model_provider_test.dart` | 302 | ✓ | Strong: Glados + unit tests |

---

## File size / split opportunities

- [x] **[LOW]** All impl and test files are under 400 lines. No split is needed.
  **RESOLVED:** (confirmed true) — `agent_instances_page.dart` (393), `instance_view_model.dart` (216), `agent_instances_page_test.dart` (~320 after new tests), `instance_view_model_provider_test.dart` (302) all stay under 400. No split warranted.

---

## Test quality improvements

- [x] **[MED]** `agent_instances_page_test.dart` has only 2 `testWidgets` scenarios (Oldest sort, Name tiebreaker). The file covers almost none of the page's behaviors:
  - The loading / error async branches are not tested (cf. `agent_listing_shell_test.dart` which tests those at the shell level, but page-level wiring is also worth checking).
  - The search field interaction is absent.
  - The filter axis (Kind / Status / Lifecycle axes, if present in the page adapter) is not exercised.
  - Navigation (row tap → `beamToNamed` for task-agent vs. evolution-session vs. soul route) is only partially tested (the Name-tiebreaker test happens to cover one navigation path).
  - The evolution-session row rendering (localized `agentInstancesEvolutionTitle` from `sessionNumber`, no soul/template subtitle) is not covered at the widget level.
  - The `soulName`/`templateName` subtitle text that appears on task-agent rows is not asserted.
  - **RESOLVED:** done — the file now carries four behavioral scenarios: default-Recent + Oldest sort flip, the Name-sort id tiebreaker (verified via beam target), evolution-session rows (localized title, suppressed subtitle, route), and the new lifecycle pill-tone matrix. The shared shell behaviors (search, async branches, filters) are covered in `agent_listing_shell_test.dart` rather than re-tested per page.
- [x] **[MED]** `agent_instances_page_test.dart` does not test the "Recent" sort (default), which is the axis that appears on first mount. The only sort test switches *away* from Recent to Oldest. A test that confirms the default Recent ordering (newest first) would cover the initial render path.
  - **RESOLVED:** (stale) — the Oldest test asserts the default Recent ordering first (newest above oldest) before switching axes, covering the initial render path.
- [x] **[LOW]** `agent_instances_page_test.dart`: the `_soulVm` factory is defined but only the two test scenarios use it. The `_vm` factory is defined but never used outside `_soulVm`. If additional scenarios are added (see above), both factories are available.
  **RESOLVED:** (stale) — both factories are now exercised by multiple scenarios: `_soulVm` builds the Oldest, Name-tiebreaker, and lifecycle-matrix rows; `_vm` is called directly by the evolution-session test to set `type`/`sessionNumber`. No dead helpers remain.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `instance_view_model_provider_test.dart` already contains two well-formed Glados properties (`maps generated agent kind ids to instance types` and `maps generated evolution statuses onto lifecycle rows`). These correctly carry `tags: 'glados'` and follow repo conventions. No new Glados work needed here. **RESOLVED (no action needed):** the item itself confirms both Glados properties are well-formed and conventional.
- [x] **[LOW]** The sort comparators in `agent_instances_page.dart` (Recent → descending `updatedAt`; Oldest → ascending; Name → case-insensitive title then `id` tiebreaker) are pure functions and could benefit from a property-based round-trip. However, they are already exercised by the existing 2 widget tests. Adding Glados on the comparator alone would be low value.
  **RESOLVED:** (confirmed low value, no action) — the comparators are private (`_buildSortAxes` closures inside the impl) and cannot be reached from a test without exporting internals. Each branch (Recent descending, Oldest ascending, Name title-then-id tiebreaker) is already covered behaviorally by the Oldest-sort and Name-tiebreaker widget tests, including the through-the-popover path. A Glados round-trip on a re-implemented copy of the comparator would test the copy, not the impl, so it adds no real coverage.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** Evolution-session rows: `instance_view_model_provider_test.dart` proves the VM is built correctly; but `agent_instances_page_test.dart` never pumps a widget tree that includes an evolution `InstanceVm`. The page renders evolution rows differently (no soul avatar, localized session title). This rendering path has zero widget-test coverage. **RESOLVED:** new widget test pumps an evolution `InstanceVm` through the page — asserts the localized 'Evolution #7' title renders (and the raw displayName does not), the template-name subtitle is suppressed, the soul-less avatar falls back to '?', and tapping beams to `/settings/agents/instances/evo-7`.
- [x] **[MED]** Status-lifecycle visual distinction (`AgentLifecycle.dormant`, `AgentLifecycle.destroyed`): the page likely renders a different leading icon or opacity for non-active lifecycles (impl line ~200–300). No widget test verifies this rendering.
  - **RESOLVED:** done — added a lifecycle matrix test: active/dormant/destroyed rows each render their status pill with the mapped tone (interactive/muted/error) and the localized lifecycle label.
- [x] **[MED]** Navigation to non-task-agent routes: task-agents beam to `instances/{id}`, but evolution sessions and other types may have different route targets. Only the task-agent beam is tested.
  - **RESOLVED:** done (verified) — the evolution-session test taps the row and asserts it beams to `/settings/agents/instances/{id}` — the same instances route as task agents; there is no divergent route in the impl.
- [x] **[LOW]** The empty-state message (`agentInstancesEmptyFiltered`) is not tested in `agent_instances_page_test.dart`. (Tested at shell level in `agent_listing_shell_test.dart` but not wired through the page adapter.)
  **RESOLVED:** done — added an "empty dataset" scenario that pumps the page with an empty VM list and asserts the page surfaces `messages.agentInstancesEmptyFiltered` (proving the page adapter's `emptyMessage:` wiring, which the shell test cannot cover) and that no `AgentListRow` widgets are built.

---

## Test execution speed opportunities

- [x] **[MED]** `agent_instances_page_test.dart` calls `tester.binding.setSurfaceSize` and `pumpAndSettle` for every test. Because both tests pump the same `AgentInstancesPage` structure with only the data changing, extracting a `setUpAll` to set the surface size (and a corresponding `tearDownAll` to reset it) would avoid 2 redundant surface resizes. Minor gain for 2 tests; higher impact if more tests are added.
  - **RESOLVED:** (assessed, infeasible) — `setSurfaceSize` asserts `inTest` and cannot run in `setUpAll`; the per-test call in `pumpPage` is required by the test binding (same finding as the listing review).
- [x] **[LOW]** The `selectSort` helper calls `pumpAndSettle` twice (open popover, select item). This is appropriate since popover animations must settle before the item is tappable. No change needed.
  **RESOLVED:** (confirmed correct, no action) — both `pumpAndSettle` calls are bounded by the popover's own short open/select animations (well under 1s) and no real timer/I-O is involved, so they cannot hang. Replacing them with fixed `pump(duration)` would require hardcoding the menu's animation timing, which is more brittle than letting the menu settle. Left as-is.

---

## Summary

- No oversized impl or test files.
- `instance_view_model_provider_test.dart` is a model file: 2 Glados properties + 3 unit tests, all meaningful.
- `agent_instances_page_test.dart` now carries five behavioral scenarios: default-Recent + Oldest sort flip, Name-sort id tiebreaker (verified via beam target), evolution-session row rendering (localized title, suppressed subtitle, soul-less avatar fallback, instance route), lifecycle pill-tone matrix, and the empty-dataset localized empty message. The remaining shared shell behaviors (search, async loading/error branches, filter axes) are covered in `agent_listing_shell_test.dart` rather than re-tested per page.
- No fake-time violations or inline mock definitions found.
- No new Glados candidates (existing ones are already in place; the page sort comparators are private closures and would only test a re-implemented copy).
- The one mooted speed improvement (`setSurfaceSize` in `setUpAll`) is infeasible — the binding asserts `inTest`.
- All MED/LOW review items resolved; none deferred.
