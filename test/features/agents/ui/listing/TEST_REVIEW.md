# TEST_REVIEW — `test/features/agents/ui/listing/`

Scope: impl files under `lib/features/agents/ui/listing/` (3 Dart files) and `lib/features/agents/ui/listing/widgets/` (5 Dart files); test files in this subdir (2 test files).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../listing/agent_list_data.dart` | 224 | Indirect via filter-state + shell tests | No dedicated mirror test |
| `lib/.../listing/agent_list_filter_state.dart` | 152 | Yes — `agent_list_filter_state_test.dart` | Well covered by Glados + unit tests |
| `lib/.../listing/agent_listing_shell.dart` | 236 | Yes — `agent_listing_shell_test.dart` | Good coverage; a few branches missing |
| `lib/.../listing/widgets/agent_list_toolbar.dart` | **808** | Indirect via shell + page tests | **Oversized**: 808 lines; no dedicated mirror test |
| `lib/.../listing/widgets/active_filters_row.dart` | 99 | Indirect via shell tests | No dedicated test |
| `lib/.../listing/widgets/agent_list_group_section.dart` | 129 | Indirect via shell tests | No dedicated test |
| `lib/.../listing/widgets/agent_list_row.dart` | 252 | Indirect via shell tests | No dedicated test; several row variants untested |
| `lib/.../listing/widgets/soul_avatar.dart` | 51 | Yes — in `agent_list_filter_state_test.dart` | Tested in the wrong file; correct but not mirrored |
| `test/.../listing/agent_list_filter_state_test.dart` | 730 | ✓ | Strong Glados + unit tests; `SoulAvatar` tested here (should be in `soul_avatar_test.dart`) |
| `test/.../listing/agent_listing_shell_test.dart` | 455 | ✓ | Good coverage; `_AxisSwapHost` harness is well-designed |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/agents/ui/listing/widgets/agent_list_toolbar.dart` — 808 lines. This is one of the two largest impl files called out in the task scope. Natural seams exist:
  - Sort popover widget cluster (sort button + popover state + sort option tiles) is self-contained and reusable.
  - Filter popover widget cluster (filter button + per-axis checkboxes + count badges) is similarly self-contained.
  - Active-filter summary row is already in a separate file (`active_filters_row.dart`).
  - Suggested split: `_sort_popover_part.dart` (~200 lines) + `_filter_popover_part.dart` (~250 lines) + toolbar shell (~350 lines), each under 400 lines. The test file (which doesn't exist yet) should mirror the split. **RESOLVED (adapted):** split via `part` files — `agent_list_toolbar_popover.dart` (filters popover panel + rows + swatch, 231 lines) and `agent_list_toolbar_search.dart` (search field + state, 232 lines); the toolbar shell is exactly 350 lines. The mirror test `agent_list_toolbar_test.dart` already exists (the coverage claim was stale) and remains the single mirror of the library.
- [x] **[LOW]** `agent_list_filter_state_test.dart` at 730 lines is large but coherently organized: Glados properties for the pipeline, unit tests for `AgentListFilterState`, and `SoulAvatar` widget tests are all in one file. If `SoulAvatar` gets its own test file, this drops to ~680 lines — still large but justified by the number of distinct filter-operation combinations.
  **RESOLVED:** (confirmed observation, no change) — `SoulAvatar` was already moved out (the file is now 701 lines: Glados `pipelineScenario`/`filterScenario` properties + `buildGroupedAgentList`, `AgentListFilterState`, and `hueForSeed` unit tests). The size is justified by the distinct filter-operation combinations and the file is one coherent mirror of `agent_list_filter_state.dart`; splitting further would scatter the pipeline coverage.

---

## Test quality improvements

- [x] **[HIGH]** `agent_list_toolbar.dart` (808 lines) has no dedicated test file. Its behaviors (sort popover open/close, filter popover toggle, active-filter chip display, search field interaction, Group-by popover) are exercised only indirectly through page-level tests (`agent_instances_page_test.dart`, `agent_templates_page_test.dart`, etc.). A dedicated `agent_list_toolbar_test.dart` pumping an isolated `AgentListToolbar` would allow testing the popover state machine, the filter chip count badge, and the Group-by label without setting up a full page. **RESOLVED:** dedicated `widgets/agent_list_toolbar_test.dart` added — pumps an isolated `AgentListToolbar` in a stateful host and covers buttons/search/count rendering, the filters popover sections with per-option count badges, outside-tap close, toggle-on/off round trip (button badge + filtered count text), per-section Clear, Group-by and Sort popover selection, and search-field state updates.
- [x] **[MED]** `agent_list_row.dart` (252 lines) has no dedicated test. The row renders several visual variants: soul avatar leading, pill list, subtitle present/absent, trailing chevron vs. custom trailing builder, compact vs. comfortable layouts. These are exercised only as a side-effect of shell/page tests. A `agent_list_row_test.dart` would directly assert the pill rendering, the subtitle conditional, and the avatar color derivation from `hueForSeed`.
  - **RESOLVED:** done — new `agent_list_row_test.dart` covers pills, conditional subtitle (inline rich-text vs stacked compact), both leading variants (SoulAvatar with hue, plain icon), chevron-vs-custom-trailing-vs-none, onTap invocation, and the <600 px compact layout switch.
- [x] **[MED]** `soul_avatar.dart` is tested inside `agent_list_filter_state_test.dart` (lines 704–728) rather than in a dedicated `soul_avatar_test.dart`. This violates the one-test-file-per-source-file rule and makes the avatar tests harder to find. Move the two `SoulAvatar` `testWidgets` into a new `test/features/agents/ui/listing/widgets/soul_avatar_test.dart`.
  - **RESOLVED:** done — the two SoulAvatar tests moved to `widgets/soul_avatar_test.dart` (mirroring the source) plus a new test pinning all three HSL-derived tile tones to the hue.
- [x] **[LOW]** `agent_list_group_section.dart` and `active_filters_row.dart` have no tests. Both are small display widgets (129 and 99 lines) whose rendering is exercised through the shell tests. Dedicated tests are low priority but would improve isolation.
  **RESOLVED:** added dedicated mirror tests. `widgets/agent_list_group_section_test.dart` covers `AgentListGroupHeader` (label + trailing item count, expanded/collapsed chevron icon, onToggle invocation, active-count cell shown when >0 / hidden at 0, avatar vs icon leading) and `AgentListGroupBody` (one `AgentListRow` per item, empty group renders none). `widgets/active_filters_row_test.dart` covers one chip per selected option in axis order, the quoted search chip, per-chip removal (`toggleOption` / search-clear via captured callback), Clear-all emitting a fully cleared state that preserves group/sort axes, and the no-selection no-search case showing only Clear-all.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `agent_list_filter_state_test.dart` already contains two Glados properties (filter-state operations and full pipeline), both well-formed with `tags: 'glados'`, matching repo conventions. These are the correct targets and are already covered. **RESOLVED (already covered):** the review itself confirms both Glados properties exist with `tags: 'glados'` and are the correct targets — nothing to add.
- [x] **[LOW]** `agent_list_row.dart` pill rendering: the `pills` list is a `List<AgentListPill>` with arbitrary length. A Glados property over `(List<AgentListPill>, String subtitle, bool hasTrailing)` could verify that every pill label appears in the rendered output. This is a minor pure-rendering invariant; low priority.
  **RESOLVED (adapted):** the invariant ("every pill label appears in the rendered output") is widget-rendering, which needs a `WidgetTester`; this repo's convention keeps Glados `.test()` bodies for pure synchronous logic only (no Glados+`WidgetTester` helper exists, confirmed across the suite). So the invariant is expressed as a parameterised `testWidgets` loop in `widgets/agent_list_row_test.dart` over representative pill-list shapes (empty, single neutral, mixed-tone wide, mixed-tone compact), each asserting every `pill.label` renders exactly once. The pure tone-resolution logic it would otherwise generate over (`_toneColors`/`_toneAccent`) is private to `_Pill`, so a Glados property could not reach it without exposing internals.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `agent_list_toolbar.dart`: the filter popover's "per-axis section with count badges" rendering path and the Group-by popover are exercised only through page-level tests. No test directly verifies: (a) the count badge number shown next to each filter option, (b) the popover closes on outside tap, (c) the toolbar re-renders correctly after a filter is toggled on and then off. **RESOLVED:** covered by the new `agent_list_toolbar_test.dart` — (a) per-option count badges asserted ('7'/'2'/'5'/'4'), (b) outside tap closes the popover, (c) toggle on→off round trip restores the unfiltered toolbar (badge gone, count text back to the all-variant).
- [x] **[MED]** `agent_listing_shell_test.dart` covers loading/error/empty async branches and search, but does not test: (a) the filter-axes path (shell with at least one `AgentListFilterAxis` showing the Filters button), (b) the group-section header rendering (relying on `AgentListGroupSection`), (c) rows with `AgentListLeading` variants (soul avatar, icon).
  - **RESOLVED:** done (partially stale) — (b) group-section headers were already asserted (`GROUP[One]`); (a) added a filter-axes pair: a configured axis surfaces the Filters toolbar button and no axes hides it; (c) the leading variants are covered in the new `agent_list_row_test.dart`.
- [x] **[MED]** `agent_list_data.dart` (224 lines) contains `buildGroupedAgentList` (the pipeline) which is tested in `agent_list_filter_state_test.dart`. However, `AgentListRowData` fields `leading`, `pills`, and `onTap: null` vs. `onTap: set` are not covered by unit tests; they are only implicitly verified at the shell level.
  - **RESOLVED:** done — the new row test covers `leading` (both variants), `pills` rendering, and the `onTap` set (chevron + tap fires) vs null (no chevron) contract at the widget level.
- [x] **[LOW]** `AgentListingShell.didUpdateWidget` coverage: the `_AxisSwapHost` harness in `agent_listing_shell_test.dart` covers both group-reseat and sort-reseat paths. The case where both axes change simultaneously (double reseat) is not tested.
  **RESOLVED:** added a third reseat test to the `didUpdateWidget axis reseat` group. It swaps both axis lists so neither the selected `groupAxisId` nor `sortAxisId` survives, forcing both ternary branches in `didUpdateWidget` to take the false path in one pass. Both consequences are independently observable: the group header relabels (`GROUP[One]` → `GROUP[Two]`) AND the row order flips to descending (Alpha-above-Bravo → false), proving both ids were reseated together.

---

## Test execution speed opportunities

- [x] **[MED]** Both test files call `tester.binding.setSurfaceSize(const Size(1200, 900))` per test. `agent_listing_shell_test.dart` has 10 `testWidgets` calls. The surface size is identical across all; using `setUpAll`/`tearDownAll` would eliminate 9 redundant surface-size round-trips. Impact is small per test but cumulative on large CI shards.
  - **RESOLVED:** (assessed, infeasible) — `setSurfaceSize` asserts `inTest` inside the test binding, so it cannot run in `setUpAll`; a comment in `pumpShell` now records why the per-test call stays.
- [x] **[LOW]** `agent_listing_shell_test.dart` `pumpShell` helper: the `settle: false` path correctly avoids `pumpAndSettle` when a `CircularProgressIndicator` is on screen. This is good practice. The `settle: true` default with `pumpAndSettle()` in 9 tests is appropriate since the shell has no indefinitely-animating widgets once providers have resolved.
  **RESOLVED:** (confirmed observation, no change) — verified in source: `pumpShell` routes the loading case through `settle: false` (a bounded `pump()` + `pump(100ms)`) precisely because the spinner animates indefinitely, and uses `pumpAndSettle()` only for the resolved data/error/empty branches, which settle once providers resolve. This matches the async/performance policy; no change needed.

---

## Summary

- `agent_list_toolbar.dart` (808 lines) is the largest impl file in scope and has no dedicated test file — the highest-value gap.
- `agent_list_row.dart` (252 lines) and `soul_avatar.dart` also lack dedicated mirror tests; `SoulAvatar` is tested in the wrong file.
- `agent_list_filter_state_test.dart` already has 2 Glados properties and 5 unit tests — the pipeline and filter-state logic are well covered.
- `agent_listing_shell_test.dart` covers async branches, search, trailing slots, and axis reseat — but misses filter-axis and group-header rendering paths.
- No fake-time violations or inline mocks found.
- 2 speed improvements: share `setSurfaceSize` in setup hooks across both test files.

**Status:** all `[HIGH]`/`[MED]`/`[LOW]` items above are resolved. Since this review, dedicated mirror tests now exist for the toolbar, row, soul avatar, group section, and active-filters row; the shell's double axis reseat is covered; and the two confirmed-good observations (filter-state file size, `pumpShell` settle policy) were verified without change.
