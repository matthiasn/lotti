# TEST_REVIEW — `test/features/agents/ui/pending_wakes/`

Scope: impl files under `lib/features/agents/ui/pending_wakes/` (3 Dart files); test files in this subdir (3 test files).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../pending_wakes/agent_pending_wakes_page.dart` | 533 | Yes — `agent_pending_wakes_page_test.dart` | Borderline size; well tested overall |
| `lib/.../pending_wakes/pending_wake_view_model.dart` | 115 | Yes — `pending_wake_view_model_test.dart` | Well covered by Glados |
| `lib/.../pending_wakes/wake_countdown_ticker.dart` | 66 | Yes — `wake_countdown_ticker_test.dart` | Excellent unit coverage |
| `test/.../pending_wakes/agent_pending_wakes_page_test.dart` | 716 | ✓ | Large but well-organized; `taker.takeException()` workaround worth noting |
| `test/.../pending_wakes/pending_wake_view_model_test.dart` | 306 | ✓ | Excellent: pure Glados + 1 unit test |
| `test/.../pending_wakes/wake_countdown_ticker_test.dart` | 66 | ✓ | Thorough pure-function coverage |

---

## File size / split opportunities

- [x] **[LOW]** `agent_pending_wakes_page.dart` at 533 lines is borderline but internally well-structured (page widget, running-instances block, row builder, sort/filter/group factory methods). No split is needed at this size.
  **RESOLVED:** Confirmed non-actionable — the item itself concludes no split is needed; the file is cohesive (one page widget + private helpers for one feature) and impl files are out of this task's edit scope anyway.
- [x] **[LOW]** `agent_pending_wakes_page_test.dart` at 716 lines is the largest test file in this subdir. It is coherently organized: each `testWidgets` maps to a distinct page behavior. No split is needed, but extracting the recurring `_record(...)` factory and `pumpPage(...)` helper into a shared `test_utils.dart` in this subdir would save 45 lines if more tests are added later.
  **RESOLVED:** Confirmed non-actionable — `_record(...)` and `pumpPage(...)` have exactly one consumer (this single page test file in the subdir). Extracting them into a separate `test_utils.dart` used by one file adds indirection with no DRY benefit (DRY is about cross-file reuse). Kept file-local; no split.

---

## Test quality improvements

- [x] **[MED]** `agent_pending_wakes_page_test.dart` calls `tester.takeException()` five times (after group-by and sort popover interactions, lines 237, 469, 525, 587, 588). The comment explains it: a Wrap inside the toolbar can briefly emit a sub-pixel overflow after popover-driven re-layout. This is a visual artifact, not a real exception; it should be noted as a known cosmetic layout issue and tracked separately. Consider filing an issue so the exception can eventually be eliminated rather than silently swallowed in tests.
  **RESOLVED:** The prior HIGH item already removed four of the five drains (they never fired) — only one remains (the group-by popover). Instead of silently draining whatever exception exists, that single drain now captures the exception and asserts it contains `'overflowed'`, so a genuinely new/unrelated exception can no longer be swallowed; only the documented cosmetic RenderFlex overflow is tolerated. Filing an external issue isn't actionable inside this repo subtree (no `gh`/file creation outside the subdir); the investigation findings are recorded inline in the test comment.
- [x] **[LOW]** `pending_wake_view_model_test.dart` has only one static `test` in addition to the Glados property. A concrete example with `subjectSlot: both` (task + project slots simultaneously) would make the "task takes priority over project" rule readable without needing to trace through the generator — per README guidance on static examples as worked examples.
  **RESOLVED:** Added a static `test` ('resolves the title from the task slot when both task and project slots are set') with both `activeTaskId` and `activeProjectId` set to distinct titles ('Task Title Wins' vs 'Project Title Loses'); asserts the row title is the task title and the agent name drops to the subtitle — a readable worked example of the `_subjectEntryId` slot-priority rule.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `pending_wake_view_model_test.dart` already has a Glados property (`maps generated pending wake rows with title fallback semantics`) that explores 35-element lists across 180 runs. This is exactly the right target and is well-formed with `tags: 'glados'`. No new Glados work needed. **RESOLVED (no action needed):** the item itself confirms the Glados property is exactly the right target and well-formed.
- [x] **[LOW]** `wake_countdown_ticker.dart` `formatWakeCountdown` is a pure formatting function. A Glados property over `Duration` could assert: `result == '00:00'` iff `duration <= Duration.zero`, the hour cell appears iff `total >= 60 minutes`, and the result never exceeds 8 characters. The existing 7 unit tests cover all branches; Glados would add confidence on boundary-adjacent values but is low priority.
  **RESOLVED:** Added a Glados property ('formats any offset with stable structural invariants', `numRuns: 160`, `tags: 'glados'`) over an int offset in `[-3600, 400000]` seconds (spanning past/zero/sub-hour/hour-boundary/over-clamp). Asserts: `'00:00'` iff offset ≤ 0; length ≤ 8; the hour cell appears iff offset ≥ 3600; and the result matches `MM:SS` or `HH:MM:SS`. Imported `glados as glados` alongside the existing `flutter_test` import (matching the sibling `pending_wake_view_model_test.dart` pattern).

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `_RunningInstancesBlock` — the running-instances block at the top of the page — is tested with a 2-row case and a navigation tap. However, the elapsed-time format for durations >= 1 hour is not tested (the `01:30:07` format is only covered in `wake_countdown_ticker_test.dart`). Add a test with `startedAt` > 60 minutes ago to verify the `H:MM:SS` format appears in the running-instances block.
  **RESOLVED:** Added a test ('renders the elapsed pill in H:MM:SS once the run crosses one hour') with `startedAt` = now − 1h 30m 07s; asserts the running pill renders `01:30:07` and that the sub-hour `30:07` form is absent — proving the hour cell appears in the block, not just in the unit-tested formatter.
- [x] **[MED]** The `ongoingWakeRecordsProvider` error branch: the page calls `ongoingAsync.value ?? const []` which silently swallows errors. If the provider errors, the running-instances block is suppressed but no test verifies this is safe (no toast, no crash).
  **RESOLVED:** Added a test ('block is suppressed (no crash, no toast) when the provider errors') that overrides `ongoingWakeRecordsProvider` to throw `StateError`. Asserts: no running heading rendered, the listing still shows its empty-state copy (no error shell), no `commonError` toast text present, and `tester.takeException()` is null — confirming the `?? const []` degradation is safe.
- [x] **[LOW]** `agent_pending_wakes_page.dart` `_typeAxisId` filter chip: when only one type (`pending` or `scheduled`) is in the dataset the Filters button is hidden. Only the "hidden when same type" and "shown when mixed" branches are tested. The "filters are active and user clears all" path is not tested for this page (it is tested at the shell level in `agent_listing_shell_test.dart`).
  **RESOLVED:** Confirmed non-actionable — clear-all is a behavior of the shared `AgentListingShell` (its `onClear → _setFilters(clearAll())`), already covered in `test/features/agents/ui/listing/agent_listing_shell_test.dart` (`agentInstancesFilterClearAll` tests). The page only supplies axes/matcher; re-testing clear-all through the page would duplicate shell coverage.
- [x] **[LOW]** The "Kind" pill rendering per `AgentKinds` value: the test at line 83 verifies `kind: AgentKinds.projectAgent` shows the correct kind pill, but `taskAgent`, `dayAgent`, and `templateImprover` kinds are not individually rendered and asserted in a widget test.
  **RESOLVED:** Added a test ('renders the localized kind pill for every AgentKinds value') that pumps one row per kind (taskAgent, dayAgent, projectAgent, templateImprover) and asserts each kind's localized label renders exactly once, plus that the four labels are mutually distinct strings (so the assertions genuinely distinguish the kinds).

---

## Test execution speed opportunities

- [x] **[HIGH]** `agent_pending_wakes_page_test.dart` wraps nearly every test in `withClock(Clock(() => now), () async { ... })`. The inner lambdas are fine, but each test also calls `pumpPage` which internally calls `pumpAndSettle`. Five tests that exercise sort/filter popover interactions call `pumpAndSettle` 3–4 times each. The `tester.takeException()` calls after each popover interaction suggest layout is briefly unstable — if the overflow can be fixed in the impl, the `takeException()` drains can be removed, reducing the settle cycles by one per test. **RESOLVED (partially):** instrumented the drains — only one of the five ever fired (a transient 4.4px RenderFlex during the popover-driven re-layout; the settled frame is clean, so there is no steady-state impl bug to fix, and the binding exposes no creator chain for the mid-animation frame). The four dead `takeException()` calls are removed; the one live drain stays with an updated comment recording these findings. The popover settles themselves are genuinely animation-coupled (Wolt/showDialog transitions) and stay.
- [x] **[LOW]** `pumpPage` uses `tester.binding.setSurfaceSize(const Size(1600, 900))` for every test. This is the largest surface size in any test file in scope. Move to `setUpAll`/`tearDownAll` once the test count grows beyond ~10 tests.
  **RESOLVED:** The suggested move is infeasible: `TestWidgetsFlutterBinding.setSurfaceSize` asserts `inTest` (see flutter_test `binding.dart`), so it cannot be called from `setUpAll`/`tearDownAll` (which run outside any test) — doing so throws an assertion. The per-test call inside `pumpPage` with a paired `addTearDown(() => setSurfaceSize(null))` reset is the documented idiomatic pattern and is correct; left as-is.

---

## Summary

- No oversized impl or test files; `agent_pending_wakes_page.dart` at 533 lines is manageable.
- `pending_wake_view_model_test.dart` is a model file: strong Glados coverage, correct tagging.
- `wake_countdown_ticker_test.dart` is thorough: all 7 format branches covered by unit tests.
- Key gaps: elapsed >1h format in running-instances block, `ongoingWakeRecordsProvider` error path, and per-kind pill rendering.
- 5 `takeException()` calls suggest a recurring Wrap overflow in the toolbar after popover interaction — worth tracking as a separate issue.
- No fake-time violations or inline mock definitions found.
- 1 high-value speed improvement: eliminate the layout overflow to remove `takeException()` drains.
