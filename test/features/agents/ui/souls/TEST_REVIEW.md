# TEST_REVIEW — `test/features/agents/ui/souls/`

Scope: impl files under `lib/features/agents/ui/souls/` (2 Dart files); test files in this subdir (2 test files).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../souls/agent_souls_page.dart` | 108 | Yes — `agent_souls_page_test.dart` | Good size; a few minor behavior gaps |
| `lib/.../souls/soul_view_model.dart` | 50 | Yes — `soul_view_model_test.dart` | Tiny; well covered by Glados |
| `test/.../souls/agent_souls_page_test.dart` | 146 | ✓ | Good; meaningful assertions |
| `test/.../souls/soul_view_model_test.dart` | 191 | ✓ | Strong: Glados + 1 unit test |

---

## File size / split opportunities

- [x] **[LOW]** All files are small (50–191 lines). No split is needed.
  **RESOLVED:** Confirmed true — both impl files (50, 108 lines) and both test files (146→ now larger, 191 lines) are well under any split threshold. No action needed.

---

## Test quality improvements

- [x] **[LOW]** `agent_souls_page_test.dart` line 64: `expect(find.byType(SoulAvatar), findsOneWidget)` is a structural assertion. It is meaningful because `SoulAvatar` is a named widget type (not just `find.byType(Container)`), and its presence confirms the leading-avatar rendering path. No change needed.
  **RESOLVED:** Confirmed true — `SoulAvatar` is a domain-specific named widget; the assertion verifies the leading-avatar render path and sits alongside name/version content assertions in the same test. No change needed.
- [x] **[LOW]** The `pumpPage` helper creates a 1200×900 surface for each test and calls `setSurfaceSize` / `addTearDown` per test. With 5 tests sharing identical setup, extracting `setUpAll`/`tearDownAll` for surface size would be marginally cleaner.
  **RESOLVED:** (assessed, infeasible) — `setSurfaceSize` asserts `inTest` inside the test binding, so it cannot run in `setUpAll` (same finding as the sibling listing/instances reviews). Added an explanatory comment to `pumpPage` recording why the per-test call must stay.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `soul_view_model_test.dart` already contains a Glados property (`maps generated soul rows with nullable active versions`) with `ExploreConfig(numRuns: 160)` and `tags: 'glados'`. This is the correct target and is well-formed. No new Glados work needed. **RESOLVED (no action needed):** the item itself confirms the Glados property is the correct target and well-formed.
- [x] **[LOW]** `agent_souls_page.dart` group-by and sort axes: the page delegates both to `AgentListingShell`, so the filter/sort/group pipeline is fully covered by `agent_list_filter_state_test.dart`. No Glados work needed at the page level.
  **RESOLVED:** Confirmed true — `AgentSoulsPage.build` only adapts VMs into `AgentListRowData` and hands axes to `AgentListingShell`; the generative pipeline coverage lives in the shared filter-state test. The page-level axis *wiring* is now additionally covered by the new sort tests below.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** Sort behavior: `agent_souls_page_test.dart` does not test any sort axis. The page uses the shared `AgentListingShell` with presumably Name and/or Recent sort axes; verifying that switching Sort reorders soul rows would exercise the sort-axis wiring in the page adapter.
  **RESOLVED:** Added two `testWidgets` that open the Sort popover and switch to Recent / Oldest, then assert the rendered vertical order of two souls with distinct `updatedAt` values inverts accordingly (newest-first for Recent, earliest-first for Oldest). This exercises the page's `_sortRecent` / `_sortOldest` comparators end-to-end through the toolbar → `AgentListingShell` → pipeline.
- [x] **[LOW]** The `activeSoulVersionProvider` returning a `wrongEntity` type (not a `SoulDocumentVersionEntity`) is handled in `soul_view_model.dart` by returning `null` for the version. This is covered in the Glados test (`_GeneratedSoulVersionSlot.wrongEntity`). No gap here.
  **RESOLVED:** Confirmed true — `_toVm` uses `rawVersion?.mapOrNull(soulDocumentVersion: ...)`, which yields `null` for any non-version entity; the Glados `wrongEntity` slot feeds a template entity and `_expectedSoulVersion` asserts `null`. Path is covered.
- [x] **[LOW]** `agent_souls_page_test.dart` does not test the search-placeholder localization key (`agentSoulsSearchPlaceholder`) directly; it uses `ctx.messages.agentSoulsSearchPlaceholder` which is fine. Not a gap.
  **RESOLVED:** Confirmed true — the "search filters rows by name" test resolves the placeholder via `ctx.messages.agentSoulsSearchPlaceholder` and uses it to locate the field, which both keeps the test resilient to copy changes and implicitly verifies the placeholder is wired through. Asserting the literal English string would be brittle. No change needed.
- [x] **[LOW]** Multiple souls sorted by name: the test only pumps 1 or 2 souls at a time. A 3-soul test with mixed-case names would verify that the case-insensitive sort comparator works end-to-end through the page's axis configuration.
  **RESOLVED:** Added a `testWidgets` that pumps three souls with mixed-case names (`cherry`, `apple`, `Banana`) in non-alphabetical order and asserts the default Name sort renders them `apple < Banana < cherry` by vertical position — verifying the `toLowerCase()` comparator in `_buildSortAxes` end-to-end.

---

## Test execution speed opportunities

- [x] **[LOW]** `agent_souls_page_test.dart`: all 5 tests call `tester.binding.setSurfaceSize` individually. Consolidated with `setUpAll`/`tearDownAll` this would save 4 redundant surface-size operations. Impact is minimal at 5 tests.
  **RESOLVED:** (assessed, infeasible) — duplicate of the cleanup item above. `setSurfaceSize` asserts `inTest` and cannot run in `setUpAll`; the per-test call in `pumpPage` is required by the test binding. An explanatory comment now records this.
- [x] **[LOW]** All `pumpAndSettle` calls are appropriate: the page is backed by an async provider and has no indefinitely-animating widgets.
  **RESOLVED:** Confirmed true — the page resolves overridden `FutureProvider`s and the only animations are the search field's 120ms `AnimatedContainer` and the `showMenu` popup, all of which settle; no indefinitely-animating widgets exist. The new sort tests follow the same pattern as the established templates-page sort tests.

---

## Summary

- Smallest subdir in scope; both impl files are clean and well-proportioned.
- `soul_view_model_test.dart` is a reference-quality file: one Glados property + one unit test, correct tagging.
- `agent_souls_page_test.dart` covers the essential paths (render, navigation, empty state, search). Sort behavior is the main missing test.
- No oversized files, no fake-time violations, no inline mocks.
- No new Glados candidates.
- 1 minor speed improvement: consolidate `setSurfaceSize` into `setUpAll`.
