# TEST_REVIEW — `test/features/agents/ui/souls/`

Scope: impl files under `lib/features/agents/ui/souls/` (2 Dart files); test files in this subdir (2 test files).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../souls/agent_souls_page.dart` | 108 | Yes — `agent_souls_page_test.dart` | Good size; a few minor behavior gaps |
| `lib/.../souls/soul_view_model.dart` | 50 | Yes — `soul_view_model_test.dart` | Tiny; well covered by Glados |
| `test/.../souls/agent_souls_page_test.dart` | 146 | ✓ | Good; meaningful assertions |
| `test/.../souls/soul_view_model_test.dart` | 191 | ✓ | Strong: Glados + 1 unit test |

---

## File size / split opportunities

- [ ] **[LOW]** All files are small (50–191 lines). No split is needed.

---

## Test quality improvements

- [ ] **[LOW]** `agent_souls_page_test.dart` line 64: `expect(find.byType(SoulAvatar), findsOneWidget)` is a structural assertion. It is meaningful because `SoulAvatar` is a named widget type (not just `find.byType(Container)`), and its presence confirms the leading-avatar rendering path. No change needed.
- [ ] **[LOW]** The `pumpPage` helper creates a 1200×900 surface for each test and calls `setSurfaceSize` / `addTearDown` per test. With 5 tests sharing identical setup, extracting `setUpAll`/`tearDownAll` for surface size would be marginally cleaner.

---

## Generative (Glados) testing opportunities

- [ ] **[HIGH]** `soul_view_model_test.dart` already contains a Glados property (`maps generated soul rows with nullable active versions`) with `ExploreConfig(numRuns: 160)` and `tags: 'glados'`. This is the correct target and is well-formed. No new Glados work needed.
- [ ] **[LOW]** `agent_souls_page.dart` group-by and sort axes: the page delegates both to `AgentListingShell`, so the filter/sort/group pipeline is fully covered by `agent_list_filter_state_test.dart`. No Glados work needed at the page level.

---

## Coverage / missing-behavior gaps

- [ ] **[MED]** Sort behavior: `agent_souls_page_test.dart` does not test any sort axis. The page uses the shared `AgentListingShell` with presumably Name and/or Recent sort axes; verifying that switching Sort reorders soul rows would exercise the sort-axis wiring in the page adapter.
- [ ] **[LOW]** The `activeSoulVersionProvider` returning a `wrongEntity` type (not a `SoulDocumentVersionEntity`) is handled in `soul_view_model.dart` by returning `null` for the version. This is covered in the Glados test (`_GeneratedSoulVersionSlot.wrongEntity`). No gap here.
- [ ] **[LOW]** `agent_souls_page_test.dart` does not test the search-placeholder localization key (`agentSoulsSearchPlaceholder`) directly; it uses `ctx.messages.agentSoulsSearchPlaceholder` which is fine. Not a gap.
- [ ] **[LOW]** Multiple souls sorted by name: the test only pumps 1 or 2 souls at a time. A 3-soul test with mixed-case names would verify that the case-insensitive sort comparator works end-to-end through the page's axis configuration.

---

## Test execution speed opportunities

- [ ] **[LOW]** `agent_souls_page_test.dart`: all 5 tests call `tester.binding.setSurfaceSize` individually. Consolidated with `setUpAll`/`tearDownAll` this would save 4 redundant surface-size operations. Impact is minimal at 5 tests.
- [ ] **[LOW]** All `pumpAndSettle` calls are appropriate: the page is backed by an async provider and has no indefinitely-animating widgets.

---

## Summary

- Smallest subdir in scope; both impl files are clean and well-proportioned.
- `soul_view_model_test.dart` is a reference-quality file: one Glados property + one unit test, correct tagging.
- `agent_souls_page_test.dart` covers the essential paths (render, navigation, empty state, search). Sort behavior is the main missing test.
- No oversized files, no fake-time violations, no inline mocks.
- No new Glados candidates.
- 1 minor speed improvement: consolidate `setSurfaceSize` into `setUpAll`.
