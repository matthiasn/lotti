# TEST_REVIEW — `test/features/agents/ui/templates/`

Scope: impl files under `lib/features/agents/ui/templates/` (2 Dart files); test files in this subdir (2 test files).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../templates/agent_templates_page.dart` | 215 | Yes — `agent_templates_page_test.dart` | Good size; well covered |
| `lib/.../templates/template_view_model.dart` | 98 | Yes — `template_view_model_test.dart` | Well covered by Glados |
| `test/.../templates/agent_templates_page_test.dart` | 370 | ✓ | Good quality; most page behaviors covered |
| `test/.../templates/template_view_model_test.dart` | 255 | ✓ | Strong: Glados + 1 unit test |

---

## File size / split opportunities

- [x] **[LOW]** All files are 98–370 lines. No split is needed.
  **RESOLVED:** Confirmed true — all impl/test files are well within size limits; no split warranted.

---

## Test quality improvements

- [x] **[LOW]** `agent_templates_page_test.dart` uses `find.textContaining('Laura', findRichText: true)` / `find.textContaining('gemini-3-pro', findRichText: true)` to work around the `Text.rich` subtitle. This is correct and deliberate. The comment on line 63 explains it. No change needed.
  **RESOLVED:** Confirmed true — `agent_list_row.dart` renders title+subtitle via `Text.rich`, so `findRichText: true` is the correct matcher. No change needed.
- [x] **[LOW]** `agent_templates_page_test.dart` line 88–93: the pending-review test asserts `find.byWidgetPredicate(w => w is Icon && w.color == AgentPalette.purple)`. This is a structural / color assertion rather than a content assertion; it verifies the leading-icon color binding is wired to `AgentPalette.purple`. It is meaningful and not a weak assertion.
  **RESOLVED:** Confirmed true — the impl binds `color: vm.hasPendingReview ? AgentPalette.purple : null`, so the color predicate is a meaningful behavior assertion. No change needed.
- [x] **[LOW]** `pumpPage` calls `setSurfaceSize` per test (13 tests). Consolidate into `setUpAll`/`tearDownAll`.
  **RESOLVED (not actionable):** `setSurfaceSize` asserts `inTest` (flutter_test 3.44.1 `binding.dart:1434`), which is false inside `setUpAll`/`tearDownAll`, so a hoist would throw. Sibling tests (`agent_souls_page_test.dart`, `agent_listing_shell_test.dart`) document the same limitation. Added the explanatory comment to `pumpPage` instead.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `template_view_model_test.dart` already contains a Glados property (`maps generated template rows with versions and pending flags`) with `ExploreConfig(numRuns: 160)` and `tags: 'glados'`. Correct and complete. No new Glados work needed. **RESOLVED (no action needed):** the item itself confirms the Glados property is correct and complete.
- [x] **[LOW]** The name-sort tiebreaker (`a.id.compareTo(b.id)`) in `agent_templates_page.dart` is exercised by the static test at line 332. A Glados property over `List<(String id, String displayName)>` could verify the full sort invariant, but the existing static test already covers the tiebreaker; low value.
  **RESOLVED (no action):** Confirmed true — the `name sort falls back to id when two templates share a display name` test exercises the tiebreaker. The comparator is a private function in the page (`_buildSortAxes`), not separately importable; a Glados property would re-test trivial `String.compareTo` semantics for low value. Existing coverage is sufficient.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** The `modelId` subtitle in the `Text.rich` row: the test at line 48 verifies the model ID appears. However, a template where `modelId` is `null` or empty is not tested. If the impl renders the bullet separator only when `modelId` is non-empty, that conditional branch is untested.
  **RESOLVED:** Added `template with an empty modelId still renders its title`. Note: `TemplateVm.modelId` is non-nullable and the page always passes `subtitle: vm.modelId`, so the bullet branch in `agent_list_row.dart` keys off `subtitle != null` (always true here), not off emptiness — there is no modelId-conditional branch in the page itself. The new test verifies an empty modelId does not break title/row rendering.
- [x] **[MED]** The `activeTemplateVersionProvider` returning a `wrongEntity` type: covered by the Glados test in `template_view_model_test.dart` (`_GeneratedTemplateVersionSlot.wrongEntity`), which verifies `activeVersion` is `null`. Not a gap in the VM; but the *widget* rendering of `null` version (no `vN` pill) is only verified by the Glados scenario, not an explicit widget test.
  **RESOLVED:** Added `template without an active version renders no version pill`, which overrides `activeTemplateVersionProvider` to `null` (`versionFor: (_) => null`) and asserts the row renders while no `vN` meta text appears.
- [x] **[LOW]** `templateImprover` kind: the Kind filter test covers `taskAgent` and `projectAgent`. The `templateImprover` kind is not exercised as a row in the widget test. If the page's `_matchRow` has a branch for `templateImprover`, it is untested at the widget level.
  **RESOLVED:** Added `templateImprover kind renders its localized kind pill` (verifies the kind pill label) and `templateImprover row is matched by its Kind filter option` (selecting the Template Improver filter keeps the improver row and hides the task row, exercising `_matchRow` for that kind).
- [x] **[LOW]** The `templatesPendingReviewProvider` error path (provider errors → page should not crash) is not tested. The page uses `ref.watch` so a provider error would surface as an `AsyncError`; the shell renders `commonError` in that case. Not tested for this page specifically.
  **RESOLVED:** Added `templates provider error renders the common error copy, not a crash`, which makes `agentTemplatesProvider` throw and asserts the shell shows `commonError` (and not the empty-filtered copy). Note: `templatesPendingReviewProvider` errors specifically cannot fail the page — the VM treats its error/loading as "nothing pending" (`.value ?? const <String>{}`), so the realistic page-error path is `agentTemplatesProvider` failing, which `agentTemplateRowVmsProvider.whenData` propagates.

---

## Test execution speed opportunities

- [x] **[MED]** `agent_templates_page_test.dart` has 13 `testWidgets` calls and calls `tester.binding.setSurfaceSize(const Size(1200, 900))` in every `pumpPage` invocation. Consolidating with `setUpAll`/`tearDownAll` removes 12 redundant surface-size operations. Impact is the most significant of any subdir in scope (13 tests).
  **RESOLVED (not actionable):** Same as the LOW item above — `setSurfaceSize` asserts `inTest` and cannot run in `setUpAll`/`tearDownAll` (would throw). Sibling agent page/shell tests document the same constraint. The per-test call is required, not redundant. Added an explanatory comment to `pumpPage`.
- [x] **[LOW]** Sort tests (lines 256–330) open the sort popover via `tester.tap(find.text(ctx.messages.agentInstancesSortName))` and call `pumpAndSettle` twice. This is appropriate for popover animations. No change needed.
  **RESOLVED:** Confirmed true — popover open/close needs `pumpAndSettle` for the menu animation; the calls have no fixed duration and settle quickly. No change needed.

---

## Summary

- No oversized impl or test files.
- `template_view_model_test.dart` is reference quality: Glados property + 1 unit test, correct tagging.
- `agent_templates_page_test.dart` is the most comprehensive page test in scope (13 scenarios covering render, filter, group, sort, navigation, and name-sort tiebreaker).
- Key gaps: `null` modelId rendering branch, `templateImprover` kind in widget tests, provider error path.
- No fake-time violations or inline mocks found.
- 1 speed improvement: consolidate `setSurfaceSize` into `setUpAll` across 13 tests.
