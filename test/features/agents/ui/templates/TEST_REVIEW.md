# TEST_REVIEW — `test/features/agents/ui/templates/`

Scope: impl files under `lib/features/agents/ui/templates/` (2 Dart files); test files in this subdir (2 test files).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../templates/agent_templates_page.dart` | 215 | Yes — `agent_templates_page_test.dart` | Good size; well covered |
| `lib/.../templates/template_view_model.dart` | 98 | Yes — `template_view_model_test.dart` | Well covered by Glados |
| `test/.../templates/agent_templates_page_test.dart` | 370 | ✓ | Good quality; most page behaviors covered |
| `test/.../templates/template_view_model_test.dart` | 255 | ✓ | Strong: Glados + 1 unit test |

---

## File size / split opportunities

- [ ] **[LOW]** All files are 98–370 lines. No split is needed.

---

## Test quality improvements

- [ ] **[LOW]** `agent_templates_page_test.dart` uses `find.textContaining('Laura', findRichText: true)` / `find.textContaining('gemini-3-pro', findRichText: true)` to work around the `Text.rich` subtitle. This is correct and deliberate. The comment on line 63 explains it. No change needed.
- [ ] **[LOW]** `agent_templates_page_test.dart` line 88–93: the pending-review test asserts `find.byWidgetPredicate(w => w is Icon && w.color == AgentPalette.purple)`. This is a structural / color assertion rather than a content assertion; it verifies the leading-icon color binding is wired to `AgentPalette.purple`. It is meaningful and not a weak assertion.
- [ ] **[LOW]** `pumpPage` calls `setSurfaceSize` per test (13 tests). Consolidate into `setUpAll`/`tearDownAll`.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `template_view_model_test.dart` already contains a Glados property (`maps generated template rows with versions and pending flags`) with `ExploreConfig(numRuns: 160)` and `tags: 'glados'`. Correct and complete. No new Glados work needed. **RESOLVED (no action needed):** the item itself confirms the Glados property is correct and complete.
- [ ] **[LOW]** The name-sort tiebreaker (`a.id.compareTo(b.id)`) in `agent_templates_page.dart` is exercised by the static test at line 332. A Glados property over `List<(String id, String displayName)>` could verify the full sort invariant, but the existing static test already covers the tiebreaker; low value.

---

## Coverage / missing-behavior gaps

- [ ] **[MED]** The `modelId` subtitle in the `Text.rich` row: the test at line 48 verifies the model ID appears. However, a template where `modelId` is `null` or empty is not tested. If the impl renders the bullet separator only when `modelId` is non-empty, that conditional branch is untested.
- [ ] **[MED]** The `activeTemplateVersionProvider` returning a `wrongEntity` type: covered by the Glados test in `template_view_model_test.dart` (`_GeneratedTemplateVersionSlot.wrongEntity`), which verifies `activeVersion` is `null`. Not a gap in the VM; but the *widget* rendering of `null` version (no `vN` pill) is only verified by the Glados scenario, not an explicit widget test.
- [ ] **[LOW]** `templateImprover` kind: the Kind filter test covers `taskAgent` and `projectAgent`. The `templateImprover` kind is not exercised as a row in the widget test. If the page's `_matchRow` has a branch for `templateImprover`, it is untested at the widget level.
- [ ] **[LOW]** The `templatesPendingReviewProvider` error path (provider errors → page should not crash) is not tested. The page uses `ref.watch` so a provider error would surface as an `AsyncError`; the shell renders `commonError` in that case. Not tested for this page specifically.

---

## Test execution speed opportunities

- [ ] **[MED]** `agent_templates_page_test.dart` has 13 `testWidgets` calls and calls `tester.binding.setSurfaceSize(const Size(1200, 900))` in every `pumpPage` invocation. Consolidating with `setUpAll`/`tearDownAll` removes 12 redundant surface-size operations. Impact is the most significant of any subdir in scope (13 tests).
- [ ] **[LOW]** Sort tests (lines 256–330) open the sort popover via `tester.tap(find.text(ctx.messages.agentInstancesSortName))` and call `pumpAndSettle` twice. This is appropriate for popover animations. No change needed.

---

## Summary

- No oversized impl or test files.
- `template_view_model_test.dart` is reference quality: Glados property + 1 unit test, correct tagging.
- `agent_templates_page_test.dart` is the most comprehensive page test in scope (13 scenarios covering render, filter, group, sort, navigation, and name-sort tiebreaker).
- Key gaps: `null` modelId rendering branch, `templateImprover` kind in widget tests, provider error path.
- No fake-time violations or inline mocks found.
- 1 speed improvement: consolidate `setSurfaceSize` into `setUpAll` across 13 tests.
