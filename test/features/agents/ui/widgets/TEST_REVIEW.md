# TEST_REVIEW — `test/features/agents/ui/widgets/`

Scope: impl files under `lib/features/agents/ui/widgets/` (1 Dart file); test files in this subdir (1 test file).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../widgets/agent_markdown_view.dart` | 75 | Yes — `agent_markdown_view_test.dart` | Small and well-covered |
| `test/.../widgets/agent_markdown_view_test.dart` | 76 | ✓ | Good quality; 3 meaningful tests |

---

## File size / split opportunities

- [x] **[LOW]** Both files are 75–76 lines. No split needed.
  **RESOLVED:** Confirmed true. The impl is 75 lines and well within size limits; the test file grew with added coverage but remains a single cohesive file for the single impl file (one-test-file-per-source rule). No split warranted.

---

## Test quality improvements

- [x] **[LOW]** `agent_markdown_view_test.dart` test 1 (`renders GptMarkdown with provided text`): asserts `gptMarkdown.data == markdownText`. This is a direct data-binding check on the widget property — a valid and meaningful assertion. Not a smoke test.
  **RESOLVED:** Confirmed true; assertion is meaningful (data-binding) and kept. Pump/setup boilerplate was extracted into a `_pumpView` helper for DRY-ness.
- [x] **[LOW]** Test 2 (`applies custom style to body text when provided`) and test 3 (`falls back to design system body.bodySmall by default`): both read the `DefaultTextStyle` from the `GptMarkdown` element's context and compare numeric properties (`fontSize`, `fontWeight`, `fontFamily`). This correctly tests the design-token wiring path. No change needed.
  **RESOLVED:** Confirmed true. Both tests kept; the style-reading boilerplate was factored into a `_resolvedBodyStyle` helper.
- [x] **[LOW]** Untested: the `onLinkTap` / `linkBuilder` callbacks in `AgentMarkdownView.build` are hardcoded to `handleMarkdownLinkTap` / `buildMarkdownLink`. Whether link taps are routed to the correct handler is not tested. A test that finds a `[link](url)` in the rendered markdown and taps it would exercise this path, but it depends on `GptMarkdown`'s link rendering internals. Low priority.
  **RESOLVED:** Added test `wires GptMarkdown link callbacks to the shared markdown handlers`. Rather than depend on `GptMarkdown`'s internal link rendering (or brittle tear-off identity comparison across function-signature widening), it invokes the wired `gptMarkdown.onLinkTap!(...)` and the widget returned by `gptMarkdown.linkBuilder!(...)` behaviorally and asserts both route an internal `/tasks/...` URL through a `RecordingMockNavService` (verifying `handleMarkdownLinkTap` and `buildMarkdownLink` are actually wired).
- [x] **[LOW]** Untested: the checkbox theming block (lines 40–60 in the impl) — custom `CheckboxTheme` with `materialTapTargetSize`, `visualDensity`, `fillColor`, `overlayColor`, `mouseCursor` — is not directly asserted. A test pumping markdown with `- [x] item` and asserting the checkbox is non-interactive (no hover color, non-standard tap target) would cover this, but it is a cosmetic concern.
  **RESOLVED:** Added test `injects a compact, non-interactive checkbox theme from tokens`. It reads `Theme.of(gptContext).checkboxTheme` and asserts `materialTapTargetSize == shrinkWrap`, `visualDensity == compact`, the `side` (color from `text.lowEmphasis`, width 1.5), `fillColor` resolving to transparent (unselected) / `interactive.enabled` (selected), `overlayColor` transparent on hover, and `mouseCursor == SystemMouseCursors.basic` — covering every property of the block against design tokens.

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** `AgentMarkdownView` is a pure rendering widget with no state or logic beyond token delegation. Glados is not appropriate here.
  **RESOLVED:** Confirmed true. The widget has no pure data-transforming logic to generate over; it maps fixed design tokens onto `GptMarkdownThemeData`/`CheckboxThemeData`. The branching that exists (style override, theme dedup, heading mapping, checkbox states) is covered by example-based widget tests. No Glados candidate.

---

## Coverage / missing-behavior gaps

- [x] **[LOW]** Heading token mapping (`h1`→`heading.heading3`, `h2`→`subtitle.subtitle1`, `h3`→`subtitle.subtitle2`) is not tested. A test pumping `# H1\n## H2\n### H3` and asserting the `TextStyle` of each heading matches the corresponding design token would cover the heading branch completely.
  **RESOLVED:** Added two tests. `maps heading styles to the design system heading/subtitle tokens` reads the injected `GptMarkdownThemeData` (via `GptMarkdownTheme.of(gptContext)`) and asserts `h1`/`h2`/`h3` fontSize/fontWeight/fontFamily equal `heading.heading3`/`subtitle.subtitle1`/`subtitle.subtitle2`, plus that the three sizes are strictly decreasing (proving each branch maps to its own token). `maps h4/h5/h6 to body-derived and caption tokens` covers `h4` (bodySmall size + semiBold weight), `h5` (plain body), and `h6` (caption). Asserting on the injected theme is more robust than digging the rendered `TextSpan` out of `GptMarkdown`'s internal `HTag` rendering, while still verifying the exact value `GptMarkdown` consumes.
- [x] **[LOW]** The `GptMarkdownThemeData` extension injection (`theme.extensions.values.where(e => e is! GptMarkdownThemeData)`) is not tested. In theory, if the host `Theme` already contains a `GptMarkdownThemeData`, the impl replaces it. No test verifies the deduplication.
  **RESOLVED:** Added test `replaces a pre-existing GptMarkdownThemeData from the host theme`. It pumps with a host theme that already carries a sentinel `GptMarkdownThemeData` (`linkColor: purple`, `h1.fontSize: 999`) alongside `DsTokens`, then asserts the resolved markdown theme is the widget's own (sentinel link color/h1 size gone), exactly one `GptMarkdownThemeData` survives in the resolved extensions, and the host `DsTokens` is preserved across injection. Note: Flutter keys theme extensions by `runtimeType` (last-wins), so the `where(...)` filter is defensive; the test verifies the observable intent (the widget's theme wins, host's DsTokens kept).

---

## Test execution speed opportunities

- [x] **[LOW]** Three `testWidgets` + `setUp`/`tearDown` for GetIt. Since `AgentMarkdownView` doesn't read GetIt directly, the `setUpTestGetIt` call may be unnecessary — but removing it would require verifying no transitive dependency calls GetIt. Low priority investigation.
  **RESOLVED:** `setUpTestGetIt`/`tearDownTestGetIt` are now load-bearing and kept. The new link-callback test re-initialises GetIt with a `RecordingMockNavService` so that `handleMarkdownLinkTap` can resolve `getIt<NavService>()`; `tearDownTestGetIt` is required to reset between tests. The base `setUp` is also the cheapest way to guarantee a clean container for the theme-only tests.
- [x] **[LOW]** All tests use `tester.pump()` (not `pumpAndSettle`) which is correct for a stateless widget with no animations.
  **RESOLVED:** Confirmed true and maintained. Every test (including the new ones) uses bounded `tester.pump()` only — no `pumpAndSettle`, no real timers/delays.

---

## Summary

- Smallest subdir in scope by file count (1 impl, 1 test file).
- All review items resolved (2026-06-08 batch).
- Tests now cover: body style (custom + token fallback), full heading mapping (h1–h6) against design tokens, the checkbox theme block (all properties), the `GptMarkdownThemeData` host-dedup path, the link color, and behavioral wiring of `onLinkTap`/`linkBuilder` to the shared markdown handlers (internal routes via `RecordingMockNavService`).
- DRY: pump (`_pumpView`) and style/theme readers (`_resolvedBodyStyle`, `_resolvedMarkdownTheme`) extracted as file-level helpers.
- No Glados candidates, no fake-time violations, no inline mocks (uses central `RecordingMockNavService`).
- No speed improvements needed — bounded `pump()` only.
