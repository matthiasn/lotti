# TEST_REVIEW — `test/features/agents/ui/widgets/`

Scope: impl files under `lib/features/agents/ui/widgets/` (1 Dart file); test files in this subdir (1 test file).

Generated by automated test-quality review (static analysis).

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../widgets/agent_markdown_view.dart` | 75 | Yes — `agent_markdown_view_test.dart` | Small and well-covered |
| `test/.../widgets/agent_markdown_view_test.dart` | 76 | ✓ | Good quality; 3 meaningful tests |

---

## File size / split opportunities

- [ ] **[LOW]** Both files are 75–76 lines. No split needed.

---

## Test quality improvements

- [ ] **[LOW]** `agent_markdown_view_test.dart` test 1 (`renders GptMarkdown with provided text`): asserts `gptMarkdown.data == markdownText`. This is a direct data-binding check on the widget property — a valid and meaningful assertion. Not a smoke test.
- [ ] **[LOW]** Test 2 (`applies custom style to body text when provided`) and test 3 (`falls back to design system body.bodySmall by default`): both read the `DefaultTextStyle` from the `GptMarkdown` element's context and compare numeric properties (`fontSize`, `fontWeight`, `fontFamily`). This correctly tests the design-token wiring path. No change needed.
- [ ] **[LOW]** Untested: the `onLinkTap` / `linkBuilder` callbacks in `AgentMarkdownView.build` are hardcoded to `handleMarkdownLinkTap` / `buildMarkdownLink`. Whether link taps are routed to the correct handler is not tested. A test that finds a `[link](url)` in the rendered markdown and taps it would exercise this path, but it depends on `GptMarkdown`'s link rendering internals. Low priority.
- [ ] **[LOW]** Untested: the checkbox theming block (lines 40–60 in the impl) — custom `CheckboxTheme` with `materialTapTargetSize`, `visualDensity`, `fillColor`, `overlayColor`, `mouseCursor` — is not directly asserted. A test pumping markdown with `- [x] item` and asserting the checkbox is non-interactive (no hover color, non-standard tap target) would cover this, but it is a cosmetic concern.

---

## Generative (Glados) testing opportunities

- [ ] **[LOW]** `AgentMarkdownView` is a pure rendering widget with no state or logic beyond token delegation. Glados is not appropriate here.

---

## Coverage / missing-behavior gaps

- [ ] **[LOW]** Heading token mapping (`h1`→`heading.heading3`, `h2`→`subtitle.subtitle1`, `h3`→`subtitle.subtitle2`) is not tested. A test pumping `# H1\n## H2\n### H3` and asserting the `TextStyle` of each heading matches the corresponding design token would cover the heading branch completely.
- [ ] **[LOW]** The `GptMarkdownThemeData` extension injection (`theme.extensions.values.where(e => e is! GptMarkdownThemeData)`) is not tested. In theory, if the host `Theme` already contains a `GptMarkdownThemeData`, the impl replaces it. No test verifies the deduplication.

---

## Test execution speed opportunities

- [ ] **[LOW]** Three `testWidgets` + `setUp`/`tearDown` for GetIt. Since `AgentMarkdownView` doesn't read GetIt directly, the `setUpTestGetIt` call may be unnecessary — but removing it would require verifying no transitive dependency calls GetIt. Low priority investigation.
- [ ] **[LOW]** All tests use `tester.pump()` (not `pumpAndSettle`) which is correct for a stateless widget with no animations.

---

## Summary

- Smallest subdir in scope by file count (1 impl, 1 test file).
- Three meaningful tests covering the primary style paths. No weak assertions.
- Minor gaps: heading token mapping, link-tap handler, checkbox theme verification.
- No Glados candidates, no fake-time violations, no inline mocks.
- No speed improvements needed.
