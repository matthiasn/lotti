# Part 1 of 2 — ai/repository inference/generation repos

**Scope:** Inference, generation, and embedding repositories — impl files in
`lib/features/ai/repository/` and their tests in
`test/features/ai/repository/`. Covers: `unified_ai_inference_repository`,
`unified_ai_inference_repository_labels`, `cloud_inference_repository`,
`cloud_inference_wrapper`, `gemini_inference_repository`, `gemini_utils`,
`gemini_thinking_config`, `gemini_stream_parser`, `ollama_inference_repository`,
`ollama_model_management`, `ollama_embedding_repository`,
`mistral_inference_repository`, `voxtral_inference_repository`,
`dashscope_inference_repository`, `inference_repository_interface`, and
`tool_call_accumulator`.

_Generated by automated test-quality review (static analysis)_

---

## Per-file table

| File | Impl lines | Test file | Test lines | Has test? | Top issue |
|---|---|---|---|---|---|
| `unified_ai_inference_repository.dart` | 1 382 | `unified_ai_inference_repository_test.dart` | 7 306 | Yes | **[HIGH]** Impl is the single largest file; test is 7 306 lines with 92 `test()` calls, 10+ inline `getIt` manipulation blocks scattered through test body, duplicate mock classes, and no `_TestBench`; both files need splitting |
| `unified_ai_inference_repository.dart` (labels) | — | `unified_ai_inference_repository_labels_test.dart` | 226 | Yes | **[MED]** `MockJournalRepository2` / `MockChecklistRepository2` are inline duplicates of centralized mocks; `getIt.reset()` in tearDown instead of `tearDownTestGetIt()` |
| `cloud_inference_repository.dart` | 727 | `cloud_inference_repository_test.dart` | 4 404 | Yes | **[HIGH]** 10 separate `setUp()` / group scaffolding blocks, each building a new `ProviderContainer` and `MockHttpClient`; 88 `test()` calls with heavily duplicated boilerplate that could be a shared `_TestBench` |
| `cloud_inference_wrapper.dart` | 113 | `cloud_inference_wrapper_test.dart` | 568 | Yes | **[LOW]** `FakeChatCompletionTool` inline; centralise with mocks.dart |
| `gemini_inference_repository.dart` | 1 115 | `gemini_inference_repository_test.dart` | 3 288 | Yes | **[HIGH]** Impl at 1 115 lines (clear split seam at multi-turn); 60 tests; `_FakeStreamClient`/`_RoutingFakeClient`/`_RoutingErrorClient`/`_RoutingCountingClient` four inline fake HTTP clients; no `_TestBench` despite heavily repeated provider fixture setup |
| `gemini_utils.dart` | 501 | `gemini_utils_test.dart` | 1 160 | Yes | **[MED]** One Glados property test (numRuns: 180) at line 1147 — well-covered; `buildMultiTurnRequestBody` message-conversion logic has no Glados coverage |
| `gemini_thinking_config.dart` | 124 | `gemini_thinking_config_test.dart` | 513 | Yes | **[LOW]** Good Glados coverage (numRuns: 160); no gaps |
| `gemini_stream_parser.dart` | 128 | `gemini_stream_parser_test.dart` | 168 | Yes | **[LOW]** Good Glados chunk-boundary property; minor gap: `maxBufferSize` trim alignment not property-tested |
| `ollama_inference_repository.dart` | 852 | `ollama_inference_repository_test.dart` | 3 063 | Yes | **[HIGH]** 852-line impl file (clear split: text-gen vs model-management vs embedding); 60 tests; `FakeRequest` inline; per-test mock response setup not extracted into helper |
| `ollama_inference_repository.dart` (mgmt) | — | `ollama_model_management_test.dart` | 723 | Yes (split) | **[MED]** `MockStreamedResponse`, `MockInstallProgressResponse`, `MockStreamedResponseWithError` inline; `fakeAsync`/retry paths are well handled via `retry_fake_time.dart` |
| `ollama_embedding_repository.dart` | 163 | `ollama_embedding_repository_test.dart` | 496 | Yes | **[LOW]** Good coverage; no Glados on float-vector extraction logic |
| `mistral_inference_repository.dart` | 542 | `mistral_inference_repository_test.dart` | 1 793 | Yes | **[MED]** `FakeRequest` defined inline (exists in mocks.dart); SSE-parsing helpers (`createSseStreamedResponse`, `createSseChunkEvent`) duplicated verbatim in voxtral test — extract to shared `test_utils.dart` |
| `voxtral_inference_repository.dart` | 502 | `voxtral_inference_repository_test.dart` | 1 607 | Yes | **[MED]** SSE response helper duplicated from mistral test; `FakeRequest` inline |
| `dashscope_inference_repository.dart` | 267 | `dashscope_inference_repository_test.dart` | 926 | Yes | **[MED]** DashScope SSE-framing parser (`buildSseResponse`) is pure logic, no Glados coverage; `textEvent`/`imageEvent` builder helpers are file-local but could serve multi-file tests |
| `inference_repository_interface.dart` | 72 | `inference_repository_interface_test.dart` | 214 | Yes | **[LOW]** Concrete `_RecordingInferenceRepository` is a good pattern; coverage is complete for the interface contract |
| `tool_call_accumulator.dart` | 234 | `tool_call_accumulator_test.dart` | 839 | Yes | **[LOW]** Three Glados property tests with `_AccumulatorBench` are excellent; `tags: 'glados'` present on all three; no issues |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/ai/repository/unified_ai_inference_repository.dart` — 1 382 lines, far over the 1 000-line soft limit. Clear split seams: **RESOLVED (adapted):** all three named seams extracted as same-library extension part files — `unified_ai_prompt_context.dart`, `unified_ai_tool_call_processor.dart`, `unified_ai_response_processor.dart`; core file 536 lines (from 1382). Parts keep the source a single library, so the mirror test stays one file. All 103 mirror tests pass.
  1. **Prompt-routing / context-filtering** (`getActivePromptsForContext`, `_isPromptActiveForEntity`, lines 99–196) → extract to `prompt_context_filter.dart`.
  2. **Tool-call processing** (`processToolCalls` and all its inner branches, lines 845–1265) → extract to `inference_tool_call_processor.dart`.
  3. **Post-processing / persistence** (`_processCompleteResponse`, `_handlePostProcessing`, lines 576–843) → extract to `inference_response_processor.dart`.
  The corresponding test (`unified_ai_inference_repository_test.dart`, 7 306 lines) would naturally mirror-split into 3–4 test files.

- [x] **[HIGH]** `lib/features/ai/repository/gemini_inference_repository.dart` — 1 115 lines. Clear split seam at multi-turn (`generateTextWithMessages` and its `buildMultiTurnRequestBody` support; approximately lines 400–700 of the impl) vs. single-turn streaming. Extract multi-turn path to `gemini_multiturn_inference_repository.dart`. **RESOLVED (adapted):** the multi-turn path moved to a `gemini_multiturn_inference.dart` extension part file (256 ln) and the image-generation path to `gemini_image_generation.dart` (233 ln); the shared `_processGeminiPayload` static stays on the class (unqualified class-internal calls don't resolve extension members). Core file is 639 lines. All 62 mirror tests pass.

- [x] **[HIGH]** `lib/features/ai/repository/ollama_inference_repository.dart` — 852 lines. Three distinct concern groups: **RESOLVED (adapted):** image analysis moved to an `ollama_image_analysis.dart` extension part file and model management + warm-up to `ollama_model_management.dart` (matching the existing standalone mirror test's name); core file 643 lines (from 852). All 74 tests across both mirrors pass.
  1. Text generation (`generateText` / `_generateTextWithChat`, lines ~64–500).
  2. Image analysis (`generateWithImages`, lines ~500–650).
  3. Model management + warm-up (`installModel`, `isModelInstalled`, `warmUpModel`, lines ~650–852).
  `ollama_model_management_test.dart` already tests group 3 separately; split the impl to match.

- [x] **[HIGH]** `test/features/ai/repository/unified_ai_inference_repository_test.dart` — 7 306 lines / 92 tests. Mirror-split opportunity: move `getActivePromptsForContext` tests (~lines 370–1149), `processToolCalls` tests (~lines 2600–6900), and the runInference/post-processing tests into separate files when the impl is split. **RESOLVED (assessed, no change):** the impl split used part files, so the source remains a single library and the one-test-file-per-source rule keeps the mirror test as a single file; its group structure already mirrors the new part boundaries.

- [x] **[HIGH]** `test/features/ai/repository/cloud_inference_repository_test.dart` — 4 404 lines / 88 tests across 10 separate `group`+`setUp` blocks. Each group builds its own `ProviderContainer`; extract provider-routing tests for Gemini, Ollama, Mistral, and image-generation into separate test files to track the impl's routing switch. **RESOLVED (assessed, no change):** `cloud_inference_repository.dart` is one source file — splitting its tests into per-provider files would violate the one-test-file-per-source rule; the 10 routing groups organise the file internally.

- [x] **[MED]** `test/features/ai/repository/gemini_inference_repository_test.dart` — 3 288 lines / 60 tests. Given `gemini_inference_repository.dart` will split (see above), the test file should split to match. **RESOLVED:** (assessed, no change) the impl split used `part` files (one library), so this remains the single mirror under the one-test-file-per-source rule.

- [x] **[MED]** `test/features/ai/repository/ollama_inference_repository_test.dart` — 3 063 lines / 60 tests. Already exists alongside `ollama_model_management_test.dart`; once the impl splits the text-gen vs. image-analysis seam, add `ollama_image_analysis_test.dart` to mirror. **RESOLVED:** (assessed, no change) the impl split used `part` files; the existing pair (repository mirror + `ollama_model_management_test.dart` per-part mirror) already matches the library layout.

---

## Test quality improvements

- [x] **[HIGH]** `unified_ai_inference_repository_test.dart` — Inline `getIt` manipulation is sprinkled across multiple individual tests (lines 5386–5391, 5408–5415, 6755–6765, 6780, 6871–6879). These contradict the guideline "Never write inline `getIt.isRegistered` / `getIt.unregister` / `getIt.registerSingleton` boilerplate" and will cause suite contamination in `very_good test` batches. All `DomainLogger` and `AgentDatabase` registration should move to `setUp`/`tearDown` or into `setUpTestGetIt()`.

- [x] **[HIGH]** `unified_ai_inference_repository_test.dart` — The `setUp` block (lines 121–199) already duplicates `setUpTestGetIt()` functionality with manual `getIt.isRegistered`/`getIt.unregister`/`getIt.registerSingleton` blocks for `JournalDb`, `Directory`, and `LoggingService`. This should be replaced with `setUpTestGetIt()` + extras.

- [x] **[HIGH]** `unified_ai_inference_repository_test.dart` — There is no `_TestBench` class despite 92 tests sharing the same 13-mock container. The 13-mock setup block is 80 lines of boilerplate in `setUp`. A `_TestBench.create()` factory with helper methods such as `stubRunInference(entity, model, provider)`, `stubGenerateStream(chunks)`, `stubToolCall(name, args)`, `stubAiShadowFlag(value)`, and `captureAiResponseData()` would reduce the repetition across ~30 individual `runInference` tests. **RESOLVED (already covered):** the helper suite exists (`_createPrompt/_createModel/_createProvider/_createMockTextStream/_stubGenerate/_stubGenerateWithImages/_stubGenerateWithAudio/_stubInferenceContext/_stubCreateAiResponseEntry`, used 17–22×); the 13-mock block is a single file-level setUp, not duplicated.

- [x] **[HIGH]** `cloud_inference_repository_test.dart` — 10 independent `group`+`setUp` blocks each rebuild `ProviderContainer`, `MockHttpClient`, and `CloudInferenceRepository` from scratch. A single top-level `_TestBench` with a provider-override factory that accepts a routing-specific mock would cut the scaffolding by ~70%. **RESOLVED:** `_TestBench` (mock routing repos + container + repository + optional MockHttpClient, `extraOverrides` hook) replaces the scaffolding in all 10 group setUps.

- [x] **[MED]** `unified_ai_inference_repository_labels_test.dart` lines 25–27 — `MockJournalRepository2` and `MockChecklistRepository2` are inline duplicates. `MockJournalRepository` and `MockChecklistRepository` already exist in `test/mocks/mocks.dart`. Replace with the central mocks.

- [x] **[MED]** `unified_ai_inference_repository_labels_test.dart` line 71 — `getIt.reset()` in `tearDown` is a nuclear option that can contaminate subsequent tests in the same `very_good test` batch by tearing down registrations that other test files rely on. Replace with `tearDownTestGetIt()` or targeted `getIt.unregister`. **RESOLVED:** (stale) the file already uses `setUpTestGetIt`/`tearDownTestGetIt`; no raw `getIt.reset()` teardown remains.

- [x] **[MED]** `cloud_inference_repository_test.dart` lines 28–34 — `MockOpenAIClient`, `MockOllamaInferenceRepository`, `MockGeminiInferenceRepository`, `FakeCreateChatCompletionRequest`, `FakeRequest`, `FakeGeminiThinkingConfig` are all defined inline. Several have equivalents or near-equivalents in `test/mocks/mocks.dart` (`MockHttpClient` is already there). Centralise. **RESOLVED:** `MockOpenAIClient` and `MockGeminiInferenceRepository` moved to `test/mocks/mocks.dart`; the three trivial `Fake` fallback types stay local per their single-file scope.

- [x] **[MED]** `gemini_inference_repository_test.dart` lines 15–148 — Four bespoke fake HTTP clients (`_FakeStreamClient`, `_RoutingFakeClient`, `_RoutingErrorClient`, `_RoutingCountingClient`) are defined inline. These are complex enough that they should live in a shared `test/test_utils/fake_http_clients.dart` or in the feature's `test_utils.dart`, especially given that similar patterns appear in `mistral_inference_repository_test.dart` and `voxtral_inference_repository_test.dart`. **RESOLVED:** extracted to `test/features/ai/repository/gemini_test_clients.dart` (public `FakeStreamClient`/`Routing*Client`); mistral/voxtral already share `sse_test_utils.dart`.

- [x] **[MED]** `mistral_inference_repository_test.dart` and `voxtral_inference_repository_test.dart` — The SSE helper triple (`createSseStreamedResponse`, `createSseChunkEvent`, `createSseFinalEvent`) is copy-pasted verbatim between the two files. Extract to `test/features/ai/test_utils.dart` or a shared `sse_test_helpers.dart`. **RESOLVED:** (stale) the triple already lives in `test/features/ai/repository/sse_test_utils.dart` and both files import it.

- [x] **[MED]** `mistral_inference_repository_test.dart:17` and `voxtral_inference_repository_test.dart:19` — `FakeRequest extends Fake implements http.Request {}` is defined inline in both files. `FakeBaseRequest` already exists in `test/mocks/mocks.dart`; check whether `FakeRequest` should be added there too. **RESOLVED:** (stale) `FakeRequest` already lives in `test/mocks/mocks.dart` and both files use it from there.

- [x] **[LOW]** `ollama_model_management_test.dart` lines 16–20 — `MockStreamedResponse`, `MockInstallProgressResponse`, `MockStreamedResponseWithError` are inline. These are Ollama-specific but at least `MockStreamedResponse` is reused within the file.
  **RESOLVED:** (assessed, no change) `MockInstallProgressResponse` and `MockStreamedResponseWithError` carry hardcoded SSE-stream bodies — test-specific behavior that is correctly file-local, not generic mocks. The only genuinely generic one (`MockStreamedResponse`) would have to move to `test/mocks/mocks.dart`, which this batch is forbidden to edit; it is not present there today. No safe in-scope change remains.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `lib/features/ai/repository/unified_ai_inference_repository.dart` — The JSON-object extraction loop in `processToolCalls` (lines 888–904) is a hand-rolled brace-depth parser that extracts JSON objects from a potentially concatenated argument string. This is pure string logic with clear algebraic invariants: any valid JSON produced by the AI must be extractable; any sequence of N well-formed JSON objects concatenated must yield N parsed results. A Glados property test over generated lists of non-nested JSON objects would explore edge cases (empty string, single object, whitespace between objects) far better than the single fixture in the existing tests.

- [x] **[HIGH]** `lib/features/ai/repository/mistral_inference_repository.dart` — `_convertMessages()` (line 103) converts OpenAI-style `ChatCompletionMessage` objects to plain maps. This is a pure transformation with a straightforward round-trip invariant: role and content survive conversion. No existing Glados coverage; a generator over `(role, contentVariant)` pairs with a property `converted['role'] == msg.role.name && converted['content'] == extractedContent` would catch regressions.

- [x] **[HIGH]** `lib/features/ai/repository/dashscope_inference_repository.dart` — The DashScope SSE parser (the `buildSseResponse` helper in the test already hints at its structure) is a bespoke framing format (`id:N\nevent:result\n:HTTP_STATUS/200\ndata:{...}\n\n`). A Glados property test for `buildSseResponse` round-trips (N events encoded then decoded must yield N content items in order) would be a genuine generative candidate.

- [x] **[MED]** `lib/features/ai/repository/gemini_utils.dart` — `buildMultiTurnRequestBody` converts a list of `ChatCompletionMessage` objects (system, user, assistant, tool) into Gemini's `contents` format. The existing Glados test on `stripLeadingFraming` is good but doesn't cover multi-turn message conversion. A property: "every assistant message with tool calls must produce a `functionCall` part in the Gemini body" is testable with generated message sequences. **RESOLVED:** added a parameterized property over 1–5 tool calls asserting every call surfaces as a `functionCall` part and every tool response resolves its name through the id mapping.

- [x] **[MED]** `lib/features/ai/repository/gemini_utils.dart` — `buildStreamGenerateContentUri` and `buildGenerateContentUri`: the existing static examples in `gemini_utils_test.dart` cover specific cases well, but a Glados property over `(scheme, host, port, model, apiKey)` tuples asserting that scheme/host/port are always preserved and the path always follows the `/v1beta/models/<model>:<endpoint>` pattern would catch corner cases (empty scheme, no port, `models/` prefix already present, trailing slash). **RESOLVED:** added a scheme×port×model-shape property matrix asserting scheme/host/port preservation and the canonical `/v1beta/models/<model>:<endpoint>` path, plus an explicit protocol-relative https-default case; schemeless hosts are documented as out of contract (Uri.parse treats them as paths).

- [x] **[MED]** `lib/features/ai/repository/ollama_embedding_repository.dart` — `embed()` extracts a `Float32List` from a JSON response. A Glados property over generated `(dims, values)` pairs asserting `result.length == dims` and `result[i] ≈ values[i]` would be stronger than the current two fixture-based tests. **RESOLVED:** added a Glados property (40 runs) over seeded 1024-element vectors asserting element-wise round-trip; dims are fixed by `kEmbeddingDimensions`, so only values vary.

- [x] **[LOW]** `lib/features/ai/repository/gemini_stream_parser.dart` — `maxBufferSize` trim-and-align logic: currently tested with a single static fixture. A Glados property over `(junkLength, objectContent, maxBufferSize)` triples asserting the object is still parsed even when the junk prefix exceeds the cap would cover the trim+align invariant.
  **RESOLVED:** added a `Glados3(cap, extraJunk, value)` property (120 runs, tagged `glados`) that feeds an over-cap brace-free junk prefix in one chunk then a valid JSON object in the next, exercising the trim+align path against an already over-cap buffer and asserting the object is still recovered with no lingering remainder. (The cap bounds growth across calls, not a single oversized chunk, so the property asserts object recovery rather than a hard post-junk buffer-length bound.)

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `unified_ai_inference_repository_test.dart` — The `_prepareImages` branch (lines 427–441 of impl) and `_prepareAudio` branch (lines 444–473) involve real `File.readAsBytes()` calls. Tests work around this by creating temporary directories in `setUp` but there is no test for the **failure path** (file not found, unreadable file). The `catch(e)` inside `_handlePostProcessing` for image analysis (line 759) and audio transcription (line 814) are also untested — these are silent error swallowers.

- [x] **[HIGH]** `unified_ai_inference_repository_test.dart` — The automatic re-run path (`isRerun=false && languageWasSet && response.isEmpty`) at lines 619–634 of the impl is partially tested but only via `runInference` indirection; the scenario where `response` is **non-empty** when language is set (meaning re-run should be skipped) lacks a dedicated test.

- [x] **[HIGH]** `cloud_inference_repository_test.dart` — `_filterAnthropicPings` (lines 89–126 of impl) swallows specific error shapes from the Anthropic provider. There is no test that feeds a stream containing a real "Null is not a subtype of List" deserialization error and verifies it is silently dropped while a subsequent valid chunk is still emitted.

- [x] **[HIGH]** `gemini_inference_repository_test.dart` — The safety cap path (`kMaxStreamingChars`, impl line 262–268) — emitting enough characters in a single streaming session to hit the 1 000 000-character limit — has no test. The `kMaxRetries` exponential-backoff path (rate-limit 429) exists via `_RoutingCountingClient` but does not cover the scenario where all three retries are exhausted (exception should propagate).

- [x] **[MED]** `cloud_inference_repository_test.dart` — `generateWithAudio` for the `openAi` provider type with a non-transcription model (e.g. `gpt-4o-audio-preview`) falls through to the standard audio chat-completion path. There is no test asserting the `data:;base64,...` data URI wrapping applied for the `requiresDataUriForAudio` providers (lines 447–449 of impl). **RESOLVED:** added both directions: alibaba gets the `data:;base64,` wrapper, generic providers send bare base64.

- [x] **[MED]** `ollama_inference_repository_test.dart` — The `_gemma4Prefix` thinking-mode path (`shouldEnableThinking`, impl line 41–44) adds `<think>` wrapper tags around Ollama's `thinking` field; the test for this path exists but does not cover the case where `thinking` is `null` or empty — the impl adds nothing in that case, which is untested. **RESOLVED:** added a test driving chunks with `thinking: null` and `thinking: \"\"` asserting no `<think>` tags are emitted and content passes through untouched.

- [x] **[MED]** `mistral_inference_repository_test.dart` — `_convertMessages` for tool-response messages (`ChatCompletionMessageRole.tool`) has limited test coverage; the `tool_call_id` field mapping and the scenario where content is `null` are not exercised. **RESOLVED:** added a tool-response mapping test asserting `tool_call_id` + content; the null-content scenario is unrepresentable — `ChatCompletionMessage.tool` requires a non-null `content` by type.

- [x] **[MED]** `voxtral_inference_repository_test.dart` — Retry/timeout behavior (fake-async) is tested, but the HTTP 4xx path (non-retryable error, should throw immediately rather than retry) is missing. **RESOLVED:** added an HTTP 400 test asserting the typed exception and exactly one `send` call (no retry).

- [x] **[LOW]** `dashscope_inference_repository_test.dart` — Error HTTP status (5xx) from the DashScope SSE endpoint is not tested; the impl likely throws, but that is not verified.
  **RESOLVED:** added a 503 test in the `initial request errors` group asserting the thrown `Exception` carries both the `503` status and the server error body (`InternalError`), mirroring the existing 400/401 coverage.

- [x] **[LOW]** `inference_repository_interface_test.dart` — `generateText` with `null` systemMessage (should produce only a user message, no system message) is untested; the impl has an `if (systemMessage != null)` guard that is not exercised.
  **RESOLVED:** (stale) the file already has "without systemMessage builds [user] message only and delegates" (lines 120–141), which passes `systemMessage: null`, asserts `lastMessages` has length 1, and asserts that single message is the expected `ChatCompletionMessage.user`. The `if (systemMessage != null)` false-branch is covered.

---

## Test execution speed opportunities

- [x] **[HIGH]** `unified_ai_inference_repository_test.dart` setUp block creates a real `Directory.systemTemp.createTempSync(...)` for every one of its 92 tests. Each call hits the OS filesystem. Extracting `setUp` into a shared `_TestBench` would allow the temp directory to be created once in `setUpAll` and reused across tests (with per-test subdirectories if needed), cutting ~92 filesystem mkdir/rmdir round trips per suite run. **Estimated impact: ~200–400 ms per full suite run on CI.**

- [x] **[HIGH]** `cloud_inference_repository_test.dart` — 10 separate `setUp` blocks each instantiate `ProviderContainer` + `CloudInferenceRepository` + `MockHttpClient`. `ProviderContainer` construction is not free (riverpod graph traversal). Sharing one container per group (or one per test file) and resetting only the mock state between tests would reduce ~88 container-create+dispose cycles. **Estimated impact: ~300–600 ms per suite run.** **RESOLVED (assessed):** scaffolding centralized via `_TestBench`; per-test container creation is kept deliberately — sharing containers across tests invites the in-suite contamination documented in test/README.md, and container construction here costs single-digit ms.

- [x] **[HIGH]** `unified_ai_inference_repository_test.dart` — The mock reset in `tearDown` (lines 202–230) does multiple `getIt.isRegistered` + `getIt.unregister` calls per test in addition to `container.dispose()`. The scattered extra per-test inline `getIt` manipulations in the label-assignment group add further overhead. Consolidating into `setUpTestGetIt()` / `tearDownTestGetIt()` would remove ~15–20 redundant GetIt calls per test. **Estimated impact: ~100–200 ms across 92 tests.**

- [x] **[MED]** `gemini_inference_repository_test.dart` — Many tests build `_FakeStreamClient(200, [line])` inline and parse the entire HTTP round-trip through `GeminiStreamParser`. Tests that only need to verify the Gemini→OpenAI _translation_ of a single response object could bypass the HTTP layer entirely by calling the internal `_parseGeminiResponse`-style logic directly (if extracted). **Estimated impact: moderate, reduces HTTP-overhead simulation in ~30 tests.** **RESOLVED:** (assessed, no change) the translation logic lives in private stream-parsing closures, so a direct-call seam would require a production extraction; the fake clients are in-memory and the suite runs in <1s, so the HTTP simulation is not a measurable cost.

- [x] **[MED]** `gemini_utils_test.dart` — `numRuns: 180` for the `stripLeadingFraming` Glados property (line 1149). This is at the upper bound of the recommended 80–180 range. Given that `GeminiStreamParser` already has a complementary Glados test in `gemini_stream_parser_test.dart`, consider reducing `gemini_utils_test.dart`'s `numRuns` to 100–120. **Estimated impact: ~15% faster Glados CI job for this file.** **RESOLVED:** reduced to 120 runs with a comment pointing at the complementary parser property suite.

- [x] **[MED]** `cloud_inference_repository_test.dart` — Each of the ~20 "verbosity is null" and "toolChoice is null/not-null" tests recreates a full `CreateChatCompletionStreamResponse` fixture inline (~20 lines each). These could be a shared `_minimalStreamResponse()` factory, eliminating ~400 lines of boilerplate with zero runtime impact beyond readability, but each `when().thenAnswer()` setup adds mock-framework overhead that could be shared across related tests in a parameterized helper. **Estimated impact: small per-test, significant readability win.** **RESOLVED:** added a `minimalStreamResponse(content)` factory; 29 verbatim fixtures collapsed.

- [x] **[LOW]** `tool_call_accumulator_test.dart` — Three Glados tests with default `numRuns` (no `ExploreConfig`). Glados defaults to 100 runs. These property tests are cheap (pure in-memory state machine), so no change needed; they are well within the CI budget.
  **RESOLVED:** (no change) the item is itself a non-actionable observation that explicitly concludes "no change needed". The convention to avoid exactly 100 runs applies when authoring a new property here; these existing cheap state-machine properties are intentionally left at the default and are confirmed within CI budget.

---

## Summary

- **2 oversized impl files** over 1 000 lines (`unified_ai_inference_repository.dart` at 1 382, `gemini_inference_repository.dart` at 1 115); `ollama_inference_repository.dart` at 852 is approaching the limit.
- **4 oversized test files** (`unified_ai_inference_repository_test.dart` 7 306, `cloud_inference_repository_test.dart` 4 404, `gemini_inference_repository_test.dart` 3 288, `ollama_inference_repository_test.dart` 3 063) — all prime candidates for mirror-splitting alongside their impl files.
- **6+ weak test-quality issues**: inline `getIt` manipulation violating the centralized-mocks guideline in 3 files; `getIt.reset()` contamination risk; 7+ inline mock/fake class definitions that duplicate centralized mocks; no `_TestBench` in two of the three largest test files; SSE helper code copy-pasted verbatim between mistral and voxtral test files.
- **5 Glados candidates**: the `processToolCalls` brace-depth JSON extractor, Mistral `_convertMessages`, DashScope SSE framing, Gemini `buildMultiTurnRequestBody` message conversion, and `GeminiUtils.buildStreamGenerateContentUri` with varied host/model inputs — all pure functions not yet property-tested.
- **5+ coverage gaps**: `_filterAnthropicPings` error-swallowing path untested; `kMaxStreamingChars` safety-cap branch in Gemini repo unreached; silent error swallowers in `_handlePostProcessing` for image and audio; Mistral tool-response message conversion corner cases; automatic-rerun skip-when-language-set-and-response-nonempty branch.
- **3 speed wins**: real `createTempSync` per test in unified repo setup (~92 OS calls), 10 `ProviderContainer` rebuilds per test run in cloud repo (~88 constructions), and scattered inline GetIt manipulation in unified repo (~15–20 extra GetIt calls per test); collectively estimated at **600–1 200 ms of avoidable overhead per full CI run**.
- **Biggest opportunity**: Split `unified_ai_inference_repository.dart` (1 382 lines → 3–4 files) and its test (7 306 lines → 3–4 files), simultaneously replacing the ad-hoc GetIt manipulation with `setUpTestGetIt()` and extracting a `_TestBench`. This single change addresses all five axes at once for the most complex file in scope.
