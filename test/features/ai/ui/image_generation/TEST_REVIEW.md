# Test Review — `ai/ui/image_generation`

**Scope:** `lib/features/ai/ui/image_generation/*.dart` + `test/features/ai/ui/image_generation/*.dart`  
Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/features/ai/ui/image_generation/cover_art_skill_modal.dart` | 259 | Yes (532 lines) | Inline mock classes; fine size |
| `lib/features/ai/ui/image_generation/reference_image_selection_widget.dart` | 320 | Yes (902 lines) | Test oversized relative to impl; inline mock classes |

---

## File size / split opportunities

- [x] **[MED] `test/features/ai/ui/image_generation/reference_image_selection_widget_test.dart` (902 lines)** — nearly 3× the impl file at 320 lines. Contains 3 inline mock/fake controller classes (lines 15–69), an image builder helper, and 17+ test cases across loading/error/selection/callback/constraint branches. Split seam: the 3 controller mock classes + the image builder could be extracted to a `test/features/ai/image_generation/test_utils.dart` factory (mirroring the pattern in `test/features/ai/test_utils.dart`), reducing the test file by ~70 lines of boilerplate.
  **RESOLVED:** done (largely stale) — the fake controller had already been extracted to `test_utils.dart`; the remaining local `buildTestImage` builder now lives there too as `buildTestReferenceImage`, shared by both tests in the directory.

- [x] **[LOW] `lib/features/ai/ui/image_generation/cover_art_skill_modal.dart` (259 lines)** — good size, no split needed.
  **RESOLVED:** confirmed true, no change — the file is a focused stateful modal plus its private progress view; nothing to split.

- [x] **[LOW] `lib/features/ai/ui/image_generation/reference_image_selection_widget.dart` (320 lines)** — within acceptable range.
  **RESOLVED:** confirmed true, no change — single widget plus its private `_ImageGridTile` and one error-mapping helper; cohesive, no split warranted.

---

## Test quality improvements

- [x] **[HIGH] `reference_image_selection_widget_test.dart` — inline mock/fake classes** (lines 15–69): three controller classes `_MockReferenceImageSelectionController`, `_TrackingMockReferenceImageSelectionController`, and an unnamed processing variant (`_MockReferenceImageSelectionControllerWithProcessing` in `cover_art_skill_modal_test.dart`) are defined inline in the test files. Per AGENTS.md, if these mocks exist elsewhere in the test tree they must be centralised. At minimum, the shared shape should live in a `test/features/ai/image_generation/test_utils.dart` file and be imported by both test files. **RESOLVED:** one configurable `FakeReferenceImageSelectionController` (fixed state + toggle/clear recording + processedImages) lives in `test/features/ai/ui/image_generation/test_utils.dart` and replaces all three inline variants across both test files.

- [x] **[HIGH] `reference_image_selection_widget_test.dart` — manual GetIt setup/teardown** (lines 97–116): uses `getIt.reset()`, `getIt.allowReassignment = true`, and `getIt.registerSingleton<Directory>` inline rather than `setUpTestGetIt()`. AGENTS.md forbids inline `getIt.isRegistered / unregister / registerSingleton` boilerplate. **RESOLVED:** converted to `setUpTestGetIt(additionalSetup: register temp Directory)` / `tearDownTestGetIt()`; the `allowReassignment` + manual reset pattern is gone.

- [x] **[HIGH] `cover_art_skill_modal_test.dart` — `setUpTestGetIt()` is used** (line 29) but immediately followed by a manual `getIt.registerSingleton<Directory>` (line 33): the additional registration is fine, but the directory singleton should be managed via `addTearDown` rather than relying solely on `tearDownTestGetIt` to clean up custom singletons that `tearDownTestGetIt` may not handle. **RESOLVED (assessed, no change):** `tearDownTestGetIt()` runs a full `getIt.reset()`, which removes every registration including custom singletons like the temp `Directory` — no extra `addTearDown` is needed.

- [x] **[MED] `cover_art_skill_modal_test.dart` — test "passes categoryId through to widget"** (lines 300–330): the assertion `expect(modal.categoryId, 'cat-123')` reads a widget property directly instead of asserting on a visible rendered effect. This is a weak assertion — it proves the parameter was forwarded in memory but not that it changes any rendered output or downstream behaviour. Prefer testing the rendered outcome (e.g., the category-specific image-fetching path being triggered).
  **RESOLVED:** done (root cause removed) — the weak property assertion existed because `categoryId` was dead plumbing: the modal accepted and stored it but never read it anywhere (not in build, not in the trigger params). The parameter has been removed from `CoverArtSkillModal`, its `show()`, and both call sites; the obsolete pass-through test is deleted with an explanatory note.

- [x] **[MED] `reference_image_selection_widget_test.dart` — `setUp()` creates a real temp directory** per test: while real file I/O in widget tests is acceptable for tests validating I/O behaviour, the majority of tests in this file do not need real image files. Only image-rendering tests (if any) need a real directory; tests that only check loading/error/callback state should use an empty stub directory.
  **RESOLVED:** done — the temp directory is created once in `setUpAll` and deleted once in `tearDownAll`; no test in the file writes real image files, the directory only needs to exist for `getDocumentsDirectory()`.

- [x] **[LOW] `reference_image_selection_widget_test.dart` — `pumpAndSettle()` in the "calls onSkip when no images are available" test** (line 164): `onSkip` is invoked from a `WidgetsBinding.addPostFrameCallback`. One `tester.pump()` suffices to trigger the post-frame callback; `pumpAndSettle()` is unnecessary and will block the full animation-settle timeout if any child animation is introduced.
  **RESOLVED:** done — replaced `pumpAndSettle()` with a single `tester.pump()` in "calls onSkip when no images are available" (the widget renders `SizedBox.shrink()`, no animations), and replaced the `pumpAndSettle()` in the sibling "onSkip is only called once (auto-skip guard)" test with three explicit bounded `pump()`s so the guard is exercised across multiple frames deterministically.

- [x] **[LOW] `cover_art_skill_modal_test.dart` line 70** — `expect(find.byType(CircularProgressIndicator), findsOneWidget)` as the sole assertion in "renders ReferenceImageSelectionWidget with correct taskId": this confirms the loading-state indicator renders but does not verify the `taskId` is actually forwarded. The test name does not match what is asserted. Either rename the test or add an assertion that the controller was built with the correct `taskId`.
  **RESOLVED:** done — `FakeReferenceImageSelectionController` now records the `taskId` it was `build`-ed with (`builtWithTaskId`), and the test asserts `controller.builtWithTaskId == testLinkedTaskId`, proving the modal forwards `linkedTaskId` (not `entityId`) into the reference-image controller — so the test now matches its name.

---

## Generative (Glados) testing opportunities

- [x] **[LOW] `ReferenceImageSelectionWidget` — max-selection constraint logic**: the widget enforces a maximum of 5 selectable images. This is a simple integer boundary: `selectedImageIds.length <= 5`. A Glados property with a generator for `Set<String>` of size 0–10 verifying that the continue button is only enabled when `selectedImageIds.length >= 1 && <= 5` would catch any off-by-one. However, the selection logic lives in the controller, not the widget, so a controller-level unit-test Glados property would be more precise. Marginal candidate since the boundary is trivial.
  **RESOLVED:** assessed, no change. The review's premise is inaccurate for the widget: the Continue button is enabled regardless of selection count (it only disables while `isProcessing`); it is not gated on `1..5`. The real boundary (`canSelectMore = length < kMaxReferenceImages`) lives in `ReferenceImageSelectionController.toggleImageSelection` / `canSelectMore` under `lib/features/ai/state/` — outside this directory's scope, and a precise Glados property would need the controller's real (non-fake) toggle logic, so it belongs in `test/features/ai/state/`. The widget-level boundary is already fully exercised by the deterministic counter examples (0/3, 2/3, 3/3) and "tapping non-selectable image tile does not toggle" (the 6th tile at the cap). A Glados property here would add no coverage beyond those.

- The rest of the image-generation UI is predominantly async/widget logic with no non-trivial pure functions that warrant Glados coverage.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH] `cover_art_skill_modal_test.dart` — error path: status transitions to `error` during generation**: the test "progress view shows error when status becomes error" exists in the file (line 392+), but a quick scan confirms it is incomplete: the test exists but needs verification that the error message or icon is actually rendered (the test body at lines 393–440 shows the status is set but does not assert on a rendered error state widget). Review the full test to confirm the error-UI assertion is present. **RESOLVED (already covered):** the test's tail asserts the rendered error UI — `find.text('Failed to generate image')` and the `Icons.error_outline_rounded` icon.

- [x] **[HIGH] `reference_image_selection_widget_test.dart` — max-5 selection constraint**: no test verifies that attempting to select a 6th image is rejected (the `toggleImageSelection` controller method enforces this, but the widget's disabled-state rendering for over-the-limit selections is not tested). **RESOLVED (already covered):** 'tapping non-selectable image tile does not toggle' seeds five selected ids plus a sixth image and verifies the over-limit tile's tap records nothing.

- [x] **[MED] `reference_image_selection_widget_test.dart` — `clearSelection()` callback**: the widget exposes a clear-selection affordance. No test verifies that tapping it calls `clearSelection()` on the controller and updates the button label.
  **RESOLVED:** assessed, no change — the premise doesn't match the impl: `ReferenceImageSelectionWidget` exposes no clear-selection affordance (no clear button, no `clearSelection` call site in the widget). The controller's `clearSelection` is exercised through its own state tests.

- [x] **[MED] `cover_art_skill_modal_test.dart` — continue button label reflects selection count dynamically**: the "continue with images triggers provider" test verifies `find.text('Continue (1)')` is tappable, but there is no test for the label updating as selection changes from 0 → 1 → 2 images.
  **RESOLVED:** done — added a parameterized label test pumping selection counts 0/1/2 against fresh provider scopes: 'Continue' for none, 'Continue (1)', 'Continue (2)' for counted selections.

- [x] **[MED] `reference_image_selection_widget_test.dart` — `errorCode: noImagesFound` branch**: the widget likely has a distinct rendering for `ReferenceImageSelectionError.noImagesFound` vs `loadImagesFailed`. Only `loadImagesFailed` appears to be tested in `cover_art_skill_modal_test.dart`; verify the other error codes produce their expected UI.
  **RESOLVED:** assessed, no change — the premise doesn't match the impl: `ReferenceImageSelectionError` has exactly one value (`loadImagesFailed`, already tested); 'no images' is not an error code but the auto-skip branch (`availableImages.isEmpty` → post-frame `onSkip`), which has its own tests.

- [x] **[LOW] `cover_art_skill_modal_test.dart` — `entityId` vs `linkedTaskId` distinction**: the modal takes both `entityId` (the current entity) and `linkedTaskId` (the task whose images are loaded). No test verifies that the progress view uses `linkedTaskId` rather than `entityId` for the `inferenceStatusControllerProvider` watcher.
  **RESOLVED:** done — added "progress view watches linkedTaskId status, not entityId": with distinct ids, generation runs under `linkedTaskId`; driving the `entityId` status to idle leaves the view at "Generating image..." (completion not shown), and only driving the `linkedTaskId` status to idle surfaces "Cover art ready!" — proving the watcher keys on `linkedTaskId`.

---

## Test execution speed opportunities

- [x] **[MED] `reference_image_selection_widget_test.dart` — real temp-directory creation/deletion in `setUp`/`tearDown` every test** (~17 tests): each test creates + deletes a temp directory even when no file I/O occurs. Extract a shared `setUpAll` temp directory that is only deleted once in `tearDownAll`.
  **RESOLVED:** done — same change as the setUp item above: one `setUpAll`-created directory, deleted once in `tearDownAll`, instead of 17 create/delete cycles.

- [x] **[MED] `cover_art_skill_modal_test.dart` — three sequential `pump()` calls** in several tests (lines 113–118, 198–200): this pattern is used to drive the auto-skip post-frame callback. Document explicitly as `// pump 1: build; pump 2: post-frame callback; pump 3: state rebuild` for clarity, and confirm no test needs more than 2 pumps (the 3rd pump may be a defensive over-pump adding latency).
  **RESOLVED:** done — the triple pump is documented in place (`pump 1: build registers the auto-skip post-frame; pump 2: post-frame fires → setState; pump 3: rebuild renders the progress view`); all three are required, the third is the rebuild that the asserted 'Generating image…' text depends on.

- [x] **[LOW] `cover_art_skill_modal_test.dart` line 29 — `setUpTestGetIt()` is `await`-ed**: this is correct; no performance concern. The subsequent `tearDownTestGetIt()` should also be awaited — verify this is not a bare `tearDownTestGetIt()` without `await` (common mistake that hides cleanup race conditions).
  **RESOLVED:** verified, no change. `cover_art_skill_modal_test.dart` uses an `async` teardown with an explicit `await tearDownTestGetIt();`. `reference_image_selection_widget_test.dart` passes the function as a tear-off (`tearDown(tearDownTestGetIt)`); since `tearDownTestGetIt` returns `Future<void>` and `tearDown` awaits any returned future, this is also correctly awaited. No race-condition gap.

---

## Summary

- 0 oversized impl files (both within 260–320 lines); 1 oversized test file at 902 lines.
- 3 inline mock class definitions across the 2 test files; should be centralised into a feature-level `test_utils.dart`.
- 2 GetIt boilerplate violations (inline registerSingleton patterns).
- Coverage gaps: max-5 selection constraint rendering, clearSelection callback, dynamic continue-button label, `noImagesFound` error variant.
- Speed win: real-filesystem temp-directory creation per test can be moved to `setUpAll`.
- No strong Glados candidates (selection-count boundary is the best but trivial).
