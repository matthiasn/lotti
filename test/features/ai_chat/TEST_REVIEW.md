# ai_chat — Feature Test-Review Index

Generated by automated test-quality review (static analysis); aggregates the per-subdir reports linked below.

---

## Sub-reports

| Sub-report | Top opportunity |
|---|---|
| [repository/TEST_REVIEW.md](repository/TEST_REVIEW.md) | Extract `_stubWorkEntries()` helper in `task_summary_repository_test.dart` (1 651 ln, 10× impl); centralise inline `MockSelectable` |
| [services/TEST_REVIEW.md](services/TEST_REVIEW.md) | Fix 3 real-timer fake-time violations in `realtime_transcription_service_test.dart`; migrate `scheduleDone` to `fakeAsync` |
| [database/TEST_REVIEW.md](database/TEST_REVIEW.md) | Remove wall-clock `Stopwatch` assertions; add `_makeTask`/`_makeAiResponse`/`_makeLink` factories to shrink 909-ln test |
| [ui/controllers/TEST_REVIEW.md](ui/controllers/TEST_REVIEW.md) | Split `chat_recorder_controller.dart` (806 ln); move 4 inline mock classes to central mocks; fix 10+ `Future.delayed` violations |
| [ui/widgets/TEST_REVIEW.md](ui/widgets/TEST_REVIEW.md) | Merge duplicate `thinking_parser_test.dart`; eliminate 29 `pumpAndSettle` in `chat_interface_test.dart`; add tests for 2 untested widgets |
| [ui/models/TEST_REVIEW.md](ui/models/TEST_REVIEW.md) | Add injectable clock to `toDomain()` to make empty-messages timestamp test meaningful |
| [ui/pages/TEST_REVIEW.md](ui/pages/TEST_REVIEW.md) | Replace repeating-animation `pumpAndSettle` in `chat_modal_page_test.dart`; add 4 missing provider coverage cases |

---

## Aggregate counts

| Metric | Count |
|---|---|
| Oversized impl files (> 500 ln) | 2 (`chat_recorder_controller.dart` 806, `realtime_transcription_service.dart` 659) |
| Oversized test files (> 600 ln) | 6 (`chat_recorder_controller_test.dart` 3 340, `task_summary_repository_test.dart` 1 651, `realtime_transcription_service_test.dart` 1 626, `audio_transcription_service_test.dart` 1 345, `chat_session_controller_test.dart` 1 492, `chat_message_processor_test.dart` 1 362) |
| Weak / near-smoke tests | ~6 (`toString()` assertion in `chat_repository_test.dart`; `isNotNull`-only `toDomain` test; `isA<>` type-hierarchy tests in `chat_exceptions_test.dart`; `findsOneWidget`-only `ChatInterface` presence test; dead Stopwatch instrumentation) |
| Glados candidates | 8 (`buildPromptFromMessages`, `accumulateToolCalls`, date-validation in `TaskSummaryRepository`, MLX delta-deduplication, audio model selection, `ChatMessage`/`ChatSession` JSON round-trips, `getNormalizedAmplitudeHistory`, `thinking_parser` chunking-invariant) |
| Speed wins (fake-time violations / excess `pumpAndSettle` / real delays) | ~60+ (`chat_interface_test` 29 `pumpAndSettle`; `chat_recorder_controller_test` `Stream.periodic` 250 ms/test × N; `realtime_transcription_service_test` 3+ real delays; 100 ms production delay in `InputArea._sendMessage`) |

---

## Top feature-wide opportunities

- [x] **[HIGH]** Split `chat_recorder_controller.dart` (806 ln) into state/batch/realtime files and split the 3 340-line test file to mirror — the largest impl and test file in the entire feature. _(ui/controllers/TEST_REVIEW.md)_ **RESOLVED (adapted):** state types extracted to a `chat_recorder_state.dart` part; the batch/realtime method split is blocked because `Notifier.state` is `@protected` (methods writing state cannot move to extensions/mixins) and both paths share private session fields; the test stays the single mirror per the one-test-file-per-source rule. Details in ui/controllers/TEST_REVIEW.md.
- [x] **[HIGH]** Fix all fake-time policy violations: `Future.delayed(ms)` real waits in `realtime_transcription_service_test.dart` (lines 415, 950, 991) and `Future.delayed(Duration.zero)` in `chat_session_controller_test.dart` (10+ locations) and `chat_sessions_controller_test.dart` (line 64) — replace with `fakeAsync`/`pumpEventQueue()`/`async.flushMicrotasks()`. _(services/TEST_REVIEW.md, ui/controllers/TEST_REVIEW.md)_
- [x] **[HIGH]** Merge the two `thinking_parser_test.dart` files (495 ln + 292 ln covering the same source) — violates the one-test-file-per-source-file rule; causes confusion about authoritative coverage location. _(ui/widgets/TEST_REVIEW.md)_
- [x] **[HIGH]** Eliminate 29 `pumpAndSettle` calls in `chat_interface_test.dart` — the `_AmbientPulseBorder` repeating animation makes this a correctness risk (can hang indefinitely) in addition to a speed cost; replace with `tester.pump(duration)`. _(ui/widgets/TEST_REVIEW.md)_ **RESOLVED (stale):** `chat_interface_test.dart` contains zero `pumpAndSettle` calls — the sweep was completed in the ui/widgets pass.
- [x] **[HIGH]** Extract `_stubWorkEntries()` shared helper in `task_summary_repository_test.dart` and migrate the inline `MockSelectable` to the central `test/mocks/mocks.dart` — removes ~300 lines of copy-paste setup from the 1 651-line test. _(repository/TEST_REVIEW.md)_ **RESOLVED (stale):** the file-level `stubWorkEntries(entries, {anyArgs})` helper exists and the file uses the central `MockSelectable<T>` from `test/mocks/mocks.dart` (no local copy remains).
- [x] **[HIGH]** Move 4 inline mock classes in `chat_recorder_controller_test.dart` (lines 29–113) to `test/mocks/mocks.dart`; standardise all controller tests to use `setUpTestGetIt()` / `tearDownTestGetIt()`. _(ui/controllers/TEST_REVIEW.md)_ **RESOLVED:** the file already uses `setUpTestGetIt`/`tearDownTestGetIt` (earlier pass) and contains no `class Mock*` duplicates — the remaining `_Throwing*`/`_ThrowOnCancel*` classes are scenario-specific throwing fakes that belong file-local. This pass converted the last residual inline `isRegistered/unregister/registerSingleton` block (DomainLogger swap in the cancel-error test) to the cascade form.
- [x] **[HIGH]** Refactor `InputArea._sendMessage` to accept an injectable scroll delay (or use `addPostFrameCallback`) so tests no longer pay a 100 ms real-time wait per test execution. _(ui/widgets/TEST_REVIEW.md)_
- [x] **[MED]** Add Glados property tests for `ChatMessage` and `ChatSession` JSON round-trips (pure serialization with enum paths); fix the `ChatSession.toJson()` deep-serialization gap surfaced in `chat_session_model_test.dart`. _(database/TEST_REVIEW.md)_
  **RESOLVED:** (stale) all three pieces already in place. Deep-serialization gap fixed via `@JsonSerializable(explicitToJson: true)` on `ChatSession` (`lib/features/ai_chat/models/chat_session.dart:16`). `ChatMessage` round-trip property `fromJson(toJson(m)) == m for all roles and arbitrary metadata` covers all three enum roles + null/non-null/nested metadata, tagged `glados` (`test/features/ai_chat/models/chat_message_model_test.dart:59-84`). `ChatSession` round-trip property `fromJson(toJson(s)) == s for sessions with 0..5 messages` exercises nested-message deep serialization, optional categoryId, and metadata, tagged `glados` (`test/features/ai_chat/models/chat_session_model_test.dart:61-90`). Both use freezed value equality for a meaningful `==` round-trip.
- [x] **[MED]** Add Glados property test for `ChatMessageProcessor.accumulateToolCalls` (pure accumulation, random interleaving) and `buildPromptFromMessages` (content appears exactly once, correct role prefix order). _(repository/TEST_REVIEW.md)_
  **RESOLVED:** (stale) both methods are public seams (no production hooks needed). `accumulateToolCalls` interleaving property `one entry per unique effective id; arguments equal the in-order concatenation of that id's chunks` randomly interleaves up to 16 deltas across 3 tool ids and asserts dedup + in-order concatenation + name, tagged `glados` (`test/features/ai_chat/repository/chat_message_processor_accumulate_tool_calls_test.dart:140-206`). `buildPromptFromMessages renders every message once, role-prefixed, in order, with the new user message last` asserts exact line count and per-line role prefix + content, and the sibling `buildFinalPromptFromMessages` property asserts the fixed closing instruction; both tagged `glados` (`test/features/ai_chat/repository/chat_message_processor_test.dart:1342-1413`).
- [x] **[MED]** Remove wall-clock `Stopwatch` assertions from `bulk_linked_entities_test.dart` (lines 484–678); replace with functional correctness assertions only. _(database/TEST_REVIEW.md)_
  **RESOLVED:** (stale) the file (618 ln total) contains zero `Stopwatch`/`elapsed` usage. The former performance case is now functional-only — `getBulkLinkedEntities(taskIds)` is asserted on result count and per-task linked-entity counts, with an explicit comment that wall-clock expectations belong in a dedicated benchmark (`test/features/ai_chat/database/bulk_linked_entities_test.dart:392-401`).
- [x] **[MED]** Add dedicated tests for `bubble_corner_action.dart` and `error_banner.dart` (currently 0 test coverage, only indirect coverage via full `ChatInterface`). _(ui/widgets/TEST_REVIEW.md)_
  **RESOLVED:** (stale) dedicated mirror test files exist for both widgets, both using `makeTestableWidget()` and bounded `tester.pump(Duration)` (no real timers/`DateTime.now()`). `test/features/ai_chat/ui/widgets/chat_interface/bubble_corner_action_test.dart` asserts the rendered icon, circular `CircleBorder` shape + elevation 2, the tooltip message, and that a tap invokes `onTap` exactly once. `test/features/ai_chat/ui/widgets/chat_interface/error_banner_test.dart` asserts the displayed error text, the `errorContainer` background, and that the Retry button fires `onRetry` only and the Close icon fires `onDismiss` only (each verifying the other callback is not invoked).
- [x] **[LOW]** Add 4 missing behavioral cases for `eligibleChatModelsForCategoryProvider` in `chat_model_providers_test.dart` (empty list, all-ineligible, provider-ID miss, single eligible). _(ui/pages/TEST_REVIEW.md)_
  **RESOLVED:** (stale) — `test/features/ai_chat/ui/providers/chat_model_providers_test.dart` already contains all four cases, each asserting the actual filtered/returned model-id list: `'returns an empty list when no models are configured'` (empty), `'returns an empty list when every model is ineligible'` (all-ineligible, asserts `isEmpty` after both function-calling and text-modality misses), `'a single eligible model is returned as-is'` (single, asserts `['m1']`), and `'a provider-lookup miss sorts under the empty provider name'` (provider-ID miss, asserts `['m-orphan', 'm-known']`). These sit alongside the original normal filtered+sorted case (asserts `['m2', 'm1']`).

---

## Recurring patterns

- **Fake-time policy violations.** `Future.delayed(ms)` and `Future.delayed(Duration.zero)` appear across services, controllers, and repository tests; the correct replacements (`fakeAsync`, `pumpEventQueue`, `async.flushMicrotasks`) are already used in other tests in the same files, demonstrating that the pattern is understood but inconsistently applied.
- **Inline mock / GetIt boilerplate instead of central helpers.** Inline `Mock` subclasses appear in `chat_recorder_controller_test.dart` (4 classes), `chat_interface_test.dart` (manual `GetIt.pushNewScope`), `chat_modal_page_test.dart`, `audio_transcription_service_test.dart`, and `task_summary_repository_test.dart`. All violate the centralized-mocks and `setUpTestGetIt()` rules.
- **Excess `pumpAndSettle` on non-settling animations.** `chat_interface_test.dart` (29), `chat_modal_page_test.dart` (3 on a repeating animation), `assistant_settings_sheet_test.dart` (8), and `message_bubble_test.dart` (5); the risk escalates when a `repeat()` animation is present.
- **Massive copy-paste mock stubs dwarfing test logic.** The `generate()` stub in `chat_repository_test.dart` (10 lines × 5 tests), `workEntriesInDateRange` stub in `task_summary_repository_test.dart` (12 lines × 9+ tests), and `createSession`/`saveSession`/`sendMessage` triple-stub in `chat_session_controller_test.dart` (10+ occurrences) are the dominant source of test-file inflation.
- **Weak assertions on meaningful behavior.** `toString()` smoke test, `isNotNull`-only timestamp check, `findsOneWidget`-only on `ChatInterface`, dead `Stopwatch` instrumentation with no enforced assertion — all confirm testing existence rather than behavior.
- **One-file-per-source rule violation.** Two separate test files cover `thinking_parser.dart`; this is the only confirmed violation but indicates that test placement was not consistently enforced during feature development.
