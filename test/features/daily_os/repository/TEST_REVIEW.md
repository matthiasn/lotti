# Test Review: daily_os/repository

**Scope:** `lib/features/daily_os/repository/` (1 impl file, 138 lines + 61-line generated `.g.dart`)
and `test/features/daily_os/repository/` (1 test file, 352 lines).

_Generated by automated test-quality review (static analysis)_

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/.../repository/day_plan_repository.dart` | 138 | Yes | Well within budget |
| `lib/.../repository/day_plan_repository.g.dart` | 61 | n/a (generated) | Do not edit |
| `test/.../repository/day_plan_repository_test.dart` | 352 | n/a | `Future.delayed(Duration.zero)` in one stream test |

---

## File size / split opportunities

- [x] **[LOW]** Both impl file (138 lines) and test file (352 lines) are well within the 500-line target. No split is warranted or beneficial.
  **RESOLVED:** confirmed non-actionable — impl is 138 lines and the test file remains well under the 500-line target; no split warranted.

---

## Test quality improvements

- [x] **[MED]** `test/.../repository/day_plan_repository_test.dart:344` — The `updateStream` test uses `await Future<void>.delayed(Duration.zero)` to drain the event loop so the stream listener fires. This is a real-time yield disguised as `Duration.zero`. Per AGENTS.md and `test/README.md`, `Future.delayed` (even at zero) is prohibited in tests. Replace with `await Future<void>.microtask(() {})` plus `await tester.runAsync(...)`, or restructure the test to use a `StreamQueue` + `.next` pattern (`await StreamQueue(repository.updateStream).next` directly after `controller.add(ids)`) which avoids the need for any delay.
  - **RESOLVED:** done — replaced with `await pumpEventQueue()`, draining the queued turns deterministically without a real-time yield.
- [x] **[LOW]** `test/.../repository/day_plan_repository_test.dart:192–273` — The `save` group has three tests that each construct a full `makePlan` + mock wiring. The first two tests differ only in whether `getDayPlanById` returns `null` vs. the plan; the third is a subset of the second. Extracting a private `_setupSaveMocks({bool existing})` helper would remove ~15 lines of duplication, though at 352 lines total the file is not large enough to mandate this.
  **RESOLVED:** done — extracted two `main()`-scoped stub helpers, `stubCreateBranch(plan, {throws})` and `stubUpdateBranch(original, updatedMeta, {throws})`, that wire the new-plan and update branches (including the optional throw path). All five `save` tests (create, update, update-metadata, create-throw, update-throw) now call them, removing the duplicated `when(...)` wiring.
- [x] **[LOW]** `test/.../repository/day_plan_repository_test.dart:1` — `// ignore_for_file: avoid_redundant_argument_values` is file-level. A targeted per-line suppression on the specific `makePlan` call-site would be narrower and more reviewable.
  **RESOLVED:** done — the only `avoid_redundant_argument_values` trigger was `plannedBlocks: const []` in `makePlan`, which equals the `@Default([])` on `DayPlanData.plannedBlocks`. Removed that redundant argument and the file-level ignore entirely — strictly better than a per-line suppression, since no ignore is needed at all.

---

## Generative (Glados) testing opportunities

- [x] **[MED]** The microtask-coalescing batch logic in `getDayPlan` is a pure-logic stateful algorithm. A Glados property that would genuinely strengthen confidence: generate a random set of N dates (1–20), fire `getDayPlan` for all of them in the same synchronous sweep, and assert that (a) exactly one `getDayPlansByIds` call was made, (b) every date in the request set appears in the captured argument, and (c) every returned result maps to the correct date. The existing tests cover N=3, N=2 (same wave), N=1 (same date twice), and N=2 (separate waves) as concrete cases. A Glados generator for sets of 1–15 `DateTime` values would sweep the space more thoroughly. `ExploreConfig(numRuns: 80)` with `tags: 'glados'`.
  - **RESOLVED:** done — added a Glados property (1–16 dates, numRuns 80, `tags: 'glados'`): every same-sweep request set produces exactly one `getDayPlansByIds` call, the captured id set equals the requested set, and each date resolves to its own plan (alternating stored/missing plans cover the null path).
- [x] **[LOW]** The `getDayPlansInRange` delegation test is a simple pass-through; no Glados value here.
  **RESOLVED:** confirmed non-actionable — `getDayPlansInRange` is a one-line verbatim delegation to `JournalDb.getDayPlansInRange` with no logic to sweep; a Glados generator would only re-exercise the mock. No change.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `save` — the path where `_persistenceLogic.updateDbEntity` throws (update branch error propagation) is not tested. Currently only `createDbEntity` errors are implicitly untested; the impl silently propagates them. Add a test that stubs `updateDbEntity` to throw and asserts the exception surfaces to the caller.
  - **RESOLVED:** done — `'propagates an updateDbEntity throw on the update branch'` stubs the throw and asserts it surfaces to the caller.
- [x] **[MED]** `save` — the path where `_persistenceLogic.createDbEntity` throws (new-plan branch) is not tested. Same pattern as above.
  - **RESOLVED:** done — `'propagates a createDbEntity throw on the new-plan branch'` ditto for the create branch.
- [x] **[MED]** `getDayPlan` — the concurrent-call coalescing tests verify "same microtask wave" behaviour. There is no test for what happens when `_pendingBatch` is non-null but `_pendingIds` is null at the flush microtask (the `if (toFetch == null || target == null) return;` guard at line 83). This is a defensive guard path that is currently dead code; confirm with a comment or add a test that shows it cannot be reached by normal usage.
  - **RESOLVED:** done (documented) — the guard is genuinely unreachable (the single flush microtask is scheduled exactly once per batch by the first enqueued id, and nothing else nulls the fields); a comment in the impl now records that it is defensive only.
- [x] **[LOW]** `updateStream` — only one event is emitted and checked. The existing test does not verify that the stream relay is persistent (i.e., a second event on `controller.add(ids2)` also reaches `emitted`). A second `controller.add` + assertion would complete the stream delegation contract.
  **RESOLVED:** done — the `updateStream` test now adds a second event (`ids2`) after `pumpEventQueue()` and asserts `emitted == [ids1, ids2]`, proving the relay is persistent, not one-shot. Renamed the test accordingly.
- [x] **[LOW]** `getDayPlansInRange` — there is no test for when `rangeStart > rangeEnd` (backwards range). The impl simply delegates to `_journalDb.getDayPlansInRange`; documenting or testing the expected behaviour (presumably the DB handles it) would prevent silent breakage.
  **RESOLVED:** done — added a backwards-range test (`rangeStart > rangeEnd`) asserting the repo forwards the inverted bounds verbatim (verified via `verify`/`verifyNever` that no swap/normalization occurs) and returns the DB's result. This pins the contract: validation is the DB's job, not the repository's.

---

## Test execution speed opportunities

- [x] **[MED]** `test/.../repository/day_plan_repository_test.dart:344` — `await Future<void>.delayed(Duration.zero)` is flagged under fake-time policy. Beyond correctness, this is also a speed concern: injecting a real event-loop yield introduces non-determinism under high CI load. Replacing with a microtask flush or `StreamQueue.next` removes the dependency on OS scheduling. Estimated impact: negligible per test, but determinism benefit is high.
  - **RESOLVED:** done — same fix as the fake-time item: `pumpEventQueue()` removes the OS-scheduling dependency.
- [x] **[LOW]** The test file has no `setUp`/`tearDown` overhead beyond creating plain Dart objects — no `GetIt`, no Flutter binding. This is ideal; no speed improvements needed here.
  **RESOLVED:** confirmed non-actionable — `setUp` only instantiates three mocktail mocks and the repository; there is no GetIt registration or Flutter binding to optimize. No change.

---

## Summary

- No oversized files in this subdir; impl is 138 lines, test is 352 lines.
- 1 genuine fake-time violation: `Future.delayed(Duration.zero)` in the `updateStream` test at line 344 — LOW effort to fix (replace with `StreamQueue.next` or microtask flush).
- 1 genuine Glados opportunity: property-test the microtask-coalescing logic over generated sets of N dates (MED value, ~30 lines of new test code).
- 3 coverage gaps in error propagation paths (`createDbEntity` throws, `updateDbEntity` throws, dead-guard code in the flush microtask).
- Stream relay completeness: only one event tested; easy to extend.
