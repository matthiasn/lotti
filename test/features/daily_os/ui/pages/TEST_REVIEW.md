# Test Review: daily_os/ui/pages

**Scope:** `lib/features/daily_os/ui/pages/` (2 files, 508 impl lines) and
`test/features/daily_os/ui/pages/` (2 test files, 1351 test lines).

_Generated by automated test-quality review (static analysis)_

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/.../ui/pages/daily_os_page.dart` | 220 | Yes | Healthy size |
| `lib/.../ui/pages/set_time_blocks_page.dart` | 288 | Yes | Healthy size |
| `test/.../ui/pages/daily_os_page_test.dart` | 704 | n/a | Non-DRY setup; 5 inline Riverpod controller stubs; `RiverpodWidgetTestBench` repeated raw in 3 tests |
| `test/.../ui/pages/set_time_blocks_page_test.dart` | 647 | n/a | `DateTime.now()` usage; two near-duplicate navigator-setup helpers |

---

## File size / split opportunities

- [x] **[LOW]** Both impl files are under 300 lines; no split is needed.
  **RESOLVED:** confirmed true — `daily_os_page.dart` is 220 lines and `set_time_blocks_page.dart` is 290 lines; neither warrants a split. Non-actionable observation.
- [x] **[LOW]** Both test files are under 750 lines; they are within acceptable bounds. The 704-line `daily_os_page_test.dart` is the larger, but the verbosity comes mainly from the three tests that bypass `createTestWidget` and construct `RiverpodWidgetTestBench` + `TimeHistoryData` inline (lines 424–607). Refactoring those into the shared helper would reduce the file to ~550 lines.
  **RESOLVED:** the HIGH DRY refactor (line 30) already routed those three tests through `createTestWidget`'s `unifiedControllerFactory`; `daily_os_page_test.dart` is now 479 lines and `set_time_blocks_page_test.dart` is well under 750. Both files are within bounds — no further action.

---

## Test quality improvements

- [x] **[HIGH]** `test/.../ui/pages/daily_os_page_test.dart:424–607` — Three tests (`tapping agree button on draft banner calls agreeToPlan`, `tapping re-agree button on review banner calls agreeToPlan`, `shows no banner when unified data controller errors`) each construct a full `RiverpodWidgetTestBench` with all six overrides and a `TimeHistoryData` object inline, duplicating ~40 lines of boilerplate per test. These tests differ only in: which controller subclass is supplied, which plan status is used, and which assertion is made. Extract a file-level helper `_pumpPageWithController(tester, controller, {planStatus, historyData})` (or extend `createTestWidget` to accept a custom controller factory). Impact: reduces ~120 lines of duplication to ~20. **RESOLVED:** `createTestWidget` now accepts `unifiedControllerFactory`; the three tests pass their tracking/error controller through it and reuse `createTestPlan`/`createTestTimelineData` — the ~120 lines of duplicated bench/override/TimeHistoryData construction are gone.
- [x] **[HIGH]** `test/.../ui/pages/daily_os_page_test.dart:24–94` — Five inline Riverpod controller/notifier subclasses are defined (`_TestUnifiedController`, `_TrackingUnifiedController`, `_ErrorUnifiedController`, `_TestTimeHistoryController`, `_TestDailyOsSelectedDate`). Per AGENTS.md, controllers that override async builds are not centralized mocks, so these are acceptable in principle. However, `_TestTimeHistoryController` and `_TestDailyOsSelectedDate` are generic stubs reusable across the ui/pages subdir; consider moving them to `test/features/daily_os/ui/pages/test_helpers.dart` (mirroring the pattern in `time_history_header/test_helpers.dart`). **RESOLVED:** `_TestUnifiedController`, `_TestTimeHistoryController`, and `_TestDailyOsSelectedDate` were exact duplicates of the shared `TestUnifiedController`/`TestTimeHistoryController`/`TestDailyOsSelectedDate` in `../widgets/time_history_header/test_helpers.dart` (already imported) — the inline copies are deleted and the shared classes used. `_TrackingUnifiedController`/`_ErrorUnifiedController` stay file-local (test-specific behavior).
- [x] **[MED]** `test/.../ui/pages/daily_os_page_test.dart:202–254` — Six separate `testWidgets` cases each do `createTestWidget()` + `pumpAndSettle()` and then assert `findsOneWidget` for a single widget type (`DailyOsPage`, `TimeHistoryHeader`, `DailyTimeline`, `TimeBudgetList`, `DaySummary`, `RefreshIndicator`). Each is a one-line `expect` on a structural element. Per AGENTS.md, "`findsOneWidget` alone is not a valid test — it only proves the widget tree built without crashing." Consolidate these into one `testWidgets('renders all primary structural sections', ...)` test that asserts all six widget types together in a single pump, or merge into the interaction tests that already check the same widgets.
  - **RESOLVED:** done — the nine structural one-liners (main structure, TimeHistoryHeader, DailyTimeline, TimeBudgetList, DaySummary, RefreshIndicator, FAB, scrollable, SafeArea, layout) consolidated into one `'renders all primary structural sections'` test with a single pump; the redundant `'FAB is tappable'` onPressed-isNotNull test dropped (subsumed by the real `'tapping FAB opens AddBlockSheet'` interaction test).
- [x] **[MED]** `test/.../ui/pages/set_time_blocks_page_test.dart:501–551` — The "back button pops the navigator" test constructs a two-route `Navigator` manually, duplicating the approach already established in `pumpPageWithNavigator` (lines 370–416). The two differ only in which button is tapped. Extract the navigator boilerplate into a shared private helper, or generalize `pumpPageWithNavigator` to cover the back-button scenario.
  - **RESOLVED:** done — `pumpPageWithNavigator` hoisted to file level and the back-button test now reuses it; the duplicated two-route Navigator boilerplate is gone. The helper factories also now mint a fresh `_TestUnifiedController` per provider init (the page stops watching the autoDispose provider after init, so riverpod may dispose and re-create it — returning the same notifier instance from `overrideWith` throws).
- [x] **[LOW]** `test/.../ui/pages/set_time_blocks_page_test.dart:156–200` — The "layout" group has five separate `testWidgets` calls that each pump the same page and assert a single `find.text(...)` result. These could be merged into one `testWidgets('renders expected layout elements', ...)` test, which would also cut five `pumpPage + pumpAndSettle` round-trips (speed benefit).
  **RESOLVED:** done — the five layout one-liners (header title + back icon, Favourites header, Other categories header, the four category rows, Save plan button) are merged into one `'renders all expected layout elements in a single pump'` test, eliminating four redundant `pumpPage` round-trips.

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** No Glados tests are appropriate for widget tests; both pages are UI-composition layers with no pure-logic invariants to property-test. No gaps here.
  **RESOLVED:** confirmed true — both files are `ConsumerStatefulWidget`/`ConsumerWidget` composition layers; their only logic (`_hasChanges`, `_initFromExistingPlan` idempotence) is now covered by example-based widget tests. No pure function with a property invariant to fuzz. Non-actionable observation.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `test/.../ui/pages/daily_os_page_test.dart` — The `_onDragActiveChanged` callback test at line 626 correctly exercises the toggle. However, there is no test for the **scroll-disable/re-enable round-trip after a full drag gesture** (start → move → end) rather than direct callback invocation. The current test invokes the callback directly, bypassing the actual `DraggablePlannedBlock` gesture recognition. This means the path from a pointer-down event through to the callback is untested. **RESOLVED (already covered):** the real pointer-down → long-press(600ms) → up round-trip through `DraggablePlannedBlock`'s gesture recognition is exercised in `draggable_planned_block_test.dart` ('long press initiates drag and calls onDragActiveChanged', via `tester.startGesture`), asserting the callback flips true then false. The page test deliberately invokes the callback directly per the project's deterministic-interaction policy for batched `very_good` runs (real gesture-driving at page level is a known in-suite flake source); both halves of the path are covered.
- [x] **[MED]** `test/.../ui/pages/daily_os_page_test.dart` — The `_AgreementBanner` private widget is tested only via its containing page. The banner's icon/colour distinction between warning and non-warning (`isWarning: true` uses `Colors.orange`, `isWarning: false` uses `colorScheme.primary`) has no direct assertion. Add an assertion that the review banner produces an orange icon (warning visual) while the draft banner does not.
  - **RESOLVED:** done — the draft-banner test asserts the clipboard icon uses `colorScheme.primary` (and is not orange); the review-banner test asserts the alert icon is `Colors.orange`.
- [x] **[MED]** `test/.../ui/pages/set_time_blocks_page_test.dart` — `_hasChanges` getter path: when `_hadExistingBlocks` is true and all blocks are then deleted (all categories cleared to empty), `_hasChanges` should still be `true` (to allow saving an empty plan). This "clear all blocks" state transition is untested.
  - **RESOLVED:** done — added `'deleting every existing block still allows saving an empty plan'`: expands Work, deletes its only block, asserts the save button stays enabled (`_hadExistingBlocks` keeps `_hasChanges` true), saves, and verifies the controller received an empty block list and the page popped.
- [x] **[MED]** `test/.../ui/pages/set_time_blocks_page_test.dart` — The `_isSaving = true` state (button disabled during async save) is not directly tested. The test at line 418 verifies the final state (success/error) but does not assert that the button was disabled while the save was in flight.
  - **RESOLVED:** done — added `'save button is disabled while the save is in flight'`: a `Completer` save gate keeps `setPlannedBlocks` pending, the button is asserted disabled mid-flight and `savedBlocks` unset, then the gate completes and the save finishes/pops.
- [x] **[LOW]** `test/.../ui/pages/set_time_blocks_page_test.dart` — `_initFromExistingPlan` idempotence: calling it twice with the same date should not clear and re-populate `_pendingBlocks`. This is guarded by the `_initializedDate == date` check, but it is not directly tested.
  **RESOLVED:** done — added `'rebuild after a user edit does not re-seed from the existing plan'`: seeds an existing Work block, then adds a Study block and collapses Study (each step triggers a rebuild that re-invokes `_initFromExistingPlan`). It asserts two `check_circle` icons (both blocks survive) and that the persisted plan contains exactly `cat-study` + `cat-work` — proving the `_initializedDate == date` guard prevents re-seeding and the existing block was not duplicated.

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/.../ui/pages/daily_os_page_test.dart` — `pumpAndSettle()` is called 20 times across 23 tests (every test uses it). For tests that only pump async providers (no animations), `await tester.pump()` (for microtasks) + `await tester.pump(Duration(milliseconds: 100))` (for provider resolution) would be faster and would eliminate the 10-second hang risk if a provider never settles. At minimum, the structure tests (lines 202–254) should use `tester.pump()` since they test static composition only. **RESOLVED:** all 25 settles replaced with bounded `pump()` + `pump(300ms)` pairs; full file passes.
- [x] **[MED]** `test/.../ui/pages/set_time_blocks_page_test.dart` — `pumpPage` always calls `await tester.pumpAndSettle()` at line 153. The layout tests (lines 156–200) each call `pumpPage` and then do nothing that involves animations. Replace `pumpAndSettle()` in `pumpPage` with `tester.pump()` and a single extra `tester.pump()` for provider microtasks, or make `pumpAndSettle` opt-in.
  - **RESOLVED:** (stale) — `pumpPage` already uses `pump()` + bounded `pump(300ms)`; no `pumpAndSettle` remains in it. The settles left in the file are route-transition settles in `pumpPageWithNavigator`, which genuinely need them.
- [x] **[MED]** `test/.../ui/pages/set_time_blocks_page_test.dart:558` — Uses `DateTime.now()` in the "shows Today prefix when selected date is today" test to construct `todayDate`. Per AGENTS.md fake-time policy, tests must use deterministic dates. This test's correctness depends on `_date.dayAtMidnight == DateTime.now().dayAtMidnight` inside the impl, making it date-sensitive. The test can be made deterministic by injecting a fake "today" clock into the widget or by using a provider override for the current date comparison. Until then the test is potentially flaky if it runs near midnight.
  - **RESOLVED:** done — root-cause fix: the impl now reads `clock.now()` (package:clock, already used across the codebase) instead of `DateTime.now()`, and the test wraps `pumpPage` in `withClock(Clock.fixed(...))` pinned to the deterministic test date — no more midnight flakiness.

---

## Summary

- Neither impl file is oversized; no split needed.
- 1 oversized test file (`daily_os_page_test.dart` at 704 lines) driven by 3 tests that bypass the shared `createTestWidget` helper — HIGH-priority DRY refactor.
- 5 structural smoke-only `findsOneWidget` tests should be consolidated (MED).
- `DateTime.now()` usage in `set_time_blocks_page_test.dart:558` is the only fake-time violation — makes the test flaky near midnight (HIGH impact, LOW fix effort: inject a clock override).
- 3 speed wins from replacing `pumpAndSettle()` with `tester.pump()` in structure/layout tests.
- Key coverage gap: `_hasChanges` after block deletion + button-disabled-during-save state are not tested.
