# Test-Quality Review — `lib/features/daily_os/ui/widgets/` (top-level)

**Scope:** `lib/features/daily_os/ui/widgets/*.dart` + `test/features/daily_os/ui/widgets/*_test.dart`
(excludes `time_history_header/` sub-dir which has its own report)

Generated by automated test-quality review (static analysis)

---

## File table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `daily_timeline.dart` | 1203 | yes (2759 ln) | Oversized on both sides; private pure functions untested; inline getIt boilerplate |
| `time_budget_card.dart` | 1015 | yes (2578 ln) | Oversized on both sides; 4 near-smoke tests; 5 copy-paste status permutations; inline getIt |
| `draggable_planned_block.dart` | 530 | yes (1159 ln) | 16 `pumpAndSettle` calls; inline getIt boilerplate |
| `daily_os_empty_states.dart` | 489 | yes (397 ln) | 20 `pumpAndSettle` per test; animation-only assertions |
| `planned_block_edit_modal.dart` | 409 | yes (744 ln) | 8 `pumpAndSettle`; test-local `setUpAll` fallbacks instead of central fallbacks |
| `add_budget_sheet.dart` | 401 | yes (898 ln) | 38 `pumpAndSettle`; inline getIt boilerplate |
| `category_block_row.dart` | 353 | yes (507 ln) | 11 `pumpAndSettle`; uses `setUpTestGetIt` correctly |
| `compressed_timeline_region.dart` | 269 | yes (696 ln) | 9 `pumpAndSettle`; raw `MaterialApp` wrappers (not `makeTestableWidget`) |
| `day_summary.dart` | 244 | yes (142 ln) | 7 `pumpAndSettle`; 1 smoke-only test (`findsOneWidget DaySummary`) |
| `time_block_editor.dart` | 227 | yes (453 ln) | 8 `pumpAndSettle`; good structure otherwise |
| `time_budget_list.dart` | 207 | yes (332 ln) | 14 `pumpAndSettle` |
| `planned_block_drag_state.dart` | 112 | yes (575 ln) | Glados candidate (value-class properties); tests are unit-style and good |
| `zigzag_fold_indicator.dart` | 102 | no own file — tested via `compressed_timeline_region_test.dart` | No dedicated test file (per AGENTS one-test-per-source-file rule) |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/daily_os/ui/widgets/daily_timeline.dart` (1203 ln) — exceeds the 1000-line hard limit. Concrete split seams:
  - Lines 25–155: pure utility module — `_slotContains`, `_groupNestedSlots`, `_assignLanes`, `_getLaneCount`, `_assignLanesToSlots` could move to `lib/features/daily_os/util/lane_assignment_utils.dart` with their own test file. These are non-trivial algorithms with no widget dependency.
  - Lines 774–1014: `_ActualBlockWidget` + `_ActualBlockContent` + `_NestedChildBlock` form a self-contained sub-tree that could become `actual_block_widget.dart`.
  - Lines 263–513: `_TimelineContent` + `_FoldedTimelineGrid` + `_TimelineHeader` could become `timeline_grid.dart`.
  - The remaining skeleton (`DailyTimeline` + state classes + `_CurrentTimeIndicator`) would drop to ~250 ln.

- [x] **[HIGH]** `lib/features/daily_os/ui/widgets/time_budget_card.dart` (1015 ln) — exceeds the 1000-line hard limit. Concrete split seams:
  - Lines 641–977: `_TaskProgressRow`, `_TaskGridTile`, `_DueBadge`, `_DueGridBadge`, `_PriorityBadge`, `_PriorityGridBadge`, `_TaskCompletionIndicator`, `_TaskListContent` all form a self-contained task-list sub-system → `task_list_content.dart`.
  - Lines 348–572: `_MiniProgressBar`, `_NoBudgetBadge`, `_StatusText` → `budget_card_indicators.dart`.
  - `TimeBudgetCard` widget itself + state would drop to ~200 ln.
  **RESOLVED:** split via `part` files along these seams — `budget_card_task_list.dart` (task rows/tiles/badges + list content, 526 lines) and `budget_card_indicators.dart` (mini progress bar, no-budget badge, status text, 145 lines); `time_budget_card.dart` is down to 350 lines and the single mirror test is untouched.

- [x] **[MED]** Test file `test/features/daily_os/ui/widgets/daily_timeline_test.dart` (2759 ln) — deeply oversized. Mirror-splits should follow the impl splits above; the lane-assignment utilities section would become its own test file once extracted. **RESOLVED:** (adapted) the lane utilities were extracted to `timeline_lane_layout.dart` with their own mirror during the HIGH pass; the remaining `daily_timeline_layout.dart` part is a 38-line private helper rendered only through `DailyTimeline`, so the single widget mirror stays per the one-test-per-source rule.

- [x] **[MED]** Test file `test/features/daily_os/ui/widgets/time_budget_card_test.dart` (2578 ln) — once the impl is split, the test should follow. In the meantime, the `TimeBudgetCard - Pinned Tasks Section` and `TimeBudgetCard - Contributing Tasks Section` groups duplicate `createTask` / `taskToProgress` helpers defined twice at local scope (lines 427–458 and 851–879); extracting a single top-level helper would already shrink the file ~100 ln. **RESOLVED:** done — the duplicated `createTask` is hoisted to one top-level factory; the part classes (`_Task*`, `_DueBadge`, …) are all private and render only through `TimeBudgetCard`, so every test legitimately mounts the card and the single mirror stays.

- [x] **[LOW]** `lib/features/daily_os/ui/widgets/draggable_planned_block.dart` (530 ln) — just above the ~500 soft limit; not urgent unless lint logic grows further.
  **RESOLVED:** (no change) confirmed at 530 ln — comfortably under the 1000-line hard limit and only marginally over the ~500 soft guideline. The review itself flags this as "not urgent unless lint logic grows further", and it has not grown. No split performed: it would mean editing an impl file outside the test scope of this pass, and it cannot be verified to compile under the no-commands constraint of this run.

- [x] **[LOW]** `lib/features/daily_os/ui/widgets/daily_os_empty_states.dart` (489 ln) — borderline; the `_TimelineIllustrationPainter` (custom paint) could be extracted but it is not urgent.
  **RESOLVED:** (no change) confirmed at 489 ln — below the 1000-line hard limit and the ~500 soft limit. The review marks the `_TimelineIllustrationPainter` extraction as "not urgent"; extracting a `CustomPainter` is an impl-file change outside this pass's test scope and would alter the rendering tree without the ability to verify it compiles or that the painter tests still pass under the no-commands constraint. Left intact.

---

## Test quality improvements

- [x] **[HIGH]** `time_budget_card_test.dart:261–269` and `:408–423` — two tests named "shows progress bar" and "shows progress bar with correct visual states" each assert only `find.byType(TimeBudgetCard), findsOneWidget`. These are pure constructor-smoke tests with zero behavioral value. They must be replaced with assertions on the actual bar width/color or the displayed status text.

- [x] **[HIGH]** `time_budget_card_test.dart:483–497` (`hides pinned tasks section when empty`) — asserts `findsOneWidget(TimeBudgetCard)` as its primary behavioral assertion. The real test intent is "task section divider is absent"; the secondary assertion at line 492 (`findsNothing`) is the only meaningful one; the `TimeBudgetCard` line should be removed.

- [x] **[HIGH]** `time_budget_card_test.dart:570–771` — five copy-paste task-status tests ("does not show check icon for in-progress/open/blocked/on-hold/groomed") are nearly identical test bodies differing only by `TaskStatus` variant. Convert to a parameterized loop over `[TaskStatus.inProgress, TaskStatus.open, TaskStatus.blocked, TaskStatus.onHold, TaskStatus.groomed]`. Similarly the two "shows check_circle" variants should be parameterized over `[TaskStatus.done, TaskStatus.rejected]`.

- [x] **[HIGH]** `daily_timeline_test.dart:521–556` (`highlights planned block when category is highlighted`) — sole assertion is `findsOneWidget(DailyTimeline)`. Test says "Widget should render (test passes if no exception)". This is a smoke test, not a behavioral test. Replace with an assertion on the `AnimatedContainer`'s border decoration or the `BoxDecoration` color.

- [x] **[HIGH]** `daily_timeline_test.dart:2712–2756` (`long press on actual block without category does not crash`) — primary assertion is `find.byType(GestureDetector), findsWidgets`. The smoke nature is documented in the comment but a genuine assertion on `state.highlightedCategoryId == null` already exists at line 2754; the `findsWidgets` line at 2744 should be removed as it adds nothing.

- [x] **[MED]** `time_budget_card_test.dart` — inline `class MockNavService extends Mock implements NavService` (lines 30–37) with a `navigationHistory` list, while `test/mocks/mocks.dart:510` already defines `MockNavService`. The test hides the centralized version (`hide MockNavService`). The local override is needed for the history list. The fix is to add `navigationHistory` tracking to the centralized mock (or use `beamToNamedOverride` pattern from `daily_timeline_test.dart`). **RESOLVED:** done — the bespoke subclass is gone; the production calls go through the top-level `beamToNamed`, so the tests now capture via `beamToNamedOverride` and register the centralized `MockNavService`.

- [x] **[MED]** `daily_timeline_test.dart:82–93` and `draggable_planned_block_test.dart:216–226` — inline `getIt.isRegistered / getIt.unregister / getIt.registerSingleton` in `setUp`/`tearDown` instead of using `setUpTestGetIt()` / `tearDownTestGetIt()` from `test/widget_test_utils.dart`. Same pattern in `add_budget_sheet_test.dart:72–89` and `time_budget_card_test.dart:1919–1935`. **RESOLVED:** done — daily_timeline, draggable_planned_block, and add_budget_sheet now use `setUpTestGetIt(additionalSetup:)` / `tearDownTestGetIt`; the two time_budget_card groups were rewired alongside the nav-seam change with explicit reset+register plus the documented override teardown.

- [x] **[MED]** `compressed_timeline_region_test.dart` — every test wraps with raw `MaterialApp(home: Scaffold(body: ...))` instead of `makeTestableWidget()`. Impacts 21 test bodies. Extract a `_pump(tester, widget)` helper using `makeTestableWidget`. **RESOLVED:** done — `_pumpRegion(tester, region: …)` replaced all 12 raw MaterialApp wraps.

- [x] **[MED]** `daily_os_empty_states_test.dart:50–66` (`animates in on mount`) — asserts `find.byType(TimelineEmptyState), findsOneWidget` twice, once mid-animation and once after settle. Both are smoke assertions with zero behavioral value; the animation itself is never measured. Either remove the test or assert the opacity/transform state via `AnimatedBuilder.value`. **RESOLVED:** done — the test now reads the entrance `Opacity` value: partial (<1) mid-animation, exactly 1 after completion, and strictly increasing.

- [ ] **[LOW]** `time_history_header/time_history_header_test.dart:59–73` (`shows selection highlight on selected day`) — asserts `find.ancestor(...matching: find.byType(Container)), findsWidgets`. A structural finder that only proves a Container exists. Replace with an assertion on the `BoxDecoration.border` property of the selected-day container.
  **DEFERRED:** out of scope. This item targets `time_history_header/time_history_header_test.dart`, which lives in the `time_history_header/` sub-directory that has its own dedicated review and is explicitly excluded from this top-level review. It should be addressed in that sub-dir's review pass, not here.

- [x] **[LOW]** `time_budget_card_test.dart:332–351` (`shows highlighted border when category is highlighted`) — asserts `find.byType(AnimatedContainer), findsWidgets`. Should verify the `Border` on the outer `AnimatedContainer`'s `BoxDecoration` is non-null, since that is the precise branch under test.
  **RESOLVED:** the highlighted test now reads the outer `AnimatedContainer`'s `BoxDecoration.border` and asserts a 2px `Border` tinted with `colorFromCssHex(testCategory.color)`; added a companion test proving the border is `null` in the non-highlighted case (the else branch). `ModernBaseCard` uses a plain `Container`, so `find.byType(AnimatedContainer)` resolves uniquely to the outer one.

- [x] **[LOW]** `day_summary_test.dart:135–140` (`renders DaySummary widget`) — sole assertion is `findsOneWidget(DaySummary)`. Pure smoke test. Remove or replace with a meaningful assertion (e.g., that the progress percentage is displayed).
  **RESOLVED:** the smoke test is replaced with two behavioral tests — one asserts the `2h 30m` remaining value (exercising the `dailyOsDurationHoursMinutes` formatting branch that was otherwise untested), and one asserts the over-budget value `1 hour` is rendered in `colorScheme.error` (the previously-untested `valueColor: error` branch).

---

## Generative (Glados) testing opportunities

None of the files in this scope currently have Glados tests.

- [x] **[HIGH]** `lib/features/daily_os/ui/widgets/daily_timeline.dart` — `_slotContains`, `_groupNestedSlots`, `_assignLanes`, `_getLaneCount`, `_assignLanesToSlots` (lines 48–161, 953–1013) are pure functions operating on structured `ActualTimeSlot` inputs. Key invariants to probe: **(Now lives in `lib/features/daily_os/ui/widgets/timeline_lane_layout.dart` — the helpers were extracted there with direct unit + Glados coverage in `timeline_lane_layout_test.dart`.)**
  - `_assignLanes` never assigns two overlapping slots to the same lane (non-overlap invariant for all input sequences).
  - `_groupNestedSlots` never assigns the same slot as both a parent and a child.
  - `_getLaneCount` equals `max(laneIndex) + 1` for all non-empty inputs.
  - These are worth ~2–3 Glados property tests once moved to `lane_assignment_utils.dart`.

- [x] **[HIGH]** `lib/features/daily_os/ui/widgets/time_budget_card.dart` — `_formatCompactDuration` (lines 21–29) is a pure `Duration → String` formatter. Invariants: for any `duration` with `inHours > 0` the output always starts with `${hours}h`; for `mins == 0` no "m" suffix appears; round-trip parsing is testable. A handful of Glados property tests over arbitrary `Duration` values would guard against future formatting regressions.
  - `_getTaskStatusColor` (lines 32–43): a pure `TaskStatus → Color` switch — all status enum values are covered, no nesting. Parameterized static tests are sufficient; not a Glados candidate.
  - `_calculatePriorityBadgeTop` (lines 971–976): pure arithmetic — `isCompleted × completedBadgeHeight + isDueOrOverdue × dueBadgeHeight + initialTop`. Algebraic invariant (additive, bounded). A single Glados property test over `(bool, bool)` input pairs.

- [x] **[MED]** `lib/features/daily_os/ui/widgets/planned_block_drag_state.dart` — `PlannedBlockDragState.copyWith` (line 78): idempotence invariant — `state.copyWith() == state` for all valid states. `hasChanged` invariant — `hasChanged` iff `currentStart != originalStart || currentEnd != originalEnd`. Both can be expressed as Glados properties over `(int startMin, int endMin, int deltaMin)` generators. **RESOLVED:** done — two properties: `copyWith()` is the identity (incl. hashCode), and `hasChanged` holds iff the (day-window-clamped) delta is non-zero for both move and resize shapes.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/features/daily_os/ui/widgets/daily_timeline.dart` — the lane-assignment and nested-slot grouping algorithms (`_slotContains`, `_groupNestedSlots`, `_assignLanes`, `_assignLanesToSlots`) have **no direct unit tests**. They are exercised only indirectly through widget integration tests, making branch coverage opaque. Move to a utility file and add direct unit tests covering: empty input, single slot, two fully-overlapping slots (same/different category), three-way overlap, slot spanning multiple lanes. **(Now lives in `lib/features/daily_os/ui/widgets/timeline_lane_layout.dart` — the helpers were extracted there with direct unit + Glados coverage in `timeline_lane_layout_test.dart`.)**

- [x] **[HIGH]** `_ActualBlockContent` (daily_timeline.dart:842) nesting-depth branches (`nestingDepth > 0`) — the `isNested` visual path is exercised by the "renders single nested child" widget test but the multi-level nesting (depth > 1) path in `_NestedChildBlock` is never directly asserted. Add a test with a parent containing two overlapping children to verify both nested-lane splitting and nesting-depth > 1 rendering.

- [x] **[MED]** `_buildTimeRow` in `time_budget_card.dart:282–345` — three conditional scenarios (Scenario A: no budget + no time; Scenario B: no budget + time recorded; Normal) are present. Only "Normal" and Scenario A are tested via `hasNoBudgetWarning`. Scenario B (no budget but time recorded) should have a dedicated test asserting `"Xm / 0m"` format alongside the "No time budgeted" badge. **RESOLVED:** (stale) 'Scenario B: no budget, has time - shows Xm / 0m with No time budgeted badge' exists in the No Budget Display group.

- [x] **[MED]** `_TimeBudgetCardState.didUpdateWidget` (time_budget_card.dart:90–98) — `_userHasToggled` guard: after a manual toggle the expand state must not be overridden by `isFocusActive` changes. There is no test asserting this. Add a test: expand manually → change `isFocusActive` → verify still expanded. **RESOLVED:** (stale) 'preserves user toggle when focus changes' covers exactly this flow (manual collapse → focus off → focus back → still collapsed).

- [x] **[MED]** `daily_os_empty_states.dart` — `BudgetEmptyState`, `NoPlansEmptyState` and other empty-state variants have some tests but the "Add a Plan" / navigation tap tests at lines 248–290 assert only via `pumpAndSettle()` plus `findsOneWidget` on the widget type. Navigation callbacks are never verified as invoked. **RESOLVED:** the beam test already pins the delegate landing on `/calendar/set-time-blocks`; the Add Budget tap now also asserts the `AddBlockSheet` modal actually opens.

- [x] **[MED]** `zigzag_fold_indicator.dart` — no dedicated test file (rule: one test per source file). The `ZigzagFoldIndicator` and `ZigzagFoldPainter` are tested inside `compressed_timeline_region_test.dart:135–190`. Create `test/features/daily_os/ui/widgets/zigzag_fold_indicator_test.dart` with painter hit-test and canvas-call assertions. **RESOLVED:** done — the zigzag groups moved to the new mirror, which additionally records the canvas: one path spanning the full height and `zigzagWidth`, painted with the configured color/stroke.

- [x] **[LOW]** `_CurrentTimeIndicator` (daily_timeline.dart:1096) — the `isHourInCompressedRegion` guard that hides the indicator is partially tested (line 2462). The `top < 0` guard (line 1126) returning `SizedBox.shrink()` has no dedicated test.
  **RESOLVED:** added a dedicated test ("hides indicator when current hour is in a compressed region") in the Current Time Indicator group that freezes the clock at 04:00 today with a single noon block (which compresses 0-12), asserting the `CompressedTimelineRegion` exists yet the red current-time dot is `findsNothing` — directly covering the `isHourInCompressedRegion` → `SizedBox.shrink()` branch (line 903/908). The separate `top < 0` guard (line 919) is defensive dead code: `timeToFoldedPosition` starts at `0.0` and only accumulates non-negative terms, so it can never return a negative value through the widget API; left unguarded by a test because reaching it would require fabricating an impossible folding state.

- [x] **[LOW]** `add_budget_sheet.dart` — time auto-adjustment logic (lines 71–78): when start is set past end, end auto-adjusts to `start + 1h` clamped at 23:00. The clamp-at-23:00 path is untested.
  **RESOLVED:** added "start time at 23:00 clamps auto-adjusted end time at 23:00". The local `createTestWidget` now accepts a `mediaQueryData` override; the test passes `alwaysUse24HourFormat: true` so the keyboard hour field accepts `23` directly (no AM/PM toggle), then asserts both Start and End selectors read `23:00` (`findsNWidgets(2)`), exercising `(23 + 1).clamp(0, 23) == 23`. A guaranteed dialog-opened assertion guards the rare case where the Material keyboard toggle is unavailable.

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/features/daily_os/ui/widgets/add_budget_sheet_test.dart` — **38 `pumpAndSettle` calls** across 31 test cases. Most are used to settle simple state updates (form field display, label rendering) that have no running animations. Replace with `tester.pump()` for non-animation tests. Estimated suite time impact: ~380s of headroom (38 × 10s timeout risk); realistically 1–2s per test on fast hardware but ~5–10s per test on slow CI runners with animation-heavy paths. **Highest impact file in scope.**

- [x] **[HIGH]** `test/features/daily_os/ui/widgets/daily_os_empty_states_test.dart` — **20 `pumpAndSettle` calls** across 18 tests. The widget uses `AnimationController` with 800ms duration. Every `pumpAndSettle` must drain the full animation. Replace with `tester.pump(const Duration(milliseconds: 900))` (one pump past the animation duration) to deterministically settle without risking infinite animation hang.

- [x] **[HIGH]** `test/features/daily_os/ui/widgets/draggable_planned_block_test.dart` — **16 `pumpAndSettle` calls** (0 were needed vs actual count confirmed). Many tests pump after a simple `tap` or widget rebuild where there are no animations. Replace with `tester.pump()` or `tester.pump(const Duration(milliseconds: 250))` for drag/gesture tests that need one animation frame.

- [x] **[MED]** `test/features/daily_os/ui/widgets/time_budget_list_test.dart` — **14 `pumpAndSettle` calls** across 12 tests. The list has `AnimatedSize` expansion but tests that don't interact with expand/collapse don't need settle. Replace 8–10 of these with `tester.pump()`. **RESOLVED:** (stale) the file carries zero `pumpAndSettle` calls now.

- [x] **[MED]** `test/features/daily_os/ui/widgets/compressed_timeline_region_test.dart` — **9 `pumpAndSettle` calls**. The `AnimatedTimelineRegion` widget drives animations but the `CompressedTimelineRegion` sub-tests at lines 10–135 have no animations. Use `tester.pump()` for static rendering tests; reserve `pumpAndSettle` only for the `AnimatedTimelineRegion` expand/collapse tests. **RESOLVED:** done — the static `CompressedTimelineRegion` wraps run through the single-frame `_pumpRegion` helper; every remaining settle sits in the `AnimatedTimelineRegion` animation tests.

- [x] **[MED]** `test/features/daily_os/ui/widgets/category_block_row_test.dart` — **11 `pumpAndSettle` calls**. Most tests pump after simple tap/state change without animation. Replace majority with `tester.pump()`. **RESOLVED:** (stale) the file carries zero `pumpAndSettle` calls now.

- [x] **[LOW]** `test/features/daily_os/ui/widgets/planned_block_edit_modal_test.dart` — **8 `pumpAndSettle` calls** concentrated in time-picker interaction tests (lines 386–410, 563, 693–737). These interact with `showTimePicker` dialogs which do use animations; however, three back-to-back `pumpAndSettle` at lines 406–410 could be collapsed into a single one.
  **RESOLVED:** (no change) inaccurate after prior edits shifted the lines. The calls at 386/406/410 are not back-to-back: they span two separate tests, and the only same-test pair (406 then 410) is split by an intervening `tester.tap(find.text('Cancel').last)`, so collapsing would skip the dialog-dismiss interaction. Every `pumpAndSettle` here settles a genuine AlertDialog/TimePickerDialog open or close animation and is separated from the next by an assertion (e.g. line 696 verifies the picker is visible before tapping OK). None are safely collapsible.

- [x] **[LOW]** `test/features/daily_os/ui/widgets/daily_timeline_test.dart` — **2 `pumpAndSettle` calls** (line 1 call at 2433 for provider error propagation — justified; line 2 is a comment). No action needed.
  **RESOLVED:** (no change) confirmed — the file has a single real `pumpAndSettle` (in the "shows error state when provider throws" Async States test), which is justified for draining the Riverpod async-notifier error propagation; the other match is inside a comment. No collapsible/wasteful settles. Note: the new compressed-region indicator test added in this pass uses only `tester.pump()`, so it does not add any `pumpAndSettle`.

- [ ] **[LOW]** Per-test `getIt.reset()` at `time_budget_card_test.dart:1920` and `:2325` (two groups) runs a full GetIt teardown/setup on every test. Consider grouping the navigation tests that require full GetIt setup into a single `setUpAll` + `tearDownAll` to avoid per-test GetIt churn.
  **DEFERRED:** unsafe. Both groups create fresh mocktail mocks per test and assert with `verify(...).called(1)` and `verify(...).captured` (Quick Create Task Button: `createTaskEntry` called once + captured `data.due`; Task Row Navigation: captured `categoryId == null`). Sharing mocks via `setUpAll` would make interaction counts accumulate across tests (`.called(1)` would fail on the 2nd test) and leak `when(...)` stubs between tests. The per-test `getIt.reset()` + fresh-mock pattern is also what keeps GetIt clean for the file's other groups (which register nothing) and satisfies the CI same-thread cleanup requirement in AGENTS.md. Collapsing to `setUpAll`/`tearDownAll` would trade a negligible speed win for real cross-test contamination.

---

## Summary

- 2 oversized impl files (`daily_timeline.dart` 1203 ln, `time_budget_card.dart` 1015 ln); 2 severely oversized test files (`daily_timeline_test.dart` 2759 ln, `time_budget_card_test.dart` 2578 ln).
- 8 weak/smoke-only tests found (`findsOneWidget(WidgetType)` or `findsWidgets` as sole meaningful assertion): `time_budget_card_test` ×4, `daily_timeline_test` ×2, `daily_os_empty_states_test` ×1, `day_summary_test` ×1.
- 7 copy-paste test permutations in `time_budget_card_test.dart` (task-status check-icon tests) — convertible to a parameterized loop saving ~150 ln.
- 3 Glados candidates: lane-assignment pure functions (`daily_timeline.dart` — **HIGH**, ~2–3 properties), `_formatCompactDuration` and `_calculatePriorityBadgeTop` in `time_budget_card.dart` (**MED**), `PlannedBlockDragState.copyWith`/`hasChanged` (**MED**).
- 5 speed wins: `add_budget_sheet_test` (38 `pumpAndSettle`, highest risk), `daily_os_empty_states_test` (20), `draggable_planned_block_test` (16), `time_budget_list_test` (14), `category_block_row_test` (11).
- Biggest single opportunity: extract `_slotContains` / `_groupNestedSlots` / `_assignLanes` from `daily_timeline.dart` into a dedicated utility file — it simultaneously reduces the oversized impl, enables direct unit + Glados tests for non-trivial pure algorithms, and removes the dependency on full widget tests to cover algorithm correctness.
