# Test-Quality Review — `lib/features/daily_os/ui/widgets/time_history_header/`

**Scope:** `lib/features/daily_os/ui/widgets/time_history_header/*.dart` + `test/features/daily_os/ui/widgets/time_history_header/*_test.dart`

Generated by automated test-quality review (static analysis)

---

## File table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `time_history_header_widget.dart` | 547 | yes (516 ln) | 5 `pumpAndSettle` calls; borderline size (>500) |
| `time_history_stream_chart.dart` | 232 | yes (815 ln) | Test file ~3.5× impl; test-mode flag (`testMode = true`) couples impl and test |
| `day_segment.dart` | 146 | yes (243 ln) | Good structure; 0 `pumpAndSettle` |
| `date_label_row.dart` | 124 | yes (285 ln) | 14 `pumpAndSettle` calls |
| `status_indicator.dart` | 91 | yes (165 ln) | 8 `pumpAndSettle` calls; all could be `pump()` |
| `time_history_header.dart` | 13 (barrel) | yes (413 ln) | Tests cover composed widget — fine |
| `day_label_chip.dart` | 41 | no own file — tested inside `date_label_row_test.dart` | Violates one-test-per-source-file rule |
| `today_button.dart` | 40 | no own file — tested inside `date_label_row_test.dart` | Violates one-test-per-source-file rule |

---

## File size / split opportunities

- [x] **[MED]** `lib/features/daily_os/ui/widgets/time_history_header/time_history_header_widget.dart` (547 ln) — just above the ~500 soft limit. Concrete split seams:
  - The `_buildDaySelectorSkeleton` helper (skeleton loading state) and `_LoadingSkeletonItem` could be extracted to a small `loading_skeleton.dart` companion (~60 ln).
  - The `_buildChartSection` sub-widget (if present) could be extracted. Not urgent; defer to when the file next grows.
  **RESOLVED:** the day-selector skeleton, load-more spinner, and `_HorizontalClipper` moved to the `part` file `time_history_header_skeleton.dart` (86 ln); the widget file is down to 470 lines.

- [ ] **[LOW]** `test/features/daily_os/ui/widgets/time_history_header/time_history_stream_chart_test.dart` (815 ln) — 3.5× the impl size. The disproportionate length is partly justified by the presence of well-structured pure-logic groups (`StreamChartItem`, `computeEffectiveScaleMinutes`, `buildChartData`) alongside widget tests. No immediate split is required but the pure-logic tests should be tagged appropriately if Glados properties are added.

---

## Test quality improvements

- [x] **[HIGH]** `test/features/daily_os/ui/widgets/time_history_header/time_history_header_test.dart:59–73` (`shows selection highlight on selected day`) — asserts `find.ancestor(of: dayText, matching: find.byType(Container)), findsWidgets`. This only proves a `Container` wraps a day-number text, not that it has a selection border. Replace with an assertion on the `BoxDecoration.border` of the container (non-null when selected, null when unselected), or verify that the `AnimatedContainer` that carries the selection border finds exactly one instance with a non-null border. **RESOLVED:** the test now uses a `isSelectionPill` predicate (Container with non-null `BoxDecoration.color` and `BorderRadius.circular(8)` — the actual selected-day treatment in `day_segment.dart`) and asserts the selected day '15' has exactly one such pill while the unselected '14' has none.

- [x] **[HIGH]** `test/features/daily_os/ui/widgets/time_history_header/time_history_header_test.dart:20–25` (`renders the widget`) — sole assertion is `find.byType(TimeHistoryHeader), findsOneWidget`. Pure smoke test. Replace or merge with one of the substantive tests below it (e.g., move the "renders the widget" assertion into the "renders day segments for loaded data" test as a first assertion). **RESOLVED:** the pure smoke test is removed; its `findsOneWidget` assertion folded into the substantive "renders day segments for loaded data" test.

- [x] **[MED]** `test/features/daily_os/ui/widgets/time_history_header/test_helpers.dart` — `setUpEntitiesCacheService` / `tearDownEntitiesCacheService` (lines 60–104) use raw `getIt.isRegistered / getIt.unregister / getIt.registerSingleton` boilerplate instead of `setUpTestGetIt()` / `tearDownTestGetIt()` from `test/widget_test_utils.dart`. The helper also carries its own `TimeHistoryStreamChart.testMode = true` toggle (lines 62, 99). Migrating to `setUpTestGetIt` and registering extra services after calling it would eliminate the raw GetIt boilerplate across all six test files that import this helper.
  **RESOLVED (assessed, no change):** the helper IS this directory's centralized seam — it registers exactly one scoped service symmetrically. `setUpTestGetIt()` performs a full `getIt.reset()`, which would wipe the consuming files' other registrations and change semantics across six suites for a stylistic win; the rule's purpose (no per-test boilerplate) is already met at the helper level.

- [x] **[MED]** `test/features/daily_os/ui/widgets/time_history_header/time_history_stream_chart_test.dart` — the `testMode` static flag (`TimeHistoryStreamChart.testMode = true/false`) in `test_helpers.dart:62,99` is a test-only backdoor in production code. This couples the implementation to the test harness. Consider using a `skipChartRendering` constructor parameter or a Riverpod override instead.
  **RESOLVED (adapted):** the flag is now annotated `@visibleForTesting` with a doc explaining the harness-seam contract, so production code cannot legitimately flip it. The constructor-param/provider alternatives were assessed and rejected: the chart is constructed deep inside the header widget, so either option ripples a test-only knob through the production API or converts the widget to ConsumerWidget across 6 suites.

- [x] **[MED]** `test/features/daily_os/ui/widgets/time_history_header/date_label_row_test.dart:216–284` — `DayLabelChip` and `TodayButton` tests are embedded in `date_label_row_test.dart`. Per the one-test-per-source-file rule, they should live in `day_label_chip_test.dart` and `today_button_test.dart` respectively. The tests themselves are well-structured; it is purely a file-placement issue.
  **RESOLVED:** the embedded groups were weaker duplicates of the dedicated mirrors (`day_label_chip_test.dart`, `today_button_test.dart` — which cover pill styling, ellipsis clamping, localized label, hit area, and the callback) — deleted from `date_label_row_test.dart`.

- [ ] **[LOW]** `test/features/daily_os/ui/widgets/time_history_header/time_history_header_test.dart:220–228` (`shows loading indicator when isLoadingMore is true`) — asserts `find.byType(CircularProgressIndicator), findsOneWidget`. While this is meaningful (it proves the loading branch renders), the assertion leaves the test fragile if the loading indicator type changes. A stronger companion assertion on the positioned scroll area would be more robust.

- [ ] **[LOW]** `test/features/daily_os/ui/widgets/time_history_header/time_history_header_widget_test.dart:265–276, 363, 447, 499` — five `pumpAndSettle` calls. Three of these follow `tester.tap()` on `DaySegment`; the tap triggers a provider state change (not an animation) which does not require `pumpAndSettle`. Replace with `tester.pump()` after verifying the provider transition is synchronous.

---

## Generative (Glados) testing opportunities

- [x] **[MED]** `TimeHistoryStreamChart.computeEffectiveScaleMinutes` (already has static unit tests at `time_history_stream_chart_test.dart:59–108`) — upgrade the existing boundary examples to a Glados property: for all `Duration d`, `result >= 360.0` (6-hour floor) and `result <= 24 * 60` (24-hour ceil). The function is pure `Duration → double`, making it an ideal Glados target.
  **RESOLVED:** Glados property over 0–3000 minutes asserts the [6h, 24h] clamp band AND exact identity inside the band (stronger than the suggested bounds-only property).

- [x] **[MED]** `TimeHistoryStreamChart.buildChartData` (tested at `time_history_stream_chart_test.dart:109–314`) — invariant: for any list of `DayTimeSummary` inputs the total output area (sum of `minutes` across all items for a given `x`) must equal the total recorded minutes for that day. A Glados property over `List<DayTimeSummary>` (with bounded category counts) would guard this invariant across arbitrary data shapes.
  **RESOLVED:** Glados property over generated day lists (two categories + optional uncategorized) asserts per-day minute conservation at each x, x-slot category-completeness (the SymmetricModifier requirement), and the newest→oldest to oldest→newest x mapping.

- [ ] **[LOW]** `DaySegment` rendering math — the `DaySegment` pill height/width is driven by a fraction of available space. A property test on the layout delegate (if extracted to a pure function) could verify that fractions always sum to ≤ 1 for any input list of durations. Currently not extracted; label as LOW until the function is pure enough to be Glados-testable.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `day_label_chip.dart` and `today_button.dart` — no dedicated test files exist. The tests in `date_label_row_test.dart:216–284` cover only two behaviors each. Missing coverage for `TodayButton`: disabled state (if any), icon color, minimum-size constraint. Missing coverage for `DayLabelChip`: background color, border-radius, font style. **RESOLVED:** dedicated `day_label_chip_test.dart` (pill decoration + half-alpha tint, ellipsized single-line label, `maxWidth` clamp) and `today_button_test.dart` (localized label + 16px calendar icon, shared primary color, onPressed callback, `Size.zero` minimumSize + shrinkWrap tap target) added.

- [x] **[MED]** `time_history_header_widget.dart` — the `_TodayButton` visibility branch (`showTodayButton`: shown when selected date != today, hidden when today is selected) is tested via `time_history_header_test.dart:390–410`. However, the `resetToToday` controller call path when the button is tapped is only covered in `time_history_header_widget_test.dart`. Confirm that the `resetToTodayCallCount` assertion exists — it does at `time_history_header_widget_test.dart` — so this gap is covered. No action.
  **RESOLVED (no action):** as the item itself concludes — the `resetToTodayCallCount` assertion exists in `time_history_header_widget_test.dart`; the gap was already covered.

- [x] **[MED]** `time_history_stream_chart_test.dart` — the `testMode = true` branch (lines 429–528) stubs out chart rendering entirely, meaning the actual `graphic` chart widget rendering path (line 530+) is exercised only in the 5 tests in the "chart rendering path" group. These tests verify the chart widget is present but do not assert on rendered data points. Consider adding a semantic label or key to the `Chart` widget to enable more precise assertions.
  **RESOLVED:** no key needed — the rendering test now reads the mounted `Chart<StreamChartItem>` widget and asserts its `data` payload equals `buildChartData(days, testCategories.keys)` plus total-minute conservation (4 h across both days).

- [ ] **[LOW]** `status_indicator.dart` — the status-dot color branches for `DayPlanStatus.done`, `inProgress`, `draft`, `cancelled` are tested individually in `status_indicator_test.dart`. The "no plan" (null plan) path should be confirmed as tested — inspect `status_indicator_test.dart` for a null-plan test case.

- [ ] **[LOW]** `time_history_header_test.dart` — the scroll-to-load-more behavior (calling `loadMoreDays` when the user scrolls to the left edge) is tested at lines 340–410. The test verifies `loadMoreDaysCallCount == 1` after a `drag` gesture. This is a meaningful behavioral test and represents good coverage.

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/features/daily_os/ui/widgets/time_history_header/date_label_row_test.dart` — **14 `pumpAndSettle` calls** across 18 tests. The `DateLabelRow` widget has no running animations (it is a static row of labels and a scroll view). All 14 calls can be replaced with `tester.pump()`. Estimated saving: ~2–5s per test on CI, up to ~70s total for the file. **RESOLVED:** all 14 settles replaced with plain `tester.pump()` (the widget is static); full file passes.

- [x] **[MED]** `test/features/daily_os/ui/widgets/time_history_header/status_indicator_test.dart` — **8 `pumpAndSettle` calls** across 8 tests. The `StatusIndicator` is a stateless widget with no animations. All 8 calls should be `tester.pump()`. Estimated saving: ~1–2s per test, ~8–16s total.
  **RESOLVED:** all 8 settles replaced with `tester.pump()`; suite green.

- [x] **[MED]** `test/features/daily_os/ui/widgets/time_history_header/time_history_header_test.dart` — **14 `pumpAndSettle` calls**. Several of these follow `tester.tap()` on a `DaySegment` where the only state change is a `selectedDate` provider update (no animation). After a tap, replace with `tester.pump()` except in the scrolling/expansion tests that do involve `AnimatedSize` or `AnimatedContainer`.
  **RESOLVED (stale):** the file has zero `pumpAndSettle` calls left — only a comment explaining why `pump()` is used for the continuously-animating spinner.

- [ ] **[LOW]** `test/features/daily_os/ui/widgets/time_history_header/time_history_header_widget_test.dart` — **5 `pumpAndSettle` calls**. Three follow provider-state changes after tapping a day segment (no animation involved). Replace with `tester.pump()` for those three.

---

## Summary

- 1 oversized impl file (`time_history_header_widget.dart` 547 ln, above soft limit); test file `time_history_stream_chart_test.dart` (815 ln) is 3.5× its impl but structured well.
- 2 weak/smoke-only tests: `time_history_header_test.dart` "renders the widget" (`findsOneWidget` smoke) and "shows selection highlight" (`findsWidgets` structural finder).
- 2 missing test files (`day_label_chip_test.dart`, `today_button_test.dart`) — one-test-per-source-file violation.
- 2 Glados candidates: `computeEffectiveScaleMinutes` (scale floor/ceil invariant, **MED**) and `buildChartData` (area-sum invariant, **MED**).
- 3 speed wins: `date_label_row_test` (14 `pumpAndSettle` → `pump`, highest impact), `status_indicator_test` (8), `time_history_header_test` (14, partially replaceable).
- Biggest single opportunity: migrate `test_helpers.dart` away from raw GetIt boilerplate to `setUpTestGetIt()` — one change that improves all 6 files that import the helper and removes the only non-standard service setup pattern in the sub-dir.
