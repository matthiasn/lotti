# Test Review: daily_os/util

**Scope:** `lib/features/daily_os/util/` (4 files, 751 impl lines) and
`test/features/daily_os/util/` (4 test files, 3293 test lines).

_Generated by automated test-quality review (static analysis)_

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/.../util/task_sort_comparators.dart` | 70 | Yes | Well within budget |
| `lib/.../util/drag_position_utils.dart` | 107 | Yes | Well within budget |
| `lib/.../util/time_range_utils.dart` | 104 | Yes | Well within budget |
| `lib/.../util/timeline_folding_utils.dart` | 470 | Yes | Borderline; no split needed |
| `test/.../util/task_sort_comparators_test.dart` | 626 | n/a | Model duplication is intentional (comparator specification), acceptable |
| `test/.../util/drag_position_utils_test.dart` | 408 | n/a | Good shape; minor smoke-only constant tests |
| `test/.../util/time_range_utils_test.dart` | 565 | n/a | Very healthy; property tests with oracle |
| `test/.../util/timeline_folding_utils_test.dart` | 1694 | n/a | **Oversized** — primary refactor candidate |

---

## File size / split opportunities

- [x] **[HIGH]** `test/features/daily_os/util/timeline_folding_utils_test.dart` is 1694 lines for a 470-line impl file — a 3.6x ratio. The file has seven distinct logical groups: `VisibleCluster`, `CompressedRegion`, `TimelineFoldingState`, `calculateFoldingState` (two groups: main + edge cases), `calculateFoldedTimelineHeight`, `timeToFoldedPosition` (two groups: main + edge cases), `isHourInCompressedRegion`, `blockOverlapsCompressedRegion`, `calculateContiguousDragBounds`, and `Named constants`. These could be split into two files: one covering the data-model types and `calculateFoldingState` logic (roughly lines 1–668), another covering the coordinate/position functions and drag-bounds utilities (lines 669–1694). However, the one-test-file-per-source-file rule means this split would require splitting the impl file at the same seam. Given the current policy this is a **refactoring discussion item**, not a mandatory split. Document the seam for future consideration. **RESOLVED (assessed, no change):** as the item itself concludes, the split is blocked by the one-test-file-per-source rule unless the impl splits at the same seam — the impl is a healthy 470 lines, so no split is warranted; the seam (data model + calculateFoldingState vs. coordinate/position/drag-bounds functions) is documented here for when the impl ever grows past the threshold.
- [ ] **[LOW]** All four impl files are well under 500 lines; no impl split is needed.

---

## Test quality improvements

- [ ] **[MED]** `test/.../util/drag_position_utils_test.dart:103–130` — The "Constants" group has seven individual `test()` calls that each do a single `expect(kFoo, equals(N))`. These are constructor smoke tests (verify the constant's value against itself). They add zero protection against a functional regression. **Recommendation:** collapse into one `test('constants have documented values', ...)` or remove entirely — they fire if a constant is renamed but not if the behavior that depends on it breaks.
- [ ] **[MED]** `test/.../util/timeline_folding_utils_test.dart:243–253` — `VisibleCluster` equality tests include a dedicated case `'equality differentiates on endHour when startHour matches'` and `'equality returns false for a non-VisibleCluster object'`. These are valid, but four separate `test()` calls (lines 218–242) could be merged into two with parameterized loop or table-driven cases to reduce boilerplate.
- [ ] **[LOW]** `test/.../util/task_sort_comparators_test.dart` — The reference model functions (`compareByPriorityUrgencyTitleModel`, `compareByTimeSpentThenPriorityModel`, lines 150–184) are verbatim copies of the impl. This is an intentional specification pattern and acceptable for comparator oracle testing, but it means any copy-paste error in the model would go undetected. Adding one or two concrete golden-list assertions (already present, e.g. lines 392–434) is sufficient guard — this is already done, so no action needed beyond awareness.
- [ ] **[LOW]** `test/.../util/timeline_folding_utils_test.dart:964–1041` — Several edge-case tests use loose assertions like `expect(state.visibleClusters.isNotEmpty, isTrue)` or `expect(..., lessThanOrEqualTo(3))` (lines 975, 1009) without specifying the exact expected cluster structure. While acceptable for algorithmic boundary exploration, prefer documenting why the exact value is uncertain rather than leaving an open range.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `task_sort_comparators_test.dart` already has Glados model-match tests. A missing genuine property is **transitivity**: for any three `TaskDayProgress` items `a`, `b`, `c`, if `compare(a,b) < 0 && compare(b,c) < 0` then `compare(a,c) < 0`. Add a `Glados3` test on `any.taskProgressSpec` triples. This is the classic comparator correctness invariant that the current model-match test cannot catch (both sides could be wrong the same way). `ExploreConfig(numRuns: 120)` is sufficient.
- [x] **[HIGH]** `task_sort_comparators_test.dart` — Add a **totality / antisymmetry** property: for any two items `a`, `b`, `sign(compare(a,b)) == -sign(compare(b,a))` (unless zero). A `Glados2` test on pairs of `_GeneratedTaskProgressSpec`. This property is already partially validated by the model-match test, but not directly enforced.
- [ ] **[MED]** `timeline_folding_utils_test.dart` already has two `Glados` tests. One genuine gap: **idempotence of `calculateFoldingState`** — applying the function twice with the same generated input should return an equivalent folding state (same cluster/region count and hour boundaries). This would catch any mutation of input lists in the impl. `ExploreConfig(numRuns: 100)`.
- [ ] **[MED]** `timeline_folding_utils_test.dart` — `calculateContiguousDragBounds` has no Glados property. A property worth adding: the returned `(startHour, endHour)` must always satisfy `startHour <= sectionStartHour` and `endHour >= sectionEndHour` (bounds can only widen, never narrow). `Glados` over `any.timelineFoldingScenario` with a generated section index. `ExploreConfig(numRuns: 100)`.
- [ ] **[LOW]** `drag_position_utils_test.dart` — `formatDurationMinutes` has no Glados test. A minimal property: for any `minutes >= 0`, `formatDurationMinutes` produces a string that, when parsed back to minutes, equals the input (round-trip). The same `_AnyGeneratedDragPosition` generator can supply `intInRange(0, 1440)` inputs.

---

## Coverage / missing-behavior gaps

- [ ] **[MED]** `timeline_folding_utils_test.dart` — `blockOverlapsCompressedRegion` has no test for the case where `expandedRegions` contains *multiple* keys and the block overlaps only one of the collapsed remaining regions. Currently test at line 1502 covers two compressed regions with both collapsed; a gap is: two compressed regions, one expanded and one collapsed, block overlaps only the expanded one — should return `false`.
- [ ] **[MED]** `timeline_folding_utils_test.dart` — `calculateContiguousDragBounds` test "handles multiple visible clusters with expanded region between" (line 1650) verifies that expansion through a middle region reaches the second visible cluster's end (18). There is no test for the symmetric "backward expansion through two adjacent expanded regions without a collapsed region blocking" — i.e., expansion stops correctly when the leftmost expanded region is itself bounded by another collapsed region.
- [ ] **[LOW]** `drag_position_utils_test.dart` — `formatDurationMinutes(0)` is tested; `formatDurationMinutes` with negative input is not. The impl has no guard for negative values (hours would be 0, mins would be negative). Documenting this boundary (or asserting the current behavior) would prevent silent regressions if the call-sites change.
- [ ] **[LOW]** `time_range_utils_test.dart` — The `TimeRange.merge` method is only tested with two hand-crafted fixtures; `mergeOverlappingRanges` is tested with multiple scenarios. No test for `merge(self)` (a range merged with itself should equal itself).

---

## Test execution speed opportunities

- [ ] **[MED]** `test/.../util/timeline_folding_utils_test.dart` — The file has two Glados runs at `numRuns: 160` (line 586) and `numRuns: 120` (line 808). At 1694 lines and 70 test cases, this is the most CPU-heavy test file in the subdir. Both numRuns values are within the recommended 80–180 range, so no change is needed; however if CI sharding shows this as a hot path, `numRuns: 120` for the folding-state partition test would shave roughly 25% of that test's cost.
- [ ] **[LOW]** `test/.../util/drag_position_utils_test.dart:100` — `TestWidgetsFlutterBinding.ensureInitialized()` is called at the top of `main()` for a pure-unit test file that has no widget tests. This binding initialisation is unnecessary overhead. Remove it (or suppress it) since none of the tested functions touch Flutter UI.
- [ ] **[LOW]** `test/.../util/timeline_folding_utils_test.dart:161` — Similarly, `TestWidgetsFlutterBinding.ensureInitialized()` is called in a file whose tests are all pure-logic unit tests plus Glados. No widget pumping occurs. Removing this saves one binding setup per test run.

---

## Summary

- `timeline_folding_utils_test.dart` at 1694 lines is the only oversized file in this subdir (3.6x the impl); the seam for a future split has been documented.
- 4 Glados tests are in place and correctly tagged; 3 genuine generative gaps remain: comparator transitivity/totality (HIGH), folding idempotence (MED), and drag-bounds monotonicity property (MED).
- 2 speed wins: remove unnecessary `TestWidgetsFlutterBinding.ensureInitialized()` calls in two pure-unit-test files.
- 1 real-quality improvement: collapse 7 single-assertion constant smoke tests in `drag_position_utils_test.dart`.
- Coverage is comprehensive; the `blockOverlapsCompressedRegion` expanded-vs-collapsed interaction gap is the most valuable missing path.
