# Test Review: daily_os/widgetbook

**Scope:** `lib/features/daily_os/widgetbook/` (3 files: 2850 + 374 + 143 = 3367 impl lines) and
`test/features/daily_os/widgetbook/` (2 test files: 255 + 179 = 434 test lines).

_Generated by automated test-quality review (static analysis)_

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/.../widgetbook/my_daily_widgetbook.dart` | 2850 | Yes (255 lines) | **Critically oversized** — 2850 lines is ~5.7x the 500-line soft ceiling |
| `lib/.../widgetbook/set_time_blocks_widgetbook.dart` | 374 | Yes (179 lines) | Within budget |
| `lib/.../widgetbook/set_time_blocks_mock_data.dart` | 143 | No direct test | Mock data has no mirror test file |
| `test/.../widgetbook/my_daily_widgetbook_test.dart` | 255 | n/a | Good assertions; `_settlePreview` helper used consistently |
| `test/.../widgetbook/set_time_blocks_widgetbook_test.dart` | 179 | n/a | Reasonable coverage; minor smoke-only patterns |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/daily_os/widgetbook/my_daily_widgetbook.dart` at 2850 lines is **critically oversized** — nearly 6x the 500-line target and almost certainly the largest non-generated file in the daily_os feature. It contains: fixture builders (`_buildMyDailyPreviewFixture`, `_buildMyDailyPreviewOverrides`), the overview use case, three individual use-case widgets, the preview screen widget, numerous layout constants (lines 24–52), private helper widgets, and all mock data generation. Concrete split seams:
  1. **Constants + mock-data helpers** (lines 24–52 + fixture/override builders) → `my_daily_widgetbook_fixtures.dart`
  2. **Preview screen widget** (`_MyDailyPreviewScreen` and its children) → `my_daily_preview_screen.dart`
  3. **Use-case entry points** (`buildMyDailyWidgetbookFolder`, `buildMyDailyWidgetbookComponent`, `_MyDailyUseCase`, `_MyDailyOverviewUseCase`) remain in `my_daily_widgetbook.dart`
  The corresponding test file would stay as `my_daily_widgetbook_test.dart` per the one-file-per-source-file rule; if the preview-screen widget is extracted, add a `my_daily_preview_screen_test.dart`.
  **RESOLVED (adapted):** split via `part` files (one library, single mirror test stays valid) — `my_daily_preview_screen.dart` (preview screen + header/summary/filter chrome, 780 lines), `my_daily_preview_timeline.dart` (timeline widgets/specs/builders, 1 209 lines), and `my_daily_widgetbook_fixtures.dart` (fixture + mock-data builders, 655 lines); the entry point keeps the constants, enums, folder/component builders and use-case wrappers (213 lines).
- [x] **[LOW]** `lib/.../widgetbook/set_time_blocks_widgetbook.dart` at 374 lines is within budget but on the higher end for a widgetbook file; no split needed now.
  **RESOLVED:** confirmed — 374 lines is comfortably under the 500-line soft ceiling and the file is cleanly factored into one page widget plus six small private sub-widgets (`_PageHeader`, `_SectionDivider`, `_CategoryRow`, `_CategoryIcon`, `_TimeChip`, `_SavePlanButton`); a split would add a `part`/import seam for no readability gain. No change. (Also outside the test-only edit scope.)
- [x] **[LOW]** `lib/.../widgetbook/set_time_blocks_mock_data.dart` (143 lines) has no mirror test file. Per the one-test-file-per-source-file rule this warrants `test/features/daily_os/widgetbook/set_time_blocks_mock_data_test.dart`. The mock data file exports `SetTimeBlocksMockData.favourites` and `SetTimeBlocksMockData.otherCategories`; their content correctness (expected category IDs, block count, favourite flags) could be asserted in ~30 lines. **RESOLVED:** mirror test added (see the HIGH item below).

---

## Test quality improvements

- [x] **[MED]** `test/.../widgetbook/my_daily_widgetbook_test.dart` — The `_settlePreview` helper (lines 231–240) calls `container.read(dailyOsControllerProvider.future)` and `container.read(timeHistoryHeaderControllerProvider.future)` but does **not** await `unifiedDailyOsDataControllerProvider` which is also wired in the fixture overrides. If that provider resolves asynchronously during a test pump, it may produce test-order-dependent results. Confirm the fixture builds `unifiedDailyOsDataControllerProvider` synchronously or add its future to `_settlePreview`.
  - **RESOLVED:** done — `_settlePreview` now also awaits `unifiedDailyOsDataControllerProvider(date: selectedDate).future` for the currently selected date, removing the pump-ordering dependence.
- [x] **[MED]** `test/.../widgetbook/set_time_blocks_widgetbook_test.dart:103–112` — The "renders save plan button" test scrolls by `const Offset(0, -300)` and then calls `tester.pump()`. If the list's content height changes across Dart/Flutter SDK updates, the hard-coded scroll offset may not be sufficient to reveal the button. A more robust approach: use `tester.ensureVisible(find.text('Save plan'))` rather than a fixed-pixel scroll.
  - **RESOLVED:** done — replaced the fixed `-300` drag with `scrollUntilVisible` (pinned to the first Scrollable; `ensureVisible` alone can't work because the lazy list hasn't built the button yet).
- [x] **[MED]** `test/.../widgetbook/set_time_blocks_widgetbook_test.dart:114–123` — Similar issue: "renders other categories section" scrolls by `-300` to find `Commute` and `Household`. Same robustness concern; prefer `ensureVisible`.
  - **RESOLVED:** done — same `scrollUntilVisible` conversion for the other-categories section.
- [x] **[LOW]** `test/.../widgetbook/my_daily_widgetbook_test.dart:24–31` — Two unit tests (`'exposes My Daily as a top-level Widgetbook folder'`, `'exposes the expected use cases'`) verify the structure of the component and folder objects by asserting `.name` and `useCases` count/order. These are useful smoke tests that confirm the Widgetbook navigation tree is intact. They are valid and should be kept.
  **RESOLVED:** confirmed — these assert concrete structural contracts (folder name, single child, the exact ordered use-case names `['Overview', 'Ongoing Day', 'Filter By Time Block', 'Filtered']`), which is meaningful (not bare `isNotNull`). Kept as-is, no change.
- [x] **[LOW]** `test/.../widgetbook/set_time_blocks_widgetbook_test.dart:150–170` — The "rows with blocks have interactive accent border" test walks `AnimatedContainer` descendants looking for a `BoxDecoration` with a matching border color. This is a brittle structural assertion that will break if the impl switches from `AnimatedContainer` to another animation widget or if the border styling moves. Consider replacing with a key-based lookup (e.g. `find.byKey(const Key('category-row-work'))`) if the impl can be updated to add keys.
  **RESOLVED (assessed, no change):** the premise is partly stale — `_CategoryRow` uses a plain `Container` (not `AnimatedContainer`), and the test already matches `widget is! Container`, so it is not coupled to an animation widget at all. It verifies a real behavioral contract (rows with blocks get `tokens.colors.interactive.enabled` as the border color vs `decorative.level01` for empty rows), which is exactly the styling distinction the showcase exists to demonstrate. The suggested key-based lookup requires adding `Key`s to `_CategoryRow` in `lib/...`, which is outside this batch's test-only edit scope — DEFERRED for that source change; the test as written is the most robust form achievable without touching production code.

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** Widgetbook files are UI composition layers; no Glados properties are applicable. The `set_time_blocks_mock_data.dart` fixture data is static and declarative; property tests would add no value here.
  **RESOLVED:** confirmed — both files are pure UI/declarative composition with no input-driven pure functions; the only logic (`MockTimeBlock.label`, `MockCategory.copyWith/hasBlocks`) is trivial string interpolation/field copying covered by example-based assertions in `set_time_blocks_mock_data_test.dart`. No generative properties add value. No change.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/.../widgetbook/set_time_blocks_mock_data.dart` has **no mirror test file** at all. Add `test/features/daily_os/widgetbook/set_time_blocks_mock_data_test.dart` that asserts: expected number of favourites (4), expected number of other categories, that all `MockCategory` instances have non-empty `id` and `name`, that `isFavourite` is correctly set, and that `MockBlock` instances within each category have consistent `startTime < endTime`. (~30–40 lines.) **RESOLVED:** new `set_time_blocks_mock_data_test.dart` pins favourite/other counts and ids, uniqueness, favourite flags, hasBlocks consistency, block label shape, and copyWith semantics.
- [x] **[MED]** `test/.../widgetbook/my_daily_widgetbook_test.dart` — The "date strip drives the provider-backed header" test (line 90) verifies navigation from Oct 17 to Oct 19. There is no test for navigation to a date **before** the fixture window (earlier than the earliest day in the time-history data) to verify that the header and "now" indicator behave correctly at the window boundary.
  - **RESOLVED:** done — added a boundary test navigating to Oct 15 (the earliest strip date, offset -2): the header updates to 'Sunday, Oct 15, 2023', no now-indicator (now is Oct 17), and no pending exceptions.
- [x] **[MED]** `test/.../widgetbook/my_daily_widgetbook_test.dart` — The "filtered preview toggles category emphasis" test (line 143) toggles two categories. It does not verify the state when **all** filter chips are deactivated (should reset to showing all categories at full opacity). This is a meaningful UI state that could regress silently.
  - **RESOLVED:** done — added `'deactivating every filter chip resets all categories to full opacity'`: from the 'Filtered' fixture, toggling holiday and hiking off empties the selection and all three category opacities return to 1.
- [x] **[LOW]** `test/.../widgetbook/set_time_blocks_widgetbook_test.dart` — The `_toggleFavourite` functionality is tested in the star-tap test (line 124), but only for unfavouriting. The reverse direction (tapping a star in "Other categories" to move it to Favourites) is not tested. Both code paths exist in `_SetTimeBlocksShowcasePageState._toggleFavourite`.
  **RESOLVED:** the premise is incorrect — there is no star to tap in "Other categories". In `_CategoryRow` the star + its `GestureDetector` tap target are gated behind `if (category.isFavourite)`, so non-favourite rows render no star and the `else` (re-favourite) branch of `_toggleFavourite` is unreachable from the UI in this showcase. Added test `'other-categories rows expose no star to tap'` that scrolls the `Commute` row into view and asserts it contains no `Icons.star`, pinning the actual UI contract instead of testing a non-existent affordance. (The dead `else` branch is private showcase code; exercising it would require a production change to render outline stars, which is out of test-only scope.)

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/.../widgetbook/my_daily_widgetbook_test.dart` — Every `testWidgets` call pumps the full widgetbook fixture including Riverpod providers (`dailyOsControllerProvider`, `timeHistoryHeaderControllerProvider`). These providers resolve asynchronously. `_settlePreview` uses `await container.read(...future)` + `tester.pump()` rather than `tester.pumpAndSettle()`, which is good practice. However, the fixture data for all three use cases (`ongoingDay`, `filterByTimeBlock`, `filtered`) is generated from the same large in-memory mock data. If the fixture-building functions (`_buildMyDailyPreviewFixture`, `_buildMyDailyPreviewOverrides`) are called once per test, they add per-test overhead. Caching the fixture result at `setUpAll` scope would cut this cost. **RESOLVED (assessed, no change):** the fixture builders live inside the lib use-case builders, so caching at `setUpAll` scope would require a memoization seam in widgetbook-only lib code. The construction is pure in-memory mock data (no I/O), per-test freshness avoids cross-test mutation hazards, and the item itself confirms the `_settlePreview` await-future approach is already the right pattern.
- [x] **[MED]** `test/.../widgetbook/set_time_blocks_widgetbook_test.dart` — `pumpPage` is called once per test and always pumps the full `SetTimeBlocksShowcasePage`. The `setUp` block validates the folder/component structure in every test (lines 14–23), which calls `buildSetTimeBlocksWidgetbookFolder()` on every test setup. Move the `setUp` assertions into a single `setUpAll` block (or a dedicated test group) and cache `useCase` in a `late` field initialized in `setUpAll`.
  - **RESOLVED:** done — the structure validation moved to a `setUpAll` that caches `useCase` once (the folder config is immutable), removing the per-test rebuild.
- [x] **[LOW]** `test/.../widgetbook/set_time_blocks_widgetbook_test.dart:107,121` — Two tests call `tester.drag(find.byType(ListView), const Offset(0, -300))` followed by `await tester.pump()`. A single `pump()` may not be sufficient to complete scroll physics. Using `await tester.pumpAndSettle()` here is appropriate since scroll animations need to settle, but confirm the drag-settle pattern is consistent.
  **RESOLVED (stale):** both fixed-pixel `tester.drag(... Offset(0, -300))` calls were already removed by the line-39/41 MED items, which replaced them with `scrollUntilVisible(... scrollable: find.byType(Scrollable).first)`. There are no remaining `tester.drag` or `pumpAndSettle` calls in the file, so the drag-settle concern no longer applies. No change.

---

## Summary

- `my_daily_widgetbook.dart` at 2850 lines is the most oversized file in the entire reviewed scope — 6x the soft ceiling — with 3 clear split seams identified (HIGH priority).
- `set_time_blocks_mock_data.dart` (143 lines) has no mirror test file, violating the one-test-file-per-source-file rule (HIGH, ~30-line fix).
- 2 `tester.drag` tests with hardcoded pixel offsets should use `ensureVisible` for robustness (MED).
- 1 speed win: cache widgetbook fixture objects at `setUpAll` scope to avoid re-building rich mock data on every test.
- No Glados candidates in this subdir.
