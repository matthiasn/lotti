# Test-Quality Review: daily_os_next/agents/domain

**Scope:** `lib/features/daily_os_next/agents/domain/` (5 impl files) and
`test/features/daily_os_next/agents/domain/` (3 test files).

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../domain/day_agent_config.dart` | 35 | No | No test file at all; `toJson` round-trip untested |
| `lib/.../domain/day_agent_kind.dart` | 16 | No | No test file; `DayAgentKind.matches` untested |
| `lib/.../domain/day_agent_plan_models.dart` | 209 | Yes | `DayAgentLearningBullet`/`DayAgentLearningCard` lack Glados round-trip property |
| `lib/.../domain/day_agent_reconcile_models.dart` | 373 | Yes | No issues (excellent Glados coverage) |
| `lib/.../domain/day_agent_slots.dart` | 21 | Yes | `dayAgentPlanEntityId` is not exercised in the test |
| `test/.../domain/day_agent_plan_models_test.dart` | 173 | — | Missing Glados property for `DayAgentLearningBullet`/`Card` |
| `test/.../domain/day_agent_reconcile_models_test.dart` | 668 | — | None; gold-standard example |
| `test/.../domain/day_agent_slots_test.dart` | 102 | — | `dayAgentPlanEntityId` helper is never called |

---

## File size / split opportunities

- [ ] **[LOW]** All impl files are well under 400 lines; no splits required.
- [ ] **[LOW]** `day_agent_reconcile_models_test.dart` (668 lines) is the largest test file. It is logically split across three private helper functions (`_expectTokenRoundTrip`, `_expectProjections`, `_expectDedupeAndSortEdges`) which keep it navigable. No split is urgent, but if the reconcile model grows further, the token-round-trip group could move to its own file.

---

## Test quality improvements

- [ ] **[MED]** `day_agent_plan_models_test.dart:40-54` — `DayAgentLearningBullet` has only one static round-trip test covering a single concrete `warning`-tone bullet. The other two tones (`info`, `positive`) are exercised indirectly in `DayAgentLearningCard` tests but never verified through `fromJson`/`toJson` directly. Add at-minimum a loop over all `DayAgentLearningBulletTone` values or a Glados property (see Generative section).
- [ ] **[MED]** `day_agent_plan_models_test.dart:56-115` — `DayAgentLearningCard` is tested with two static cases but has no Glados round-trip. The `kind` field defaults to `'standard'`; the non-default path (e.g. `kind: 'nudge'`) is never round-tripped. Add `kind` variation to coverage.
- [ ] **[LOW]** `day_agent_slots_test.dart` — the file tests `hasActiveDay` and `dayAgentIdForDate` (with Glados) but completely omits `dayAgentPlanEntityId` (line 21 of the impl). Its contract ("same prefix used by `DayAgentPlanService`") is important for cross-service correctness but is verified only indirectly in the service tests outside this scope.

---

## Generative (Glados) testing opportunities

- [ ] **[MED]** `day_agent_plan_models_test.dart` — `DayAgentLearningBullet` and `DayAgentLearningCard` are genuine JSON round-trip types with structured input (enum tones, arbitrary strings, variable-length bullet lists). A Glados property over a `_GeneratedLearningCard` (varying `kind`, bullet count 0–5, all three tones) would catch `_bulletListsEqual` / `hashCode` parity issues across any input. This is the primary gap in the domain test suite.
- [ ] **[LOW]** `day_agent_reconcile_models_test.dart` — the existing Glados properties for `classifyParsedItemMatch` and `dedupeAndSortPendingItems` are exemplary; no new property is needed here.
- [ ] **[LOW]** `day_agent_slots_test.dart` — the Glados property for `dayAgentIdForDate` is well-formed with `numRuns: 120`. No new property needed.

---

## Coverage / missing-behavior gaps

- [ ] **[HIGH]** `lib/.../domain/day_agent_config.dart` — zero tests. The `toJson` method serialises all five fields; the default constructor values are part of the public API contract (e.g. `capacityMinutes: 480`). A single test verifying the JSON output of the default config and one with custom values would suffice. A test file `test/features/daily_os_next/agents/domain/day_agent_config_test.dart` is required by the one-file-per-source-file rule.
- [ ] **[HIGH]** `lib/.../domain/day_agent_kind.dart` — zero tests. `DayAgentKind.matches` is a pure predicate; the test should verify it returns `true` for an identity with `agentKind` and `false` for a different kind. File `test/.../domain/day_agent_kind_test.dart` is missing.
- [ ] **[MED]** `day_agent_slots_test.dart` — `dayAgentPlanEntityId` is untested. Its return value `'day_agent_plan:$dayId'` is the key used in cross-service lookups; a simple value-equality test would lock the format.
- [ ] **[LOW]** `day_agent_reconcile_models_test.dart` — `DecidedTaskRef.toJson` and `DayAgentCorpusMatch` equality are exercised only through the projection helpers. Direct construction + round-trip tests are absent; `DayAgentCorpusMatch` has no `fromJson`, so `toJson` field names are the only API surface — already exercised at `line 553-571`.

---

## Test execution speed opportunities

- [ ] **[LOW]** No `Future.delayed`, real `Timer`, or `pumpAndSettle` calls detected. All tests are synchronous or use Glados generative loops. No speed improvements needed.
- [ ] **[LOW]** Glados `numRuns: 120` is consistent with the rest of the codebase and within the recommended 80–180 range. No reduction warranted.

---

## Summary

- 2 impl files (`day_agent_config.dart`, `day_agent_kind.dart`) have no test file at all — both are `[HIGH]` gaps requiring new test files.
- 1 existing test file (`day_agent_slots_test.dart`) omits the `dayAgentPlanEntityId` helper entirely.
- Biggest opportunity: add Glados round-trip property for `DayAgentLearningBullet` and `DayAgentLearningCard` — these two value types have `fromJson`/`toJson` + custom `==`/`hashCode` with a non-trivial `_bulletListsEqual` path that only a generative test will reliably exercise.
- No fake-time violations, no inline mocks, no `pumpAndSettle` misuse found.
- Test quality in `day_agent_reconcile_models_test.dart` is exemplary (Glados properties, static edge cases, comprehensive token round-trips); it should serve as the local style reference.
