# Test-Quality Review: daily_os_next/agents/domain

**Scope:** `lib/features/daily_os_next/agents/domain/` (5 impl files) and
`test/features/daily_os_next/agents/domain/` (3 test files).

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../domain/day_agent_config.dart` | 35 | No | No test file at all; `toJson` round-trip untested |
| `lib/.../domain/day_agent_kind.dart` | 16 | No | No test file; `DayAgentKind.matches` untested |
| `lib/.../domain/day_agent_plan_models.dart` | 187 | Yes | Learning models expose serialization only; no unused deserialization API remains |
| `lib/.../domain/day_agent_reconcile_models.dart` | 373 | Yes | No issues (excellent Glados coverage) |
| `lib/.../domain/day_agent_slots.dart` | 21 | Yes | `dayAgentPlanEntityId` is not exercised in the test |
| `test/.../domain/day_agent_plan_models_test.dart` | 198 | — | Learning-card serialization and equality are covered |
| `test/.../domain/day_agent_reconcile_models_test.dart` | 668 | — | None; gold-standard example |
| `test/.../domain/day_agent_slots_test.dart` | 102 | — | `dayAgentPlanEntityId` helper is never called |

---

## File size / split opportunities

- [x] **[LOW]** All impl files are well under 400 lines; no splits required.
  **RESOLVED:** Confirmed by reading the directory — the largest domain impl file is `day_agent_trigger_tokens.dart` (~200 lines); all are well under 400. No action needed.
- [x] **[LOW]** `day_agent_reconcile_models_test.dart` (668 lines) is the largest test file. It is logically split across three private helper functions (`_expectTokenRoundTrip`, `_expectProjections`, `_expectDedupeAndSortEdges`) which keep it navigable. No split is urgent, but if the reconcile model grows further, the token-round-trip group could move to its own file.
  **RESOLVED:** The token-round-trip vocabulary already moved out of `day_agent_reconcile_models.dart` into `day_agent_trigger_tokens.dart` (with its own `day_agent_trigger_tokens_test.dart`), so the reconcile test is now 397 lines and remains navigable via `_expectProjections`/`_expectDedupeAndSortEdges`. No split needed.

---

## Test quality improvements

- [x] **[MED]** `DayAgentLearningBullet` and `DayAgentLearningCard` previously exposed unused JSON deserializers whose only callers were round-trip tests. **RESOLVED:** the unused deserialization API and its implementation-coupled property tests were removed. The remaining tests exercise the production serialization contract, non-default card kind, immutable bullet list, equality, and hash behavior.
- [x] **[LOW]** `day_agent_slots_test.dart` — the file tests `hasActiveDay` and `dayAgentIdForDate` (with Glados) but completely omits `dayAgentPlanEntityId` (line 21 of the impl). Its contract ("same prefix used by `DayAgentPlanService`") is important for cross-service correctness but is verified only indirectly in the service tests outside this scope.
  **RESOLVED:** Added a `dayAgentPlanEntityId` group: a static test locking the `day_agent_plan:dayplan-2026-05-25` format, an empty-input test, and a Glados property over `letterOrDigits` asserting `entityId == 'day_agent_plan:$dayId'` and that the dayId is embedded verbatim.

---

## Generative (Glados) testing opportunities

- [x] **[MED]** The former learning-model round-trip Glados generators existed only for unused deserializers. **RESOLVED:** those generators were removed with the deserialization surface; `DayAgentEnergyBand`, which still supports both directions in production, retains its Glados round-trip coverage.
- [x] **[LOW]** `day_agent_reconcile_models_test.dart` — the existing Glados properties for `classifyParsedItemMatch` and `dedupeAndSortPendingItems` are exemplary; no new property is needed here.
  **RESOLVED:** Confirmed by reading the file — both Glados properties assert threshold invariants and dedupe/sort ordering invariants respectively. No new property needed; non-actionable observation.
- [x] **[LOW]** `day_agent_slots_test.dart` — the Glados property for `dayAgentIdForDate` is well-formed with `numRuns: 120`. No new property needed.
  **RESOLVED:** Confirmed; the `dayAgentIdForDate` Glados property is well-formed. Non-actionable observation, no change.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/.../domain/day_agent_config.dart` — zero tests. The `toJson` method serialises all five fields; the default constructor values are part of the public API contract (e.g. `capacityMinutes: 480`). A single test verifying the JSON output of the default config and one with custom values would suffice. A test file `test/features/daily_os_next/agents/domain/day_agent_config_test.dart` is required by the one-file-per-source-file rule.
- [x] **[HIGH]** `lib/.../domain/day_agent_kind.dart` — zero tests. `DayAgentKind.matches` is a pure predicate; the test should verify it returns `true` for an identity with `agentKind` and `false` for a different kind. File `test/.../domain/day_agent_kind_test.dart` is missing.
- [x] **[MED]** `day_agent_slots_test.dart` — `dayAgentPlanEntityId` is untested. Its return value `'day_agent_plan:$dayId'` is the key used in cross-service lookups; a simple value-equality test would lock the format.
  **RESOLVED:** Added the `dayAgentPlanEntityId` test group (static format-lock test, empty-input test, and a Glados verbatim-embedding property). See the resolution under "Test quality improvements".
- [x] **[LOW]** `day_agent_reconcile_models_test.dart` — `DecidedTaskRef.toJson` and `DayAgentCorpusMatch` equality are exercised only through the projection helpers. Direct construction + round-trip tests are absent; `DayAgentCorpusMatch` has no `fromJson`, so `toJson` field names are the only API surface — already exercised at `line 553-571`.
  **RESOLVED:** `DayAgentCorpusMatch.toJson` field names were already directly asserted via `corpusMatchFromTask -> toJson serializes every field`. Closed the residual `DecidedTaskRef` gap by adding a `DecidedTaskRef` group: one test locking the exact drafting-prompt JSON field names (`id`/`title`/`categoryId`), and one verifying a null `categoryId` keeps the key rather than dropping it.

---

## Test execution speed opportunities

- [x] **[LOW]** No `Future.delayed`, real `Timer`, or `pumpAndSettle` calls detected. All tests are synchronous or use Glados generative loops. No speed improvements needed.
  **RESOLVED:** Confirmed across all three domain test files (and the new additions) — no `Future.delayed`, real `Timer`, `sleep`, or `pumpAndSettle`. All tests are synchronous or Glados loops. Non-actionable observation.
- [x] **[LOW]** Glados `numRuns: 120` is consistent with the rest of the codebase and within the recommended 80–180 range. No reduction warranted.
  **RESOLVED:** Confirmed; every Glados property (including the newly added `dayAgentPlanEntityId` property) uses `numRuns: 120`, within the 80–180 range and != 100. No change.

---

## Summary

- 2 impl files (`day_agent_config.dart`, `day_agent_kind.dart`) have no test file at all — both are `[HIGH]` gaps requiring new test files.
- 1 existing test file (`day_agent_slots_test.dart`) omits the `dayAgentPlanEntityId` helper entirely.
- Learning-card tests now match the production API: serialization, equality, hashing, and defensive list freezing are covered without retaining unused deserializers solely for round-trip tests.
- No fake-time violations, no inline mocks, no `pumpAndSettle` misuse found.
- Test quality in `day_agent_reconcile_models_test.dart` is exemplary (Glados properties, static edge cases, comprehensive token round-trips); it should serve as the local style reference.
