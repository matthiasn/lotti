# Test-Quality Review: daily_os_next/agents/tools

**Scope:** `lib/features/daily_os_next/agents/tools/` (2 impl files) and
`test/features/daily_os_next/agents/tools/` (2 test files).

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../tools/day_agent_tool_names.dart` | 109 | Yes | Routing predicate for `recordObservations` only partially tested |
| `lib/.../tools/day_agent_tools.dart` | 511 | Yes | `matchToCorpus`, `setNextWake`, `recordObservations` schema details not verified |
| `test/.../tools/day_agent_tool_names_test.dart` | 151 | — | Minor: `isWorkflowHandlerTool('unknown-tool')` false path tested but non-workflow paths for `isPlanTool`/`isCaptureReconcileTool` with `recordObservations` are absent |
| `test/.../tools/day_agent_tools_test.dart` | 265 | — | Schemas for five tools never individually inspected |

---

## File size / split opportunities

- [x] **[MED]** `lib/.../tools/day_agent_tools.dart` (511 lines) is approaching the 500-line comfort ceiling. It is a single flat `const` list of `AgentToolDefinition` objects with no extractable logic; however, a natural split seam exists between capture/reconcile tools (lines ~74–232) and plan/refine tools (lines ~234–511). If more tools are added, consider splitting into `day_agent_capture_tools.dart` and `day_agent_plan_tools.dart` re-exported from a barrel. No split is urgent today.
  - **RESOLVED:** (assessed, no change) — the file is a flat const declaration list with no logic; the capture/plan split seam is documented in the item itself and only becomes worthwhile when more tools land.
- [ ] **[LOW]** `day_agent_tools_test.dart` (265 lines) and `day_agent_tool_names_test.dart` (151 lines) are both within acceptable bounds. No split needed.

---

## Test quality improvements

- [x] **[MED]** `day_agent_tools_test.dart` — five tool schemas are never individually inspected by the test suite: `setNextWake` (required fields `at`, `reason`), `recordObservations` (required field `observations`, nested `oneOf` item shape), `matchToCorpus` (required `phrase`, optional `categoryHint`), `matchToCorpus` `additionalProperties: false` on the nested item is present but not validated by any test. The existing test at line 43 checks `additionalProperties` only at the top-level; nested object `additionalProperties` on items like `parseCaptureToItems` item schema and `proposePlanDiff` `from`/`to` sub-objects are not verified.
  - **RESOLVED:** done — added `'nested item schemas keep their own strict contracts'`: setNextWake required fields, matchToCorpus phrase/categoryHint, the parseCaptureToItems item object's `required` + `additionalProperties: false`, and strict `from`/`to` sub-objects on proposePlanDiff.
- [x] **[MED]** `day_agent_tool_names_test.dart:92-150` — the `classifies routed tools consistently` test checks only 8 out of 17 tool names against the four routing predicates. The remaining 9 names (`parseCaptureToItems`, `matchToCorpus`, `linkCapturePhraseToTask`, `breakCaptureLink`, `surfacePendingDecisions`, `applyTriage`, `createTaskFromPhrase`, `summarizeRecentPatterns`, `proposePlanDiff`) are not individually asserted. Although the set-equality tests above implicitly cover membership, an adversarial rename that moves a name between sets would not be caught by the current assertion style. A loop-based membership test over all 17 names would make the routing invariant explicit.
  - **RESOLVED:** done — added a per-name routing table over all 17 tools asserting each of the four predicates explicitly (including `workflowHandler == capture || plan || wake`), so an adversarial set move fails by name.
- [ ] **[LOW]** `day_agent_tools_test.dart:15-41` — the "defines every Daily OS backend tool once" test uses `containsAll` rather than `equals`, so a tool that exists in `dayAgentTools` but was accidentally omitted from `DayAgentToolNames` constants would not be caught. Adding a `hasLength(17)` assertion (matching the 17 constant names) would close this gap.
- [ ] **[LOW]** `day_agent_tool_names_test.dart:26-46` — the `toolNames` list is assembled manually and compared to expected wire-name strings. This is effectively a copy-paste regression test. If a new tool is added to the impl but not to the test list, the test still passes. Adding an assertion that `toolNames.length == DayAgentToolNames.workflowHandlerTools.length + 1` (the `+1` for `recordObservations`, which is outside `workflowHandlerTools`) would make additions self-detectable.

---

## Generative (Glados) testing opportunities

- [ ] **[LOW]** The tool definitions are `const` data structures with no non-trivial domain logic. Neither file contains pure functions with structured input that would benefit from generative testing. Glados is not warranted here.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `day_agent_tools_test.dart` — the `recordObservations` schema includes a `oneOf` on the `observations` array items (string or object with `text`/`priority`/`category`). The test suite never verifies this `oneOf` structure, the allowed `priority` values (`routine`, `notable`, `critical`), or the allowed `category` values (`grievance`, `excellence`, `templateImprovement`, `operational`). These enum values are part of the model contract sent to the LLM.
  - **RESOLVED:** done — `'recordObservations pins the oneOf item shape and its enum contracts'` asserts the two-arm oneOf, the strict object arm (`required: [text]`, no extra properties), and the exact priority/category enum values.
- [x] **[MED]** `day_agent_tools_test.dart` — `applyTriage` action enum (`today`, `doNow`, `defer`, `done`, `drop`) and the conditional `deferTo` field are not validated in the test. The existing required-field test at line 54 only checks `taskId` and `action` are required, not that the enum values are correct.
  - **RESOLVED:** done — `'applyTriage pins the action enum and the conditional deferTo'` asserts the five action values and that `deferTo` exists but is not required (defer-only per the description).
- [ ] **[LOW]** `day_agent_tools_test.dart` — `setNextWake` schema (required `at`/`reason`) is listed in `requiredFor` checks but never tested by name. A dedicated assertion group matching the style of `commit_day and uncommit_day require dayId only` would be consistent.
- [ ] **[LOW]** `day_agent_tool_names_test.dart` — `isSetNextWakeTool` is verified for one positive and one negative case; the remaining foundation tools set membership (currently just `setNextWake`) is exhaustive but would silently miss a second foundation tool added later. No action needed now.

---

## Test execution speed opportunities

- [ ] **[LOW]** All tests are pure in-memory map traversals on `const` data. No async, no I/O, no timers. Test execution is negligible. No improvements needed.

---

## Summary

- 0 oversized test files; `day_agent_tools.dart` (511 lines) is at the size threshold but not yet over it — watch on next tool addition.
- 0 weak smoke-only tests; all assertions are meaningful schema/routing verifications.
- 0 Glados candidates (const data, no pure domain logic).
- 2 `[MED]` coverage gaps: `recordObservations` `oneOf`/enum schema and `applyTriage` action enum are unverified.
- Biggest opportunity: add per-tool schema assertions for `setNextWake`, `recordObservations` (with `oneOf`), and `applyTriage` (action enum + `deferTo`) — these LLM-facing enum constraints are silently unvalidated today.
