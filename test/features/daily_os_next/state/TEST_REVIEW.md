# Test Review — daily_os_next/state

**Scope:** `lib/features/daily_os_next/state/` and `test/features/daily_os_next/state/`

Generated by automated test-quality review (static analysis)

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/…/state/actual_time_blocks_provider.dart` | 175 | Yes | Pure helpers are well-isolated; Glados opportunity for `_projectCategory` hex normalisation |
| `lib/…/state/capture_controller.dart` | 796 | Yes | Oversized; two transcription paths entangled in one class |
| `lib/…/state/daily_os_preferences_controller.dart` | 140 | Yes | Small; well tested |
| `lib/…/state/day_agent_provider.dart` | 105 | Yes | Small; wiring-only |
| `lib/…/state/drafting_controller.dart` | 158 | Yes | Small; one `Future.delayed` drain pattern is fragile |
| `lib/…/state/reconcile_controller.dart` | 153 | Yes | Small; well tested |
| `lib/…/state/refine_controller.dart` | 425 | Yes | Borderline; well tested |
| `lib/…/state/shutdown_controller.dart` | 106 | Yes | Small; well tested |
| `test/…/state/actual_time_blocks_provider_test.dart` | 579 | — | Good functional coverage; no Glados |
| `test/…/state/capture_controller_test.dart` | 1607 | — | Oversized; two large setUp blocks repeat much of the same mock wiring |
| `test/…/state/daily_os_preferences_controller_test.dart` | 182 | — | Good; one speed concern (pumpEventQueue) |
| `test/…/state/day_agent_provider_test.dart` | 383 | — | Reasonable size; helper duplication |
| `test/…/state/drafting_controller_test.dart` | 194 | — | `Future.delayed` drain loop instead of fakeAsync |
| `test/…/state/reconcile_controller_test.dart` | 429 | — | Good quality; minor DRY opportunity |
| `test/…/state/refine_controller_test.dart` | 809 | — | Borderline size; 7 local subclasses of MockDayAgent |
| `test/…/state/shutdown_controller_test.dart` | 178 | — | Good quality |

---

## File size / split opportunities

- [x] **[HIGH]** `capture_controller.dart` is 796 lines with two complete, nearly independent transcription paths embedded in one class — the **realtime path** (`_beginListeningRealtime`, `_finishListeningRealtime`, `_resolveRealtimeFinalTranscript`, `_cleanupRealtime`) and the **batch path** (`_beginListeningBatch`, `_finishListeningBatch`, `_onBatchAmplitude`). A natural split would extract a `_RealtimeCaptureSession` and a `_BatchCaptureSession` helper object, leaving `CaptureController` as a coordinator (~300 lines). The test file's two separate groups (`batch path`, `realtime path`) already confirm these are independent domains. **RESOLVED (adapted):** the state types (`CapturePhase`, `CaptureError`, `CaptureState`) moved to the `part` file `capture_state.dart` (109 lines; controller file down to 690). The session-object extraction is blocked the same way as the chat recorder: `Notifier.state` is `@protected`, so the path methods that mutate state cannot move to extensions/mixins/helper objects without exposing state mutation through callbacks — a production-architecture change beyond test-review scope; the file is well under the hard limit.

- [x] **[HIGH]** `capture_controller_test.dart` is 1,607 lines. The two `setUp` blocks (lines 88–131 for batch, lines 326–383 for realtime) each wire up ~40 lines of stubs. The pattern `_aliveContainer(recorder: ..., transcriber: ..., realtimeService: ..., persistAudio: ...)` and `buildContainer()` are the only helpers. No file-level `_TestBench` exists. A shared bench extracting the repeated mock construction and common stubs would cut ~200 lines. **RESOLVED:** added a file-level `_batchWiring({transcript})` record helper carrying the recorder/transcriber/realtime mocks plus the standard "realtime not configured" stub set; the five verbatim ~30-line setUp wiring blocks (batch path, toggle edge phases, error branches, amplitude clipping, attaches-transcripts) now assign from it. The realtime group keeps its own setUp — its wiring (pcm streams, onDelta capture, stop contract) is genuinely path-specific, so a single merged bench would obscure more than it saves.

- [x] **[MED]** `refine_controller_test.dart` is 809 lines. Seven local subclasses of `MockDayAgent` (`_RecordingRefineAgent`, `_ThrowingRefineAgent`, `_EmptyDiffRefineAgent`, `_HoldingRefineAgent`, `_ThrowingAcceptAgent`, `_ThrowingRevertAgent`, plus the implicit base usage) occupy lines 598–809 (212 lines). These subclasses each duplicate the `MockDayAgent` constructor call with all-`Duration.zero` latencies; a common `_BaseTestAgent` superclass with zeroed latencies would DRY this. **RESOLVED:** Stale — exactly that base class exists as `_ZeroLatencyAgent` (zero latencies + fixed clock); all scripted agents extend it.

- [x] **[LOW]** `actual_time_blocks_provider_test.dart` is 579 lines but all in one group (`actualTimeBlocksForEntries`) with sensible fixtures. No split needed.
  **RESOLVED:** Confirmed non-actionable — the file is now organised into six focused top-level groups (`actualTimeBlocksForEntries`, `debugProjectCategory`, `debugResolveLinkedFrom`, `actualTimelineUpdateBatches`, `dailyOsActualTimeUpdateProvider`, `dailyOsActualTimeBlocksProvider`) each scoped to one impl seam, with shared `_entry`/`_task`/`_link`/`_category` factories. No split needed; no change made.

---

## Test quality improvements

- [x] **[HIGH]** `drafting_controller_test.dart` lines 99–111: the test `'phase flips to ready and draft is populated once draftDayPlan resolves'` drains the fire-and-forget `draftFuture` listener with a manual loop: `for (var i = 0; i < 4; i++) { await Future<void>.delayed(Duration.zero); }`. This is fragile (the count `4` is magic) and violates the fake-time policy. The `DraftingController.build()` uses `unawaited(draftFuture.then(...))` which resolves via a microtask chain. Replace the loop with `await pumpEventQueue()` (from `flutter_test`) or, for unit isolation, `async.flushMicrotasks()` inside `fakeAsync`.

- [x] **[HIGH]** `refine_controller_test.dart` lines 598–809: seven local subclasses of `MockDayAgent`, each repeating `super(parseLatency: Duration.zero, ..., clock: () => DateTime(2026, 5, 25, 9))`. Extract a file-level `_ZeroLatencyAgent` base class with these defaults so each subclass only overrides the specific method under test. This removes ~70 lines of duplicated constructor boilerplate. **RESOLVED:** extracted `_ZeroLatencyAgent` (zero latencies + fixed clock) and rebased all six scripted agents (`_RecordingRefineAgent`, `_ThrowingRefineAgent`, `_EmptyDiffRefineAgent`, `_HoldingRefineAgent`, `_ThrowingAcceptAgent`, `_ThrowingRevertAgent`) on it — each subclass now only overrides the method under test.

- [x] **[MED]** `capture_controller_test.dart` — the `_aliveContainer` helper (lines 52–73) already centralises container creation for the batch group, but the realtime group uses a separate `buildContainer()` at lines 389–405. These two helpers differ only in which mocks/factories are injected. Consider unifying them into one `_makeContainer({bool realtime = false, ...})` helper or a `_CaptureBench` class. **RESOLVED:** Done — `_aliveContainer` gained optional `recorder` / `realtimeRecorderFactory` params and the realtime group's `buildContainer` is now a one-line delegation to it.

- [x] **[MED]** `day_agent_provider_test.dart` — the `freshAgent()` factory (line 64) and the `makeContainer` helper (line 73) are duplicated between `currentDraftPlanProvider` group and (as seen at line 64) at least one more group. Lifting to file-level removes duplication. **RESOLVED:** Stale — `freshAgent()`/`MockDayAgent(...)` is constructed exactly once in the file; the three `makeContainer` helpers override different provider sets and keep different providers alive, so there is no duplication left to lift.

- [x] **[MED]** `capture_controller_test.dart` lines 431–443: `capturedOnDelta!('hello '); await Future<void>.delayed(Duration.zero);` appears three times in a row. The `await Future<void>.delayed(Duration.zero)` calls are real-time micro-delays, not fakeAsync yields. They work but violate the fake-time policy for unit/service tests. Replace with `async.flushMicrotasks()` inside `fakeAsync` if this code is ever re-examined; for now, flag as [MED] since the actual delay is negligible. **RESOLVED:** Done (adapted) — all 14 `Future<void>.delayed(Duration.zero)` micro-delays replaced with `pumpEventQueue()` (the realtime tests are genuinely async — streams + completers — so fakeAsync is not applicable, but draining the full event queue removes the single-turn scheduling assumption).

- [x] **[LOW]** `reconcile_controller_test.dart` — the `_DateRecordingDayAgent`, `_RefreshingDayAgent`, and `_SeededPreferencesController` classes at the bottom of the file (lines 366–429) are well-scoped but all three override `MockDayAgent` without a zero-latency base class. Same pattern as `refine_controller_test.dart`; less impactful since only 3 subclasses.
  **RESOLVED:** Extracted `_ZeroLatencyAgent` (zero parse/pending/triage latencies + fixed clock, mirroring the `refine_controller_test` base) and rebased `_DateRecordingDayAgent` and `_RefreshingDayAgent` on it; the inline `setUp` `agent` now also uses it, removing four copies of the four-argument constructor. `_SeededPreferencesController` extends `DailyOsPreferencesController` (not `MockDayAgent`), so it is intentionally left as-is.

- [x] **[LOW]** `actual_time_blocks_provider_test.dart` — the `_entry`, `_task`, `_link`, `_category` factory helpers (bottom of file) are clean. No structural concern.
  **RESOLVED:** Confirmed non-actionable — the four file-level factories take only the varying parameters, build the entities through the public `JournalEntity`/`EntryLink`/`CategoryDefinition` constructors, and are reused across all six groups (including the new Glados properties). No change made.

---

## Generative (Glados) testing opportunities

- [x] **[MED]** `actual_time_blocks_provider.dart` line 140–156: `_projectCategory` is a pure function that normalises a raw hex color string. A Glados property test with a generator over arbitrary strings could assert: the output `colorHex` is always exactly 6 characters; the fallback color `'8E8E8E'` is used when the input is null/empty; and the `#` prefix is always stripped. This fills a genuine gap — the current tests only cover the happy path with `#5ED4B7` and `#FFAA00`. **RESOLVED:** Done — added a `debugProjectCategory` seam + Glados2 property (numRuns 120) over categoryId {null, empty, present} x 8 raw color shapes: 6-char colorHex always, no `#`, fallback identity for null/empty ids, prefix-stripping, and short-color fallback.

- [x] **[MED]** `actual_time_blocks_provider.dart` line 123–138: `_resolveLinkedFrom` is pure over a set of linked-from entities. A Glados generator over arbitrary sets of `{Task, JournalEntry, RatingEntry, null}` linked-from lists could assert: a `Task` is always preferred over non-task; `RatingEntry` is always excluded; `null` linked-from maps are handled safely. Current tests use only one fixed combination per scenario. **RESOLVED:** Done — added a `debugResolveLinkedFrom` seam + Glados property (numRuns 140) over generated id lists drawn from {task, entry, rating, deleted-task, missing}: oracle-checked task preference, rating exclusion, tombstone exclusion, first-survivor fallback, and null-ids short-circuit.

- [x] **[LOW]** `DailyOsPreferences.allowsCategory` and `allowsCategoryId` are pure one-liners but the invariant `allowsCategory(c) == allowsCategoryId(c.id)` is trivially verifiable by Glados over random category IDs. Small value since the implementation is a single `!excludedCategoryIds.contains(...)` call.
  **RESOLVED:** Added a pure `Glados2` property (numRuns 120, `tags: 'glados'`) in `daily_os_preferences_controller_test.dart` over a candidate category id and an arbitrary excluded subset (both drawn from overlapping pools so allowed/excluded branches both fire). It asserts the two predicates agree for the same id (`allowsCategory(c) == allowsCategoryId(c.id)`) and that both equal `!excluded.contains(id)`, i.e. the rule is exactly excluded-set membership.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `capture_controller.dart` lines 573–606: `_resolveRealtimeFinalTranscript` has five code paths: (1) `!_verifyRealtimeTranscript` → return realtime; (2) `audioFilePath == null/empty` → return realtime; (3) `batchTranscript.isEmpty` → return realtime; (4) `realtimeTranscript.isEmpty || result.usedTranscriptFallback` → return batch; (5) batch is meaningfully longer → return batch. Only paths (4) and (5) are currently tested (`capture_controller_test.dart` lines 466–519). Paths (1), (2) (`audioFilePath == null`), and (3) (`batchTranscript.isEmpty`) are not covered. Path (1) in particular is the `skipRealtimeTranscriptVerificationForNextCapture()` feature, which also has no dedicated test. **RESOLVED:** path (2) was already covered by 'keeps the realtime transcript when no audio file is written'; new tests cover path (1) — `skipRealtimeTranscriptVerificationForNextCapture` keeps realtime verbatim with `verifyNever(transcribe)` and proves the skip is one-shot (next capture verifies again) — and path (3) — a whitespace-only batch transcript keeps the realtime text.

- [x] **[HIGH]** `capture_controller.dart` line 371–395: `_beginListeningRealtime` has an error path where `startRealtimeTranscription` throws — it should set `CaptureError.realtimeTranscriptionStartFailed`. This error path is not tested in `capture_controller_test.dart`. **RESOLVED (stale):** the error path is covered — 'startRealtimeTranscription throwing surfaces error and disposes recorder' stubs the service to throw and asserts `CapturePhase.error` + `CaptureError.realtimeTranscriptionStartFailed`.

- [x] **[MED]** `capture_controller.dart` line 463–515: `_finishListeningRealtime` has an error path when `_realtimeService.stop(...)` throws — it should set `CaptureError.realtimeTranscriptionFailed`. Not tested. **RESOLVED:** Done — added a test re-stubbing `realtimeService.stop` to throw after a successful start: phase=error, error=realtimeTranscriptionFailed, nothing persisted.

- [x] **[MED]** `capture_controller.dart` line 456–481: `_finishListeningRealtime` early-return path when `recorder == null || outputBase == null || ...` (the `noActiveRealtimeSession` error). Not tested. **RESOLVED:** Done — the guard is defensive (unreachable through the public state machine, which always sets the session fields before finishing), so a documented `debugFinishListeningRealtime` seam invokes it directly and the test pins phase=error + noActiveRealtimeSession.

- [x] **[MED]** `day_agent_provider_test.dart` — `capturesForDateProvider` (lines 73–105 of `day_agent_provider.dart`) has no tests at all: the `agent == null → empty list` branch; the `audioRef` lookup path; and the audio entry deleted-at filter are all untested. **RESOLVED:** Stale — a full `capturesForDateProvider` group exists: no-day-agent -> empty, audioRef -> JournalAudio pairing, soft-deleted capture filtering, and null-audioRef handling.

- [x] **[MED]** `actual_time_blocks_provider_test.dart` — `dailyOsActualTimeUpdateProvider` and `actualTimelineUpdateBatches` are not tested directly beyond what the provider wires up. The `actualTimelineUpdateBatches` helper (line 31–33 of the impl file) filters empty sets; a direct test asserting that empty sets are dropped and non-empty sets pass through would be a 5-line unit test. **RESOLVED:** Stale — `actualTimelineUpdateBatches` has a direct emitsInOrder test dropping the empty set, and `dailyOsActualTimeUpdateProvider` is tested for both the unregistered-notifications empty stream and batch forwarding.

- [x] **[LOW]** `refine_controller.dart` line 376–385: `_indicesForDecision` is a pure method. Edge case: called when `state.diff == null` returns `const []` — this is exercised indirectly but never in isolation.
  **RESOLVED (adapted):** The literal `state.diff == null` sub-branch is dead through the public API — both callers (`accept` line 175, `revert` line 207) guard `if (diff == null) return;` before invoking `_indicesForDecision`, so the only reachable "empty result" case is "no `pending` decisions remain". Added `'accept is a no-op once every change is already resolved'`: it accepts every diff change individually (phase → `accepted`), then calls `accept()` and asserts no extra `acceptDiff` agent call and phase stays `accepted` — directly exercising `_indicesForDecision` returning an empty list and the `itemIndices.isEmpty` early-return. No production seam added for the unreachable `diff == null` branch.

---

## Test execution speed opportunities

- [x] **[HIGH]** `drafting_controller_test.dart` lines 99–111: the magic-count `Future.delayed(Duration.zero)` loop runs 4 real async turns in sequence. Replace with `await pumpEventQueue()` (single call, no timing dependency) or `fakeAsync` + `flushMicrotasks`. **Estimated saving: negligible wall time but removes flakiness risk.**

- [x] **[MED]** `capture_controller_test.dart` uses real microphone/file system mocks with multiple `await Future<void>.delayed(Duration.zero)` calls scattered through the realtime tests (e.g., lines 431–443). These are already `Duration.zero` so wall-clock impact is minimal, but replacing with `async.flushMicrotasks()` inside `fakeAsync` would make the tests immune to event-loop scheduling changes across Dart SDK upgrades. **RESOLVED:** Done — same change as the DRY item above: all micro-delays are now `pumpEventQueue()`.

- [x] **[MED]** `refine_controller_test.dart` lines 334–373: the `_HoldingRefineAgent` test freezes `proposePlanDiff` on a `Completer` and needs `await Future<void>.value()` to let the thinking phase start. This is a valid pattern but could be simplified with `fakeAsync` + direct completer resolution rather than `Future<void>.value()` (which relies on microtask ordering assumptions). **RESOLVED:** Done (adapted) — the `Future<void>.value()` single-microtask yield became `pumpEventQueue()` with a comment; a full fakeAsync rewrite was unnecessary once the microtask-ordering assumption was gone.

- [x] **[LOW]** `actual_time_blocks_provider_test.dart` — the `dailyOsActualTimeBlocksProvider` widget tests use real `ProviderContainer` with `pumpEventQueue()`; no `pumpAndSettle` misuse detected.
  **RESOLVED:** Confirmed true — the `dailyOsActualTimeBlocksProvider` tests read provider futures directly (`container.read(...future)`) with no `pumpAndSettle`. (Separately noted as a verifier risk flag: the unrelated `dailyOsActualTimeUpdateProvider` "forwards non-empty batches" test still uses a real `Future.delayed(200ms)` to wait out `UpdateNotifications`' concrete 100ms `Timer` debounce; it is not part of any open review item and a fakeAsync rewrite around the real `UpdateNotifications` + Riverpod stream wiring is risky, so it was left untouched.)

---

## Summary

- 2 impl files oversized (`capture_controller.dart` 796 lines with two entangled transcription paths; `refine_controller.dart` 425 lines — borderline).
- 2 test files oversized (`capture_controller_test.dart` 1,607 lines; `refine_controller_test.dart` 809 lines).
- 3 quality issues: `_resolveRealtimeFinalTranscript` has 3 untested code paths; `drafting_controller_test.dart` uses a fragile `Future.delayed` drain loop; 7 subclasses in `refine_controller_test.dart` all repeat the same constructor boilerplate.
- 2 Glados candidates: `_projectCategory` hex normalisation (genuine pure function); `_resolveLinkedFrom` linked-entity preference logic.
- 2 speed wins: `drafting_controller_test.dart` loop → `pumpEventQueue()`; `capture_controller_test.dart` `Future.delayed(Duration.zero)` calls → `flushMicrotasks()`.
- Biggest opportunity: `_resolveRealtimeFinalTranscript` — three of five code paths are untested, including the `skipRealtimeTranscriptVerificationForNextCapture` feature that has no test at all.
