# Test-Quality Review — design_system/state

**Scope:** `lib/features/design_system/state/` (1 impl file, 251 lines)
and `test/features/design_system/state/` (1 test file, 724 lines).

Generated by automated test-quality review (static analysis).

---

## File size / shape overview

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `pane_width_controller.dart` | 251 | Yes | Well-sized; clearly structured |
| `pane_width_controller.g.dart` | 63 | — | Generated; not tested directly |
| `pane_width_controller_test.dart` | 724 | — | **Oversized test file** — approaching ceiling |

---

## File size / split opportunities

- [ ] **[MED]** `test/features/design_system/state/pane_width_controller_test.dart` is 724 lines for a single 251-line implementation file. The test file is more than 2.5× the implementation. While high coverage is good, the file has 8 `group()` blocks. Consider extracting the `PaneWidths` value-object group (lines 67–111) into a separate `pane_widths_test.dart` if the file continues to grow. For now, the 724-line ceiling is technically compliant (< 1 000) but should not grow further without a split.

- [ ] **[LOW]** No split is needed for the implementation file itself — at 251 lines it is well within bounds.

---

## Test quality improvements

- [ ] **[LOW]** The `_awaitHydration` helper uses a 16-iteration `Future<void>.value()` loop (lines 40–43). The comment explains this is resilient to future async hops, which is a sensible design choice. However, if `_loadPersistedWidths` ever gains more awaits, the loop count may silently become insufficient. Add a brief doc comment citing the current implementation's single `await` so future authors know to increase the count if new awaits are added.

- [ ] **[LOW]** The `setUp` at line 50 stubs `itemsByKeys` without the `sidebarCollapsedKey` in the returned map (the map only sets `sidebarWidthKey: null` and `listPaneWidthKey: null`). This means the `sidebarCollapsedKey` entry would be `null` by default in a real Dart map, but the stub returns a typed `<String, String?>` map that doesn't include the key. The `_loadPersistedWidths` method does `values[sidebarCollapsedKey] == 'true'` which evaluates to `false` for a missing key — so tests pass correctly. This is subtle though; explicitly including `sidebarCollapsedKey: null` in the setUp stub would make the intent clearer and prevent accidental test passes if the lookup semantics changed.

- [ ] **[LOW]** `pane_width_controller_test.dart` lines 418–431 (`resetToDefaults persists immediately without debounce`) verifies that both saves are called via `verify(...).called(1)` without advancing fake time. This works because `resetToDefaults` calls `_persistSidebarWidth()`/`_persistListPaneWidth()` synchronously (not through the debounce timer). The test is correct but could be made more explicit with a comment clarifying that no time advancement is needed.

---

## Generative (Glados) testing opportunities

- [ ] **[MED]** `_parseWidth(stored, defaultValue, minValue, maxValue)` is a pure function with structured inputs and algebraic invariants well-suited to Glados:
  - **Clamping invariant**: For any `parsed ∈ [minValue, maxValue]`, `_parseWidth(parsed.toStringAsFixed(1), default, min, max)` must equal `parsed.clamp(min, max)`.
  - **Default fallback**: For any non-numeric string or `null`, `_parseWidth` must return `defaultValue`.
  - **Boundary invariant**: `_parseWidth(result.toStringAsFixed(1), d, min, max)` applied twice must be idempotent (result already clamped, re-parse → same value).
  
  `_parseWidth` is currently private but could be extracted to a package-visible static for testing, or tested indirectly through hydration with generated string inputs. Add to `pane_width_controller_test.dart` with `tags: 'glados'`.

- [ ] **[LOW]** `PaneWidths.copyWith` + `PaneWidths.==` + `PaneWidths.hashCode` form a trio of algebraic properties (copy-roundtrip, equality symmetry, hash consistency) that Glados can verify over arbitrary `(double, double, bool)` triples. This is a small but genuinely generative opportunity.

---

## Coverage / missing-behavior gaps

- [ ] **[LOW]** The `_persistCollapseFlag` path during `resetToDefaults` (line 249) is exercised indirectly by `resets both widths to defaults` but not through a `fakeAsync` test that verifies the `sidebarCollapsedKey` is persisted with `'false'`. If the user had previously collapsed the sidebar and calls reset, the persistence of the false flag should be explicitly verified.

- [ ] **[LOW]** The `onDispose` callback (lines 84–87) cancels both debounce timers. No test verifies that disposing the provider actually cancels pending writes. A test that disposes the container mid-debounce and verifies `saveSettingsItem` is never called would close this gap.

---

## Test execution speed opportunities

- [ ] **[LOW]** The `fakeAsync` tests (e.g. `persists to SettingsDb after debounce`, `debounce coalesces rapid updates`) all use `async.elapse(persistDebounce)` which is the correct approach — no real time is spent. Execution speed of this file is already optimal. No changes needed.

- [ ] **[LOW]** The async hydration tests use a 16-iteration microtask flush loop. This is correct and fast (all futures resolve via `Future.value()`). No speed concern.

---

## Summary

- **0 oversized impl files** — `pane_width_controller.dart` is 251 lines, well within bounds.
- **1 oversized test file**: `pane_width_controller_test.dart` at 724 lines (compliant but at risk of breaching 1 000 if more groups are added).
- **0 weak-quality tests** — the test file is thorough with meaningful behavioral assertions throughout; every test asserts observable state beyond widget presence.
- **2 genuine Glados candidates**: `_parseWidth` pure function (clamping + fallback invariants) and `PaneWidths` value-object properties (copy-roundtrip, equality, hashCode).
- **0 speed wins needed** — all timer-dependent tests use `fakeAsync`; no real waits present.
- **Biggest opportunity**: add a Glados property test for `_parseWidth` covering the clamping invariant with arbitrary numeric strings — this is a non-trivial pure function that hand-rolled examples can miss edge cases for (NaN, ±Infinity, negative-zero, subnormal values).
