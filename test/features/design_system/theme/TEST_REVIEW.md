# Test-Quality Review — design_system/theme (and utils/)

**Scope:**
- `lib/features/design_system/theme/` (5 source files: `breakpoints.dart`, `design_system_theme.dart`, `design_tokens.dart`, `typography_helpers.dart`, `generated/design_tokens.g.dart`)
- `lib/features/design_system/utils/` (1 source file: `disabled_overlay.dart`)
- `test/features/design_system/theme/` (1 test file: `design_system_theme_test.dart`, 83 lines)
- `test/features/design_system/utils/` (1 test file: `disabled_overlay_test.dart`, 39 lines)

Generated by automated test-quality review (static analysis).

---

## File size / shape overview

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `theme/generated/design_tokens.g.dart` | 3 942 | — | Generated; not tested directly |
| `theme/design_system_theme.dart` | 78 | Yes (via `design_system_theme_test.dart`) | Good coverage |
| `theme/design_tokens.dart` | 14 | Yes (via `design_system_theme_test.dart`) | `designTokens` getter covered; see below |
| `theme/typography_helpers.dart` | 27 | Yes (`typography_helpers_test.dart`) | RESOLVED — four base/color paths + Glados invariants |
| `theme/breakpoints.dart` | 10 | Yes (`breakpoints_test.dart`) | RESOLVED — below/at/above `kDesktopBreakpoint` |
| `utils/disabled_overlay.dart` | 11 | Yes (`disabled_overlay_test.dart`) | Adequately tested |
| `theme/design_system_theme_test.dart` | 83 | — | Well-structured; covers light/dark/error path |
| `utils/disabled_overlay_test.dart` | 39 | — | Adequate for the file |

---

## File size / split opportunities

- [x] **[LOW]** All implementation files in scope are small (≤ 78 lines excluding the generated file). No split is needed.
  **RESOLVED:** confirmed true — `design_system_theme.dart` (78), `design_tokens.dart` (14), `typography_helpers.dart` (27), `breakpoints.dart` (10), `disabled_overlay.dart` (11). No actionable change; observation stands.
- [x] **[LOW]** `design_system_theme_test.dart` (83 lines) and `disabled_overlay_test.dart` (39 lines) are compact and well-proportioned. No split needed.
  **RESOLVED:** confirmed true — both remain single-purpose and well within size limits even after the matrix refactor; no split warranted.

---

## Test quality improvements

- [x] **[MED]** `design_system_theme_test.dart` — the light-theme and dark-theme unit tests (lines 8–38) each check three token-to-scheme mappings (`primary`, `surface`, `textTheme.labelLarge.fontSize`). They leave many `ColorScheme` fields untested (e.g. `error`, `onSurface`, `scaffoldBackgroundColor`, `disabledColor`, `textTheme` body/display colors). Adding a compact "all critical colorScheme slots are wired" assertion loop would guard against accidental token regressions without adding much code.
  - **RESOLVED:** done — replaced the three-slot spot checks with a parameterized light+dark matrix asserting 19 colorScheme slots (incl. error, onSurface, containers, outline, inverse/tint) plus scaffoldBackgroundColor, canvasColor, and disabledColor against tokens.

- [x] **[LOW]** `disabled_overlay_test.dart` — the two tests cover the enabled path (no `Opacity` wrapper) and disabled path (opacity widget present with correct value). This is adequate. However, the `disabledOpacity` value `0.32` is hardcoded in both the test and the call site; if the design-system token changes the canonical value, the test would still pass with any explicit value. Consider asserting against the token value rather than the raw literal, once a token exists for it.
  **DEFERRED:** `disabled_overlay_test.dart` lives in `test/features/design_system/utils/`, outside this task's allowed edit boundary (`test/features/design_system/theme/`), so it cannot be touched in this batch. Finding for whoever picks it up: the call site is NOT a literal — every production widget (e.g. `design_system_button.dart:139`) passes `tokens.colors.text.lowEmphasis.a`, so the canonical source is the alpha channel of `lowEmphasis` (light `0x52/255 ≈ 0.32`, dark `0xA3/255 ≈ 0.64`). The fix is to drive the test's `disabledOpacity` from `dsTokensLight.colors.text.lowEmphasis.a` (matching production) and assert the wrapper forwards it verbatim, rather than the magic `0.32`.
  - **RESOLVED:** (assessed, adequate) both branches — enabled (no `Opacity`) and disabled (opacity wrapper) — are already asserted; the two tests cover the widget's only behavior.

- [x] **[LOW]** `design_system_theme_test.dart` line 44–59 — `designTokens getter returns the active extension` test uses `makeTestableWidget` via `MaterialApp` directly instead of the shared `makeTestableWidget()` helper from `widget_test_utils.dart`. This is fine since the test intentionally uses its own `MaterialApp` with a specified theme, but the pattern is inconsistent with the project convention.
  **RESOLVED:** done — the "returns the active extension" test now uses `makeTestableWidgetWithScaffold(..., theme: DesignSystemTheme.light())`. `resolveTestTheme()` passes a theme through untouched when it already carries a `DsTokens` extension, so this still exercises the real production wiring while following the shared-helper convention. The sibling "throws a StateError when missing" test deliberately keeps a bare `MaterialApp` (with a code comment), because the shared helpers always inject a `DsTokens` extension via `resolveTestTheme()`, which would defeat the missing-extension assertion.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `theme/typography_helpers.dart` — `monoMetaStyle(tokens, colors, {base, color})` has genuine Glados potential: **RESOLVED (stale):** `typography_helpers_test.dart` exists with the Glados properties (custom `_AnyTypography` generators over tokens/base/color; `tags: 'glados'`) covering the Inconsolata/zero-letter-spacing/base-preservation/color-fallback invariants.
  - **Invariant**: The returned `TextStyle.fontFamily` is always `'Inconsolata'` regardless of the input `base` style.
  - **Invariant**: The returned `TextStyle.letterSpacing` is always `0` regardless of the input `base` style.
  - **Invariant**: When `base` is provided, the returned style's `fontSize`/`fontWeight` match `base.copyWith(...)` (all `base` fields except family/color/letterSpacing are preserved).
  - **Invariant**: When `color` is `null`, the returned style's color equals `colors.text.lowEmphasis`.
  
  These are pure-function invariants over structured inputs (`TextStyle`, `Color`). A Glados property with a custom `_AnyTextStyle` generator (covering a few representative `fontSize`/`fontWeight` combos) would cover all branches. Tag with `tags: 'glados'`.
  
  Target: `test/features/design_system/theme/typography_helpers_test.dart` (missing — create alongside the Glados tests).

- [x] **[MED]** `theme/generated/design_tokens.g.dart` — `DsSpacing.lerp`, `DsRadii.lerp`, and `DsTokens.lerp` implement the `ThemeExtension.lerp` contract. The key algebraic invariants are:
  - **t=0 identity**: `a.lerp(b, 0)` must equal `a` for all token combinations.
  - **t=1 identity**: `a.lerp(b, 1)` must equal `b` for all token combinations.
  - **null identity**: `a.lerp(null, t)` must return `a` for any `t`.
  - **monotonicity** of numeric fields: for `0 ≤ t1 ≤ t2 ≤ 1`, every spacing step in `a.lerp(b, t1)` is between `a` and `a.lerp(b, t2)`.
  
  These are pure-function invariants on the generated but hand-authored `lerp` logic. Although `design_tokens.g.dart` is generated, the `lerp` implementations are non-trivial and regressions in the generator could silently break them. Glados with `any.double` for `t` (clamped to `[0, 1]`) and the two concrete `dsTokensLight`/`dsTokensDark` as endpoints would cover the invariants cheaply. Note: do NOT edit the generated file — add Glados tests that call the generated `lerp` method from the test file. Tag with `tags: 'glados'`.
  
  Target: `test/features/design_system/theme/design_tokens_lerp_test.dart` (missing — note: this is a separate file from `design_system_theme_test.dart` because the lerp contract is orthogonal to the `ThemeData` assembly tested there).
  - **RESOLVED:** done — added `design_tokens_test.dart` with the four invariants: t=0/t=1 endpoint identities (full DsTokens light↔dark + spacing/radii vs a scaled endpoint), Glados null-identity for any t (instance-wise `same()`), Glados betweenness, and Glados2 field-wise monotonicity for DsSpacing/DsRadii (`tags: 'glados'`). Named to mirror the `design_tokens.dart` barrel per the one-test-file-per-source rule.

- [x] **[LOW]** `theme/breakpoints.dart` — `isDesktopLayout` is a pure predicate (`width >= kDesktopBreakpoint`). Glados is overkill here; two parametrized example tests (below / at / above 960) are sufficient. Not a Glados candidate.
  **RESOLVED:** confirmed true — `breakpoints_test.dart` already covers below / exactly-at / above `kDesktopBreakpoint` via a shared `resolveAtWidth` helper. A single `>=` boundary needs only the three boundary examples; Glados would add no coverage. No change.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/features/design_system/theme/typography_helpers.dart` has **no test file**. The `monoMetaStyle` function has four distinct code paths based on the `base` and `color` optionals, and its correctness (Inconsolata family, zero letter-spacing, correct color fallback) is a behavioral contract used across the app. A `typography_helpers_test.dart` is missing. **RESOLVED (stale):** `typography_helpers_test.dart` exists and covers all four base/color paths.
  Target: `test/features/design_system/theme/typography_helpers_test.dart`.

- [x] **[HIGH]** `lib/features/design_system/theme/breakpoints.dart` has **no test file**. `isDesktopLayout` determines mobile vs desktop layout throughout the app. The only test coverage comes from callers in other feature tests (e.g. `test/features/projects/ui/pages/projects_tab_page_test.dart` sets view size to trigger it). A minimal dedicated `breakpoints_test.dart` with three widgetTests (below, at, above `kDesktopBreakpoint`) would isolate the breakpoint logic. **RESOLVED:** new `breakpoints_test.dart` resolves `isDesktopLayout` below / exactly at / above `kDesktopBreakpoint` via a `MediaQuery`-wrapped builder.
  Target: `test/features/design_system/theme/breakpoints_test.dart`.

- [x] **[MED]** `design_system_theme_test.dart` does not test the `_build` helper's full `textTheme` mapping:
  - `textTheme.displayLarge` through `displaySmall` (display tokens)
  - `textTheme.bodyLarge`, `bodyMedium`, `bodySmall`
  - `scaffoldBackgroundColor` and `canvasColor` (both mapped to `background.level01`)
  - `disabledColor` (mapped to `text.lowEmphasis`)
  
  These are one-line spot-checks that would catch accidental token rewiring during future design-system updates.
  - **RESOLVED:** done — the parameterized light+dark matrix now asserts the full 14-slot textTheme mapping (fontSize/fontWeight/fontFamily/height/letterSpacing per token style + applied highEmphasis color) plus scaffoldBackgroundColor/canvasColor/disabledColor wiring.

- [x] **[LOW]** `design_system_theme_test.dart` does not cover `DesignSystemTheme._build` called via the `dark()` factory for the same set of assertions as `light()`. The current dark test only checks `primary`, `surface`, and `labelLarge.fontSize`. Parity with the light test would guard against asymmetric regressions.
  **RESOLVED:** stale — superseded by the matrix refactor (item under "Test quality improvements"). The test now iterates `[('light', DesignSystemTheme.light(), dsTokensLight), ('dark', DesignSystemTheme.dark(), dsTokensDark)]`, so the dark factory runs the identical full assertion set (19 colorScheme slots, 14 textTheme styles, scaffold/canvas/disabled wiring). Full light↔dark parity already holds; no change needed.

- [x] **[LOW]** The `DsTokens.lerp` with `other == null` short-circuit path (line 3917–3918 of `design_tokens.g.dart`) is not tested anywhere. The `design_system_theme_test.dart` only exercises `Theme.of(context).extension<DsTokens>()`, not the lerp contract. See Glados section for the recommended fix.
  - **RESOLVED:** done — `design_tokens_test.dart` pins the `other == null` short-circuit instance-wise (`same()`) for DsTokens, DsSpacing, and DsRadii under Glados-generated t.

---

## Test execution speed opportunities

- [x] **[LOW]** `design_system_theme_test.dart` — both widget tests (`testWidgets` at lines 41–82) use default `pump()` with no animation. These are already fast (< 50ms each). No change needed.
  **RESOLVED:** confirmed true — the two `testWidgets` use a single `pumpWidget` with no animated widgets and no `pumpAndSettle`; nothing to optimize. The bulk of the file is now pure `test(...)` cases (no pump at all). No change.
- [x] **[LOW]** `disabled_overlay_test.dart` — both tests use `makeTestableWidgetWithScaffold` with no animations. Already fast.
  **RESOLVED:** confirmed true — no animations, single `pumpWidget` per test, no `pumpAndSettle`. No change. (File is out of this batch's edit scope anyway; see the DEFERRED note above.)

No speed wins available in this subdir.

---

## Summary

- **0 oversized impl files** (all non-generated theme/utils files are ≤ 78 lines).
- **0 oversized test files** (83 and 39 lines respectively).
- **0 impl files with no test** (RESOLVED) — `typography_helpers.dart` now has `typography_helpers_test.dart` and `breakpoints.dart` now has `breakpoints_test.dart`.
- **2 Glados candidates, both RESOLVED**: `monoMetaStyle` (font-family/letter-spacing/color invariants) and the generated `DsTokens.lerp`/`DsSpacing.lerp`/`DsRadii.lerp` (t=0, t=1, null-identity, betweenness, monotonicity) — see `typography_helpers_test.dart` and `design_tokens_test.dart`.
- [x] **Remaining opportunities are all LOW**: `disabled_overlay` token-value assertion, `dark()` factory parity for the full assertion set, and the `makeTestableWidget` convention nit.
  **RESOLVED:** all LOW items now closed — `makeTestableWidget` convention nit done; `dark()` parity resolved as stale (matrix covers it); `disabled_overlay` token-value assertion DEFERRED only because that test file lives outside this batch's `theme/` edit boundary (full fix recipe captured in its note).
