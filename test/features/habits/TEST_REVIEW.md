# Habits Feature — Test Quality Review

**Scope:** `lib/features/habits/**` (20 impl files, ~2 885 lines excluding generated) and
`test/features/habits/**` (12 test files, ~3 898 lines).

_Generated by automated test-quality review (static analysis)_

---

## Per-file summary

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/features/habits/state/habits_controller.dart` | 367 | Yes | Real `DateTime.now()` in controller is mirrored in tests via wall-clock coupling; fake-time not achievable without clock injection |
| `lib/features/habits/state/habits_state.dart` | 158 | Yes | Glados candidate: `completionRate`, `habitMinY`, `activeBy` |
| `lib/features/habits/state/habit_settings_controller.dart` | 260 | Yes | Inline `getIt` in test setUp/tearDown instead of `setUpTestGetIt()` |
| `lib/features/habits/state/habit_completion_controller.dart` | 74 | Yes | Good |
| `lib/features/habits/repository/habits_repository.dart` | 140 | Yes | Good |
| `lib/features/habits/ui/habits_page.dart` | 174 | Yes (page) | Tests rely on `MockHabitsController` defined inline in test file instead of central mocks |
| `lib/features/habits/ui/widgets/habit_completion_card.dart` | 213 | Yes | 5 `pumpAndSettle` calls; one uses real `DateTime.now()` |
| `lib/features/habits/ui/widgets/habits_filter.dart` | 114 | Yes | Inline `getIt` in test (does not use `setUpTestGetIt`) |
| `lib/features/habits/ui/widgets/habits_search.dart` | 62 | Yes | 9 `pumpAndSettle` calls for a non-animated search field |
| `lib/features/habits/ui/widgets/habit_category.dart` | 27 | Yes | Smoke-only assertions (`findsOneWidget` / `byType`) |
| `lib/features/habits/ui/widgets/habit_dashboard.dart` | 117 | Yes | 6 `pumpAndSettle` calls; no assertion after `clearDashboard` |
| `lib/features/habits/ui/widgets/habit_completion_color_icon.dart` | 25 | Yes | Good |
| `lib/features/habits/ui/widgets/habit_page_app_bar.dart` | 77 | **No test file** | **UNTESTED** — non-trivial sliver app-bar with conditional icon rendering |
| `lib/features/habits/ui/widgets/habit_streaks.dart` | 21 | **No test file** | **UNTESTED** — displays completion count from controller |
| `lib/features/habits/ui/widgets/status_segmented_control.dart` | 54 | **No test file** | **UNTESTED** — 4-segment filter control |
| `test/features/habits/state/habits_controller_test.dart` | 779 | — | **OVERSIZED TEST** — 779 lines; real `DateTime.now()` at lines 272, 279, 607 |
| `test/features/habits/ui/widgets/habit_completion_card_test.dart` | 581 | — | **OVERSIZED TEST** — 581 lines; `pumpAndSettle` misuse (×5) |
| `test/features/habits/state/habit_settings_controller_test.dart` | 410 | — | Inline `getIt` setup instead of `setUpTestGetIt` |
| `test/features/habits/state/habits_state_test.dart` | 401 | — | Good overall; `getHabitDays` uses real time |
| `test/features/habits/repository/habits_repository_test.dart` | 350 | — | Good; uses `fakeAsync` correctly |
| `test/features/habits/ui/pages/habits_tab_page_test.dart` | 349 | — | Inline `MockHabitsController` class (not in `test/mocks/mocks.dart`) |
| `test/features/habits/ui/widgets/habits_search_test.dart` | 232 | — | 9 `pumpAndSettle` calls on non-animated widget |
| `test/features/habits/ui/widgets/habits_filter_test.dart` | 208 | — | Inline controller stubs; `getIt.reset()` instead of `tearDownTestGetIt()` |
| `test/features/habits/ui/widgets/habit_dashboard_test.dart` | 118 | — | 6 `pumpAndSettle` calls; missing post-clear assertion |

---

## File size / split opportunities

- [x] **[HIGH]** `test/features/habits/state/habits_controller_test.dart` (779 lines) spans six conceptually independent test groups: initial state, simple mutations, `_determineHabitSuccessByDays`, update-stream handling, `setInfoYmd`, and category filtering. Each group could be an independent test file under `test/features/habits/state/` (e.g., `habits_controller_mutations_test.dart`, `habits_controller_stream_test.dart`, `habits_controller_info_ymd_test.dart`) to stay under the 300-line guideline. (`test/features/habits/state/habits_controller_test.dart:1`) **RESOLVED (assessed, no change):** splitting one source file's tests across multiple files contradicts AGENTS.md — "One test file per source file. Never split tests for one source file across multiple test files." The existing `group()` seams provide the internal organisation.

- [x] **[MED]** `test/features/habits/ui/widgets/habit_completion_card_test.dart` (581 lines) covers null-habit, name display, colors, trailing button, day-cell tap, sheet background, opacity, and priority groups. Split into at least two files: interaction behavior (tap, bottom-sheet) and visual state (color, opacity, decoration). (`test/features/habits/ui/widgets/habit_completion_card_test.dart:1`)
  **RESOLVED (assessed, no change):** Splitting would violate the one-test-file-per-source rule (`lib/.../habit_completion_card.dart` → exactly one mirror test file). The file is organized into clear groups (interaction vs. visual state) which provides the navigability benefit without breaking the mirror convention.

- [x] **[LOW]** `lib/features/habits/state/habits_controller.dart` (367 lines) contains the full state machine, nav-index subscription, streak calculation, category filtering, and percentage math. The streak-calculation block (lines 182-219) and the `setInfoYmd` percentage clamp (lines 343-365) are self-contained and could move to `habits_state.dart` or a dedicated `habit_streak_utils.dart`, improving both testability and file size.
  **RESOLVED:** Both self-contained blocks are now extracted into pure top-level functions. The streak-counting half was already pulled out as `countHabitsWithStreak(...)` (see the MED Glados item above). The `setInfoYmd` percentage half is now the pure `dayPercentages(HabitsState) -> ({success, skipped, failed})` in `habits_state.dart`, with the `failed` headroom clamp documented there; `setInfoYmd` just calls it and spreads the record. This removed the controller's only `dart:math` use (import dropped). Added a `dayPercentages` test group to `habits_state_test.dart` covering the under-100 case, the clamp-to-zero headroom case, and the all-zero case in isolation, while the controller test keeps the end-to-end `setInfoYmd` coverage.

---

## Test quality improvements

- [x] **[HIGH]** `test/features/habits/state/habit_settings_controller_test.dart` uses raw `getIt.registerSingleton` / `getIt.reset()` inline (lines 54-64) instead of `setUpTestGetIt()`/`tearDownTestGetIt()`. This sidesteps the standard harness and may leave stale services if tests are run in different order. (`test/features/habits/state/habit_settings_controller_test.dart:34-64`)

- [x] **[HIGH]** `test/features/habits/ui/widgets/habits_filter_test.dart` uses `getIt.registerSingleton` (lines 54-57) and `await getIt.reset()` (line 63) directly instead of `setUpTestGetIt()`/`tearDownTestGetIt()`. Additionally, `_EmptyController`, `_WithHabitsController`, and `_TrackingController` are defined inline in the test file. If a `MockHabitsController` generalization is added to `test/mocks/mocks.dart`, these three stubs could be replaced. (`test/features/habits/ui/widgets/habits_filter_test.dart:28-65`)

- [x] **[HIGH]** `test/features/habits/ui/pages/habits_tab_page_test.dart` defines `MockHabitsController` inline (lines 21-50) with per-test-file overrides of every `HabitsController` method. Per the "centralized mocks" rule, this class should live in `test/mocks/mocks.dart`. The inline definition creates drift risk when `HabitsController`'s interface changes. (`test/features/habits/ui/pages/habits_tab_page_test.dart:21`)

- [x] **[MED]** `test/features/habits/ui/widgets/habit_completion_card_test.dart:403` uses `DateTime.now().ymd` inside a test body (for "today's cell" path). This introduces a real-time dependency. The test comment notes this explicitly but offers no mitigation. The card's tap handler branches on `DateTime.now().ymd != res.dayString`; injecting a clock or using a fixed date that matches `_today = DateTime(2024, 3, 15)` for that branch would make the test fully deterministic. (`test/features/habits/ui/widgets/habit_completion_card_test.dart:403`)
  **RESOLVED:** Injected `clock.now()` into `habit_completion_card.dart` (3 call sites, `package:clock`) and pinned the day-cell test with `withClock(Clock.fixed(DateTime(2024, 3, 15, 14, 30)))` so the today-branch is fully deterministic.

- [x] **[MED]** `test/features/habits/ui/widgets/habit_category_test.dart` — both test cases assert only `findsOneWidget` / `byType` (`SelectCategoryWidget`, `TextField`). These are smoke tests; they prove rendering but not selection behavior. Add assertions for: (a) category name appears after opening modal, (b) tapping a category calls the persistence layer (verify via mock), (c) the text field shows the selected category name. (`test/features/habits/ui/widgets/habit_category_test.dart:48-84`)
  **RESOLVED:** First test now asserts the field renders the resolved category name; second test uses a `ProviderContainer` + `UncontrolledProviderScope` and, after tapping the category in the modal, asserts `habitSettingsControllerProvider` state has `dirty == true` and `categoryId == categoryMindfulness.id` (the persistence write happens on save, outside this widget — state landing is the widget's contract).

- [x] **[MED]** `test/features/habits/ui/widgets/habit_dashboard_test.dart` — the "clears dashboard when close icon tapped" test (lines 88-117) ends with `expect(find.byIcon(Icons.close_rounded), findsNothing)`. That only proves the icon is gone; it does not verify that the dashboard ID was actually cleared (e.g., by checking the text field is now empty or that `upsertHabitDefinition` was called with `dashboardId: null`). (`test/features/habits/ui/widgets/habit_dashboard_test.dart:88-117`)
  **RESOLVED:** Test now reads the controller state through a `ProviderContainer` and asserts `dashboardId == null` + `dirty == true` after the close tap, plus the text field text emptying. Strengthening this exposed a fixture collision (both shared dashboards use `id: ''`), worked around with a locally distinct-id copy.

- [x] **[LOW]** `test/features/habits/state/habits_state_test.dart` — `getHabitDays` tests (lines 47-80) use the live `DateTime.now()` indirectly, then verify only length and format, not specific dates. Since `getHabitDays` is a pure function once the current time is fixed, these tests are non-deterministic if run at midnight. Consider parameterizing or providing a clock injection to make them fully deterministic.
  **RESOLVED:** Switched `getHabitDays` from `DateTime.now()` to `clock.now()` (`package:clock`, matching the precedent already set in `habit_completion_card.dart`), then wrapped all four `getHabitDays` tests in `withClock(Clock.fixed(DateTime(2024, 3, 15, 10, 30)))`. Tests now assert the exact enumerated date list (`2024-03-08`..`2024-03-15` for span 7, `2024-03-01`..`2024-03-15` for span 14) instead of just length/regex, removing the midnight flakiness.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `lib/features/habits/state/habits_state.dart` — `completionRate(state, byDay)` is a pure function: given total active habits and a count for a day, it returns a rounded percentage. Glados can verify: (a) result is always in [0, 100]; (b) if `n == total`, result == 100; (c) if `n == 0`, result == 0; (d) rounding is consistent with `(n/total * 100).round()`. No I/O. (`lib/features/habits/state/habits_state.dart:81-95`)

- [x] **[HIGH]** `lib/features/habits/state/habits_state.dart` — `habitMinY(days, state)` is pure: it computes `max(lowestRate - 20, 0)` across a list of days. Invariants: result is always ≥ 0; result is always ≤ 100; when all rates are ≥ 20, result equals lowestRate - 20. Glados can explore arbitrary rate distributions. (`lib/features/habits/state/habits_state.dart:128-148`)

- [x] **[MED]** `lib/features/habits/state/habits_state.dart` — `activeBy(habits, ymd)` filters by `activeFrom`. Property: for any list of habits and any date string, every returned habit has `activeFrom ≤ parsed(ymd)`, and no excluded habit satisfies that condition. Trivially generative with `any.dateTime` generators. (`lib/features/habits/state/habits_state.dart:108-125`)
  **RESOLVED:** Replaced the fixed ±1-day even/odd property pair with one oracle property over generated scenarios (0–12 habits with day offsets 0–40 + null `activeFrom`, mixed time-of-day, generated query day): result ids must equal the date-only-≤ oracle filter, order preserved. Covers same-day boundary and the null→`DateTime(0)` fallback the old properties never hit.

- [x] **[MED]** `lib/features/habits/state/habits_controller.dart` — streak counting logic (lines 208-219): for a given set of completion days and a target day range, `shortStreakCount` and `longStreakCount` must satisfy: count ≤ total number of habits; when a habit has completions on all streak days, it must be counted; when it's missing any day, it must not be counted. A Glados test over arbitrary habit/day combinations would find edge cases in the `Set.containsAll` logic. (`lib/features/habits/state/habits_controller.dart:208-219`)
  **RESOLVED:** Extracted the inline forEach into a pure `countHabitsWithStreak(habitSuccessDays, streakDays)` (used by production for both windows) and added a Glados property: per-habit 10-bit day masks + a streak-window mask, asserting the count equals the bit-arithmetic oracle (`mask & streakMask == streakMask`), count ≤ habit count, and empty window counts all habits.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/features/habits/ui/widgets/habit_page_app_bar.dart` (77 lines) has **no test file**. The `HabitsSliverAppBar` widget conditionally renders the zero-baseline toggle button (line 54: `if (state.minY > 20)`), the search toggle, and the time-span toggle. All three are user-visible interactive elements with state-dependent rendering. A test should cover at least: initial render, all three icon button tap callbacks, and the `minY > 20` conditional. (`lib/features/habits/ui/widgets/habit_page_app_bar.dart:1`)

- [x] **[HIGH]** `lib/features/habits/ui/widgets/status_segmented_control.dart` (54 lines) has **no test file**. The `HabitStatusSegmentedControl` renders 4 segments and invokes `onValueChanged` when a segment is selected. Tests should verify: all four labels are rendered (localized), tapping each segment invokes the callback with the correct `HabitDisplayFilter` value. (`lib/features/habits/ui/widgets/status_segmented_control.dart:1`)

- [x] **[HIGH]** `lib/features/habits/ui/widgets/habit_streaks.dart` (21 lines) has **no test file**. Although small, it reads `state.habitDefinitions.length` and `state.completedToday.length` and displays the string. A one-test widget test verifying the displayed string format would complete coverage. (`lib/features/habits/ui/widgets/habit_streaks.dart:1`)

- [x] **[MED]** `test/features/habits/state/habits_controller_test.dart` — the nav-index trigger test (lines 712-778) uses `identical(stateWhileAway, stateOnReturn)` to detect recomputation. This is fragile because a `copyWith` that returns an equal-but-not-identical object would incorrectly fail the test. Prefer asserting a concrete state field that is known to change (e.g., `openNow` list identity after definitions change), or use a `verify` on the repository mock. (`test/features/habits/state/habits_controller_test.dart:712-778`)
  **RESOLVED:** Replaced both `identical()` checks with a concrete signal: re-stubbed `getHabitCompletionsInRange` with a counting answer; inactive→active asserts exactly 1 refetch, and on→on / →off / off→off assert 0 refetches. Immune to equal-but-not-identical `copyWith` results.

- [x] **[MED]** `lib/features/habits/state/habits_controller.dart` — the `_handleNavIndex` path that fetches completions on re-activation is tested in the controller test, but not tested through the full widget integration (i.e., pumping `HabitsTabPage`, switching nav index, and verifying the page reflects updated data). This edge case is only covered at the unit level. (`lib/features/habits/state/habits_controller.dart:279-291`)
  **RESOLVED (assessed, no change):** The reactivation edge is now verified concretely at the unit level (refetch counting + recomputed state). A full `HabitsTabPage` integration would re-test the same controller logic through heavy NavService/page scaffolding while the page-renders-provider-state contract is already covered in `habits_tab_page_test.dart` — mock setup would dwarf the one extra assertion.

- [x] **[LOW]** `lib/features/habits/ui/habits_page.dart` — the `showSearch` branch renders `HabitsSearchWidget` and the `showTimeSpan` branch renders `TimeSpanSegmentedControl`. Both are covered in `habits_tab_page_test.dart`. However, the search-filter pipeline (`searchString` filtering inside `HabitsTabPage.build`) — the list rebuild when `searchString` matches partial names — is tested with a fixed string ("later today") but not with edge cases (empty string, case variants, no match). (`lib/features/habits/ui/habits_page.dart`)
  **RESOLVED:** Added four edge-case tests to `habits_tab_page_test.dart` covering the full `filterMatching` pipeline: (a) empty `searchString` is a no-op (both cards stay); (b) lowercase `'flossing'` matches the capitalised "Flossing" names, proving the `name.toLowerCase()` lowering (controller already lowercases the input); (c) `'gums'` matches via the description branch (`description.toLowerCase().contains(...)`), not the name; (d) non-matching `'zzz'` hides every habit and renders zero `HabitCompletionCard`s. Each asserts concrete card-key presence/absence.

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/features/habits/ui/widgets/habits_search_test.dart` uses **9 `pumpAndSettle()` calls** across 7 test cases for a plain text field with no animations. The `pumpAndSettle` calls after `enterText` and `tap` can be replaced with `tester.pump()`. Estimated saving: ~0.5–1 second per test (× 7 = up to 7 seconds on a slow shard). (`test/features/habits/ui/widgets/habits_search_test.dart:74,93,101,117,122,150,175,199,227`)

- [x] **[HIGH]** `test/features/habits/ui/pages/habits_tab_page_test.dart` uses **10 `pumpAndSettle()` calls** across its tests. `HabitsTabPage` with a `MockHabitsController` that returns a synchronous `HabitsState` has no pending animations; replacing all calls with `tester.pump()` is safe. (`test/features/habits/ui/pages/habits_tab_page_test.dart`)

- [x] **[MED]** `test/features/habits/ui/widgets/habits_filter_test.dart` uses **7 `pumpAndSettle()` calls** for modal open/close interactions. The modal open is a `showModalBottomSheet` which can be fully settled with `tester.pump(const Duration(milliseconds: 300))` (a single frame advance after the animation) rather than the full `pumpAndSettle` scan. (`test/features/habits/ui/widgets/habits_filter_test.dart:80,101,122,129,147,158,165`)
  **RESOLVED:** All 7 settles replaced with bounded `pump()` + `pump(300ms)` pairs; suite green.

- [x] **[MED]** `test/features/habits/ui/widgets/habit_completion_card_test.dart` uses **5 `pumpAndSettle()` calls** — all in tests that open a bottom sheet. `pumpAndSettle` after `tap(find.byIcon(...))` is reasonable for sheet animations, but the setup pump on line 103 (`await tester.pump()`) correctly uses a single pump. The subsequent `pumpAndSettle` calls in trailing-button and day-cell tests (lines 329, 386, 421, 462, 487) could use `tester.pump(const Duration(milliseconds: 250))` to bound sheet open time instead of relying on full settle. (`test/features/habits/ui/widgets/habit_completion_card_test.dart:329,386,421,462,487`)
  **RESOLVED:** All 5 sheet-opening settles replaced with bounded `pump()` + `pump(300ms)` pairs; all 20 tests green.

- [x] **[MED]** `test/features/habits/ui/widgets/habit_dashboard_test.dart` uses **6 `pumpAndSettle()` calls** for a modal-search widget. Same rationale as filter: bounded `tester.pump(duration)` instead of unbounded `pumpAndSettle`. (`test/features/habits/ui/widgets/habit_dashboard_test.dart:61,74,77,86,113,116`)
  **RESOLVED:** All settles replaced with bounded `pump()` + `pump(300ms)` pairs (habit_category_test's modal pumps bounded too); suite green.

- [x] **[LOW]** `test/features/habits/ui/widgets/habit_category_test.dart` uses **4 `pumpAndSettle()` calls** for a stateless widget with a modal. Replace with `tester.pump(const Duration(milliseconds: 300))` after modal open/close taps. (`test/features/habits/ui/widgets/habit_category_test.dart:55,62,76,83`)
  **RESOLVED (already done):** The file no longer contains any `pumpAndSettle()` calls — every modal open/tap now uses bounded `pump()` + `pump(300ms)` pairs (converted alongside the dashboard-test work noted above). Verified by grep: zero `pumpAndSettle` occurrences remain.

---

## Summary

- 1 oversized test file (`habits_controller_test.dart`, 779 lines) and 1 near-oversized test file (`habit_completion_card_test.dart`, 581 lines) warrant splitting along their existing test group boundaries.
- 3 impl files have **no test file**: `habit_page_app_bar.dart` (conditional icon rendering), `status_segmented_control.dart` (4-segment filter), and `habit_streaks.dart` (completion count display).
- Inline `getIt` boilerplate appears in 3 test files (`habit_settings_controller_test.dart`, `habits_filter_test.dart`, `habits_search_test.dart`) — all should migrate to `setUpTestGetIt()`/`tearDownTestGetIt()`.
- 4 strong Glados candidates exist in pure functions in `habits_state.dart` (`completionRate`, `habitMinY`, `activeBy`) and the streak-counting logic in `habits_controller.dart`; zero Glados tests currently exist for this feature.
- `pumpAndSettle` is overused in search (×9), page (×10), filter (×7), and card (×5) tests — all on non-animated or bounded-animation widgets; replacing with `tester.pump()` or bounded pumps is the highest-ROI speed improvement.
- The biggest single opportunity is extracting the `MockHabitsController` defined inline in `habits_tab_page_test.dart` into `test/mocks/mocks.dart`, which would allow the inline controller stubs in filter/search tests to be consolidated too.
