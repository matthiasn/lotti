# Test-Quality Review: `test/features/journal/ui/` (root files)

**Scope:** `test/features/journal/ui/entry_actions_copy_test.dart` and its implied impl targets.
**Note:** `widgets/` subdirectory is covered by a separate review agent and is excluded here.

_Generated by automated test-quality review (static analysis)_

---

## Per-file table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/features/journal/ui/widgets/editor/editor_widget.dart` (indirect) | — | Partial via `entry_actions_copy_test.dart` | Test not co-located with its source |
| `lib/features/journal/ui/widgets/entry_details/header/modern_action_items.dart` (indirect) | — | Partial via `entry_actions_copy_test.dart` | Test not co-located with its source |
| `lib/features/journal/ui/widgets/entry_details/header/initial_modal_page_content.dart` (indirect) | — | Partial via `entry_actions_copy_test.dart` | Test not co-located with its source |
| `test/features/journal/ui/entry_actions_copy_test.dart` | 259 | N/A (test file) | Misplaced: covers `widgets/` sources; uses ad-hoc `_wrapWithApp` instead of `makeTestableWidget()`; one smoke-only test; inline `TestEntryController` class |

---

## File size / split opportunities

- [ ] **[LOW]** `entry_actions_copy_test.dart` (259 lines) is under the 500-line soft limit. No split needed. However it should be **moved** to align with its source files (see below).

---

## Test quality improvements

- [x] **[HIGH]** `entry_actions_copy_test.dart` violates the **one-test-file-per-source-file** rule (AGENTS.md). It lives at `test/features/journal/ui/entry_actions_copy_test.dart` but covers source files that live under `lib/features/journal/ui/widgets/entry_details/header/` and `lib/features/journal/ui/widgets/editor/`. The tests should be merged into the corresponding `*_test.dart` files under `test/features/journal/ui/widgets/...` (which the separate widgets/ reviewer will address).

- [x] **[HIGH]** `entry_actions_copy_test.dart:78-92` defines a private `_wrapWithApp()` helper that re-implements `ProviderScope` + `MaterialApp` + localization + `Scaffold`. This directly contradicts the AGENTS.md rule: _"Use `makeTestableWidget()` for widget tests. Do not create ad-hoc `MaterialApp` / `ProviderScope` / `MediaQuery` wrappers."_ The `EditorWidget` test (lines 197-222) and `InitialModalPageContent` test (lines 224-258) also construct their own `ProviderScope/MaterialApp` inline rather than going through `makeTestableWidget()`. **RESOLVED (stale):** `entry_actions_copy_test.dart` was deleted in the earlier review pass (#3262); neither the file nor `_wrapWithApp` exists anymore.

- [ ] **[MED]** `entry_actions_copy_test.dart:197-222` — the 'Editor toolbar builds (coverage)' test asserts only `expect(find.byType(QuillSimpleToolbar), findsOneWidget)`. This is a construction smoke test: it verifies the widget tree didn't crash but tests no behavior. Per AGENTS.md _"`findsOneWidget` alone is not a valid test."_ Replace or augment with an assertion on an observable behavioral outcome (e.g., confirm toolbar items are enabled/disabled based on entry state).

- [ ] **[MED]** `entry_actions_copy_test.dart:26-73` defines `TestEntryController` inline in the test file. `MockTimeService`/`MockPersistenceLogic`/etc. already live in `test/mocks/mocks.dart`. While `TestEntryController` is a subclass rather than a pure mock, the pattern of inlining controller subclasses in test files should be minimized — at minimum, document why it cannot use a centralized mock.

- [ ] **[MED]** `entry_actions_copy_test.dart:97-104` uses raw `getIt` registration (`getIt.reset(dispose: true)`, `getIt.registerSingleton(...)`) in `setUpAll` instead of the centralized `setUpTestGetIt()` / `tearDownTestGetIt()` pattern from `test/widget_test_utils.dart`. This bypasses the standard cleanup lifecycle and risks inter-test contamination.

- [ ] **[LOW]** `entry_actions_copy_test.dart` does not import `test/helpers/fallbacks.dart` and calls no `registerFallbackValue()`. Depending on the full mock interaction graph, missing fallbacks could make tests fragile. Verify the fallback registration is complete.

---

## Generative (Glados) testing opportunities

No pure logic resides in the root `lib/features/journal/ui/` directory (it contains no `.dart` files at that level). The widgets covered by `entry_actions_copy_test.dart` are UI components — Glados is a poor fit. **No Glados candidates identified for this scope.**

---

## Coverage / missing-behavior gaps

- [ ] **[MED]** The 'Copy actions hidden when no text' test (lines 174-195) correctly asserts `findsNothing` for both copy items. However, no test covers the transition from empty→non-empty text (i.e., typing content into the quill editor and verifying the copy actions appear dynamically). This reactive visibility behavior is untested.

- [ ] **[MED]** `InitialModalPageContent` is only tested in the happy-path (entry with text, both copy items visible). No test covers `inLinkedEntries: true` or a non-null `link` parameter.

- [ ] **[LOW]** `EditorWidget` smoke test only checks toolbar existence. The `entryId`-driven content loading, toolbar state for empty/non-empty documents, and the `shouldShowEditorToolBar` flag from `EntryState` are unverified.

---

## Test execution speed opportunities

- [ ] **[MED]** `entry_actions_copy_test.dart:129,165,219,253` — four `pumpAndSettle()` calls with no animation-settling justification. Each is driven by async provider loading, which in mock-backed tests completes in one or two frames. Replace each `pumpAndSettle()` with `pump()` + `pump(const Duration(milliseconds: 1))` (or `tester.pump()` twice) to avoid the 10-second fallback timeout risk.

- [ ] **[LOW]** `entry_actions_copy_test.dart` has a `setUpAll` that instantiates real `JournalDb(inMemoryDatabase: true)` and `EditorDb(inMemoryDatabase: true)` singletons. These are heavier than mock objects and are shared across all tests in the file. Because the `setUp`/`tearDown` lifecycle is absent, these singletons persist for the lifetime of the suite, which can cause state leaks if the test file is run in the same process as other tests that share `getIt`.

---

## Summary

- `entry_actions_copy_test.dart` is the only root-level test file in scope; there are **no impl files** at `lib/features/journal/ui/` root level (the source is all in subdirectories).
- **1 misplacement issue**: the test covers `widgets/` sources but lives one level up.
- **1 ad-hoc widget wrapper** (`_wrapWithApp`) violating `makeTestableWidget()` mandate.
- **1 smoke-only test** (Editor toolbar builds) with no behavioral assertion.
- **0 Glados candidates** (all UI, no pure logic in scope).
- **4 `pumpAndSettle()` calls** that could be replaced with bounded pumps (~10-second timeout exposure per call).
- Biggest opportunity: **merge this file into the correct `widgets/` test files and replace `_wrapWithApp` with `makeTestableWidget()`** — doing so resolves the misplacement, the ad-hoc wrapper, and the `setUpAll` leak risk in one pass.
