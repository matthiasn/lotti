# Test Quality Review — `journal/ui/widgets` (root level)

**Scope:** `lib/features/journal/ui/widgets/*.dart` + `test/features/journal/ui/widgets/*.dart` (root only; `create/`, `editor/`, `list_cards/` covered separately)

Generated by automated test-quality review (static analysis)

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/…/entry_details_widget.dart` | 673 | Yes (`entry_details_widget_test.dart` 2349 L + `entry_details_collapsible_test.dart` 1119 L) | Two test files for one source file (rule violation); both test files massively over-size |
| `lib/…/entry_detail_linked.dart` | 130 | Yes (`entry_detail_linked_test.dart` 485 L) | OK size |
| `lib/…/entry_detail_linked_from.dart` | 89 | Yes (`entry_detail_linked_from_test.dart` 381 L) | OK size |
| `lib/…/entry_image_widget.dart` | 132 | Yes (`entry_image_widget_test.dart` 367 L) | OK size |
| `lib/…/helpers.dart` | 31 | **No** | No test file at all |
| `lib/…/journal_app_bar.dart` | 26 | **No** | No test file at all |
| `lib/…/linked_entries_activity_filter_bar.dart` | 241 | Yes (`linked_entries_activity_filter_bar_test.dart` 111 L) | OK |
| `lib/…/linked_entries_filter_modal.dart` | 178 | Yes (`linked_entries_filter_modal_test.dart` 101 L) | OK |
| `lib/…/linked_entries_with_timer.dart` | 44 | **No** | No test file at all |
| `lib/…/nested_ai_responses_widget.dart` | 341 | Yes (`nested_ai_responses_widget_test.dart` 560 L) | 16 `pumpAndSettle` calls; not using `setUpTestGetIt` |
| `lib/…/text_viewer_widget.dart` | 64 | **No** | No test file at all |
| `lib/…/text_viewer_widget_non_scrollable.dart` | 151 | Yes (`text_viewer_widget_non_scrollable_test.dart` 522 L) | 33 `findsOneWidget`/`findsWidgets`-only assertions; 4 `pumpAndSettle` |

---

## File size / split opportunities

- [x] **[HIGH]** `test/…/entry_details_widget_test.dart` is **2349 lines** — the single largest test file in this scope. It contains 9 independent `setUp` blocks (each ~40 lines of near-identical raw `getIt.registerSingleton` boilerplate), 8 `group` blocks, and covers the `EntryDetailsWidget`, `EntryDetailsContent`, `_CollapsibleBody`, `_PulsingBorder`, and `_GlowBorderPainter` private widgets. The file mixes layout tests, highlight tests, timer tests, labels tests, coverage branch tests, collapsible-body tests, and auto-scroll tests. It should be split along its existing `group` boundaries into at least 3 files — e.g. `entry_details_widget_layout_test.dart`, `entry_details_widget_highlights_test.dart`, `entry_details_widget_content_test.dart` — keeping the 9 `setUp` blocks in appropriate files. **RESOLVED (adapted):** the multi-file split is rejected per the one-test-file-per-source rule (`entry_details_widget.dart` is a single source). The substantive halves are done: the 9 raw `getIt.registerSingleton` setUp blocks are consolidated into a shared `_registerEntryDetailsMocks()` helper, `entry_details_collapsible_test.dart` is merged in as the `Collapsible Tests` group, and the three `isActiveTimer` constructor smoke tests (banned by the test-quality rules) were removed.

- [x] **[HIGH]** `test/…/entry_details_collapsible_test.dart` (**1119 lines**) tests `EntryDetailsWidget` and `EntryDetailsContent`, which live in `entry_details_widget.dart`. This is a **direct violation of the one-test-file-per-source-file rule** (AGENTS.md). The collapsible test group, the `onToggleCollapse` group, and the error-handling group that currently live here must be merged into `entry_details_widget_test.dart` (which already covers the auto-scroll path that calls `onToggleCollapse`). Together the two files create unnecessary duplication of test infrastructure.

- [x] **[MED]** `test/…/text_viewer_widget_non_scrollable_test.dart` at **522 lines** for a 151-line source file (3.5× ratio). The file has 24 test cases and no shared `_pumpWidget` helper, so each test repeats the same `makeTestableWidget(const TextViewerWidgetNonScrollable(entryText: ..., maxHeight: ...))` pattern. Extracting a `_pump(tester, {EntryText? entryText, double maxHeight = 200})` helper would cut the file by ~150 lines.
  **RESOLVED (stale):** the file is down to 250 lines and has exactly the suggested `_pump(tester, {entryText, maxHeight})` helper plus `editorController`/`editorText` accessors.

- [ ] **[LOW]** `lib/…/entry_details_widget.dart` at **673 lines** approaches the 500-line soft threshold. The `_GlowBorderPainter`, `_PulsingBorder`, `_TimerBorder`, and `_TimerBorderPainter` classes (lines 186–400) are pure painting helpers with no dependency on the rest of the file; they could move to a private `_border_painters.dart` in the same directory to keep `entry_details_widget.dart` under 300 lines.
  **DEFERRED:** this is a production-code (not test-quality) structural refactor of a 673-line widget, and the four classes are library-private (`_`-prefixed). Splitting them out while keeping them private requires a `part`/`part of` setup, and `entry_details_widget_test.dart` reaches into them in 37 places via `_byPrivateType('_PulsingBorder' | '_TimerBorder' | '_GlowBorderPainter')`. Runtime type names would survive a `part` move, but verifying the directive setup, analyzer cleanliness, and that all 37 references still resolve requires running build_runner/analyzer/tests, which is out of this pass's no-commands scope. Left for a dedicated lib-refactor change.

---

## Test quality improvements

- [x] **[HIGH]** `entry_details_widget_test.dart` — 9 `setUp` blocks (lines ~242, ~472, ~717, ~837, ~1069, ~1261, ~1601, ~1796, ~2095) each repeat the same 12–15 `getIt.registerSingleton<X>()` calls. None of them calls `setUpTestGetIt()` from `test/widget_test_utils.dart`, violating the "Use `setUpTestGetIt()` / `tearDownTestGetIt()`" rule. Extract a single shared setup helper (or use `setUpTestGetIt` with `additionalSetup`) and call it from each group's `setUp`. The boilerplate alone accounts for approximately 350 of the file's 2349 lines.

- [x] **[HIGH]** `entry_details_widget_test.dart` — Groups 'Highlight Tests' (lines 232–460) and 'Timer Highlight Tests' (lines 462–705) contain tests whose only assertion is `expect(find.byType(EntryDetailsWidget), findsOneWidget)` (e.g. lines 320–321, 344–346, 388–390, 434–436, 458–459, 649–650, 702–703). These are pure smoke tests — they prove the widget tree built without crashing, but assert nothing about the behavior being tested (pulsing border present, animation state, etc.). Replace each `findsOneWidget` guard with a meaningful assertion about the widget variant being tested (e.g. presence of `CustomPaint`, `_PulsingBorder` opacity, `_TimerBorder` in stack).

- [x] **[HIGH]** `text_viewer_widget_non_scrollable_test.dart` — **33 assertions** in 15+ tests are exactly `expect(find.byType(TextViewerWidgetNonScrollable), findsOneWidget)` optionally followed by `expect(find.byType(QuillEditor), findsOneWidget)`. Tests like 'renders with plainText only' (line 28), 'renders with markdown content' (line 76), 'fallback to plainText when markdown is null' (line 261), 'handles special characters in text' (line 423), 'handles null quill content gracefully' (line 441) are all structurally identical — they pump the widget and check that _it is still there_. These either need to assert the actual rendered text content, verify the controller's document differs between content types, or be merged into a single parameterized loop.

- [x] **[MED]** `entry_details_collapsible_test.dart` — The `setUp` block (line 57) registers all `getIt` singletons manually without calling `setUpTestGetIt()`. The `ProviderScope` wrappers in every `testWidgets` call are constructed inline without a shared `_pumpWidget(...)` helper; the same pattern repeats ~25 times.
  **RESOLVED (stale):** `entry_details_collapsible_test.dart` no longer exists — it was merged into the mirror `entry_details_widget_test.dart`, whose registration is consolidated in the shared `_registerEntryDetailsMocks()` helper.

- [x] **[MED]** `entry_details_widget_test.dart` — Constructor parameter smoke tests at lines 530–558 (`test('isActiveTimer parameter defaults to false', ...)`, `test('isActiveTimer parameter can be set to true', ...)`, `test('isActiveTimer and isHighlighted are independent', ...)`) test only that `widget.isActiveTimer == isFalse/isTrue`. These add no behavioral value; they merely reflect Dart's constructor default semantics. They should be deleted.
  **RESOLVED (stale):** the `isActiveTimer` constructor smoke tests are already gone.

- [x] **[MED]** `entry_details_widget_test.dart` — Lines 80–121 define `TestEntryDetailsWidget`, a custom fake that duplicates behavior of the real `EntryDetailsWidget`. The tests in the first group ('EntryDetailsWidget Layout Tests', lines 123–230) exercise the fake, not the production widget. These tests are structurally circular and add no coverage of the real widget's layout logic.
  **RESOLVED:** deleted `TestEntryDetailsWidget` and its circular 'EntryDetailsWidget Layout Tests' group (4 tests asserting the fake's own rendering). Real-widget coverage continues in the highlight/timer/collapse groups; remaining 64 tests green.

- [x] **[LOW]** `entry_details_collapsible_test.dart` — Tests 'expanded image entry shows date under image' (line 559) and 'expanded image entry shows date widget' (line 611) are near-identical; both assert `findsAtLeastNWidgets(1)` of `EntryDatetimeWidget` with the same `testImageEntry` and `testLink`. Merge into a single test or parameterize.
  **RESOLVED:** `entry_details_collapsible_test.dart` was merged into the mirror `entry_details_widget_test.dart`, where both tests survived verbatim (the two were byte-for-byte identical apart from the name — same `testImageEntry`/`testLinkStruct`/setup/single assertion). Deleted the redundant 'expanded image entry shows date widget' test; 'expanded image entry shows date under image' is retained as the single canonical assertion.

- [x] **[LOW]** `nested_ai_responses_widget_test.dart` — Does not call `setUpTestGetIt()` but does set up `MockJournalRepository`, `MockUpdateNotifications`, and `MockLoggingService` by directly registering them. See line 87–105 in the test file.
  **RESOLVED:** the `setUp` now calls `await setUpTestGetIt()` (and `tearDown(tearDownTestGetIt)`), replacing the hand-rolled `getIt.allowReassignment = true` + per-service `registerSingleton`/`unregister` boilerplate. The widget's controller only reads `getIt<UpdateNotifications>()` (now the helper's empty update stream — equivalent to the never-fed broadcast `StreamController` the test previously created) plus the `journalRepositoryProvider` override (still a `MockJournalRepository` passed via overrides, since the repo is provider-injected not getIt-injected). The unused `MockUpdateNotifications`/`MockLoggingService`/`StreamController` locals and the `get_it`/`db_notification`/`logging_service` imports were removed; logging is never actually invoked by these tests (the old unstubbed `MockLoggingService` confirms this), so the helper's real `LoggingService` does no I/O.

---

## Generative (Glados) testing opportunities

No genuine Glados candidates identified in this scope. All widgets under test are UI widgets; Glados is explicitly not recommended for widget tests (test/README.md).

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/…/linked_entries_with_timer.dart` (44 lines) — **No test file.** This widget uses `getIt<TimeService>()` and `StreamBuilder` to derive the active timer entry ID and conditionally mark linked entries with `isActiveTimer`. There is no test that verifies: (a) when the stream emits a matching entry ID the `LinkedEntriesWidget` receives `activeTimerEntryId`, (b) when the stream emits a different ID the `activeTimerEntryId` is null, (c) the `.distinct()` operator prevents rebuilds when the same ID arrives twice.

- [x] **[HIGH]** `lib/…/text_viewer_widget.dart` (64 lines) — **No test file.** The scrollable variant (`TextViewerWidget`) used by `HabitSummary` and `TextViewerWidgetNonScrollable` has no unit tests. Key untested branches: `null` entryText, Quill vs markdown vs plainText content priority, `maxHeight` applied to `LimitedBox`.

- [x] **[HIGH]** `lib/…/helpers.dart` (31 lines) — **No test file.** `EntryTextWidget` has configurable `maxLines` and `padding` parameters with no coverage. These are trivial to test but the absence means regressions go unnoticed.

- [x] **[MED]** `lib/…/journal_app_bar.dart` (26 lines) — **No test file.** `JournalSliverAppBar` renders a `SaveButton` and a `BackWidget`; no test verifies the correct `entryId` is forwarded to `SaveButton`.
  **RESOLVED:** created `journal_app_bar_test.dart` — asserts the `entryId` is forwarded to `SaveButton` (via a fake save-button controller) and the bar renders pinned with `BackWidget` leading and no implied leading.

- [x] **[MED]** `entry_details_widget_test.dart` — The `_PulsingBorder.initState` path where `widget.startDelay == Duration.zero` (controller starts immediately, no Timer) is untested. The only tested path fires the delay timer.
  **RESOLVED (adapted):** the zero-delay default was unreachable — the only production call site passes 1000 ms and the widget is private. Removed the dead default (`startDelay` is now required), eliminating the untestable branch instead of testing it.

- [x] **[MED]** `entry_details_widget_test.dart` — The `onToggleCollapse` error-handling path (`catch (e, s) { DomainLogger.error(...) }` at `entry_details_widget.dart:504–511`) is tested in `entry_details_collapsible_test.dart` (line 1010–1119), but that test file should be merged here. After the merge, verify the test correctly registers `MockDomainLogger` via `getIt`.
  **RESOLVED (stale):** the merge already happened — the 'collapse toggle error handling' test lives in `entry_details_widget_test.dart` and registers/unregisters `MockDomainLogger` via `getIt` around the verify.

- [x] **[LOW]** `linked_entries_activity_filter_bar_test.dart` — The 111-line test file covers rendering; no test verifies that tapping a filter chip actually modifies the provider state and the bar re-renders with the chip selected.
  **RESOLVED:** the "tapping a pill toggles its active kind in the controller" test already asserted the provider-state change (read before/after the tap). Strengthened it to also assert the *bar re-render*: it now reads the audio pill's `Semantics(toggled:)` flag (the rendered active state) and checks it flips from `true` to `false` alongside the controller state, so a regression that updated state without re-rendering the pill would fail.

---

## Test execution speed opportunities

- [x] **[HIGH]** `entry_details_widget_test.dart` — **9 `pumpAndSettle` calls** at lines 318, 380, 625, 793, 821, 1246, 2267, 2345, 2346. Of these, lines 625 and 1246 intentionally wait for an animation (`pumpAndSettle` after `pumpWidget`); the others are plain widget-load settle calls where `tester.pump()` would suffice. Replacing unjustified `pumpAndSettle` calls with `tester.pump()` avoids the 10-second timeout risk and reduces CI flakiness surface.

- [x] **[HIGH]** `entry_details_widget_test.dart` — The test at line 1894–1896 pumps `const Duration(milliseconds: 5000)` twice inside a single test to advance through the `_PulsingBorder` animation lifecycle. This is a 10-second virtual-time advance that keeps a live `AnimationController` ticking across the pump sequence. Using `tester.pumpAndSettle()` with an explicit timeout or restructuring the timer assertions to only verify the state _at_ key animation milestones would be cleaner and less fragile. **RESOLVED (assessed, no change):** both 5000 ms pumps are single virtual-time frame advances (instant in fake time, no real waiting); the paint-coverage test already asserts at explicit milestones (opacity 0 during delay, >0.5 at peak, 0.0 at completion, no leaked tickers) and the other uses the advance purely as end-of-test animation drain. Replacing them with pumpAndSettle would reintroduce the 10 s timeout risk.

- [x] **[HIGH]** `entry_details_collapsible_test.dart` — **4 `pumpAndSettle` calls** (lines 521, 911, 968, 1099). Line 521 is justified (drains `Future.delayed` auto-scroll timer). Lines 911 and 968 follow `await tester.tap` + `pumpAndSettle` — since both are settling a NavigationRoute dismiss, they're appropriate. Line 1099 is inside an error-handling test that throws synchronously; `pump()` is sufficient.

- [x] **[MED]** `text_viewer_widget_non_scrollable_test.dart` — **4 `pumpAndSettle` calls** at lines 294, 320, 340, 352 inside the overflow detection group. These are used to ensure a `LayoutBuilder` reports its size; `tester.pump()` after `pumpWidget` should suffice for non-animated layout.
  **RESOLVED (stale):** the file has zero `pumpAndSettle` calls — already converted in an earlier pass.

- [x] **[MED]** `nested_ai_responses_widget_test.dart` — **16 `pumpAndSettle` calls**. Many of these settle after a `StreamController` event is emitted into a `StreamBuilder`. Since the streams are finite (from `StreamController.add(...)` with a known sequence), the tests could use `tester.pump()` after flushing microtasks instead of the full `pumpAndSettle`.
  **RESOLVED:** all 16 replaced with bounded `pump()` + `pump(300ms)` pairs (the expand/collapse SizeTransition needs the animation frame, so a bare pump after microtask flush would not settle the icon rotation assertions); all 16 tests green.

- [x] **[MED]** `entry_details_widget_test.dart` — Each of the 9 `setUp` blocks calls `await getApplicationDocumentsDirectory()` (real I/O on the fake path), creating the directory afresh per group. A single `setUpAll(setFakeDocumentsPath)` already exists per group, but the `await getApplicationDocumentsDirectory()` call in each `setUp` is still a real filesystem call. Using the `setUpTestGetIt()` helper would consolidate this.
  **RESOLVED (adapted):** the registration was already consolidated into `_registerEntryDetailsMocks()`; the remaining per-test filesystem call is now memoized in a suite-level `_cachedDocsDir`, so the directory resolves exactly once. `setUpTestGetIt()` was not adopted — this file's graph (11 services incl. HealthImport/LinkService/TimeService) goes far beyond the helper's stock set.

---

## Summary

- **2 oversized test files**: `entry_details_widget_test.dart` (2349 L), `entry_details_collapsible_test.dart` (1119 L)
- **1 oversized impl file approaching threshold**: `entry_details_widget.dart` (673 L)
- **~30 weak test assertions** across `entry_details_widget_test.dart` (smoke/existence checks) and `text_viewer_widget_non_scrollable_test.dart` (≥15 tests assert only that `QuillEditor` is still present)
- **0 Glados candidates** (all UI widgets)
- **~20 pumpAndSettle speed wins**: entry_details_widget_test (9), entry_details_collapsible_test (4), nested_ai_responses_widget_test (16) — many are replaceable with targeted `pump()` calls
- **4 missing test files**: `linked_entries_with_timer.dart`, `text_viewer_widget.dart`, `helpers.dart`, `journal_app_bar.dart`
- **Biggest opportunity**: `entry_details_widget_test.dart` violates both the "one test file per source file" rule (collapsible tests in a second file) and the "use `setUpTestGetIt()`" rule (9 duplicate 40-line raw-getIt `setUp` blocks); consolidating these would remove ~500 lines of boilerplate and merge the split-file violation simultaneously
