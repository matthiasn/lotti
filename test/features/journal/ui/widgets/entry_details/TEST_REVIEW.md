# Test Quality Review — `journal/ui/widgets/entry_details` (subtree)

**Scope:** `lib/features/journal/ui/widgets/entry_details/**` + `test/features/journal/ui/widgets/entry_details/**` (recursive)

Generated by automated test-quality review (static analysis)

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/…/duration_widget.dart` | 220 | **Two** test files (`duration_widget_test.dart` 370 L + `duration_widget_timer_text_test.dart` 57 L) | Two test files for one source — rule violation |
| `lib/…/entry_datetime_multipage_modal.dart` | 484 | Yes (`entry_datetime_multipage_modal_test.dart` 1057 L) | Test file 2× the impl; 0 `pumpAndSettle` (good) |
| `lib/…/entry_datetime_widget.dart` | 50 | Yes (`entry_datetime_widget_test.dart` 115 L) | OK |
| `lib/…/entry_detail_footer.dart` | 60 | Yes (`entry_detail_footer_test.dart` 222 L) | OK |
| `lib/…/habit_summary.dart` | 89 | **No** | No test file at all |
| `lib/…/header/action_menu_list_item.dart` | 123 | Yes (`header/action_menu_list_item_test.dart` 507 L) | 22 `pumpAndSettle` calls |
| `lib/…/header/entry_detail_header.dart` | 261 | **Two** test files (`entry_detail_header_test.dart` 343 L + `entry_detail_header_collapsible_test.dart` 711 L) | Two test files for one source — rule violation |
| `lib/…/header/extended_header_modal.dart` | 50 | **No** | No test file at all |
| `lib/…/header/initial_modal_page_content.dart` | 175 | Yes (`header/initial_modal_page_content_test.dart` 584 L) | 15 `pumpAndSettle` calls |
| `lib/…/header/modern_action_items.dart` | **882** | Yes (`header/modern_action_items_test.dart` **1774 L**) | Impl oversized (882 L); test oversized (1774 L) |
| `lib/…/header/switch_icon_widget.dart` | 54 | Yes (`header/switch_icon_widget_test.dart` 217 L) | 0 `pumpAndSettle` (good); quality OK |
| `lib/…/health_summary.dart` | 39 | **No** | No test file at all |
| `lib/…/measurement_summary.dart` | 51 | Yes (`measurement_summary_test.dart` 113 L) | OK |
| `lib/…/save_button.dart` | 34 | **No** | No test file at all |
| `lib/…/survey_summary.dart` | 67 | Yes (`survey_summary_test.dart` 83 L) | OK |
| `lib/…/workout_summary.dart` | 51 | Yes (`workout_summary_test.dart` 56 L) | OK |

Extra test files with no direct source counterpart:
- `header/modern_generate_cover_art_item_test.dart` (281 L) — tests `ModernGenerateCoverArtItem` which lives in `modern_action_items.dart`
- `header/modern_labels_item_test.dart` (563 L) — tests `ModernLabelsItem` which lives in `modern_action_items.dart`
- `header/modern_rate_session_item_test.dart` (138 L) — tests `ModernRateSessionItem` which lives in `modern_action_items.dart`

---

## File size / split opportunities

- [ ] **[HIGH]** `lib/…/header/modern_action_items.dart` — **882 lines**, the only impl file in this scope that exceeds the 1000-line threshold is close; it already surpasses the 500-line soft cap. It defines 14 independent `ConsumerWidget` / `StatelessWidget` classes: `ModernToggleStarredItem`, `ModernTogglePrivateItem`, `ModernToggleFlaggedItem`, `ModernToggleMapItem`, `ModernDeleteItem`, `ModernSpeechItem`, `ModernShowInFileManagerItem`, `ModernShareItem`, `ModernUnlinkItem`, `ModernToggleHiddenItem`, `ModernCopyImageItem`, `ModernCopyEntryTextItem`, `ModernLabelsItem`, `ModernSetTaskLanguageItem`, `ModernGenerateCoverArtItem`, `ModernSetCoverArtItem`, `ModernLinkFromItem`, `ModernLinkToItem`, `ModernRateSessionItem`. Natural split seams: (a) read-only toggle actions (starred/private/flagged/map) → `toggle_action_items.dart`, (b) media actions (show-in-file-manager, share, copy-image, copy-text) → `media_action_items.dart`, (c) linking/moderation actions (link-from, link-to, unlink, hide, delete) → `link_action_items.dart`, (d) advanced actions (language, labels, rate-session, cover-art generation/set) → `advanced_action_items.dart`.

- [ ] **[HIGH]** `test/…/header/modern_action_items_test.dart` — **1774 lines**, exceeding the 1000-line hard limit. The file corresponds exactly to the split seams above. Additionally, three separate satellite test files (`modern_generate_cover_art_item_test.dart`, `modern_labels_item_test.dart`, `modern_rate_session_item_test.dart`) test classes that live in the same source file, creating a **multi-file split** that violates the one-test-file-per-source-file rule from a different direction. All satellite tests should either be consolidated into a split-aligned version of `modern_action_items_test.dart` or moved alongside the corresponding impl split.

- [ ] **[HIGH]** `test/…/header/entry_detail_header_collapsible_test.dart` (711 L) + `test/…/entry_detail_header_test.dart` (343 L) — **two test files for `entry_detail_header.dart`** (261 L). Per AGENTS.md, test file paths must mirror source paths with exactly one test file per source. The collapsible behaviors belong in `entry_detail_header_test.dart`; merge the 711-line collapsible file into it (total ~900 L is still reasonable, or further split if needed after merge).

- [ ] **[HIGH]** `test/…/entry_datetime_multipage_modal_test.dart` — **1057 lines** for a 484-line impl. The file is logically structured but has no shared `_setupModal` helper beyond `_openModal`, causing ~30 nearly identical per-group `setUp` patterns. Extracting a file-level `_setUp()` into `setUpAll` / `setUp` would shrink the file substantially.

- [ ] **[MED]** `test/…/duration_widget_test.dart` (370 L) + `test/…/duration_widget_timer_text_test.dart` (57 L) — **two test files for `duration_widget.dart`**. The timer-text file is small and should be merged into the primary test file per the one-per-source rule.

- [ ] **[MED]** `test/…/header/initial_modal_page_content_test.dart` — **584 lines** for a 175-line source. 15 `pumpAndSettle` calls inflate runtime; extracting a `_pumpContent(tester, {Widget child, overrides})` helper would consolidate the 15 separate `_buildWithRoute` + `pumpWidget` + `pumpAndSettle` sequences.

---

## Test quality improvements

- [ ] **[HIGH]** `test/…/header/modern_action_items_test.dart` — The `ModernDeleteItem` and `ModernUnlinkItem` render tests (lines ~371–389, ~754–773) each assert only `expect(find.byType(ActionMenuListItem), findsOneWidget)` — a pure smoke test with no behavioral assertion. Verify that the correct `isDestructive: true` flag reaches `ActionMenuListItem`, or that the correct icon is rendered.

- [ ] **[HIGH]** `test/…/header/modern_action_items_test.dart` — Contains **10 private class definitions** at the bottom of the file (lines 1606–1774): `_FakeSharePlatform`, `_DeletingFakeEntryController`, `_CopyImageFakeEntryController`, `_RemoveLinkTrackingController`, `_FakeNoRatingController`, `_FakeHasRatingController`, `_RecordingProcessRunner`, `_ProcessCall`, `_FakeLinkedEntriesController`, `_TrackingLinkedEntriesController`. Several of these (tracking controllers, fake linked-entries controllers) are duplicated in spirit across test files. Any that aren't tied to share-platform or process-runner specifics belong in `test/helpers/` as shared fakes.

- [x] **[HIGH]** `test/…/header/action_menu_list_item_test.dart` — **22 `pumpAndSettle` calls** in a 507-line file. `ActionMenuListItem` is a `StatelessWidget` with no animations; `tester.pump()` is entirely sufficient after building the widget, and `pumpAndSettle` is only necessary when a modal animation must complete (a handful of tap-interaction tests). Replacing the ~18 render-only `pumpAndSettle` calls with `pump()` reduces CI time for this file.

- [ ] **[MED]** `test/…/entry_datetime_multipage_modal_test.dart` — The `setUp` block (lines 150–178) uses raw `getIt.reset()` and `getIt.registerSingleton()` instead of `setUpTestGetIt()`. The file was written before the centralized helper existed or as an oversight. Migrating to `setUpTestGetIt(additionalSetup: ...)` would remove ~15 lines per group and eliminate the `await getIt.reset()` calls.

- [ ] **[MED]** `test/…/entry_detail_header_collapsible_test.dart` — The 711-line file tests `EntryDetailHeader` internal chevron-rotation state. Many assertions are on animation controller values (e.g. `expect(sizeTransition.sizeFactor.value, 1.0)`) without asserting on the UI change the user would actually observe (chevron icon direction, aria state, etc.). Some of these widget-tree introspection tests could be simplified.

- [ ] **[MED]** `test/…/header/initial_modal_page_content_test.dart` — Uses a custom ad-hoc `_buildWithRoute` helper that builds `ProviderScope` + `MaterialApp` + localization + `Scaffold` by hand (lines 42–73 in `modern_action_items_test.dart` which it shares). This helper duplicates what `makeTestableWidget()` + navigator setup could provide.

- [ ] **[LOW]** `test/…/header/modern_generate_cover_art_item_test.dart` (281 L), `test/…/header/modern_labels_item_test.dart` (563 L), `test/…/header/modern_rate_session_item_test.dart` (138 L) — each of these files tests a class whose canonical home is `modern_action_items.dart`. They are satellite test files for a single source file. They should be merged into the `modern_action_items_test.dart` suite (after the impl split described above) so the one-per-source rule is honoured.

- [ ] **[LOW]** `test/…/entry_detail_footer_test.dart` (222 L) — Quality is acceptable. One minor point: some tests assert only on `find.byType(EntryDetailFooter)` without checking the rendered state of child widgets (e.g. the delete/star/private buttons). Strengthening at least two tests to verify button icon states would improve behavioral coverage.

---

## Generative (Glados) testing opportunities

No genuine Glados candidates identified. All files in scope are UI widgets. The `entry_datetime_multipage_modal.dart` contains some date arithmetic (`durationString` formatting, `isDirty` comparisons) that are pure logic, but they delegate to `entry_tools.dart` which is outside this scope and likely already covered there.

---

## Coverage / missing-behavior gaps

- [ ] **[HIGH]** `lib/…/health_summary.dart` (39 lines) — **No test file.** The widget renders a `DashboardHealthChart` with a chart config derived from `QuantitativeEntry.data.dataType` plus an `InfoText` from `entryTextForQuant`. Untested: (a) `showChart = false` hides `DashboardHealthChart`, (b) correct `healthType` is forwarded from `qe.data.dataType`, (c) `InfoText` displays the formatted text.

- [ ] **[HIGH]** `lib/…/habit_summary.dart` (89 lines) — **No test file.** The widget uses a `notificationDrivenItemStream` that calls `getIt<JournalDb>().getHabitById(data.habitId)`. Untested: (a) when `habitDefinition == null` the widget shows `SizedBox.shrink`, (b) when `showIcon = false` the `CategoryIconCompact` is absent, (c) when `showText = false` the `TextViewerWidget` is absent, (d) the notification stream triggers a rebuild when `habitsNotification` fires.

- [ ] **[HIGH]** `lib/…/save_button.dart` (34 lines) — **No test file.** The widget animates opacity based on `saveButtonControllerProvider`. Untested: (a) when `unsaved = false` opacity is 0, (b) when `unsaved = true` opacity is 1, (c) tapping the button calls `notifier.save()` and blurs focus.

- [ ] **[HIGH]** `lib/…/header/extended_header_modal.dart` (50 lines) — **No test file.** `ExtendedHeaderModal.show` wires together `InitialModalPageContent` and `SpeechModalContent` inside a multi-page modal. Untested: (a) the modal opens with `InitialModalPageContent` on page 0, (b) the `pageIndexNotifier` switching to 1 shows `SpeechModalContent`, (c) `onTapBack` resets the notifier to 0.

- [ ] **[MED]** `entry_datetime_multipage_modal_test.dart` — The error path at `entry_datetime_multipage_modal.dart` (inside the Save handler's `catch` block that logs via `DevLogger`) is exercised by `_ThrowingEntryController` (defined at line 69) but the test that uses it should explicitly assert that the modal remains open (not dismissed) and that `DevLogger` was notified. Currently the test only checks that `tester.takeException()` is null.

- [ ] **[MED]** `entry_detail_header_test.dart` + `entry_detail_header_collapsible_test.dart` — The `entry_detail_header.dart` chevron rotation animation is tested through the raw `AnimationController` value, but the case where `isCollapsed` is toggled multiple times in quick succession (e.g. before the animation completes) is not explicitly covered.

- [ ] **[LOW]** `measurement_summary_test.dart` (113 L) — Tests rendering of `MeasurementSummary` with a nominal measurable type. The case where `measurableType` is null (no matching definition in the database) is not tested.

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/…/header/action_menu_list_item_test.dart` — **22 `pumpAndSettle` calls** in a 507-line file. `ActionMenuListItem` is a static `StatelessWidget` wrapping `ListTile` with no animations. At least 18 of the 22 `pumpAndSettle` calls in render/icon-check tests should be `tester.pump()`. Impact: each unnecessary `pumpAndSettle` can wait up to 10 s if the widget tree has an open animation context; even in normal operation they add latency vs a single `pump()`.

- [x] **[HIGH]** `test/…/header/modern_action_items_test.dart` — **84 `pumpAndSettle` calls**. A majority occur in the render/icon-check assertions for `ModernToggleStarredItem`, `ModernTogglePrivateItem`, `ModernToggleFlaggedItem`, `ModernSpeechItem`, `ModernShowInFileManagerItem`, `ModernShareItem`, `ModernCopyImageItem`, `ModernLinkFromItem`, `ModernLinkToItem`, and `ModernUnlinkItem`. These widgets are `ConsumerWidget` or `StatelessWidget` without animations; after `pumpWidget`, a single `pump()` to flush the Riverpod provider is sufficient. Only the tests that open modals (`_buildWithRoute` + tap) genuinely need `pumpAndSettle` to wait for the navigation animation. Estimate: ~60 of 84 calls are replaceable.

- [x] **[HIGH]** `test/…/header/initial_modal_page_content_test.dart` — **15 `pumpAndSettle` calls**. The modal has an entrance animation, so a settle is needed once per modal open; however, each individual assertion within the same open modal then calls `pumpAndSettle` again unnecessarily. Replacing post-assertion `pumpAndSettle` calls with `pump()` where no animation is pending would save time.

- [ ] **[MED]** `test/…/entry_detail_header_test.dart` + `entry_detail_header_collapsible_test.dart` — Combined **38 `pumpAndSettle` calls**. The collapsible file uses `await tester.pump(AppTheme.chevronRotationDuration)` appropriately in some cases, then still calls `pumpAndSettle()` after the animation's fixed duration has already been elapsed. These can be replaced with `pump()`.

- [ ] **[MED]** `test/…/header/modern_labels_item_test.dart` (563 L, 26 `pumpAndSettle` calls) and `test/…/header/modern_generate_cover_art_item_test.dart` (281 L, 0 `pumpAndSettle` — good). The `modern_labels_item_test.dart` uses `pumpAndSettle` universally across all render tests; since `ModernLabelsItem` is a `ConsumerWidget` with no inherent animations, most of these can become `pump()`.

- [ ] **[LOW]** `test/…/entry_detail_footer_test.dart` (222 L) — `pumpAndSettle` calls count is modest; minor speed wins available but not a priority.

---

## Summary

- **1 oversized impl file**: `modern_action_items.dart` (882 L) — prime split candidate into 4 files by action type
- **2 oversized test files**: `modern_action_items_test.dart` (1774 L), `entry_datetime_multipage_modal_test.dart` (1057 L); also `entry_detail_header_collapsible_test.dart` (711 L)
- **4 rule violations** (multiple test files per source): `modern_action_items.dart` (3 satellite files), `entry_detail_header.dart` (2 files), `duration_widget.dart` (2 files), plus `modern_action_items_test.dart` 1774 L itself that needs splitting
- **~70+ weak pumpAndSettle calls** across `action_menu_list_item_test.dart` (22), `modern_action_items_test.dart` (84), `initial_modal_page_content_test.dart` (15), `entry_detail_header_*` (38)
- **0 Glados candidates** (all UI widgets)
- **4 missing test files**: `health_summary.dart`, `habit_summary.dart`, `save_button.dart`, `extended_header_modal.dart`
- **Biggest opportunity**: `modern_action_items.dart` (882 L) should be split by action type, and its fragmented test coverage (1 big file + 3 satellites) should be reorganised to match — this single refactor eliminates the rule violations, brings the oversized impl under 250 L per file, and makes the 1774-line test file manageable
