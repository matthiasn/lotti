# Test Review: journal/ui/widgets/list_cards/

**Scope:** `lib/features/journal/ui/widgets/list_cards/` and `test/features/journal/ui/widgets/list_cards/`
**Generated by automated test-quality review (static analysis)**

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/.../animated_task_card.dart` | 46 | Yes (`animated_task_card_test.dart`, 441 lines) | Inline mock class definitions; `pumpAndSettle` overuse (17×) |
| `lib/.../card_image_widget.dart` | 75 | Yes (`card_image_widget_test.dart`, 270 lines) | 7× `pumpAndSettle`; no test for `didUpdateWidget` / file-watcher lifecycle |
| `lib/.../card_wrapper_widget.dart` | 98 | Yes (`card_wrapper_widget_test.dart`, 407 lines) | Inline mock class definitions; `_DistanceBadge._colorForDistance` thresholds only partially covered |
| `lib/.../journal_card.dart` | 499 | Yes (`journal_card_test.dart`, 1262 lines) | Inline mock class definitions; `getIt.allowReassignment = true` anti-pattern; 12× `pumpAndSettle` |
| `lib/.../journal_image_card.dart` | 178 | Yes (`journal_image_card_test.dart`, 302 lines) | Inline mock class definitions; 1× `pumpAndSettle` |
| `lib/.../task_card.dart` | 319 | Yes (`task_card_test.dart`, 1331 lines) | **Oversized test** (1331 lines); 27× `pumpAndSettle`; ad-hoc `ProviderScope`/`MaterialApp` wrappers |

Note: `journal_card_timer_text_test.dart` (38 lines) has no corresponding source file — it tests `entry_tools.dart` functions, not a dedicated source file in this directory.

---

## File size / split opportunities

- [x] **[HIGH]** `test/.../list_cards/task_card_test.dart` is **1331 lines** for a 319-line source file. The file covers: (a) basic status/priority rendering, (b) `showCreationDate` group, (c) `showDueDate` group (including date-format, completion-status hiding, toggle interaction), (d) `showCoverArt` group (cover art layout, labels in cover art layout, cropX). Each group is self-contained and large enough to stand alone. **Split seams:** `task_card_creation_date_test.dart`, `task_card_due_date_test.dart`, `task_card_cover_art_test.dart`, and a primary `task_card_test.dart` for status/priority/navigation. However, the one-test-file-per-source rule forbids multiple test files per source file — the right fix is to collapse redundant tests and shrink the single file to under ~700 lines. **RESOLVED (adapted):** extracted a shared `pumpTaskCard(tester, card, {overrides})` helper (DS shell + l10n delegates + first-frame pump) replacing 33 inline `ProviderScope/MaterialApp/Scaffold` wrappers, deleted the `does not show creation date when false` duplicate (it pumped without the flag, duplicating the defaults test), folded the row-layout assertion into the `shows creation date` test, and parameterised the Done/Rejected due-date hiding pair into a loop. File is down to 1 036 lines with no coverage removed; the remaining length is genuine coverage across the four display flags, so it was not forced under 700.

- [x] **[HIGH]** `test/.../list_cards/journal_card_test.dart` is **1262 lines** — approaching critical size. The file has a large `'Comprehensive entity tests'` group (lines ~568–962) that creates test fixtures inline in `setUp` instead of using existing test data factories. The setup block alone is ~140 lines. Using existing `test_data.dart` fixtures and the `Comprehensive entity tests` group could cut ~200 lines. **RESOLVED (adapted):** the six inline fixtures differ in entity type and asserted display strings, so global `test_data.dart` fixtures don't fit; instead the repeated 9-line `Metadata` blocks were collapsed into a shared `meta(id)` builder inside the group's setUp (file now 1 202 lines, all 47 tests pass).

- [ ] **[MED]** `test/.../list_cards/animated_task_card_test.dart` is **441 lines** for a 46-line source file — a 9.6× size ratio. Several tests (`'renders task with different statuses'`, `'handles task with due date'`, `'maintains correct layout structure'`) assert only `findsOneWidget` on type-level finders, providing minimal behavioral value. Removing smoke tests could reduce the file by ~120 lines.

- [ ] **[LOW]** All impl files are within 500 lines. `journal_card.dart` at 499 lines is right at the limit; if `_JournalCardLabelsRow` (lines 464–499) grows, splitting it out would be natural.

---

## Test quality improvements

- [x] **[HIGH]** Four test files (`animated_task_card_test.dart`, `card_wrapper_widget_test.dart`, `journal_image_card_test.dart`, `journal_card_test.dart`) each define their own **inline `MockNavService`**, **inline `MockEntitiesCacheService`**, and **inline `MockTimeService`** classes. This violates the centralized mocks rule. All four should import from `test/mocks/mocks.dart`. Duplicated inline implementations are a maintenance burden (e.g., all four have nearly identical `MockNavService.beamToNamed` that appends to `navigationHistory`).

- [x] **[HIGH]** `journal_card_test.dart:100` and `animated_task_card_test.dart:67`: both use `getIt.allowReassignment = true` — a flag that masks test isolation bugs by silently overwriting registered singletons. The canonical pattern is `setUpTestGetIt()` / `tearDownTestGetIt()`, which resets the container cleanly.

- [x] **[HIGH]** `journal_card_test.dart:426`, `445`, `849`, `867`, `885`: `getIt.registerSingleton<NavService>(mockNavService)` is called **inside individual test bodies** rather than in `setUp`. This means navigation service is registered only for certain tests, while other tests in the same file have no `NavService` — a fragile and non-deterministic pattern. The `allowReassignment = true` flag makes this possible but not safe.

- [ ] **[MED]** `animated_task_card_test.dart` lines ~248–274 (`'renders task with different statuses'`), ~276–296 (`'handles task with due date'`), ~298–318 (`'maintains correct layout structure'`), ~368–400 (`'respects theme changes'`): each of these tests asserts only `findsOneWidget` on `AnimatedModernTaskCard` or `findsNothing` on exceptions — **pure existence/smoke tests** with no behavioral assertions. They should be either deleted or augmented with meaningful assertions (e.g., verifying the status chip text or that a theme-specific color is applied).

- [ ] **[MED]** `task_card_test.dart` has **no shared `_pumpTask(tester, task)` helper**. The pattern `await tester.pumpWidget(ProviderScope(child: MaterialApp(home: Scaffold(body: ModernTaskCard(task: task)))))` + `await tester.pumpAndSettle()` appears **27 times** with only the `task` argument varying. Extracting a helper would cut ~100 lines of boilerplate and comply with the "extract pump/setup helpers" guideline.

- [ ] **[MED]** `task_card_test.dart:283–365`: `'status chip + icon reflect task status variants'` iterates over all `TaskStatus` variants within a single test using a shared `pumpWithStatus` closure — this is a correct parameterized pattern. However, each sub-iteration calls `pumpAndSettle()` but never calls `pump()` to settle state between rebuilds; the test relies on accumulated `pumpWidget` re-renders. This is fine but the multiple `pumpAndSettle()` calls could be replaced with `pump()` to reduce risk.

- [ ] **[LOW]** `card_wrapper_widget_test.dart:381–405`: `'distance badge uses green color for strong matches'` finds the `Container` by traversing the widget tree from the badge key. This is a structural fragility — if `_DistanceBadge` wraps the `Container` in an additional layer, the test breaks. A dedicated `_DistanceBadge` widget test using `makeTestableWidget` would be cleaner.

- [ ] **[LOW]** `journal_image_card_test.dart:129–133`: uses a conditional `if (imageEntry.entryText != null)` inside the test body — if `entryText` is null the test silently passes without any assertion. Test data should guarantee non-null `entryText` or the condition should be removed.

---

## Generative (Glados) testing opportunities

- [ ] **[MED]** `CardWrapperWidget._DistanceBadge._colorForDistance(double d)` is a **pure function** with 4 threshold-based branches (`< 0.3`, `< 0.6`, `< 0.8`, `>= 0.8`). A Glados property could assert: (a) for any `d in [0, 1]`, the returned color is always one of the 4 expected colors; (b) color values are monotonically "warmer" as `d` increases; (c) the function never throws for any finite double. The current test covers only `d = 0.15` (green branch) — three branches are untested.

- [ ] **[LOW]** `ModernJournalCard._formatDate()` branches on `item is JournalEvent` → `dfShort.format` vs `dfShorter.format`. A property test with generated `DateTime` values could assert that both formatters return non-empty strings and that the event formatter uses the longer format (more characters). This is a minor pure-function invariant.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `card_image_widget.dart` — `_CardImageWidgetState.didUpdateWidget` (line 27–31): this lifecycle method calls `resetFileWatcher()` when `journalImage.id` changes. There is **no test** for this path. The test file verifies rendering and watcher setup but never changes the `journalImage` prop mid-test to exercise the reset branch.

- [x] **[HIGH]** `card_image_widget.dart` — `fileExists == false` branch (line 45–47): returns `SizedBox.shrink()` when the image file does not exist on disk. `card_image_widget_test.dart` creates real temp files for all tests, so this branch is never exercised. A test with a non-existent file path should assert that `SizedBox.shrink()` renders.

- [x] **[HIGH]** `_DistanceBadge._colorForDistance` — three of four color branches are untested. Only the `d < 0.3` (green) branch has a test (`card_wrapper_widget_test.dart:381`). The `orange` (`0.3 ≤ d < 0.6`), `deepOrange` (`0.6 ≤ d < 0.8`), and `red` (`d ≥ 0.8`) branches have no coverage.

- [ ] **[MED]** `ModernJournalCard._buildBody` switch — the `RatingEntry` branch (line 285–294 of `journal_card.dart`) renders a localized `'Session Rating'` label. `journal_card_test.dart:811–820` tests this with `testRatingEntry` — coverage exists but only for the label; the import of `testRatingEntry` from `test_data.dart` suggests the entry may have no category icon, so the `_shouldShowCategoryIcon()` filter is not exercised for this type.

- [ ] **[MED]** `ModernTaskCard` — `showCoverArt = true` with a `coverArtId` set, but `cover art layout shows progress on separate row` (task_card_test.dart:1146–1201): the `_buildLabelsContent` branch inside `hasCoverArt = true` path is only tested when labels are present. The empty-labels path inside the cover-art layout (returning `[]` from `_buildLabelsContent`) is not explicitly tested.

- [ ] **[LOW]** `AnimatedModernTaskCard` — the `disableShadow: true` property propagates to `AnimatedModalItem`. No test verifies this property is set; the test at line 165 checks `hoverScale`, `tapScale`, `hoverElevation`, and `margin` but omits `disableShadow`.

- [ ] **[LOW]** `journal_image_card.dart` — `_buildTextContent` returns `SizedBox.shrink()` when `entryText == null || plainText.isEmpty` (line 158–160). `journal_image_card_test.dart` only tests images with non-empty `entryText`. An explicit empty/null entryText test is missing.

---

## Test execution speed opportunities

- [x] **[HIGH]** `task_card_test.dart`: **27 `pumpAndSettle` calls** for a widget that has no animations in the test environment (no real database, no running timers). Every `pumpAndSettle` uses a 10-second default timeout. Replacing with `await tester.pump()` or `await tester.pump(const Duration(milliseconds: 50))` would make each test complete faster and eliminate timeout risks. **Estimated impact: significant in CI shard that contains this file.**

- [x] **[HIGH]** `animated_task_card_test.dart`: **17 `pumpAndSettle` calls** for a widget that primarily wraps `ModernTaskCard` in an `AnimatedModalItem`. Most of these are after static renders. The exceptions — tap-animation tests (lines 219–246) and hover tests (lines 187–217) — legitimately need pump calls, but `pumpAndSettle` is overkill when `pump(const Duration(milliseconds: 300))` would suffice for the animation duration.

- [ ] **[MED]** `journal_card_test.dart`: **12 `pumpAndSettle` calls**. Several are in tests that render fully static widgets (no animation providers, no stream watchers). Replacing with `pump()` in static-render tests would reduce risk.

- [ ] **[MED]** `card_wrapper_widget_test.dart`: 6 `pumpAndSettle` calls plus 2 `pump(const Duration(milliseconds: 100))` calls. The `pump(100ms)` calls for distance-badge tests are acceptable (waiting for a `RepaintBoundary` to settle), but the `pumpAndSettle` calls in static-card routing tests could be `pump()`.

- [ ] **[LOW]** `journal_card_test.dart:95–128`: the `setUp` block registers multiple singletons via `getIt` but also calls `Directory.systemTemp.createTempSync(...)` — a real filesystem call — on every test setup. The temp dir is only needed for image-related tests. Moving it to a sub-group `setUp` would skip the filesystem call for ~90% of tests in the file.

---

## Summary

- **1 oversized test file**: `task_card_test.dart` at 1331 lines (27 `pumpAndSettle` calls, no shared pump helper).
- **1 near-oversized test file**: `journal_card_test.dart` at 1262 lines with large inline setup.
- **4 files with inline mock class definitions** that should use centralized `test/mocks/mocks.dart`.
- **3 untested `_DistanceBadge` color branches** and **2 untested `CardImageWidget` lifecycle paths** (`didUpdateWidget` and `fileExists == false`).
- **1 genuine Glados candidate**: `_DistanceBadge._colorForDistance` (pure threshold function, 4 branches, currently only 1 branch tested).
- **Top opportunity**: Extract a shared `_pumpTask(tester, task)` helper in `task_card_test.dart` to eliminate 27 copy-pasted `pumpWidget`+`pumpAndSettle` blocks, and add `didUpdateWidget` + missing-file tests for `CardImageWidget`.
