# Notifications Feature — Test Quality Review

**Scope:** `lib/features/notifications/**` + `test/features/notifications/**`

Generated by automated test-quality review (static analysis)

---

## File inventory

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/features/notifications/model/notification_inbox_projection.dart` | 67 | Yes (341) | Test is 5× impl; heavy but warranted by Glados use. |
| `lib/features/notifications/model/notification_merge.dart` | 115 | Yes (551) | Excellent Glados coverage; no issues. |
| `lib/features/notifications/repository/notification_repository.dart` | 357 | Yes (890) | Test oversized; inline GetIt boilerplate repeated. |
| `lib/features/notifications/scheduler/notification_scheduler.dart` | 109 | Yes (434) | Good; real `NotificationsDb` (in-memory). Glados covers hash. |
| `lib/features/notifications/state/notification_inbox_controller.dart` | 107 | Yes (304) | OK. |
| `lib/features/notifications/ui/widgets/notification_bell.dart` | 430 | Yes (631) | Multiple `pumpAndSettle` calls; repeated inline GetIt teardown pattern. |

---

## File size / split opportunities

- [ ] **[HIGH]** `test/features/notifications/repository/notification_repository_test.dart` (890 lines for a 357-line impl). The test file is 2.5× the impl. Inspect pattern: the repository likely has many independent method groups each with full mock setup; consider splitting by responsibility group (CRUD vs state transitions vs sequence log) into separate test files if the one-file-per-impl rule would permit it — or at minimum extract a `_TestBench` class with the repeated `setUp` stubs.
- [ ] **[MED]** `lib/features/notifications/ui/widgets/notification_bell.dart` (430 lines). The file contains five distinct widget classes: `NotificationBell`, `_UnseenBadge`, `_InboxPanel`, `_InboxEmptyState`, `_InboxRow`. Each private class is large enough to justify its own file when/if the file grows. No urgent split needed today.
- [ ] **[LOW]** `test/features/notifications/model/notification_merge_test.dart` (551 lines vs 115 impl). The size is justified: two large Glados generators + their scenario classes. No split needed.

---

## Test quality improvements

- [ ] **[HIGH]** `test/features/notifications/ui/widgets/notification_bell_test.dart:29-47` — `setUp`/`tearDown` use manual `getIt.isRegistered<NotificationRepository>()` / `getIt.unregister` / `getIt.registerSingleton` instead of `setUpTestGetIt()` from `test/widget_test_utils.dart`. Lines 315-320, 372-376, 430-434 repeat the same pattern for `NavService` inside three test bodies. AGENTS.md mandates `setUpTestGetIt()` for all widget tests; using it here would eliminate ~40 lines of boilerplate.
- [ ] **[HIGH]** `test/features/notifications/ui/widgets/notification_bell_test.dart` — the three tests that need a `NavService` (lines 308-366, 368-424, 426-481) each manually `getIt.isRegistered` / `unregister` / `registerSingleton` + `addTearDown` for `NavService` inside the test body. Extract a shared `_setUpNavService(tester)` helper (or push it into `setUp`) to avoid the ~15-line block copy appearing three times.
- [ ] **[MED]** `test/features/notifications/ui/widgets/notification_bell_test.dart` — 15 `pumpAndSettle()` calls. For the bell widget tests that merely open the `MenuAnchor` popover (a simple animation), `pumpAndSettle` is likely correct since the `AnimatedOpacity` on `_NavigationArrow` must settle. However, tests that do not involve popover animation (e.g., the icon/badge-state tests at lines 80-138) could use `tester.pump()` instead.
- [ ] **[MED]** `test/features/notifications/repository/notification_repository_test.dart` — if mock setup per test exceeds 20 lines and assertions are 4-6 lines (the classic "mock setup dwarfing assertions" smell), extract shared stubs using a `_TestBench` class as documented in AGENTS.md. Inspect specific ratio once the file is read in full.
- [ ] **[LOW]** `test/features/notifications/model/notification_inbox_projection_test.dart` — the `_preferred` helper (line 327) mirrors `_prefers` in the impl exactly. Consider importing the impl function directly in the test, or adding a comment explaining why the test has its own copy (to avoid coupling the test to private impl logic).

---

## Generative (Glados) testing opportunities

- [ ] **[HIGH]** `lib/features/notifications/scheduler/notification_scheduler.dart` — `notificationIdFor` is covered by a Glados property in the scheduler test (tag `glados`, line 305). Additionally, the `schedule` / `reconcile` state machine has an interesting invariant: if the feature flag is on and a notification is active, `reconcile` must always produce the same OS-level call whether invoked once or twice (idempotency). A Glados property over `(bool flagEnabled, NotificationLifecycleState)` would be a genuine generative candidate for `reconcile` idempotency. **Worth adding.**
- [ ] **[MED]** `lib/features/notifications/model/notification_merge.dart` — `earliestNonNull` is a pure, algebraically rich function (commutative, associative, idempotent when `a == b`). The `mergeFull` Glados test exercises it indirectly at numRuns 180. A dedicated Glados property for `earliestNonNull` commutatively and associatively would be a minimal, high-signal test. Existing coverage via `mergeFull` is probably sufficient, so **LOW priority**.
- [ ] **[LOW]** `lib/features/notifications/model/notification_inbox_projection.dart` — `countUnseenInboxNotifications` + `deduplicateInboxNotifications` are already covered by two Glados properties (badge count + collapse). No new candidates identified.

---

## Coverage / missing-behavior gaps

- [ ] **[HIGH]** `lib/features/notifications/scheduler/notification_scheduler.dart:55` — the `!entity.meta.scheduledFor.isAfter(effectiveNow)` boundary case where `scheduledFor == effectiveNow` (exactly equal) routes to `showNotificationNow`. No existing test covers the exact-equality boundary; all tests use either `subtract(minutes: 1)` (clearly past) or `add(hours: 1)` (clearly future). Add a test for `scheduledFor == now`.
- [ ] **[MED]** `lib/features/notifications/scheduler/notification_scheduler.dart:82-103` — `reconcile` when flag is enabled and there are both due + upcoming + seen rows: the existing test `reconcile schedules every active due and upcoming notification` covers this, but it uses a `seen` notification that stays in the DB (never cancelled). Verify that a `deleted` or `actedOn` notification in the DB during `reconcile` also gets cancelled (not just `seenAt`-stamped ones). The test at line 254 covers the "flag off" path for all rows, but when the flag is on, only `schedule()` is called per row, and `schedule()` cancels `seenAt`/`deletedAt`/`actedOnAt` rows — this path is implicit in `reconcile` via delegation but not tested directly via `reconcile`.
- [ ] **[MED]** `lib/features/notifications/ui/widgets/notification_bell.dart:38-43` — `resolvePopoverWidth` is tested for three boundary cases. A fourth edge: `screenWidth == popoverMinWidth + popoverScreenMargin * 2` (exact lower boundary) is not exercised; the three existing unit tests already cover the logic branches, but the exact lower floor threshold could be a quick addition.
- [ ] **[LOW]** `lib/features/notifications/state/notification_inbox_controller.dart` — the `unseenNotificationCountProvider` and `inboxNotificationsProvider` are tested via the bell widget tests (provider override). Direct unit tests for the state controller's stream/rebuild behaviour when the underlying DB stream emits are absent.

---

## Test execution speed opportunities

- [ ] **[MED]** `test/features/notifications/ui/widgets/notification_bell_test.dart` — 15 `pumpAndSettle()` calls. Most are necessary because `MenuAnchor` uses an animation when opening/closing, but the 3 tests that only check the initial rendered state (before any tap) at lines 80-138 could use `tester.pump()`. **Estimated: −0.5s**.
- [ ] **[MED]** `test/features/notifications/repository/notification_repository_test.dart` (890 lines) — if the per-test mock setup rebuilds full GetIt containers, switching to a shared `setUp` with `setUpTestGetIt()` would cut ~10% of test overhead across 22 tests.
- [ ] **[LOW]** `test/features/notifications/model/notification_merge_test.dart` — numRuns 160/180 for two Glados tests. These are well-justified for the domain (distributed merge semantics) and run in the separate `glados` CI lane, so no speed concern for the standard suite.

---

## Summary

- 1 oversized test file: `notification_repository_test.dart` (890 lines, 2.5× impl), likely due to mock-setup duplication per test.
- 0 oversized impl files.
- 3 weak-test issues: smoke `renders drag handle`-equivalent not present here, but repeated inline GetIt boilerplate in 3 test bodies in `notification_bell_test.dart` violates AGENTS.md and adds maintenance risk.
- 1 genuine Glados candidate: `reconcile` idempotency property in `notification_scheduler_test.dart`.
- 2 meaningful coverage gaps: exact `scheduledFor == now` boundary in scheduler, and `reconcile` behaviour for `deletedAt`/`actedOnAt` rows when flag is enabled.
- Biggest opportunity: extract the repeated `NavService` GetIt boilerplate in `notification_bell_test.dart` into a setUp helper — 3 copies × ~15 lines = ~45 lines eliminated, and converts the setup to the mandated `setUpTestGetIt()` pattern.
