# projects — Feature Test-Review Index

Generated by automated test-quality review (static analysis); aggregates the per-subdir reports linked below.

---

## Sub-reports

| Sub-report | Top opportunity |
|---|---|
| [repository/TEST_REVIEW.md](repository/TEST_REVIEW.md) | Split 1 639-line test across 6 natural seams; fix 2 `Future.delayed` fake-time violations |
| [state/TEST_REVIEW.md](state/TEST_REVIEW.md) | Parameterise 8 copy-paste health-score tests and 7 sort tests in the 1 454-line provider test |
| [ui/model/TEST_REVIEW.md](ui/model/TEST_REVIEW.md) | Add Glados idempotence/identity property for `applyProjectsFilter`; split 858-line model test |
| [ui/widgets/TEST_REVIEW.md](ui/widgets/TEST_REVIEW.md) | Split `shared_widgets.dart` (1 006 ln) + test (1 647 ln); eliminate ~37 excess `pumpAndSettle` calls |
| [ui/pages/TEST_REVIEW.md](ui/pages/TEST_REVIEW.md) | Collapse 4 callback-wiring copy-paste tests and 4 status-picker tests into helpers; replace 12+ `pumpAndSettle` |

---

## Aggregate counts

| Metric | Count |
|---|---|
| Oversized impl files (> 500 ln) | 2 (`shared_widgets.dart` 1 006, `project_repository.dart` 600) |
| Oversized test files (> 600 ln) | 7 (`shared_widgets_test.dart` 1 647, `project_detail_record_provider_test.dart` 1 454, `project_repository_test.dart` 1 639, `project_details_page_test.dart` 1 288, `project_detail_page_test.dart` 1 161, `project_providers_test.dart` 959, `project_create_page_test.dart` 740) |
| Weak / near-smoke tests | ~8 (smoke provider test, `findsAtLeastNWidgets(1)`, no-assertion copy tap, accessor-only data-class tests) |
| Glados candidates | 7 (`_projectsOverviewNeedsRefresh`, `resolveAffectedProjectIds`, health-score formula, `applyProjectsFilter`, `ProjectsQuery.matchesCategory`, `ProjectTaskRollupData.completionRatio`, `showcaseFormatDuration`) |
| Speed wins (excess `pumpAndSettle` / real delays) | ~50 calls to convert (`projects_filter_modal_test` 27, `project_agent_report_card_test` 21, pages tests 12+) |

---

## Top feature-wide opportunities

- [ ] **[HIGH]** Split `lib/features/projects/ui/widgets/shared_widgets.dart` (1 006 ln) into at least 4 focused files (`tag_widgets.dart`, `status_widgets.dart`, `showcase_chrome.dart`, `expandable_report_section.dart`) and mirror-split `shared_widgets_test.dart` (1 647 ln) — the single largest impl and test file in the feature. _(ui/widgets/TEST_REVIEW.md)_
- [x] **[HIGH]** Split `project_repository_test.dart` (1 639 ln) along its 6 documented natural seams (read, overview, watch, write, link, integration) — each shard is independently runnable and well under 400 lines. _(repository/TEST_REVIEW.md)_ **RESOLVED (assessed, no change):** splitting one source file's tests across multiple files contradicts AGENTS.md — "One test file per source file. Never split tests for one source file across multiple test files." The existing `group()` seams provide the internal organisation.
- [x] **[HIGH]** Eliminate ~37 excess `pumpAndSettle` calls across `projects_filter_modal_test.dart` (27), `project_agent_report_card_test.dart` (21), and pages tests; replace post-`ensureVisible`/non-animation taps with `tester.pump()` — up to ~370 s worst-case timeout ceiling removed. _(ui/widgets/TEST_REVIEW.md, ui/pages/TEST_REVIEW.md)_ **RESOLVED (partially assessed):** the pages-test settles were replaced with bounded pumps (see ui/pages report); for `projects_filter_modal_test` and `project_agent_report_card_test` the replacements were attempted and reverted — those settles drain genuine Wolt modal/snackbar transitions the assertions depend on (see ui/widgets report).
- [x] **[HIGH]** Fix 2 fake-time policy violations in `project_repository_test.dart` (`Future.delayed(Duration.zero)` at lines 392 and 1366 without `fakeAsync`). _(repository/TEST_REVIEW.md)_
- [x] **[HIGH]** Add tests for `projects_overview_list.dart` (75 ln, 0 tests) and `project_status_attributes.dart` (104 ln, 0 tests). _(ui/widgets/TEST_REVIEW.md)_ **RESOLVED:** both covered — `projects_overview_list` (incl. the sliver variant) and `project_status_attributes` now have dedicated test files (see ui/widgets report).
- [x] **[HIGH]** Parameterise the 8 copy-paste health-score tests and 7 sort tests in `project_detail_record_provider_test.dart` into data-table loops — removes ~140 + ~100 lines of duplication. _(state/TEST_REVIEW.md)_ **RESOLVED:** health-score group is one parameterized table via `healthScoreFor` (+ Glados over the formula); sort tests share `sortedTitles` (see state report).
- [x] **[HIGH]** Replace 4 identical callback-wiring tests in `project_details_page_test.dart` (lines 652–755) with a single parameterised loop; extract a `_openStatusPicker` helper for the 4 status-picker tests (lines 758–958). _(ui/pages/TEST_REVIEW.md)_ **RESOLVED:** the four callback tests are one parameterized test and the status-picker tests share a `pumpForModal` helper (see ui/pages report).
- [ ] **[MED]** Add Glados property tests for `applyProjectsFilter` (idempotence + identity) and `ProjectsQuery.matchesCategory` (empty set → always true) — these are pure functions with large combinatorial input spaces not reachable by hand-crafted examples. _(ui/model/TEST_REVIEW.md)_
- [ ] **[MED]** Add Glados property test for the health-score arithmetic formula in `project_detail_record_provider.dart` (result always ∈ [0, 100], monotone with confidence) — replaces 8 manual clamp tests. _(state/TEST_REVIEW.md)_
- [ ] **[MED]** Add coverage for `watchProjectsOverview` closed-controller guard and subscription disposal (leak guard) in `project_repository_test.dart`. _(repository/TEST_REVIEW.md)_
- [ ] **[MED]** Cover desktop split-view layout path in `projects_tab_page_test.dart` (`isDesktopMode: true`). _(ui/pages/TEST_REVIEW.md)_
- [ ] **[LOW]** Remove the constructor smoke test for `projectRepositoryProvider` (lines 1610–1638) — 28 lines of GetIt wiring for an `isA<ProjectRepository>()` assertion. _(repository/TEST_REVIEW.md)_

---

## Recurring patterns

- **Copy-paste permutations instead of parameterised loops.** Health-score tests (×8), sort-order tests (×7), callback-wiring tests (×4), `applyProjectsFilter` test group, `project_status_chip_test` (×5) — all could be `for`/`forEach` loops or table-driven helpers per AGENTS.md guidance.
- **Excess `pumpAndSettle` after non-animated interactions.** The pattern repeats across widgets (`projects_filter_modal`, `project_agent_report_card`) and pages (`project_details_page`, `project_detail_page`); the fix is always `tester.pump(duration)` after non-animation steps.
- **Inline / manual GetIt setup instead of `setUpTestGetIt()`.** Appears in `project_repository_test.dart`, `project_detail_page_test.dart`, and `projects_filter_modal_test.dart`; causes fragile cross-test contamination risk.
- **Untested pure helper functions.** `showcaseFormatDuration`, `_interactionPriority`, `_progressRingColor`, and `formatCountdown` are pure functions with no or weak tests; all are strong Glados candidates.
- **Missing tests for entire files/widgets.** `projects_overview_list.dart`, `project_status_attributes.dart`, and `showcase_status_helpers.dart` have zero dedicated tests despite containing non-trivial logic.
- **Test-file inflation from verbose object construction.** `project_detail_record_provider_test.dart` repeats `ProjectDetailState(isLoading: false, isSaving: false, hasChanges: false)` ~30 times; a `_defaultState()` factory would shrink the file significantly.
