# Test Review: projects/ui/pages

**Scope:**
- `lib/features/projects/ui/pages/project_create_page.dart` (323 lines)
- `lib/features/projects/ui/pages/project_detail_page.dart` (344 lines)
- `lib/features/projects/ui/pages/project_details_page.dart` (242 lines)
- `lib/features/projects/ui/pages/projects_tab_page.dart` (286 lines)

Tests:
- `test/features/projects/ui/pages/project_create_page_test.dart` (740 lines)
- `test/features/projects/ui/pages/project_detail_page_test.dart` (1161 lines)
- `test/features/projects/ui/pages/project_details_page_test.dart` (1288 lines)
- `test/features/projects/ui/pages/projects_tab_page_test.dart` (772 lines)

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/.../project_create_page.dart` | 323 | Yes (740) | Test ~2.3× impl; moderate |
| `lib/.../project_detail_page.dart` | 344 | Yes (1161) | Test **3.4× impl** — oversized; many per-field single-assertion tests |
| `lib/.../project_details_page.dart` | 242 | Yes (1288) | Test **5.3× impl** — oversized; repeated callback-wiring pattern |
| `lib/.../projects_tab_page.dart` | 286 | Yes (772) | Test ~2.7× impl; moderate |

---

## File size / split opportunities

- [x] **[HIGH]** `project_details_page_test.dart:1` — 1288 lines for a 242-line impl (ratio ~5.3×). The inflation comes from five identically structured callback-wiring tests (lines 652–755) and four nearly identical status-picker tests (lines 758–958), each of which independently pumps the full widget tree. Extract: **RESOLVED:** the four callback tests are one parameterized test; the four status-picker tests share a `pumpForModal` helper (file 1288 → 1169 lines).
  - A shared `_assertCallbackWired(tester, getter, reason)` loop for the four callbacks (`onCategoryTap`, `onTargetDateTap`, `onStatusTap`, `onTaskTap`).
  - A `_openStatusSheet(tester)` helper for the four status-picker tests that share `physicalSize` setup + `pumpPageWithData` + `content.onStatusTap!()` + `pumpAndSettle`.

- [x] **[HIGH]** `project_detail_page_test.dart:1` — 1161 lines for a 344-line impl. Each of the eight "loaded state" sub-tests independently pumps a full controller + provider tree. A shared `pumpPage(tester, {...})` helper already exists but each test still repeats the full `ProjectDetailState(project: testProject, linkedTasks: const [], isLoading: false, isSaving: false, hasChanges: false)` verbatim. A factory `_defaultState({bool isLoading = false, bool isSaving = false, bool hasChanges = false})` would eliminate 40+ lines. **RESOLVED:** `defaultState({isLoading, isSaving, hasChanges})` factory added; 23 verbatim constructions collapsed.

- [x] **[MED]** `project_detail_page_test.dart` — Splitting the file into `project_detail_page_loading_test.dart`, `project_detail_page_save_test.dart`, `project_detail_page_navigation_test.dart`, and `project_detail_page_fields_test.dart` would keep each under ~350 lines. The groups are already well-named; mirroring them to files is mechanical.
  **RESOLVED (assessed, no change):** splitting one source file's tests across files violates the one-test-file-per-source rule; the well-named groups already provide the navigation.

---

## Test quality improvements

- [x] **[HIGH]** `project_details_page_test.dart:652–755` — Four consecutive `testWidgets` tests that differ only in the callback field name: **RESOLVED:** single test asserting all four callbacks from one pump.
  - `onCategoryTap` (line 653), `onTargetDateTap` (line 679), `onStatusTap` (line 705), `onTaskTap` (line 731).
  Each has the same body: `pumpPageWithData(...) → find.byType(ProjectMobileDetailContent) → expect(content.onXxx, isNotNull)`. These 104 lines can be replaced with a single parameterised loop (4 iterations × ~5 lines of setup = ~25 lines).

- [x] **[HIGH]** `project_details_page_test.dart:758–958` — Four `pumpAndSettle` status-picker tests with repeated `tester.view..physicalSize = const Size(430, 1200)` setup. Extract a `_openStatusPicker(tester)` helper that handles size setup, `pumpPageWithData`, and `content.onStatusTap!()`. Reduces ~80 lines of boilerplate to ~15. **RESOLVED:** `pumpForModal(tester)` handles surface sizing + default-state pump + content lookup for all modal tests.

- [x] **[MED]** `project_detail_page_test.dart:151–166` (loading-state test) — The sole assertion is `expect(find.byType(CircularProgressIndicator), findsOneWidget)`. This finds the spinner but does not verify that the project content is absent. Add `expect(find.byType(ProjectDetailPage).evaluate().first.widget, isA<ProjectDetailPage>())` or check that the title field is not rendered.
  **RESOLVED (stale):** the loading tests already pair the spinner assertion with `expect(find.byType(ProjectMobileDetailContent), findsNothing)` — content absence is asserted.

- [x] **[MED]** `project_detail_page_test.dart:679–757` (save navigation tests) — Each test registers a `MockNavService` directly with `getIt.registerSingleton<NavService>(...)` without using `setUpTestGetIt(additionalSetup: ...)`. The manual `getIt.registerSingleton` call is outside the centralized helper contract and could leave stale singletons if `tearDownTestGetIt` does not cover it. Verify that `tearDownTestGetIt` resets the `NavService` slot, or register it through `additionalSetup`.
  **RESOLVED (verified, no change):** the file's setUp runs `setUpTestGetIt()` and tearDown runs `tearDownTestGetIt()`, which performs a full `getIt.reset()` — the per-test NavService registration is wiped every time, exactly the 'register them after calling it' pattern the helper documents.

- [x] **[MED]** `projects_tab_page_test.dart:156–179` — The `pumpPage` helper pumps `const ProjectsTabPage()` without scrolling the group list; most assertions are on statically visible text. The test `renders grouped projects with an enabled search bar` checks 11 `expect` items, which is good, but two of them (`find.text('Projects')`, `find.text('2 projects')`) are locale-dependent strings — if the app's l10n is ever renamed these would break silently rather than compile-fail. Consider using widget-type finders or semantic labels for structural elements.
  **RESOLVED:** both assertions now resolve through `context.messages.navTabTitleProjects` / `.projectCountSummary(2)` — an l10n rename becomes a compile failure instead of a silent break (4 sites).

- [ ] **[LOW]** `project_create_page_test.dart:689–740` — Several tests that verify `pumpAndSettle` → `expect(find.text('Agent Provisioning'), findsOneWidget)` rely on the specific string from l10n. This is acceptable but fragile; pair with a widget-type assertion to reduce brittleness.

- [ ] **[LOW]** `project_details_page_test.dart:234–328` (background reload test) — Builds a `ProviderContainer` with `UncontrolledProviderScope` and a custom stream, validating scroll position is preserved. This is the best test in the file: it verifies a real UX invariant (stale-while-revalidate). No issue here; noted as a positive example to emulate elsewhere.

---

## Generative (Glados) testing opportunities

These are UI/widget tests — Glados is a poor fit per `test/README.md`. No Glados candidates in this subdir.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `project_detail_page.dart` — No test covers the `updateCategoryId` callback wiring in `ProjectDetailPage` (the page-level `_handleCategorySelected` or equivalent). The category-related UI path is tested only in `project_details_page_test.dart` (the newer page). If `ProjectDetailPage` has its own category field wiring, it is untested. **RESOLVED (assessed, no change):** `ProjectDetailPage` has no category-edit wiring — its only category use is the back-navigation `beamToNamed('/settings/categories/<id>')`, which is covered by the existing back-button test.

- [x] **[MED]** `project_details_page_test.dart` — No test verifies what happens when `onTaskTap` is invoked with a `TaskSummary` — only that the callback is non-null is verified. The `onTaskTap navigation` group (lines 1122–1159) tests only one task; there is no test for an empty task list where `onTaskTap` would never be wired at all.
  **RESOLVED (adapted):** the navigation test already invokes the callback and asserts the beamed `/tasks/<id>` route. The item's empty-list premise is wrong — the page wires `onTaskTap` unconditionally; a new test pins that contract (non-null with zero summaries, and a late task still navigates).

- [x] **[MED]** `projects_tab_page_test.dart` — No test covers the desktop split-view layout path (when `NavService.isDesktopMode` returns `true`). The `setUp` always stubs `isDesktopMode` to `false`. The `ResizableDivider` and `DesktopDetailEmptyState` widgets imported at the top of the file imply a desktop code path exists.
  **RESOLVED (adapted):** the desktop gate is the viewport width (`isDesktopLayout`), not `NavService.isDesktopMode`. New group pumps at 1400 px: list pane + `ResizableDivider` + `DesktopDetailEmptyState` render, and setting `desktopSelectedProjectId` swaps the empty state for an embedded `ProjectDetailsPage` (stubbed detail graph).

- [x] **[MED]** `project_create_page_test.dart` — No test covers the scenario where `mockProjectRepo.createProject` returns `null` (creation failure). The page presumably shows an error or prevents navigation; this path is untested.
  **RESOLVED:** new test stubs the null return — the localized error toast shows, the page stays open (no pop), and no project agent is provisioned.

- [ ] **[LOW]** `project_detail_page_test.dart` — No test covers what happens when `projectAgentProvider` returns an error (as opposed to `null`). The `ProjectAgentReportCard` only renders when data is present; an error case should silently hide the card rather than throw.

---

## Test execution speed opportunities

- [x] **[HIGH]** `project_details_page_test.dart:791,831,836,874,889,925` — Six `pumpAndSettle()` calls inside the status-picker tests. Each `pumpAndSettle` has a 10-second default timeout and runs until all animations complete. For bottom-sheet open/close animations, `tester.pump(const Duration(milliseconds: 350))` is sufficient and deterministic. Estimated saving: 6 slow paths → fast frame-based pumps. **Impact: potentially seconds per test run on slow CI.** **RESOLVED:** sheet open/close settles replaced with bounded pumps (pump + 350ms).

- [x] **[HIGH]** `project_detail_page_test.dart:243,268,271,701,751,757` — Six `pumpAndSettle()` calls, all in status-picker and navigation flows. Same fix: replace with `tester.pump(const Duration(milliseconds: 300))` or two `tester.pump()` calls to let the frame settle. **RESOLVED:** the picker/navigation settles are bounded pumps now; the one route push/pop test keeps `pumpAndSettle` (genuine chained route transitions).

- [x] **[MED]** `project_create_page_test.dart:89` — `pumpPage` ends with `await tester.pump(const Duration(milliseconds: 300))`. This is a real 300 ms wall-clock delay injected at setup time for every test. If the delay is needed only for an animation, prefer `tester.pumpAndSettle()` (which exits when frames are stable, often in < 50 ms), or identify which animation triggers it and use a targeted `pump(duration)`.
  **RESOLVED (assessed, no change):** the premise is wrong — `tester.pump(Duration)` advances FAKE time in widget tests; there is no wall-clock cost. The bounded pump is the codebase's preferred alternative to `pumpAndSettle`, not the problem.

- [x] **[MED]** `project_details_page_test.dart:997,1105,1233,1241` — Four more `pumpAndSettle()` calls in category and date picker flows. Replacing with `tester.pump(const Duration(milliseconds: 300))` for dialog/bottom-sheet transitions removes the 10-second timeout risk.
  **RESOLVED:** 4 of 5 settles converted to bounded `pump()` + `pump(300ms)` pairs; the back-navigation pop keeps its settle (genuine route transition, fails bounded).

- [ ] **[LOW]** `project_details_page_test.dart:763–769` (physicalSize setup in each of four status tests) — `tester.view.physicalSize` and `tester.view.devicePixelRatio` are reset via `addTearDown` inside each test. Extracting this setup into a shared `setUp`/`tearDown` for the `status picker modal` group would run the setup once per group entry rather than once per test.

---

## Summary

- Three oversized test files: `project_details_page_test.dart` (1288 lines, 5.3× impl), `project_detail_page_test.dart` (1161 lines, 3.4× impl), `projects_tab_page_test.dart` (772 lines, 2.7× impl).
- **12+ `pumpAndSettle` calls** across pages tests (status picker, navigation, date picker) that should be replaced with `tester.pump(duration)` — each carries a 10-second timeout risk and slows CI.
- Repeated callback-wiring pattern in `project_details_page_test.dart` (4 identical tests) and status-picker setup (4 tests) are prime parameterisation targets.
- No Glados candidates (UI layer).
- Coverage gaps: desktop split-view layout, `createProject` failure path, `updateCategoryId` in `ProjectDetailPage`, agent error state.
