# Test Review — `lib/features/speech/ui/widgets/speech_modal/` + `test/features/speech/ui/widgets/speech_modal/`

**Scope:** Four widgets: `LanguageDropdown` (58 lines), `SpeechModal` (22 lines), `TranscriptsList` (39 lines), `TranscriptsListItem` (148 lines). Plus the root-level `SpeechModal` widget (22 lines) tested via `speech_modal_test.dart` (55 lines).  
**Generated by automated test-quality review (static analysis).**

---

## File size / shape

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/…/speech_modal/language_dropdown.dart` | 58 | yes | Fine |
| `lib/…/speech_modal/speech_modal.dart` | 22 | yes (55 lines) | Test is mostly smoke — see quality notes |
| `lib/…/speech_modal/transcripts_list.dart` | 39 | yes | Fine |
| `lib/…/speech_modal/transcripts_list_item.dart` | 148 | yes | Acceptable |
| `test/…/speech_modal/language_dropdown_test.dart` | 202 | — | Acceptable |
| `test/…/speech_modal/speech_modal_test.dart` | 55 | — | Nearly all smoke; see quality notes |
| `test/…/speech_modal/transcripts_list_test.dart` | 183 | — | Fine |
| `test/…/speech_modal/transcripts_list_item_test.dart` | 606 | — | Large; many `pumpAndSettle()` calls |

## File size / split opportunities

- [ ] **[LOW]** `test/…/speech_modal/transcripts_list_item_test.dart` at 606 lines is the largest in this subdir. It covers many interaction and state scenarios for a 148-line widget, which is a reasonable ratio (4×). No structural split needed, but the per-test `getIt.reset()` + manual `registerSingleton` calls add overhead — use `setUpTestGetIt()`.

## Test quality improvements

- [ ] **[HIGH]** `test/…/speech_modal/speech_modal_test.dart:45–54` — Both tests are smoke: `expect(() => testAudioEntry, returnsNormally)` and `expect(testAudioEntry.meta, isNotNull)`. These verify that the *test fixture* loads, not that `SpeechModal` itself renders correctly or behaves as expected. A widget test pumping `SpeechModal` with a mock `JournalAudio` and asserting that the audio player widget or transcript list is present would be far more valuable. Consider replacing or augmenting with a real widget test.

- [ ] **[HIGH]** `test/…/speech_modal/transcripts_list_item_test.dart:52–60` — `setUp` uses `await getIt.reset()` followed by direct `getIt.registerSingleton` calls. Per AGENTS.md, use `setUpTestGetIt()` from `test/widget_test_utils.dart` instead of inline GetIt manipulation. This applies to all tests in this file (600 lines of setUp boilerplate risk contamination if tests run in parallel in CI).

- [ ] **[MED]** `test/…/speech_modal/transcripts_list_item_test.dart` — Approximately 28 `pumpAndSettle()` calls across 25+ tests. For a widget that handles text editing and icon buttons (no heavy animation), most of these can be replaced with `tester.pump()`. The `pumpAndSettle()` calls are a timeout risk if any async provider stalls.

- [ ] **[MED]** `test/…/speech_modal/language_dropdown_test.dart:21–48` — `_FakeEntryController` is defined locally. The same fake pattern appears in `transcripts_list_test.dart` (also local `_FakeEntryController`). These are identical implementations in two files; consolidate into `test/features/speech/ui/widgets/speech_modal/test_utils.dart` or a shared fake in a speech-feature-level test helper.

- [ ] **[LOW]** `test/…/speech_modal/transcripts_list_test.dart:21–42` — Another local `_FakeEntryController`. Third definition of the same class. See above.

## Generative (Glados) testing opportunities

- No pure logic functions in this subdir; all code is UI rendering and event handling. Not applicable.

## Coverage / missing-behavior gaps

- [ ] **[HIGH]** `lib/…/speech_modal/speech_modal.dart` (22 lines) — The `speech_modal_test.dart` only tests the fixture; no test exercises the `SpeechModal` widget itself. At minimum, add a `testWidgets` test that pumps `SpeechModal` with a valid `JournalAudio` and verifies the player/transcript list is visible.

- [ ] **[MED]** `test/…/speech_modal/language_dropdown_test.dart` — Verify that selecting a language from the dropdown calls `entryController.setLanguage(language)`. The `_FakeEntryController` already tracks `setLanguageCalls` — confirm at least one test asserts `setLanguageCalls` is populated after a dropdown selection.

- [ ] **[LOW]** `test/…/speech_modal/transcripts_list_test.dart` — Verify the case where `audio.transcripts` is empty — the list should render an appropriate empty state rather than crash.

## Test execution speed opportunities

- [ ] **[HIGH]** `test/…/speech_modal/transcripts_list_item_test.dart` — 28 `pumpAndSettle()` calls across ~25 tests. Each has a 10-second default timeout. In the worst case (provider stall), this is 280 seconds of CI time. Replace with `tester.pump()` + `tester.pump()` pairs, adding `tester.pump(const Duration(milliseconds: 100))` only where animations are genuinely needed.

- [ ] **[MED]** Same file: `await getIt.reset()` in every `setUp`. This is the correct approach but heavier than `setUpTestGetIt()` which does targeted registration. Migrating to `setUpTestGetIt()` would reduce setup time slightly per test.

---

## Summary

- `speech_modal_test.dart` tests only the test fixture, not the widget — a complete behavioral gap for a 22-line impl file.
- Three local `_FakeEntryController` definitions across three test files should be consolidated into a shared helper.
- 28 `pumpAndSettle()` calls in `transcripts_list_item_test.dart` are the main speed risk in this subdir.
- `setUpTestGetIt()` should replace inline GetIt manipulation throughout the subdir.
- No Glados candidates; no fake-time policy violations beyond `pumpAndSettle` overuse.
