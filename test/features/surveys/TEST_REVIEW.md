# Surveys Feature — Test Quality Review

**Scope:** `lib/features/surveys/**` + `test/features/surveys/**`
Generated by automated test-quality review (static analysis)

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/features/surveys/tools/calculate.dart` | 59 | Yes (417 lines) | None — excellent Glados coverage |
| `lib/features/surveys/tools/run_surveys.dart` | 87 | Yes (113 lines) | `pumpAndSettle` in shared helper |
| `lib/features/surveys/ui/fill_survey_page.dart` | 43 | Yes (149 lines) | Weak `isNotNull` assertion |
| `lib/features/surveys/definitions/cfq11_survey.dart` | 115 | Yes (52 lines) | None significant |
| `lib/features/surveys/definitions/ghq12_survey.dart` | 223 | Yes (45 lines) | None significant |
| `lib/features/surveys/definitions/panas_survey.dart` | 171 | Yes (58 lines) | None significant |

---

## File size / split opportunities

- [x] **[LOW]** All impl files are well within size limits (max 223 lines). No splits needed.
  **RESOLVED:** Confirmed via the file inventory — largest impl file is `ghq12_survey.dart` at 223 lines, all others smaller. No split needed; no code change.
- [x] **[LOW]** `test/features/surveys/tools/calculate_test.dart` at 417 lines is the largest test file; the `_ScoreScenario` scaffolding is substantial but purposeful. Consider extracting the generated-value helpers (`_AnswerSpec`, `_ScoreScenario`, `_AnyScoreScenario`) into a private `calculate_test_utils.dart` only if future tests in this directory need them; for now it is acceptable.
  **RESOLVED:** Verified no other test in `test/features/surveys/` references `_AnswerSpec`/`_ScoreScenario`/`_AnyScoreScenario`; the scaffolding stays local and purposeful. No extraction warranted; no code change.

---

## Test quality improvements

- [x] **[LOW]** `test/features/surveys/ui/fill_survey_page_test.dart:69` — `expect(rpuiTask.onCancel, isNotNull)` is a bare existence check. Replace with a meaningful assertion: call `rpuiTask.onCancel!(null)` and verify the log emits `'No result'` (the `null` branch is already tested later; the inline `isNotNull` adds nothing).
  **RESOLVED:** Replaced the bare `isNotNull` with a behavioral check in the same test: pumped a widget with a distinct submit callback, invoked `onCancel!(null)`, and asserted that the submit callback never fired and that `[SurveyWidget] No result` was logged — proving `onCancel` is wired to the widget's local cancel closure rather than the submit path.
- [x] **[LOW]** `test/features/surveys/tools/run_surveys_test.dart:45` — `pumpRunner` calls `tester.pumpAndSettle()` immediately after `tester.tap()`. Because the modal is shown via a `Future<void>` and the research_package widget tree may have open animations, this is probably correct here. However, if the modal uses an animation that never settles, this will hang for the default 10 s. Prefer `tester.pump(const Duration(milliseconds: 300))` or use `pumpAndSettle` with a short explicit duration cap to make the intent explicit.
  **RESOLVED:** Bounded the call to `tester.pumpAndSettle(const Duration(milliseconds: 500))`. The `ModalUtils.showSinglePageModal` Wolt-sheet entrance animation settles well within 500ms, so the worst-case wait is now 0.5s instead of the default 10s timeout.
- [x] **[MED]** `test/features/surveys/tools/run_surveys_test.dart` — the three `runCfq11` / `runGhq12` / `runPanas` cases are already parameterized in a loop (good). However, the test only asserts that the correct `task.identifier` appears in the `SurveyWidget`. It does **not** verify that the `resultCallback` closure passed to `SurveyWidget.task` will call `persistenceLogic.createSurveyEntry` when invoked. A stub test driving `surveyWidget.onSubmit!(someResult)` and verifying the mock capture would close this behavioral gap without adding a new file.
  **RESOLVED:** Stubbed `MockPersistenceLogic.createSurveyEntry` and registered fallbacks in `setUp`/`setUpAll`. Added a parameterized test per runner that drives the wired `surveyWidget.resultCallback(taskResult)` with deterministic per-group answers and verifies the captured `SurveyData` carries the runner's exact `scoreDefinitions`, the correctly summed `calculatedScores`, and the same `taskResult` instance. Also asserted `verifyNever` on open (the modal must not persist before submission).

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** `calculate.dart` already has an excellent Glados property (`calculate_test.dart:306`). No further Glados candidates exist: `run_surveys.dart` and `fill_survey_page.dart` are pure UI/IO with no mathematical invariants; the three definition files are declarative data with no logic to generalize over.
  **RESOLVED:** Confirmed by reading the sources — `run_surveys.dart`/`fill_survey_page.dart` only build/show widgets and forward callbacks (no pure logic), and the definition files are static `RPOrderedTask`/score-definition data. The sole generalizable invariant (score summation) is already covered by the existing Glados property. No new Glados test; no code change.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `run_surveys_test.dart` — the `resultCallback` passed to each runner is built by `createResultCallback` which is tested in isolation, but the integration between the three runner functions and their callbacks is never exercised end-to-end. Specifically, there is no test that calls `surveyWidget.onSubmit!(result)` and checks that `MockPersistenceLogic.createSurveyEntry` receives the correct `linkedId` (including the optional non-null `linkedId` path that `runCfq11` / `runGhq12` / `runPanas` expose).
  **RESOLVED:** Added two parameterized tests per runner: one drives the wired callback after opening with `linkedId: 'linked-42'` and verifies the captured `linkedId` and `SurveyData`; the other opens without a `linkedId` and verifies `createSurveyEntry` is called with `linkedId: null`. This exercises both the non-null and null `linkedId` paths end-to-end through `runCfq11`/`runGhq12`/`runPanas`.
- [x] **[LOW]** `fill_survey_page_test.dart` — `SurveyWidget._encode` is exercised indirectly through the `cancelCallBack` test (line 92), but the assertion only checks that the encoded JSON is valid and contains `identifier`. The `BoxConstraints(maxHeight: 500)` and padding constants in `build()` have no tests, but these are purely visual and do not warrant unit tests.
  **RESOLVED:** Confirmed the cancel test already asserts the `_encode` output is valid pretty-printed JSON containing the result identifier. The `BoxConstraints`/padding are static visual constants with no behavior; no unit test warranted. No code change.
- [x] **[LOW]** `ghq12_survey.dart` — the per-question choice labels differ between items (each item has its own wording for the 0–3 scale). The test at `ghq12_survey_test.dart:23` verifies only that all values are `[0,1,2,3]`, not that any label text is non-empty or distinct. Low risk given the declarative nature of the file.
  **RESOLVED:** Strengthened the "12 question steps" test to assert, for every item, that all four choice labels are non-empty (`everyElement(isNotEmpty)`) and mutually distinct (`labels.toSet()` has length 4), catching a blanked or copy/pasted label.

---

## Test execution speed opportunities

- [x] **[MED]** `test/features/surveys/tools/run_surveys_test.dart:45` — `pumpAndSettle()` with no duration cap. If the research_package modal starts an animation that does not settle within Flutter's test environment (e.g., a looping progress indicator), this test will time out after the default 10 s. Replace with `tester.pump(const Duration(milliseconds: 500))` or use `tester.pumpAndSettle(const Duration(milliseconds: 500))` to bound worst-case wait time to 0.5 s. **Estimated impact:** up to −9.5 s per test run in the worst case.
  **RESOLVED:** Changed `pumpRunner` to `tester.pumpAndSettle(const Duration(milliseconds: 500))`, capping the worst-case wait at 0.5s (same change as the line 31 item).
- [x] **[LOW]** The three definition tests (`cfq11`, `ghq12`, `panas`) are purely synchronous and very fast. No speed issues.
  **RESOLVED:** Confirmed — the definition tests do no async work (no pumps, no I/O); they assert over static `RPOrderedTask` data only. No change needed.

---

## Summary

- All impl files are small and well-structured; no split needed.
- One Glados property already exists in `calculate_test.dart` and covers the main scoring logic thoroughly.
- Primary quality gap: `run_surveys_test.dart` does not verify the `resultCallback` → persistence link end-to-end.
- One fake-time / speed risk: unbounded `pumpAndSettle` in the shared `pumpRunner` helper.
- One weak assertion: bare `isNotNull` on `rpuiTask.onCancel` in `fill_survey_page_test.dart`.
