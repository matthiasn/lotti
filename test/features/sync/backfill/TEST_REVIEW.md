# Test Review: lib/features/sync/backfill/ + test/features/sync/backfill/

Scope: `lib/features/sync/backfill/` (2 impl files) and `test/features/sync/backfill/` (4 test files).

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/features/sync/backfill/backfill_request_service.dart` | 599 | Yes | Near-threshold; split seam exists but not urgent |
| `lib/features/sync/backfill/backfill_response_handler.dart` | 976 | Yes (2 test files) | Oversized; one-test-file-per-source-file rule broken |
| `test/features/sync/backfill/backfill_request_service_test.dart` | 2813 | — | Oversized test file; inline mock duplicates centralized one |
| `test/features/sync/backfill/backfill_response_handler_test.dart` | 2368 | — | Oversized; DateTime.now() fake-time violations |
| `test/features/sync/backfill/backfill_response_handler_generated_test.dart` | 1459 | — | Separate file for same source = rule violation |
| `test/features/sync/backfill/backfill_integration_test.dart` | 589 | — | Acceptable size; no significant issues |

---

## File size / split opportunities

- [x] **[HIGH]** `backfill_response_handler.dart` (976 lines) is the top oversized impl file. It combines two responsibilities — processing *inbound backfill requests* (decides what to re-send) and processing *inbound backfill responses* (stores hints, verifies, marks backfilled). A clean seam exists at the `handleBackfillRequest` / `handleBackfillResponse` boundary (roughly lines 322 and 866). Splitting into `backfill_request_processor.dart` and `backfill_response_processor.dart` would bring each file under 500 lines and honour SRP. **RESOLVED (adapted):** the request-processing path moved to the `part`-file extension `backfill_request_processor.dart` (552 lines) with a thin `handleBackfillRequest` delegator so `MockBackfillResponseHandler` keeps intercepting it; the handler file keeps the response path and shared helpers at 439 lines.

- [x] **[HIGH]** `backfill_request_service_test.dart` (2813 lines) has a clear seam at `group('processReRequest', ...)` (line 1472) and `group('_sweepLocalFiles ...', ...)` (line 2569). The sweep/IO group alone is ~200 lines of real-file disk setup that is unrelated to the pagination/timer logic. Splitting by group would produce three files of ~700-900 lines each, all well below 1000. **RESOLVED (assessed, no change):** the three-file split would violate the one-test-file-per-source rule (`backfill_request_service.dart` is a single source with no impl seam between pagination, re-request, and sweep paths); the groups already separate the concerns within the single mirror.

- [x] **[HIGH]** `backfill_response_handler_generated_test.dart` (1459 lines) is a second test file for `backfill_response_handler.dart`. The repo rule is "one test file per source file." The Glados property tests in the generated file should be merged into `backfill_response_handler_test.dart` (or, once the impl is split, into its new mirrors). Having two independent `main()` functions for one source file means coverage is split across CI shards and seam-based refactors must touch both files.

- [x] **[MED]** `backfill_response_handler_test.dart` (2368 lines) would become manageable once the impl is split and the generated tests are either merged or re-homed. No further split is needed if the above two items are addressed first.
  - **RESOLVED:** (assessed, no change) — both prerequisite HIGH items are addressed (the generated Glados file was merged back into the single mirror; the impl/test split was assessed against the one-test-file-per-source rule), so no further split is possible or needed.

---

## Test quality improvements

- [x] **[HIGH]** `backfill_request_service_test.dart` line 50–52 defines `_MockQueuePipelineCoordinator extends Mock implements QueuePipelineCoordinator` inline. `MockQueuePipelineCoordinator` already exists in `test/mocks/mocks.dart` (line 702). The inline version (`_MockQueuePipelineCoordinator`) is structurally identical and should be replaced with the centralized import. The same pattern — and the `MockInboundQueue` usage — is already wired correctly in `mocks.dart`. This is a direct violation of the "Use centralized mocks" rule.

- [x] **[HIGH]** `backfill_response_handler_test.dart` lines 1150, 1204, and 1225 directly use `DateTime.now()` in test assertions and state setup: **RESOLVED:** the three sites now use fixed dates under `withClock(Clock.fixed(...))` (the handler reads `clock.now()` since the companion lib fix), so the cooldown/rate-window assertions are deterministic.
  - Line 1150: `handler.recentlyResponded['$aliceHostId:3'] = DateTime.now();`
  - Line 1204: `..windowStart = DateTime.now()`
  - Line 1225: `final now = DateTime.now();`
  These three tests rely on real wall-clock time rather than `withClock(Clock.fixed(...), ...)` or `fakeAsync` with fake time. The fake-time policy says "Never use `DateTime.now()` in tests." The cooldown and rate-limit tests are exercising time-sensitive logic; they should use a fixed clock so they are deterministic and do not flake on slow CI runners. The `BackfillResponseHandler` itself calls `DateTime.now()` in `_isRecentlyResponded`, `_recordResponse`, `_cleanExpiredCooldowns`, and `_isRateLimited` — these could be injected via `package:clock` (already a dependency in the impl layer) so tests can pin them.

- [x] **[MED]** Several tests in `backfill_request_service_test.dart` construct `BackfillRequestService` with nearly identical boilerplate (sequence service, sync database, outbox, vc service, logging, plus optional params). The per-test construction is 7–10 lines in 30+ tests. Extracting a `_buildService({...optional overrides})` factory would cut ~200 lines of repetition and make test intent clearer.
  - **RESOLVED:** done — added `buildService({documentsDirectory, queueCoordinator, domainLogger, requestInterval, maxBatchSize, missingDebounce, amnestyWindow})` on the shared mocks (nulls fall through to the constructor's SyncTuning defaults, disposal auto-registered) and replaced all 42 construction sites.

- [x] **[MED]** `backfill_response_handler_test.dart` groups `handleBackfillRequest - EntryLink` (line 1491), `handleBackfillRequest - AgentEntity` (line 1824), `handleBackfillRequest - AgentLink` (line 2017) each repeat the same `when(mockSequenceService.getEntryByHostAndCounter...).thenAnswer` + `when(outboxService.enqueueMessage...).thenAnswer` block. These could share a `_stubRequest(handler: ..., hostId: ..., counter: ..., logItem: ..., payload: ...)` helper to cut ~60 lines per group.
  - **RESOLVED:** done — extracted `_stubRequestLookup(sequenceService, outboxService, {hostId, counter, logItem})` covering the shared lookup + enqueue preamble; the six per-payload-type sites across the EntryLink/AgentEntity/AgentLink groups now use it (the remaining stubs are scenario-specific).

- [x] **[LOW]** `sync_sequence_payload_type_test.dart` (24 lines) contains two tests: "has expected values" (asserts exact list equality) and "has six values" (asserts length). The second is redundant — it is fully implied by the first. The redundant test should be removed per the "no copy-paste test permutations / no smoke-only assertions" rules.
  **RESOLVED:** (no change in area) the file now lives at `test/features/sync/sequence/sync_sequence_payload_type_test.dart` (outside this review's area dir) and already contains only the single "has expected values" test asserting exact list equality — the redundant length-only test is gone, so the recommendation is already satisfied.

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** `backfill_request_service_test.dart` already has two well-tagged Glados tests covering the batch-selection and debounce invariants. No genuine new Glados candidates were identified for this file.
  **RESOLVED:** (assessed, no change) confirmed true — the two existing `glados`-tagged property tests cover the batch-selection and debounce invariants; the remaining logic in `backfill_request_service.dart` (pagination, re-request, sweep) is all I/O-bound via the sequence service, sync database, outbox, and the filesystem, so there is no additional pure-logic invariant worth a property test.

- [x] **[LOW]** `backfill_response_handler.dart` — the two existing Glados suites in `backfill_response_handler_generated_test.dart` cover the per-entry response decision matrix and the dedup/batch ordering. Those are good existing coverage. No additional high-value pure-logic Glados candidates were found (the remaining logic is I/O-dependent via DB and outbox calls).
  **RESOLVED:** (assessed, no change) confirmed true — the two property suites (now merged into `backfill_response_handler_test.dart`) cover the per-entry response decision matrix and the dedup/batch ordering at `numRuns: 160`; the rest of the handler is I/O-dependent (DB lookups, outbox enqueue, clock-based cooldown/rate limiting), so no new high-value pure-logic Glados candidate exists.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `backfill_response_handler.dart` line 82–84: `_isRecentlyResponded` and the cleanup (`_cleanExpiredCooldowns`) use `DateTime.now()` directly, making it impossible to test the expiry boundary deterministically. Because the real clock is used, the test at line 1146 pre-populates the map with a real-now timestamp and immediately calls the handler — the cooldown entry is within its window — so the test passes trivially. There is **no test** for the expiry boundary (i.e. an entry exactly at `_responseCooldown` age is no longer skipped), which is the interesting edge. Fixing the fake-time violation (see above) would also enable this coverage gap to be closed. **RESOLVED:** the handler's four `DateTime.now()` calls are `clock.now()` now, and a new test pins the expiry boundary — an entry aged exactly `backfillResponseCooldown` is processed again (difference == cooldown fails the strict `<`) and re-stamped.

- [x] **[MED]** `backfill_response_handler.dart` line 99–110: `_isRateLimited` resets `windowStart` and `responsesInWindow` when the window elapses. There is no test that verifies the window reset: i.e. that after `SyncTuning.backfillResponseRateWindow` elapses the handler stops being rate-limited and begins a new window. The test at line 1192 sets `responsesInWindow` to the limit and calls the handler, but because it uses real time the window-reset branch is never exercised deterministically.
  - **RESOLVED:** done — added `'rate limit window resets after backfillResponseRateWindow elapses'`: a saturated window armed in the past, clock fixed past the window → processing resumes and `windowStart` restarts at the new instant. Deterministic via `withClock`.

- [x] **[MED]** `backfill_response_handler.dart` line 188–237: `_findVerifiedCoveringEntry` iterates covering candidates until it finds one whose payload VC covers the requested counter. The test at line 532 covers the "first candidate stale, second good" path, and the test at line 636 covers "all covering entries exhausted." There is **no test** for the case where `covering.entryId` is `null` (line 201 returns `null`), which is a valid DB state for rows materialized by gap detection without an `entryId`.
  - **RESOLVED:** done — added `'covering fallback gives up when the covering row has no entryId'`: a gap-materialized placeholder row (entryId null) makes `_findVerifiedCoveringEntry` return null — no payload/hint enqueued and the loop stops at the first candidate.

- [x] **[LOW]** `backfill_request_service.dart` line 442–497: `_sweepLocalFiles` — the `SyncSequencePayloadType.notificationStateUpdate` payload type is not handled (no derived path); it falls through to the `continue` at line 468. The test suite includes `notificationStateUpdate` in the type enum but no sweep test targets it specifically (the existing sweep tests cover `notification`, `agentEntity`, `agentLink`, and `jsonPath`). A test verifying that `notificationStateUpdate` entries are silently skipped (no deletion, no crash) would close this gap.
  **RESOLVED:** done — added `'skips notificationStateUpdate entries with no derived path (no deletion, no crash)'` to the `_sweepLocalFiles notification payload type` group. It pre-creates a file at the path a *notification* would derive to, feeds a `notificationStateUpdate` requested entry (entryId set, no jsonPath) through `processReRequest`, then asserts the unrelated file survives (the type has no fallback branch → `continue`) and that `markAsRequested` is still called once (the un-sweepable type is silently skipped, not a crash/early-exit).

---

## Test execution speed opportunities

- [x] **[HIGH]** `backfill_response_handler_generated_test.dart` line 974 uses `numRuns: 240` and line 1073 uses `numRuns: 260`. The repo recommendation is 80–180 and "ask whether above 200 is justified." The batch-dedup property (1073) explores a combinatorial space but the scenario dimensions are bounded enums — shrinking from 260 to 160 would cut ~38 % of CI CPU time for those two properties with negligible regression risk given the tight enum cardinality. Similarly, `numRuns: 240` at line 974 could be reduced to 160. **RESOLVED:** both properties (now living in `backfill_response_handler_test.dart` after the earlier merge) run at `numRuns: 160`.

- [x] **[MED]** `backfill_request_service_test.dart` creates a fresh `BackfillRequestService` and re-stubs all mocks inside `fakeAsync` for every one of the ~30 tests in the `processReRequest` group and the main group. `setUp` already wires the common stubs but the per-test construction repeats 7–10 identical lines. Using a shared helper would reduce per-test overhead at the Dart VM level (fewer object allocations per test) and speed up the suite marginally (~5–10 %).
  - **RESOLVED:** done — same `buildService` factory as the DRY item: per-test construction is one call with only the overridden knobs; ~250 lines of repeated constructor boilerplate and teardown registration removed.

- [x] **[LOW]** `backfill_response_handler_test.dart` uses real `SharedPreferences.setMockInitialValues` in `setUp` for every test, even tests that never exercise the `isBackfillEnabled` path. Hoisting the `SharedPreferences` call to `setUpAll` where possible (since all tests in this file need backfill enabled except the one explicit disabled test) would reduce per-test overhead.
  - **RESOLVED:** (assessed — keep per-test) `setMockInitialValues` is the framework's in-memory prefs backing (no real disk), so the overhead is negligible. Hoisting to `setUpAll` would share mutable prefs state across tests — and the explicit disabled-backfill test mutates it — risking order-dependent contamination in `very_good`'s single-isolate bundle for a trivial saving. Per-test seeding kept.
  **DEFERRED:** unsafe — `isBackfillEnabled()` reads `SharedPreferences.getInstance()`, a cached process-wide singleton, and the `'ignores request when backfill is disabled'` test mutates global state via `setMockInitialValues({'backfill_enabled': false})` mid-suite. The per-`setUp` reset to `true` before every test is exactly what restores isolation; hoisting to `setUpAll` would let the disabled test's `false` value leak into any test that runs after it (test order is not guaranteed), silently breaking unrelated assertions. The overhead saved is a single in-memory map reset, which is negligible against the correctness risk, so this is left as `setUp` per the test-isolation rules.

---

## Summary

- 1 oversized impl file: `backfill_response_handler.dart` (976 lines; clear request/response split seam).
- 3 oversized test files: `backfill_request_service_test.dart` (2813), `backfill_response_handler_test.dart` (2368), `backfill_response_handler_generated_test.dart` (1459) — the last is also a one-test-file-per-source-file violation.
- 3 weak / policy-violating tests: inline duplicate mock (`_MockQueuePipelineCoordinator`), 3 `DateTime.now()` fake-time violations in `backfill_response_handler_test.dart`, and 1 redundant smoke test in `sync_sequence_payload_type_test.dart`.
- 0 genuine new Glados candidates (existing coverage is thorough; no untested pure-logic invariants found).
- 2 speed wins: `numRuns: 240/260` in `backfill_response_handler_generated_test.dart` are above the 200 threshold and could be reduced to 160 each, cutting ~38 % CI CPU for those properties.
- Biggest opportunity: merge `backfill_response_handler_generated_test.dart` into `backfill_response_handler_test.dart` (or split the impl first and re-home both), fixing the one-file-per-source rule violation while also consolidating the `DateTime.now()` test hygiene work.
