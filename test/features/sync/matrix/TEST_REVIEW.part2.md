# Part 2 of 2 — sync/matrix: loaders / discovery / verification / lifecycle / misc

**Scope:** Root-level `test/features/sync/matrix/` and `lib/features/sync/matrix/` files covering
loaders, discovery, verification, lifecycle coordination, stats, config, consts, and related helpers.
Part 1 covers sender / service / event-processor; `pipeline/` + `utils/` are separate.

_Generated by automated test-quality review (static analysis)_

---

## Per-file inventory

| File | Impl lines | Test file | Test lines | Top issue |
|---|---|---|---|---|
| `smart_journal_entity_loader.dart` | 314 | `smart_journal_entity_loader_test.dart` | 1848 | Massive copy-paste per-test boilerplate (MockRoom+Client+DB created 37 times inline); split opportunity |
| `sync_journal_entity_loader.dart` | 31 | `sync_journal_entity_loader_test.dart` | 74 | Good, minimal |
| `sync_room_discovery.dart` | 270 | `sync_room_discovery_test.dart` | 901 | Inline mock classes duplicating centralized mocks |
| `sync_room_manager.dart` | 402 | `sync_room_manager_test.dart` (542) + `room_test.dart` (392) | 934 total | One-test-file-per-source-file violation; `room_test.dart` is a second test file for the same impl |
| `read_marker_service.dart` | 160 | `read_marker_service_test.dart` | 723 | Good Glados coverage; some `Future.delayed(Duration.zero)` real-time yields scattered in examples |
| `key_verification_runner.dart` | 172 | `key_verification_runner_test.dart` | 634 | Inline `_MockKeyVerification` / `_MockDeviceKeys` may duplicate centralized mocks |
| `sync_lifecycle_coordinator.dart` | 252 | `sync_lifecycle_coordinator_test.dart` | 630 | Inline mock classes (MockMatrixSyncGateway, MockMatrixSessionManager, etc.) |
| `session_manager.dart` | 112 | `session_manager_test.dart` | 466 | Inline gateway/roomManager mocks; `_GeneratedMatrixException` is a Fake, not a real scenario |
| `room_test.dart` (no impl) | — | 392 | — | Violates one-test-file-per-source-file; tests `SyncRoomManager` alongside `sync_room_manager_test.dart`; contains real `Future.delayed(Duration.zero)` waits |
| `timeline_ordering.dart` | 86 | `timeline_ordering_test.dart` | 384 | Excellent — well-structured Glados; missing `tags: 'glados'` on ordering scenario (default numRuns) |
| `vector_clock_validator.dart` | 74 | `vector_clock_validator_test.dart` | 309 | Good Glados property; tagged correctly |
| `sync_engine.dart` | 190 | `sync_engine_test.dart` | 265 | `Future.delayed(Duration.zero)` for background-lifecycle test (line 217); inline mock classes |
| `stats_signature.dart` | 47 | `stats_signature_test.dart` | 239 | Good Glados; no issues |
| `stats.dart` | 23 | `stats_test.dart` | 97 | Constructor/equality smoke tests; `hashCode` check is fine; `copyWith`/merge behavior untested |
| `descriptor_downloader.dart` | 146 | `descriptor_downloader_test.dart` | 212 | Good behavioral tests; no Glados but fits single-fixture pattern; fine |
| `client.dart` | 76 | `client_test.dart` | 194 | Uses real I/O (creates SQLite DB on disk); allowed per policy; `DateTime.now()` implicitly used |
| `journal_entity_dedup_cache.dart` | 73 | `journal_entity_dedup_cache_test.dart` | 185 | Excellent Glados (LRU invariants, fingerprint, capacity); all tagged |
| `config.dart` | 45 | `config_test.dart` | 148 | Inline `MockMatrixSyncGateway` / `MockSyncRoomManager` duplicating centralized mocks |
| `consts.dart` | 16 | `consts_test.dart` | 34 | Tests only 7 of 9 constants; `attachmentEncodingKey` and `attachmentEncodingGzip` missing |
| `last_read.dart` | 22 | **NONE** | — | No test file; contains non-trivial helpers (`isServerAssignedMatrixEventId`, parse) |

---

## File size / split opportunities

- [x] **[HIGH] `smart_journal_entity_loader_test.dart` (1848 lines)** — The entire test file is a single **RESOLVED:** a file-level `_buildPurgeChain(ev)` helper now wires the `ev.room → room.client → client.database` chain (13 verbatim copies replaced). The file stays single per the one-test-file-per-source rule, as this item itself recommends.
  outer `group` wrapping 29 test cases. The `MockRoom` + `MockMatrixClient` + `MockMatrixDatabase`
  chain (`ev.room`, `room.client`, `client.database`, `database.deleteFile`) is copy-pasted 12 times
  across different tests (lines ~297–415, 448–504, 965–1019, 1031–1100, 1104–1190, 1193–1294, etc.).
  A `_buildPurgeChain(ev, relJson)` helper would eliminate ~150 lines of duplicate wiring. The file
  could also be split at logical seams: (1) no-VC / missing-file tests (~lines 53–535), (2) VC /
  stale-descriptor tests (~lines 796–1102), (3) circuit-breaker tests (~lines 1104–1397), (4) edge
  cases (~lines 1399–1847) — while keeping them in a single file via nested `group`s for now.

- [x] **[HIGH] `room_test.dart` (392 lines)** — A second test file for `SyncRoomManager`. Violates the
  one-test-file-per-source-file rule (`AGENTS.md` §Test Infrastructure Rules). All its tests should
  migrate into `sync_room_manager_test.dart` and `room_test.dart` deleted. Some tests overlap in
  intent with `sync_room_manager_test.dart` (e.g. `inviteUser` throws, `acceptInvite`, `leaveCurrentRoom`).

- [x] **[MED] `sync_room_discovery_test.dart` (901 lines)** — Within budget, but the `_createMockRoom`
  and `_createMockRoomWithEvents` helpers (bottom of file) are long-winded. Given the file also has
  a large Glados scenario class, review if any example-based tests (~20) could be collapsed into the
  existing Glados property.
  **RESOLVED:** Reviewed — the `roomDiscoveryScenario` Glados property already covers filtering,
  confidence scoring, sort order, member-count defaulting and name extraction. The remaining
  example tests exercise orthogonal paths the generator does not model (`markRoomAsLottiSync` state
  write + error logging, `hasExistingSyncRooms`, timeline-based content detection for msgtype/base64/
  non-sync bodies, `getTimeline` error handling, and the Event-vs-StrippedStateEvent createdAt
  distinction). Collapsing them would lose specificity and bloat the generator; no meaningful
  collapse warranted. The `_createMock*` helpers are already shared factories. Inline mock
  duplication addressed under the inline-mock item below.

- [x] **[LOW] `key_verification_runner_test.dart` (634 lines)** — All 9 tests use `fakeAsync`; setup
  is long per test. A `_TestBench` class or shared `setUp` could save ~100 lines.
  **RESOLVED:** Extracted a file-level `_runnerController()` helper that builds the synchronous
  broadcast `StreamController<KeyVerificationRunner>` and registers its teardown, replacing 6
  verbatim 4-line copies. A full `_TestBench` was not pursued: the per-test verification stubs
  (step queues, done-state queues, onUpdate capture) differ substantially between tests and folding
  them into one bench would obscure intent and risk mocktail matcher cross-talk. Inline
  `_MockKeyVerification`/`_MockDeviceKeys` also replaced with the centralized mocks.

---

## Test quality improvements

- [x] **[HIGH] `room_test.dart` violates one-file-per-source-file** — `room_test.dart` (392 lines) has
  no corresponding `room.dart` impl; it tests `sync_room_manager.dart`. This test file must be merged
  into `sync_room_manager_test.dart` and deleted. (`AGENTS.md`: "One test file per source file.")

- [x] **[HIGH] `smart_journal_entity_loader_test.dart` — copy-paste test bodies** — Patterns repeated **RESOLVED:** covered by the `_buildPurgeChain` extraction above (13 chain copies). The image/audio entity constructions vary in id/date/transcript shape per scenario and read clearly inline.
  verbatim across ~12 tests: `final room = MockRoom(); final client = MockMatrixClient(); final
  database = MockMatrixDatabase(); when(() => ev.room)...` etc. Extract a helper:
  ```dart
  void _wireRoomChain(MockEvent ev, Uri mxcUri, {bool deleteSucceeds = true}) { ... }
  ```
  Similarly the image/audio entity construction (metadata + data) is repeated ~10 times; a factory
  method accepting `id` / `date` would reduce noise significantly.

- [x] **[MED] Inline mock classes duplicating centralized mocks** — Multiple files define their own
  `MockClient`, `MockMatrixSyncGateway`, `MockSyncRoomManager`, `MockMatrixSessionManager` etc.
  instead of importing from `test/mocks/mocks.dart`:
  - `sync_room_discovery_test.dart`: `MockClient`, `MockRoomSummary`, `MockStrippedStateEvent`
  - `sync_room_manager_test.dart`: `MockMatrixGateway`, `MockClient`, `MockMatrixException`,
    `MockSyncRoomDiscoveryService`, `FakeRoom`
  - `sync_lifecycle_coordinator_test.dart`: `MockMatrixSyncGateway`, `MockMatrixSessionManager`,
    `MockSyncRoomManager`, `MockPipeline`, `MockClient`
  - `room_test.dart`: `MockMatrixSyncGateway`, `MockMatrixClient`
  - `session_manager_test.dart`: `_MockGateway` (needed — has custom constructor), `_MockRoomManager`
  - `config_test.dart`: `MockMatrixSyncGateway`, `MockSyncRoomManager`
  - `sync_engine_test.dart`: `MockMatrixSessionManager`, `MockSyncRoomManager`,
    `MockSyncLifecycleCoordinator`, `MockClient`, `MockRoomSummary`
  - `key_verification_runner_test.dart`: `_MockKeyVerification`, `_MockDeviceKeys`
  Any of these that already exist in the centralized file (`MockRoom`, `MockMatrixClient`) should be
  removed from the test file and imported instead.
  **RESOLVED:** Removed every inline class that duplicates a centralized mock and switched to the
  `test/mocks/mocks.dart` versions (dropping the now-unused source imports):
  `sync_room_discovery_test.dart` (`MockClient`→`MockMatrixClient`, dropped inline
  `MockStrippedStateEvent`; kept `MockRoomSummary` — no central equivalent);
  `sync_lifecycle_coordinator_test.dart` (gateway/session/roomManager + `MockClient`→
  `MockMatrixClient`; kept `MockPipeline`/`_FakeClient` — no central equivalent);
  `config_test.dart` (gateway + roomManager); `sync_engine_test.dart` (session/roomManager/
  lifecycle + `MockClient`→`MockMatrixClient`; kept `MockRoomSummary`);
  `session_manager_test.dart` (`_MockRoomManager`→`MockSyncRoomManager`; kept `_MockGateway` for its
  custom `client` constructor); `key_verification_runner_test.dart` (`_MockKeyVerification`→
  `MockKeyVerification`, `_MockDeviceKeys`→`MockDeviceKeys`). `room_test.dart` no longer exists
  (merged in the HIGH items). `sync_room_manager_test.dart`'s mocks (`MockMatrixGateway`,
  `MockMatrixException`, `MockSyncRoomDiscoveryService`, `FakeRoom`, the room_test-group privates)
  have no central equivalents and stay local.

- [x] **[MED] `consts_test.dart` — incomplete constant coverage** — Only 7 of 9 constants in
  `consts.dart` are tested. `attachmentEncodingKey` (`'com.lotti.encoding'`) and
  `attachmentEncodingGzip` (`'gzip'`) are never asserted (`consts.dart` lines 12–17). These have
  on-wire significance and should be pinned.
  **RESOLVED:** Added tests pinning `attachmentEncodingKey == 'com.lotti.encoding'` and
  `attachmentEncodingGzip == 'gzip'`; all 9 constants are now asserted.

- [x] **[MED] `stats_test.dart` — structural smoke tests only** — The eight tests verify constructor
  field assignment, equality, `hashCode`, and `!=`. None tests `MatrixStats` behavior (e.g. there
  is no behavior, it is a data class — this is fine conceptually) but `copyWith`/merge operations
  if they exist are untested. The "identical instances are equal" test (`stats == stats`) is
  vacuously true and adds no value. The unrelated-type equality check is useful.
  **RESOLVED:** Confirmed `MatrixStats` has no `copyWith`/merge (pure value class — nothing to test).
  Removed the vacuous `stats == stats` test. Added a Glados property over generated
  `(sentCount, messageCounts)` asserting the real equality/hashCode contract: value-equal instances
  (distinct map identity) compare equal and hash identically, and changing `sentCount` or any
  `messageCounts` entry breaks equality (proving `mapEquals` participates in `==`).

- [x] **[LOW] `sync_engine_test.dart` — `Future.delayed(Duration.zero)` real wait** — Line 217 uses
  `await Future<void>.delayed(Duration.zero)` to let a background future settle after
  `connectWithLifecycleOption`. Per `test/README.md` policy this should be
  `async.flushMicrotasks()` inside a `fakeAsync` block. The test is otherwise well-structured but
  this is a micro real-time yield.
  **RESOLVED:** Converted the test to `fakeAsync`; invoke `connectWithLifecycleOption` inside the
  fake zone (capturing the result via `.then`), drain both the connect future and the
  fire-and-forget background-lifecycle closure with `async.flushMicrotasks()`, then assert. No real
  wait remains.

- [x] **[LOW] `sync_room_manager_test.dart` line 289 — `Future.delayed(Duration.zero)` real wait** —
  The `'invite stream filters invalid room ids'` test yields via `await Future<void>.delayed(Duration.zero)`
  then calls `fakeAsync(...)` immediately after. The yield should be replaced with
  `async.flushMicrotasks()` inside the surrounding `fakeAsync`.
  **RESOLVED:** Rewrote the test fully under `fakeAsync`, building a fresh `SyncRoomManager` inside
  the fake zone wired to a *synchronous* broadcast invite stream (the group's shared manager is
  created in `setUp`, so its subscription is real-zone-bound and `flushMicrotasks` could not drain
  it). Delivery to the handler is now deterministic; the dangling empty `fakeAsync` and the
  `Future.delayed` are gone.

- [x] **[LOW] `room_test.dart` — three `Future.delayed(Duration.zero)` real waits** — Lines 93, 113, 136
  inside `'emits invite requests for valid room ids'`, `'ignores invites with invalid room id'`, and
  `'marks invite as matching when ids align'`. These should be `async.flushMicrotasks()` inside
  `fakeAsync`.
  **RESOLVED:** `room_test.dart` was already merged into the `SyncRoomManager (room_test)` group of
  `sync_room_manager_test.dart` (HIGH items). All three tests now run under `fakeAsync` via a shared
  `_buildSyncInviteManager(...)` helper that constructs a manager bound to a synchronous broadcast
  invite stream inside the fake zone, replacing every `Future.delayed(Duration.zero)` with
  `async.flushMicrotasks()`.

---

## Generative (Glados) testing opportunities

- [x] **[MED] `last_read.dart` — `isServerAssignedMatrixEventId` is untested and Glados-suitable** —
  This pure function (checks `startsWith('$')`) has no test file at all. A Glados property verifying
  that any string starting with `$` is accepted and any other string is rejected would give strong
  coverage cheaply. A `roundtrip` property for `setLastRead`/`getLastRead` with a mock `SettingsDb`
  is also viable but would be example-based.
  **RESOLVED:** `last_read_test.dart` now exists (per the HIGH coverage item). Added a tagged Glados
  property (`glados.any.letterOrDigits`, numRuns 120) asserting acceptance is exactly equivalent to a
  leading `$`: the dollar-prefixed variant is always accepted, the bare value always rejected, and
  the result always equals `value.startsWith(r'$')`. The setter/getter round-trips remain
  example-based (mock `SettingsDb`).

- [x] **[LOW] `timeline_ordering_test.dart` — `sortStableByTimestamp` Glados test missing `tags: 'glados'`** —
  The `Glados(glados.any.orderingScenario)` invocation at line 231 (no `ExploreConfig`) and the one
  at line 287 (`timestampCollisionStats`) do not carry `tags: 'glados'`. Only the `isNewer` property
  at line 366 has the tag. This routes two CPU-bound Glados properties through the standard CI lane,
  slowing it down for everyone. Add `tags: 'glados'` to all three Glados invocations in the file.
  **RESOLVED:** Stale — verified all three Glados invocations in the current file already carry
  `tags: 'glados'` (the `sortStableByTimestamp`, `timestampCollisionStats`, and `isNewer`
  properties). No change needed.

- [x] **[LOW] `stats_test.dart` — Glados could verify `MatrixStats.hashCode` contract** — A `Glados2`
  property over `(sentCount, messageCounts)` combinations confirming equal instances produce equal
  hash codes and different instances are unlikely to collide would replace the current static
  examples with a proper invariant test. Low priority since `MatrixStats` is a trivial data class.
  **RESOLVED:** Added a tagged Glados property over generated `(sentCount, messageCounts)` (modeled as
  a metric-entry scenario like `stats_signature_test.dart`) asserting value-equal instances share a
  hash code and that mutating `sentCount` or any entry breaks equality. (Implemented as a single
  `Glados` over a composite scenario rather than `Glados2`, matching the file's sibling pattern.)

The following files already have thorough and correctly tagged Glados coverage; no additional
generative work is warranted: `vector_clock_validator_test.dart`, `journal_entity_dedup_cache_test.dart`,
`stats_signature_test.dart`, `sync_room_discovery_test.dart`, `sync_room_manager_test.dart`,
`read_marker_service_test.dart`, `session_manager_test.dart`, `sync_lifecycle_coordinator_test.dart`,
`key_verification_runner_test.dart`.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH] `last_read.dart` has no test file** — `isServerAssignedMatrixEventId`, **RESOLVED:** new `last_read_test.dart` covers `isServerAssignedMatrixEventId` (dollar-prefix acceptance incl. null/empty/mid-string), `setLastReadMatrixEventId` persistence, `getLastReadMatrixEventId` stored/null, and `getLastReadMatrixEventTs` parse/null/silent-`int.tryParse`-failure paths.
  `setLastReadMatrixEventId`, `getLastReadMatrixEventId`, and `getLastReadMatrixEventTs` are all
  untested. `getLastReadMatrixEventTs` includes a `int.tryParse` call that silently returns `null`
  on parse failure — a behavior gap worth a specific test.

- [x] **[MED] `consts_test.dart` misses two constants** — `attachmentEncodingKey` and
  `attachmentEncodingGzip` (added later to `consts.dart`) are not covered. These strings appear
  in on-wire encoding logic and a regression to their values would be silent.
  **RESOLVED:** Same fix as the constant-coverage item above — both constants are now pinned in
  `consts_test.dart`.

- [x] **[MED] `sync_journal_entity_loader_test.dart` — path-traversal rejection** — The existing test
  at line 55 calls `loader.load(...)` without `await` (the result of `expect(() => ..., throwsA(...))`)
  and so never actually awaits the async load. Verify the test is exercising the synchronous throw
  path or add `await expectLater(loader.load(...), throwsA(...))` to ensure the async rejection is
  also covered.
  **RESOLVED:** Confirmed the throw is NOT synchronous — `load()` is `async`, so the
  `FileSystemException` from `resolveJsonCandidateFile` surfaces as a rejected `Future`; the old
  `expect(() => loader.load(...), throwsA(...))` callback returned a Future instead of throwing.
  Replaced with `await expectLater(loader.load(...), throwsA(isA<FileSystemException>()))` so the
  async rejection is actually awaited and asserted.

- [x] **[MED] `client_test.dart` — uses real `DateTime.now()` in production code path** — The
  `createMatrixDeviceName` function embeds `DateTime.now()` for the date segment. Tests verify
  the format with a regex rather than a fixed date, which is fine per the real-I/O exception; this
  is noted rather than flagged as a violation.
  **RESOLVED:** Confirmed non-actionable — the `DateTime.now()` lives in production code
  (`createMatrixDeviceName`), not the test, and the function takes no injectable clock. The test
  correctly asserts the date *format* via regex (`\d{4}-\d{2}-\d{2}`) rather than a fixed value,
  which is the right approach here. No change.

- [x] **[LOW] `sync_room_manager_test.dart` / `room_test.dart` overlap** — After merging `room_test.dart`
  into `sync_room_manager_test.dart`, audit for identical test intent (both cover `leaveCurrentRoom`,
  `acceptInvite`, `inviteUser`) and consolidate duplicates rather than accumulating permutations.
  **RESOLVED:** (re-audited, no change) The four superficially overlapping pairs are NOT pure
  permutations — each version exercises a *distinct setup entry-point* into the same state, so both
  carry real coverage: `inviteUser` throws — top-level (`:300`) hits the **pre-`initialize`**
  uninitialized throw, room_test (`:827`) hits the **post-`initialize`, still-no-room** throw (two
  different branches into the StateError). `inviteUser` delegates — top-level (`:307`) reaches the
  current room via `initialize()` reading `settingsDb.itemByKey`, room_test (`:835`) via
  `saveRoomId()`. `leaveCurrentRoom` clears — top-level (`:399`) primes state with `saveRoomId()`,
  room_test (`:800`) with `initialize()`. `acceptInvite` — the room_test version (`:772`) is the
  strict superset (it additionally stubs `getRoomById` and asserts `currentRoom`/`currentRoomId`
  resolve), while the top-level (`:437`) asserts only the join+persist delegation. Collapsing any pair
  would drop coverage of either the alternate setup path or the resolution assertion, so the
  "duplication" is justified and no consolidation is warranted.

- [x] **[LOW] `sync_lifecycle_coordinator_test.dart` — `_ManualLoginStateStream` / `_NoopLoginStateSubscription`
  hand-crafted stream infrastructure** — These 40 lines of boilerplate implement a manually-driven
  `Stream<LoginState>`. They are functional but brittle (no backpressure, no cancellation semantics).
  A `StreamController<LoginState>` would be simpler and would match the pattern used in the
  surrounding file. This does not affect coverage but is a maintenance risk.
  **RESOLVED:** (confirmed non-actionable, no change) A `StreamController` cannot replace this. The
  only test that uses it (`'login event delivered mid-dispose short-circuits via _isDisposed'`,
  `:343`) captures the listener and invokes `manual.handler!(LoginState.loggedIn)` *after* the
  coordinator has disposed and cancelled its subscription, to simulate an event racing with
  cancellation. `StreamController.add()` after cancellation would not deliver to the cancelled
  subscription, so the `_isDisposed` short-circuit branch would never execute and the test would
  assert nothing meaningful. The hand-crafted stream is the only way to force delivery to a cancelled
  listener; it is intentional and load-bearing, so it stays.

---

## Test execution speed opportunities

- [x] **[HIGH] `smart_journal_entity_loader_test.dart` — real filesystem I/O in every test** — All 29 **RESOLVED:** one `createTemp` in `setUpAll` (deleted in `tearDownAll`); each test now gets a counter-named subdirectory via `createSync()`. The setUp also moved from raw `getIt.reset()`/`allowReassignment` to `setUpTestGetIt(additionalSetup: ...)`.
  tests use `Directory.systemTemp.createTemp(...)` and write real files. This is necessary for the
  loader (it relies on real `File` operations), but the per-test `setUp`/`tearDown` creating and
  deleting temp directories adds real wall-clock time. Use a shared `setUpAll`/`tearDownAll` where
  the temp directory is clean-started once and individual tests write to distinct subdirectories via
  unique sub-paths, avoiding 29 OS directory operations. Estimated: ~200–400 ms reduction per run.

- [x] **[MED] `sync_room_manager_test.dart` line 289 + `room_test.dart` lines 93, 113, 136 —
  `Future.delayed(Duration.zero)` in tests without `fakeAsync`** — Real event-loop yields introduce
  OS scheduling jitter. Per `test/README.md`, replace with `async.flushMicrotasks()` inside
  `fakeAsync` and `sync_engine_test.dart` line 217 similarly. Combined: low individual impact but
  contributes to suite non-determinism.
  **RESOLVED:** All four `Future.delayed(Duration.zero)` waits in `sync_room_manager_test.dart`
  (the filter test + the three merged room_test invite tests, via `_buildSyncInviteManager` + a
  synchronous invite stream inside `fakeAsync`) and the one in `sync_engine_test.dart` are converted
  to `async.flushMicrotasks()` under `fakeAsync`. No `Future.delayed` remains in either file's
  flagged tests.

- [x] **[MED] `read_marker_service_test.dart` Glados `numRuns: 180`** — This is at the high end of the
  180-run budget. The scenario space (7 orthogonal dimensions) is well-covered; `numRuns: 120`
  would maintain shrinkability while reducing CI CPU time by ~33% for this property.
  **RESOLVED:** Lowered the read-marker Glados property to `numRuns: 120`.

- [x] **[LOW] `session_manager_test.dart` Glados `numRuns: 180`** — Same rationale as above; 6
  dimensions, could be 140.
  **RESOLVED:** Lowered the session-connect Glados property to `numRuns: 140`.

- [x] **[LOW] `timeline_ordering_test.dart` — two untagged Glados tests in standard suite** — The
  `sortStableByTimestamp` (line 231) and `timestampCollisionStats` (line 287) Glados tests run in
  the standard CI shard because they lack `tags: 'glados'`. These are CPU-bound list-sort properties
  and will slow fast-feedback shards. Adding the tag moves them to the dedicated Glados runner.
  **RESOLVED:** Stale (same as the duplicate item above) — both `sortStableByTimestamp` and
  `timestampCollisionStats` Glados invocations already carry `tags: 'glados'` in the current file.
  No change needed.

- [x] **[LOW] `client_test.dart` — creates a real SQLite database file each test** — Two tests each
  create a full Matrix client with an on-disk SQLite database. These take hundreds of milliseconds
  each and involve real disk I/O. This is necessary to test the factory (no mock alternative), but
  ensure `addTearDown(() async => client.dispose())` fires promptly to avoid file handle leaks that
  slow subsequent tests.
  **RESOLVED:** Verified non-actionable — both client-creating tests already register
  `addTearDown(() async => client.dispose())` plus an `addTearDown` that deletes the temp directory.
  Real I/O is allowed per the fake-time policy's exception for OS integration. No change.

---

## Summary

- **1 oversized test file**: `smart_journal_entity_loader_test.dart` (1848 lines) — copy-paste
  boilerplate across 29 tests is the primary driver; a `_wireRoomChain` helper would cut ~200 lines.
- **1 structural violation**: `room_test.dart` is a second test file for `sync_room_manager.dart`;
  it must be merged and deleted (one-test-file-per-source-file rule).
- **6+ files with inline mock classes** that duplicate or shadow centralized mocks in
  `test/mocks/mocks.dart` (`MockClient`, `MockMatrixSyncGateway`, `MockSyncRoomManager`, etc.).
- **1 completely untested impl file**: `last_read.dart` (22 lines, 4 functions including
  `isServerAssignedMatrixEventId` and a silent `int.tryParse` path).
- **2 Glados tests missing `tags: 'glados'`** in `timeline_ordering_test.dart` — routing CPU-bound
  sort properties through the fast-feedback CI shard.
- **4 real `Future.delayed(Duration.zero)` waits** across `room_test.dart` (3) and
  `sync_room_manager_test.dart` (1) that should be `fakeAsync`/`flushMicrotasks`.
- **Biggest single opportunity**: Merging `room_test.dart` into `sync_room_manager_test.dart` and
  extracting the `_wireRoomChain` / entity-factory helpers in `smart_journal_entity_loader_test.dart`
  would together eliminate ~600 lines of redundancy, fix the structural violation, and resolve
  the most impactful quality issue in this scope.
