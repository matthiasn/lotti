# Test-Quality Review — `lib/features/sync/queue/` + `test/features/sync/queue/`

**Scope:** All 9 implementation files and 10 test files under
`lib/features/sync/queue/` and `test/features/sync/queue/`.

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/…/queue_pipeline_coordinator.dart` | 1 295 | Yes (3 874-line test + integration test) | **Oversized**: 4 distinct logical concerns squeezed into one class |
| `lib/…/inbound_event_queue.dart` | 1 164 | Yes (1 748-line test) | **Oversized**: enqueue/peek/commit/retry/resurrect/prune/stats/depth are separable layers |
| `lib/…/inbound_worker.dart` | 450 | Yes (919-line test) | Within budget; minor async-policy note |
| `lib/…/bridge_coordinator.dart` | 341 | Yes (940-line test) | OK |
| `lib/…/queue_apply_adapter.dart` | 366 | Yes (988-line test) | OK |
| `lib/…/pending_decryption_pen.dart` | 233 | Yes (606-line test) | OK |
| `lib/…/attachment_aware_bootstrap_sink.dart` | 104 | Yes (440-line test) | OK |
| `lib/…/bootstrap_sink.dart` | 105 | Yes (416-line test) | OK |
| `lib/…/queue_marker_seeder.dart` | 80 | Yes (291-line test) | OK |
| *(extra)* `test/…/queue_pipeline_integration_test.dart` | 680 | n/a — no mirror impl | Violates one-test-file-per-source rule; inline mocks duplicate centralized ones |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/sync/queue/queue_pipeline_coordinator.dart` (1 295 lines)  
  The coordinator bundles four well-separated concerns that each warrant their own file and test:
  1. **Lifecycle management** (`start`, `stop`, `onRoomChanged`) — lines 258–635.
  2. **Live-event ingress pipeline** (`_handleLiveEvent`, `_safeEnqueue`, `_trackEnqueue`, `_countSuppressedSelfEcho`, `_maybePostLoadCurrentRoom`, `_safePostLoad`) — lines 637–848.
  3. **Attachment resurrection debounce** (`_onAttachmentPathRecorded`, `_kickPendingPathResurrectionFlush`, `_flushPendingPathResurrections`, `flushPendingPathResurrectionsForTest`) — lines 772–829.
  4. **Bootstrap / gap-recovery routing** (`_runBootstrap`, `_runForwardBootstrap`, `_runBackwardBootstrap`, `_updateBarrenBridgeFlag*`, `maybeStartGapRecovery`, `_runGapRecovery`, `_readMarker`) — lines 867–1 234.  
  
  Suggested seam: extract the gap-recovery logic (concerns 4, ~368 lines) into a `GapRecoveryCoordinator` collaborator, and the attachment debounce (concern 3, ~60 lines) into `AttachmentResurrectionCoordinator`. The coordinator then delegates. Each extracted class would be ~60–370 lines and needs its own mirrored test file.
  **RESOLVED (adapted):** the bootstrap/gap-recovery routing (concern 4) moved to the `part`-file extension `queue_gap_recovery.dart` (359 lines) with mock-safe delegators for `collectHistory` and `maybeStartGapRecovery`; the coordinator keeps lifecycle, ingress, and the small attachment debounce at 961 lines.

- [x] **[HIGH]** `test/features/sync/queue/queue_pipeline_coordinator_test.dart` (3 874 lines)  
  The test file will split naturally once the implementation splits. The two Glados tests at lines 632 and 1 459 each recreate a full local coordinator harness (~90 lines of `when(…)` stubs apiece) that could be extracted into a reusable `_buildGladosCoordinator({required …})` factory. The gap-recovery group (lines 2 761–3 874, ~120 lines) already reads like a self-contained test suite for a separate class.
  **RESOLVED (assessed, no change):** the impl split used a `part` file (one library), so the test stays the single mirror under the one-test-file-per-source rule.

- [x] **[HIGH]** `lib/features/sync/queue/inbound_event_queue.dart` (1 164 lines)  
  Six responsibilities packed into `InboundQueue`:
  1. `enqueueBatch` / `enqueueLive` / `appendBootstrapPage` (enqueue layer, ~100 lines).
  2. `peekBatchReady` (peek/lease layer, ~60 lines).
  3. `commitApplied` / `_advanceMarkerIfNewer` / `_oldestActiveOriginTs` (commit + marker clamping, ~140 lines).
  4. `scheduleRetry` / `markSkipped` (retry/abandon layer, ~70 lines).
  5. `resurrectByPath` / `resurrectByPaths` / `resurrectAll` / `resurrectByReason` / `_resurrectWhere` (resurrection, ~180 lines).
  6. `pruneStrandedEntries`, `stats`, `earliestReadyAt`, `_emitDepth`, `_countTotal` (maintenance/stats/depth, ~140 lines).  
  
  The clamping logic alone (`_advanceMarkerIfNewer` + `_oldestActiveOriginTs`) is a natural extract into a `QueueMarkerAdvancer` helper, mirroring what `QueueMarkerSeeder` already does for seeding.
  **RESOLVED (adapted, partial):** the resurrection layer moved to the `part`-file extension `inbound_queue_resurrection.dart` (206 lines) with mock-safe delegators; the queue keeps enqueue/peek/commit/retry/maintenance at 983 lines — those layers are short mocked publics where delegator overhead matches method size, so further splits add indirection without compression.

- [x] **[HIGH]** `test/features/sync/queue/inbound_event_queue_test.dart` (1 748 lines)  
  Mirror-split candidate once the impl splits. The Glados group at line 807 (`generated queue ledger properties`) covers three independent properties (marker clamping, resurrection, batch accounting) that align perfectly with the suggested impl splits above and could move to their respective test files.
  **RESOLVED (assessed, no change):** single mirror of the part-file library per the one-test-file-per-source rule.

- [x] **[MED]** `test/features/sync/queue/queue_pipeline_integration_test.dart` (680 lines)   **RESOLVED:** Done — file dissolved into `queue_pipeline_coordinator_test.dart` as a `pipeline integration (real queue/worker/pen)` group reusing the file-level harness (session/room/timeline mocks) with its own real-SyncSequenceLogService + stubbable-processor setUp. All 9 local mock classes are gone: central `MockMatrixSessionManager`/`MockSyncRoomManager`/`MockRoom`/`MockMatrixClient`/`MockEvent`/`MockVectorClockService` reused, and `MockBridgeCoordinator`/`MockPreparedSyncEvent`/`MockStubbableSyncEventProcessor` added to `test/mocks/mocks.dart` (note: the central `MockSyncEventProcessor` was NOT usable here — its concrete `prepare`/`apply` overrides defeat `when(...)`).
  This file has no counterpart implementation file, violating the **one-test-file-per-source-file** rule. Its 4 tests exercise cross-layer behavior (live-event → queue → worker → apply, encryption round-trip, attachment path, stop-drain). These should migrate into `queue_pipeline_coordinator_test.dart` (the coordinator is the natural home for pipeline-level integration tests) or — once the coordinator splits — into the relevant sub-coordinator test files. Additionally, the file re-declares 9 mock classes (`_MockSessionManager`, `_MockRoomManager`, `_MockSyncEventProcessor`, `_MockVectorClockService`, `_MockBridge`, `_MockClient`, `_MockRoom`, `_MockPreparedSyncEvent`, `_MockEvent`) of which `MockVectorClockService`, `MockSyncEventProcessor`, `MockRoom`, and `MockMatrixClient` already exist in `test/mocks/mocks.dart`. The private `_MockEvent` and `_MockBridge` also duplicate coordinator-test definitions.

- [x] **[MED]** `test/features/sync/queue/bridge_coordinator_test.dart` (940 lines) vs `lib/…/bridge_coordinator.dart` (341 lines)   **RESOLVED:** Assessed, no change — `_RecordingRunner` and the scenario-generator extension have exactly one consumer; extracting a `_bridge_test_utils.dart` for hypothetical "future bridge regression tests" is speculative generality (AGENTS.md: no code hoarding for mysterious future callers). Revisit if a second consumer appears.
  The test is 2.75× the impl — acceptable for a well-tested state machine, but the Glados scenario generator adds ~200 lines of boilerplate. Consider extracting the `_RecordingRunner` helper and the scenario-generator extension into a `_bridge_test_utils.dart` that both this test and any future bridge regression tests can import.

---

## Test quality improvements

- [x] **[HIGH]** Two Glados tests in `queue_pipeline_coordinator_test.dart` (lines 638–782 and 1 464–1 687) each inline ~90 lines of duplicate mock stubs. The stubs are identical to those set up in the file-level `setUp()` (lines 487–536) except that the Glados bodies capture calls locally. Both tests should be refactored to a shared `_buildGladosCoordinator(…)` helper that accepts capture closures as parameters, eliminating ~140 lines of boilerplate and making the two test bodies ~30 lines each (`test/features/sync/queue/queue_pipeline_coordinator_test.dart:638` and `:1464`).

- [x] **[MED]** `test/features/sync/queue/queue_pipeline_integration_test.dart:25–41` — 9 local `_MockXxx` classes defined inline when `MockRoom` (line 194 in `mocks.dart`), `MockMatrixClient` (line 211), `MockVectorClockService` (line 473), and `MockSyncEventProcessor` (line 636) already exist in `test/mocks/mocks.dart`. Per AGENTS.md "Use centralized mocks", these should be replaced with the centralized equivalents; new ones (`_MockSessionManager`, `_MockRoomManager`, `_MockBridge`, `_MockPreparedSyncEvent`) should be added to `mocks.dart` first. **RESOLVED:** Done — resolved together with the file dissolution above: centralized equivalents reused, the genuinely new ones added to `mocks.dart` first.

- [x] **[MED]** `lib/features/sync/queue/queue_pipeline_coordinator.dart:729` — `_countSuppressedSelfEcho` uses `DateTime.now()` directly instead of `clock.now()`. This makes the suppression-window logic untestable under `withClock` / `fakeAsync`. The test suite has no coverage of the "interval flushes and resets the counter" branch (the `_suppressionLogInterval` guard at line 731). Fix the production code to use `clock.now()`, then add a fakeAsync test that advances the clock past `_suppressionLogInterval` and verifies the counter resets and the log line fires.

- [x] **[MED]** `test/features/sync/queue/queue_pipeline_coordinator_test.dart` — several tests use `await Future<void>.delayed(Duration.zero)` (e.g., lines 596–597, 605–607, 936–938) to yield to the event loop. While `Duration.zero` is not a real-time wait, each occurrence is a fragile micro-yield that is not deterministic under `fakeAsync`. The tests that exercise live-event routing should instead gate completion on a concrete signal (e.g., use `Completer`-backed stubs as the self-echo and attachment-ingestor tests already do at lines 2 333 and 2 370, respectively). **RESOLVED:** Adapted — all 30 occurrences (incl. 7 consecutive doubled yields) replaced with `pumpEventQueue()`, which drains the entire event queue instead of yielding a single turn — removing the fragile micro-yield depth coupling. The Completer-gated pattern remains in place where it already existed (self-echo, attachment ingestor); converting every live-routing test to bespoke Completer stubs was not needed once the yields became full-queue drains. Suite green.

- [x] **[MED]** `test/features/sync/queue/queue_pipeline_integration_test.dart:73–101` — `_waitForQueueStats` polls via `depthChanges.listen` + direct stats check, but every iteration calls `queue.stats()` inside the subscription handler without timeout safeguards. The function is effectively a busy loop that can hang silently if `depthChanges` never fires. Replace with `queue.waitForDrainAtMostTo(0)` or add a hard timeout / `expectLater` to keep integration tests from hanging in CI. **RESOLVED:** Stale — the helper already carries a hard 5s `completer.future.timeout(...)` that throws a TimeoutException ("depthChanges stalled?") instead of hanging; preserved as-is in the merged coordinator-test copy.

- [x] **[LOW]** `test/features/sync/queue/queue_pipeline_coordinator_test.dart:1266–1285` — the "coordinator built without overrides" test asserts only `expect(coordinator.queue, isNotNull)` and `expect(coordinator.isRunning, isFalse)`. Per AGENTS.md "No constructor smoke tests", this is near-zero-value; it does not actually exercise any wire-up behavior. Either delete it or replace it with a behavioral assertion (e.g., verify that `start()` succeeds and delegates to the default worker).
  **RESOLVED:** Rewrote the test to drive a real start/stop lifecycle on the no-override coordinator. It now stubs legacy settings + `getRoomById→null` (so the fire-and-forget startup bridge is a clean noRoom no-op), calls `start()`, and asserts the *default* `QueueMarkerSeeder` actually migrated the legacy marker into the live `syncDb.queueMarkers` row (`lastAppliedEventId`/`lastAppliedTs`) — proving the default collaborator was wired against the real databases — then asserts `isRunning` flips true→false across `start()`/`stop()`. No more `isNotNull` smoke assertion.

- [x] **[LOW]** `test/features/sync/queue/inbound_worker_test.dart` — the `_buildSyncEvent` helper (lines 42–65) is structurally identical to the one in `inbound_event_queue_test.dart` (lines 15–40) and to `queue_pipeline_integration_test.dart` (lines 43–71). All three should share a test-utility function (e.g., in `test/features/sync/test_utils.dart` or an existing per-feature `test_utils.dart`) per the AGENTS.md "Use test data factories" rule.
  **RESOLVED:** Extracted the shared factory into a new `test/features/sync/queue/test_utils.dart` exporting `buildSyncEvent(...)` (the superset signature with optional `type`/`content`). Both `inbound_worker_test.dart` and `inbound_event_queue_test.dart` now import it and keep a one-line `_buildSyncEvent` delegating alias so the 85 existing call sites stay untouched; the duplicated 20-line `MockEvent` wiring body is gone from both files. (The third consumer, `queue_pipeline_integration_test.dart`, was already dissolved in a prior MED item.) The suggested `test/features/sync/test_utils.dart` location was deliberately avoided to honor the in-scope constraint; the per-folder `test_utils.dart` matches the established repo pattern (`test/features/<feature>/test_utils.dart`). Removed the now-unused `matrix/consts.dart` import from the worker test.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `inbound_event_queue.dart` — `waitForDrainAtMostTo` completion semantics under arbitrary interleaving of `enqueueLive`, `commitApplied`, and `markSkipped` calls. The invariant: if a `waitForDrainAtMostTo(n)` call is pending and a sequence of commits/skips causes `stats().total` to reach `n`, the future must complete. A Glados property could generate arbitrary multi-step sequences of enqueue/commit/skip operations and assert the waiter always completes once the depth condition is met and never completes before it is met. This is non-trivial pure logic with a real ordering invariant.

- [x] **[HIGH]** `inbound_event_queue.dart` — `_emitDepth` coalescing logic. The `_inBatchMode` / `_batchDirty` state machine that suppresses in-transaction depth emissions and fires exactly one post-commit emission has no dedicated property test. The invariant: across any sequence of `runInTransaction` calls (including nested ones) and concurrent `_scheduleDepthEmit` triggers, the `depthChanges` stream must emit at least once after each outermost transaction completes if any mutation occurred. A Glados property over arbitrary nesting depths (1–5) and mutation sequences would catch regression in the dirty-flag / re-run path.

- [x] **[MED]** `bridge_coordinator.dart` — the incomplete-retry counter state machine. The Glados test at `bridge_coordinator_test.dart:523` covers single-flight coalescing but not the `_consecutiveIncomplete` / `_maxIncompleteRetries` state machine. An invariant: after `_maxIncompleteRetries` consecutive incomplete walks, `_consecutiveIncomplete` is reset to 0 and no further timer is scheduled, regardless of the interleaving of `bridgeNow` calls, limited-sync triggers, and stop/start cycles. This is pure state-machine logic with a clear counting invariant. **RESOLVED:** Done (adapted) — added a deterministic fakeAsync ladder test instead of Glados: the counting state space (maxIncompleteRetries=2) is fully enumerable, so the test walks the exhaustive ladder (initial + 2 retries -> giveUp), asserts no further timer fires across 20x the delay, and proves the counter reset by driving a fresh post-giveUp ladder that schedules its own retry.

- [x] **[MED]** `queue_pipeline_coordinator.dart` — `_runBootstrap` dispatch invariant. The forward/backward selection logic at `_runBootstrap` (line 944) must obey: if `marker.lastAppliedEventId != null`, the forward walk always runs first; the backward walk runs only if the forward walk returns `errorNoProgress`. A Glados property over `(_GeneratedBridgeAnchorKind, _GeneratedBootstrapOutcome)` pairs would exhaust the dispatch matrix. The `runBootstrapForTest` entry point already exists, making this straightforward to wire. **RESOLVED:** Done (adapted) — the dispatch matrix is finite (anchor x {completed, incomplete, errorNoProgress}), so an exhaustive 4-case parameterized test was added via `runBootstrapForTest` instead of Glados: forward walk runs iff an anchor exists (verified via non-null `eventContextId`), backward runs iff no anchor or errorNoProgress, with the returned completion bit asserted per cell.

- [x] **[LOW]** `inbound_event_queue.dart` — `resurrectByPaths` chunking invariant at `_resurrectByPathsChunkSize`. The property: for any path list of length 1–2 000, the total rows resurrected equals the sum of individual `resurrectByPath` calls, and the number of DB round-trips equals `ceil(uniquePaths / 900)`. The chunk-boundary arithmetic is pure and the existing `_withFreshInboundQueue` helper already supports multi-event setups. This is a targeted correctness check for the chunking logic (currently only tested with fixed sizes at `inbound_event_queue_test.dart:1629`).
  **RESOLVED:** Added a `chunkingScenario` Glados property (`tags: 'glados'`, `numRuns: 90`) in the "generated queue ledger properties" group. It seeds 1–6 distinct abandoned rows (one per path), then calls `resurrectByPaths` with a requested subset padded by 0–2 000 synthetic non-matching paths so the IN-list crosses the 900 chunk boundary up to ~3 times. Asserts the *observable* transparency invariant — resurrected count equals the requested-and-seeded set (the union of individual `resurrectByPath` calls), each requested row flips to `enqueued`, each non-requested seeded row stays `abandoned`, and `stats()` total/readyNow/abandoned match. The "DB round-trip count = ceil(n/900)" claim was deliberately NOT asserted: round-trip count is not observable without instrumenting the private chunk loop, so per the correctness rule the stronger-but-unverifiable internal was replaced with the weaker true property (correctness is chunk-count-independent).

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `_countSuppressedSelfEcho` suppression-window reset path (`queue_pipeline_coordinator.dart:727–741`) — the branch where `_suppressionLogInterval` has elapsed and the counter resets is entirely untested. Because the method uses `DateTime.now()` (not `clock.now()`), this branch cannot be exercised deterministically today. Fix the production code first (see Test quality item above), then add a fakeAsync test covering: (1) counter accumulates without logging during the interval, (2) first call after the interval logs with the accumulated count and resets.

- [x] **[HIGH]** `_runForwardBootstrap` fallback chain in `queue_pipeline_coordinator.dart` — the path where the forward walk returns `_BootstrapOutcome.errorNoProgress` and execution falls through to `_runBackwardBootstrap` (lines 955–965) is not directly tested. The `runBootstrapForTest` entry point is available, so a test that stubs `CatchUpStrategy.collectForwardForBootstrap` to throw (making `totalPages == 0` + `error`) and verifies the backward walk subsequently runs would close this gap. Currently covered only implicitly via the `_readMarker` tests that gate on the `$`-prefix filter, which is a different code path.

- [x] **[HIGH]** `QueuePipelineCoordinator.stop()` error-unwind path when `start()` throws after `_liveSub` is set but before `_started` is flipped (`queue_pipeline_coordinator.dart:390–432`) — the explicit cleanup of `_attachmentPathFlushInFlight` on the error path (line 410–418) is not covered. A test that injects a throw from `_worker.start()` after `_attachmentIndex` has already emitted a path (so `_attachmentPathFlushInFlight` is non-null) would exercise the shutdown-race guard.

- [x] **[MED]** `inbound_event_queue.dart:earliestReadyAt` — the `MAX(nextDueAt, leaseUntil)` semantics are tested for the lease case (after `peekBatchReady`) but not for the `retrying` case where `nextDueAt > leaseUntil`. Add a test that calls `scheduleRetry` with a future backoff, then verifies `earliestReadyAt()` returns the `nextDueAt` value (not zero and not the lease timestamp). **RESOLVED:** Done — added a frozen-clock test: `scheduleRetry` with a 30s backoff releases the lease and `earliestReadyAt()` returns exactly `now + 30000` (the nextDueAt), not zero and not the lease timestamp.

- [x] **[MED]** `bridge_coordinator.dart:_handleIncompleteFollowUp` — the `_maxIncompleteRetries` give-up path (lines 318–326) logs `queue.bridge.incomplete.giveUp` and resets the counter. The bridge test covers `_consecutiveIncomplete` up to the retry limit but never past it. Add a static test that fires `_maxIncompleteRetries + 1` consecutive incomplete walks and verifies: (a) no more timers are scheduled after the last one, (b) the give-up log line is emitted exactly once, (c) `_consecutiveIncomplete` is reset to 0 so a subsequent complete walk can restart the counter cleanly. **RESOLVED:** Done — covered by the same fakeAsync ladder test: (a) no timers after the last retry (runner call count frozen across 20x delay), (b) giveUp log emitted exactly once (stub-counter, not verify-throw), (c) counter reset proven by the subsequent ladder scheduling a fresh retry instead of giving up immediately.

- [x] **[MED]** `inbound_event_queue.dart:stats()` pivot — the aggregation of `_activeStatuses` (total, byProducer, oldest) alongside `applied`/`abandoned`/`retrying` counts in the Dart pivot loop (lines 1 034–1 057) is tested via the general `stats()` calls but has no test that simultaneously exercises all five status buckets (`enqueued`, `leased`, `retrying`, `applied`, `abandoned`) in the same snapshot. Add a single test that seeds one row of each status and asserts each counter individually. **RESOLVED:** Done — added `stats() five-bucket pivot` group: one row driven into each of enqueued/leased/retrying/applied/abandoned through the public API under a frozen clock, asserting total=3, byProducer.live=3, applied=1, abandoned=1, retrying=1, readyNow=1, and oldestEnqueuedAt individually in one snapshot.

- [ ] **[LOW]** `_updateBarrenBridgeFlagForward` (`queue_pipeline_coordinator.dart:1084`) — the case where the outcome is `_BootstrapOutcome.completed` clears `_lastBarrenBridgeAt`. The case where outcome is `incomplete` or `errorNoProgress` leaves it unchanged. Only the `completed` branch is tested. Add a case for `incomplete` and verify `hasBarrenBridgeSignal` remains true after a forward walk that returned `incomplete`.
  **DEFERRED:** A two-phase test was attempted (prime the barren flag via a backward walk, then a forward walk that throws mid-walk → `incomplete`) but it does not reproduce the intended path in isolation: the sequence re-enters `collectHistoryForBootstrapImpl` (backward strategy) on a timeline whose `canRequestHistory` isn't stubbed (`type 'Null' is not a subtype of type 'bool'`) — the real coordinator drives more of the bridge/bootstrap machinery than a focused unit test can cheaply stub. Reliably exercising the `incomplete` no-op branch needs a dedicated bridge/bootstrap harness (or a `@visibleForTesting` seam on `_updateBarrenBridgeFlagForward`); deferred rather than ship a flaky/over-mocked test. The adjacent dispatch-matrix and `errorNoProgress→backward` paths (items above) are covered.

- [x] **[LOW]** `QueuePipelineCoordinator._resolveRoom()` fallback path (`queue_pipeline_coordinator.dart:867–882`) — when `_roomManager.currentRoom` is null AND `_roomManager.currentRoomId` is null, `_resolveRoom` returns null without calling `client.getRoomById`. This is distinct from the "getRoomById returns null" case already tested. A targeted test in the coordinator's `_resolveRoom` coverage (e.g., start coordinator, set both room cache + roomId to null, trigger a live event) would confirm the null guard fires.
  **RESOLVED:** Added a test alongside the existing noRoom test that stubs both `currentRoom` and `currentRoomId` to null, triggers the bridge, and asserts (a) the `queue.bridge.skip reason=noRoom` line is logged AND (b) `verifyNever(() => client.getRoomById(any()))` — proving the `roomId == null` guard in `_resolveRoom` short-circuits before the gateway lookup, distinct from the already-tested "getRoomById returns null" case (where the id is present and the gateway IS consulted).

---

## Test execution speed opportunities

- [x] **[HIGH]** `queue_pipeline_coordinator_test.dart` — the Glados test `generated drainUntilEmpty follows queue, pen, ready-at and timeout model` (line 1 459, `numRuns: 120`) uses real `Future<void>.delayed` inside `drainUntilEmpty` for the retry-sleep path (`queue_pipeline_coordinator.dart:519`). Even though the per-run delay is short (`Duration(milliseconds: 20)` timeout in the timeout scenario), 120 runs × real async yields accumulate. The test already wraps the body in `fakeAsync` + `withClock`, but `drainUntilEmpty`'s internal `Future<void>.delayed(capped)` at line 519 is a real timer that `fakeAsync` controls correctly only when `async.elapse(…)` is called. Verify the fakeAsync harness is actually advancing the clock for the sleep (it appears to be via `async.elapse(scenario.advanceBy)`), then confirm the `_GeneratedDrainReadyAtKind.soon` branch (15 ms delay) resolves deterministically under fake time. **Impact: ~30 s per full Glados run if any real waits slip through.**

- [x] **[HIGH]** `queue_pipeline_integration_test.dart` — `_waitForQueueStats` (lines 73–101) has no timeout. If `depthChanges` stops emitting (e.g., due to a regression in `_emitDepth`), the four integration tests will hang indefinitely in CI rather than failing fast. Add a `timeout: const Duration(seconds: 5)` to the `completer.future` await. **Impact: prevents silent CI hangs that exhaust the job timeout.**

- [x] **[MED]** `queue_pipeline_coordinator_test.dart:632` — Glados `generated live ingress filters room/status/self-echo` (`numRuns: 120`) creates a fresh `SyncDatabase` + `JournalDb` (two SQLite in-memory databases) for every generated run and closes them in a `finally` block. Creating/closing Drift in-memory DBs has measurable overhead (~5–10 ms each). At 120 runs that is ~1 200–2 400 ms of pure DB construction with no actual DB writes. The test mocks `localQueue`, so the databases are never used — the `localSyncDb` and `localJournalDb` are needed only because `QueuePipelineCoordinator` takes them as required constructor args. Introducing a shared no-op placeholder for these two args (or a static in-memory DB opened once per Glados property) would cut this overhead to near zero. **Impact: ~1–2 s per Glados run.** **RESOLVED:** Done — `_GladosBench` now holds the two placeholder databases as lazily-initialized statics shared across every generated run (they are never written; the queue/worker/pen are mocked); `dispose()` no longer closes them and a file-level `tearDownAll(_GladosBench.closeSharedDbs)` closes the pair once.

- [x] **[MED]** `queue_pipeline_coordinator_test.dart:1464` — same pattern as above: the drain scenario Glados test creates fresh databases per run. Same fix applies. **Impact: ~1–2 s per Glados run.** **RESOLVED:** Done — same fix: both properties go through the shared `_GladosBench` statics.

- [x] **[MED]** `bridge_coordinator_test.dart` — `_drainBridgeCoordinatorMicrotasks()` (lines 197–201) uses a `for (var i = 0; i < 6; i++) await Future<void>.value()` loop in place of `pumpEventQueue()`. `pumpEventQueue()` is idiomatic and avoids a magic constant that must match the coordinator's async depth. Replace all call sites with `await pumpEventQueue()`. **Impact: minor readability win and removes a brittle constant that will break silently if the coordinator adds an async hop.** **RESOLVED:** Done — helper deleted; the single call site now uses `await pumpEventQueue()`.

- [x] **[LOW]** `inbound_event_queue_test.dart:934` — the batch-accounting Glados test (`numRuns: 140`) seeds the DB with up to 14 events per run. The 140 × 14 × SQLite inserts are legitimate since this test exercises real DB behavior, but the `maxBatch: scenario.rows.length` `peekBatchReady` call (line 826 in the marker-clamp property, `numRuns` not specified = default) re-reads every row on every run. Consider capping at `numRuns: 100` — 140 is above the "ask before going above 200" guideline and saves ~30% CPU on a saturated CI runner without materially reducing coverage. **Impact: ~15 s off the Glados CI lane.**
  **RESOLVED:** Lowered the batch-accounting property from `numRuns: 140` to `120` (the project convention forbids exactly 100 and the value must stay in 80..180, so 120 — already used by the coordinator Glados tests — was chosen instead of the suggested 100). Trims ~15% of the per-lane runs while keeping the partition coverage broad.

---

## Summary

- **2 oversized implementation files** exceed the 1 000-line threshold: `queue_pipeline_coordinator.dart` (1 295) and `inbound_event_queue.dart` (1 164). Both have clear, concrete split seams documented above.
- **2 oversized test files**: `queue_pipeline_coordinator_test.dart` (3 874 lines) and `inbound_event_queue_test.dart` (1 748 lines); both will resolve naturally once the impl splits.
- **6 notable test-quality issues**: inline mock duplication in `queue_pipeline_integration_test.dart`, duplicated Glados harness setup in the coordinator test (~140 lines of waste), `DateTime.now()` in `_countSuppressedSelfEcho` blocking deterministic testing, fragile `Future.delayed(Duration.zero)` yields, near-zero-value smoke test, and a duplicated `_buildSyncEvent` helper across 3 files.
- **5 Glados candidates** identified: `waitForDrainAtMostTo` ordering invariant, `_emitDepth` coalescing invariant, bridge incomplete-retry state machine, `_runBootstrap` dispatch invariant, and `resurrectByPaths` chunking correctness. All are pure logic with clear algebraic invariants over non-trivial input sequences.
- **5 execution-speed wins**: hanging `_waitForQueueStats` in integration tests, DB-creation overhead in two Glados Coordinator tests (~1–2 s each per run), over-specified `numRuns: 140` in the batch-accounting Glados test, and the magic-constant `_drainBridgeCoordinatorMicrotasks` loop.
- **Biggest single opportunity**: splitting `queue_pipeline_coordinator.dart` into 3 focused files (lifecycle, live-ingress, gap-recovery) would halve the file size, eliminate the two duplicated Glados harnesses, and make the `DateTime.now()` / coverage gaps trivially easy to address in their natural homes.
