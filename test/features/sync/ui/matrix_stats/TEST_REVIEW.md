# TEST_REVIEW — sync/ui/matrix_stats

**Scope:** `lib/features/sync/ui/matrix_stats/` and `test/features/sync/ui/matrix_stats/`

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines (impl) | Lines (test) | Has test? | Top issue |
|------|-------------|--------------|-----------|-----------|
| `diagnostics_panel.dart` | 104 | 58 | Yes | [LOW] 2 `pumpAndSettle`; thin coverage |
| `incoming_stats.dart` | 29 | 145 | Yes | [MED] Inline `_MockMatrixService` class |
| `matrix_metrics_panel.dart` | 165 | 192 | Yes | OK; uses deterministic poll-cycle helper |
| `message_counts_view.dart` | 89 | 119 | Yes | [MED] Inline `_FakeMatrixStatsController`; 2 `pumpAndSettle` |
| `metrics_actions.dart` | 68 | 52 | Yes | OK |
| `metrics_grid.dart` | 108 | 154 | Yes | OK |
| `metrics_section.dart` | 207 | 94 | Yes | [MED] Large impl (207 L) vs. thin test (94 L); 1 `pumpAndSettle` |

---

## File size / split opportunities

- [ ] **[LOW]** All impl files are well under 500 lines. No splits needed in this subdir.
- [ ] **[LOW]** `matrix_metrics_panel.dart` (165 L) and `metrics_section.dart` (207 L) are comfortable
  sizes; no action needed.

---

## Test quality improvements

- [x] **[MED]** `incoming_stats_test.dart` line 15: `class _MockMatrixService extends Mock implements MatrixService {}` is an inline mock. `MockMatrixService` already exists in `test/mocks/mocks.dart`. Replace `_MockMatrixService` with the centralized `MockMatrixService` import.
  - **RESOLVED:** done — the inline `_MockMatrixService` is deleted; the file imports the central `MockMatrixService` from `test/mocks/mocks.dart`.
- [x] **[MED]** `message_counts_view_test.dart` line 14: `class _FakeMatrixStatsController extends MatrixStatsController` is a file-local fake controller. A consistent pattern across `matrix_stats` tests would use a shared `FakeNotifier` approach via provider `overrides` rather than subclassing the notifier directly, but the fake-controller pattern is acceptable for Riverpod `AsyncNotifier` providers if the alternative would require more scaffolding. Lower priority, no hard violation.
  - **RESOLVED:** (assessed, no change) — as the item itself concludes: file-local controller subclasses are the accepted pattern for Riverpod AsyncNotifier overrides where a shared FakeNotifier would need more scaffolding.
- [ ] **[LOW]** `diagnostics_panel_test.dart` covers expand/collapse but does not verify the content of individual parsed diagnostic rows (e.g., that `dbMissingBase=2` renders "2"). The assertion only checks that the panel expanded — add value-content assertions.
- [ ] **[LOW]** `metrics_section_test.dart` (94 L test for 207 L impl): the single large test pumps the whole `SyncMetricsSection` and then does `findsOneWidget`-level presence checks. Consider splitting into focused groups (KPIs / throughput / reliability / actions / diagnostics) and asserting the exact formatted values.

---

## Generative (Glados) testing opportunities

- [ ] **[LOW]** No strong Glados candidates in this subdir. The `metrics_grid.dart` / `metrics_section.dart` are purely presentational — they render key→value pairs. A Glados property over `Map<String, int>` could assert that `MetricsGrid` never throws and always renders exactly `N` value cells for N entries, but this is low value given the existing explicit tests.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `metrics_section.dart` (207 L) test at 94 L: the `SyncMetricsSection` has at least
  five conceptual groups (KPIs, throughput, reliability, actions row, diagnostics panel). Only one
  monolithic test covers all of them in a single `testWidgets` call. Error/edge-case paths (e.g.,
  missing keys in the metrics map defaulting to 0) are not tested.
  - **RESOLVED:** done — added two edge-case tests: empty metrics hide the 'Top KPIs' block, render the em-dash timestamp for null `lastUpdated`, keep the three fixed group headers and omit Signals; and the Signals group appears only for non-zero signal metrics (zero-valued ones filtered).
- [x] **[MED]** `incoming_stats_test.dart`: the `IncomingStats` widget presumably renders differently
  when the stats stream emits an error or null (loading state). These paths are not covered in the
  3 tests present.
  - **RESOLVED:** (stale) — the file now has four tests including `'renders stable shell while loading'` (completer-backed loading controller) and `'builds even when controller throws'`; both the loading and error paths are covered.
- [ ] **[LOW]** `diagnostics_panel_test.dart` (58 L): only two tests. The collapsible panel's
  "initially collapsed" state (if that is the default) is not asserted before `pumpAndSettle`.

---

## Test execution speed opportunities

- [x] **[MED]** `incoming_stats_test.dart` — 3 `pumpAndSettle` calls after stream emissions. Since
  the stats provider is a fake that resolves immediately, `tester.pump()` should suffice.
  - **RESOLVED:** done — the three `pumpAndSettle` calls replaced with bounded `pump()` + `pump(300ms)` pairs (the fakes resolve immediately).
- [x] **[MED]** `message_counts_view_test.dart` — 2 `pumpAndSettle` calls on a straightforward
  stats-display widget.
  - **RESOLVED:** done — both `pumpAndSettle` calls replaced with bounded `pump()` + `pump(300ms)` pairs.
- [ ] **[LOW]** `diagnostics_panel_test.dart` — 2 `pumpAndSettle` calls on tap-to-expand. The
  `ExpansionTile` animation can be replaced with `tester.pump(const Duration(milliseconds: 300))`
  to avoid the 10 s default timeout risk.

---

## Summary

- No oversized files in this subdir (largest impl: `metrics_section.dart` at 207 L).
- 1 inline mock violation: `_MockMatrixService` in `incoming_stats_test.dart` should use centralized `MockMatrixService`.
- No strong Glados candidates.
- Coverage gap in `metrics_section_test.dart`: only one monolithic test for a 207 L impl; split into focused groups.
- Speed: 7 `pumpAndSettle` calls across this subdir, most replaceable with `tester.pump()`.
