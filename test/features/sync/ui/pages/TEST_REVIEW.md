# TEST_REVIEW — sync/ui/pages

**Scope:** `lib/features/sync/ui/pages/` and `test/features/sync/ui/pages/`

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines (impl) | Lines (test) | Has test? | Top issue |
|------|-------------|--------------|-----------|-----------|
| `conflicts/conflict_detail_route.dart` | 1213 | 1329 | Yes | **[HIGH]** Oversized impl (1213 L) + oversized test (1329 L) |
| `conflicts/conflicts_page.dart` | 161 | 273 (canonical) + 180 (duplicate) | Yes (2 files!) | **[HIGH]** `pages/conflicts_page_test.dart` duplicates `pages/conflicts/conflicts_page_test.dart` |
| `outbox/outbox_monitor_page.dart` | 259 | 582 (in `widgets/`) | Mislocated | [HIGH] Test lives at wrong path |
| `sync_node_profile_page.dart` | 232 | 337 | Yes | [MED] 44 `pumpAndSettle` calls |

---

## File size / split opportunities

- [x] **[HIGH]** `lib/features/sync/ui/pages/conflicts/conflict_detail_route.dart` (1213 lines).
  Clear split seams:
  - Header bar + picker pill (responsive breakpoint logic) — ~100 lines
  - Side-by-side diff card widgets (`_DiffCard`, `_DiffHeader`, `_MetaRow`, accent stripe) — ~300 lines
  - Footer "Apply" bar — ~100 lines
  - `_confirmAndApply` / `_gotoEditMerge` action handlers — ~80 lines
  - `ConflictDetailRoute` orchestrator — the remaining thin wrapper
  Splitting would allow the test to follow the same structure.
  **RESOLVED (adapted):** split via `part` files — `conflict_detail_chrome.dart` (scaffolds/header/banner, 364 lines), `conflict_detail_cards.dart` (diff cards + picker widgets, 454 lines), and `conflict_detail_footer.dart` (apply bar + edit-merge link, 210 lines); the route orchestrator file is down to 192 lines.
- [x] **[HIGH]** `test/features/sync/ui/pages/conflicts/conflict_detail_route_test.dart` (1329 lines)
  directly mirrors the impl complexity. The `_Bench` helper is well-structured, but test groups
  are large. After impl split, mirror-split the test.
  **RESOLVED (assessed, no change):** the impl split used `part` files (one library), so the single mirror test with its `_Bench` helper remains correct under the one-test-file-per-source rule.

---

## Test quality improvements

- [x] **[HIGH]** `test/features/sync/ui/pages/conflicts_page_test.dart` (180 L) is a second test
  file for `lib/features/sync/ui/pages/conflicts/conflicts_page.dart`. It covers some of the same
  widget-level scenarios as `pages/conflicts/conflicts_page_test.dart` (273 L). Per the
  one-test-file-per-source-file rule, merge all coverage into
  `pages/conflicts/conflicts_page_test.dart` and delete the misplaced file.
- [ ] **[MED]** `pages/conflicts_page_test.dart` line 21: directly calls
  `getIt.registerSingleton<UserActivityService>(UserActivityService())` — raw `getIt` manipulation
  outside `setUpTestGetIt()`. Migrate to `setUpTestGetIt()` + `tearDownTestGetIt()`.
- [ ] **[MED]** `sync_node_profile_page_test.dart`: 44 `pumpAndSettle` calls for a profile page.
  Many of these occur after tapping tabs or expansion tiles with no long-running animations.
  Replace with `tester.pump()` or `tester.pump(duration)`.
- [ ] **[LOW]** `conflict_detail_route_test.dart` lines 155–161: the `_pump` helper uses
  `makeTestableWidgetNoScroll` — correct; however, `addTearDown(() => tester.binding.setSurfaceSize(null))`
  is called but `setSurfaceSize` is deprecated in recent Flutter; verify this compiles cleanly
  with the pinned FVM version (no code change needed if it does, just a note).

---

## Generative (Glados) testing opportunities

- [ ] **[MED]** `conflict_detail_route.dart` contains a `_kStackedBreakpoint` constant (768 dp) that
  drives a responsive layout switch. A Glados property over random `Size` values can assert that
  widths ≥ 768 always render the desktop pill text (containing "fields differ") and widths < 768
  never do. This closes the responsive breakpoint boundary more thoroughly than the two hand-rolled
  mobile/desktop fixtures in the current test.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `outbox_monitor_page.dart` (259 lines) has its test (`outbox_monitor_test.dart`, 582 **RESOLVED:** relocated to `test/features/sync/ui/pages/outbox/outbox_monitor_page_test.dart` (same change recorded in the widgets-level review).
  L) at the wrong path (`test/features/sync/ui/widgets/`). The content appears thorough but the
  file layout means it is hard to locate and could be accidentally missed in coverage reports.
  Relocate to `test/features/sync/ui/pages/outbox/outbox_monitor_page_test.dart`.
- [ ] **[MED]** `conflict_detail_route_test.dart`: the "Apply" footer button's success path (selecting
  local, then confirming) is exercised, but the `_gotoEditMerge` navigation path (the "Edit &
  merge" action) only calls `_invalidateLocalEntry` + `beamToNamed` — no test verifies the
  local-entry future is invalidated after returning from the edit route.
- [ ] **[MED]** `conflicts/conflicts_page_test.dart`: check that the filter-tab state (resolved vs.
  unresolved) changes the stream subscription. The duplicate `conflicts_page_test.dart` and the
  canonical test may together cover this, but the split makes it ambiguous.
- [ ] **[LOW]** `sync_node_profile_page_test.dart`: the "capabilities" display (NodeCapability enum
  values) is not verified to appear as localized strings.

---

## Test execution speed opportunities

- [x] **[HIGH]** `sync_node_profile_page_test.dart` — 44 `pumpAndSettle` calls. At 10 s timeout each, **RESOLVED:** all 14 settles replaced with bounded `pump()` + `pump(300ms)` pairs; file passes. (The reported 44 predates earlier trims.)
  a slow-settling test risks timing out in CI. Replace with `tester.pump(duration)` for
  deterministic advancing.
- [ ] **[MED]** `conflict_detail_route_test.dart` — 44 `pumpAndSettle` calls. The `_Bench`-based
  stream-driven tests emit events then call `pumpAndSettle()` — most of these can be replaced
  with `tester.pump()` since stream-driven UI updates do not involve long animations.

---

## Summary

- 1 oversized impl: `conflict_detail_route.dart` (1213 L); 1 oversized test:
  `conflict_detail_route_test.dart` (1329 L).
- 2 structural violations: `conflicts_page_test.dart` at wrong path (duplicate of
  `conflicts/conflicts_page_test.dart`); `outbox_monitor_test.dart` mislocated under `widgets/`.
- 1 raw `getIt` registration outside `setUpTestGetIt()` in `conflicts_page_test.dart`.
- 1 Glados candidate: responsive breakpoint property for `ConflictDetailRoute`.
- Biggest speed win: 44 `pumpAndSettle` calls in `sync_node_profile_page_test.dart`, most
  replaceable with deterministic `tester.pump(duration)`.
