# TEST_REVIEW — sync/ui/provisioned

**Scope:** `lib/features/sync/ui/provisioned/` and `test/features/sync/ui/provisioned/`

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines (impl) | Lines (test) | Has test? | Top issue |
|------|-------------|--------------|-----------|-----------|
| `bundle_import_page.dart` | 312 | 850 | Yes | **[HIGH]** Test 2.7× impl; 24 `pumpAndSettle` calls |
| `provisioned_config_page.dart` | 457 | 1348 | Yes | **[HIGH]** Oversized test (1348 L); inline mock classes; 6 `pumpAndSettle` |
| `provisioned_status_page.dart` | 332 | 656 | Yes | [MED] 14 `pumpAndSettle`; inline mock classes duplicated across files |
| `provisioned_sync_modal.dart` | 95 | 249 | Yes | [MED] 4 `pumpAndSettle`; `MockClient` inline |

---

## File size / split opportunities

- [x] **[HIGH]** `test/features/sync/ui/provisioned/provisioned_config_page_test.dart` (1348 lines) **RESOLVED:** a main-scope `pumpConfigWidget(tester, state, {extraOverrides})` helper now collapses all 22 inline `pumpWidget`/`makeTestableWidgetWithScaffold`/override blocks. The file split is correctly rejected by the item itself (one-test-per-source rule).
  is the most extreme case in this subdir: a 457 L impl gets 1348 L of tests (~3× ratio). The
  test file is large because it covers the provisioning flow's many states (connected / loading /
  error / multiple ProvisioningError variants). Consider grouping into smaller focused files if
  the impl is split, but per the one-test-per-source-file rule do NOT split until the impl splits.
  Immediate improvement: extract a `_pumpProvisionedConfigPage` helper that collapses the 8+
  instances of inline `ProviderScope` / `Consumer` / `makeTestableWidgetWithScaffold` setup.
- [x] **[MED]** `test/features/sync/ui/provisioned/bundle_import_page_test.dart` (850 lines) for a
  312 L impl. The bulk comes from QR-code scan flow tests. The `_FakeMethodChannelMobileScanner`
  at line 25 is a complex platform-channel fake; extracting a `_BundleImportBench` class
  (similar to the `_Bench` pattern in `conflict_detail_route_test.dart`) would consolidate the
  repeated mock channel setup.
  **RESOLVED (adapted):** the repeated platform-flag + fake-scanner preamble is now one `setUpMobileScanner()` helper (5 folds, teardown-safe); a full bench class would add little beyond it since the per-test barcode scripts genuinely vary.

---

## Test quality improvements

- [x] **[HIGH]** `provisioned_config_page_test.dart` declares three inline mocks at lines 28–32 **RESOLVED:** `MockKeyVerificationRunner` added to `test/mocks/mocks.dart` (`MockKeyVerification`/`MockDeviceKeys` already lived there); the inline copies in `provisioned_config_page_test`, `provisioned_status_page_test`, `verification_modal_test`, and `incoming_verification_modal_test` are deleted.
  (`MockDeviceKeys`, `MockKeyVerificationRunner`, `MockKeyVerification`) and
  `provisioned_status_page_test.dart` declares the same three at lines 27–31. These are identical
  class definitions repeated across two files. All three should be moved to `test/mocks/mocks.dart`
  and imported. The `verification_modal_test.dart` (in `widgets/matrix/`) declares them a third
  time — centralizing eliminates all three copies simultaneously.
- [x] **[MED]** `provisioned_config_page_test.dart` line 1013 comment: `// Use pump+duration rather than pumpAndSettle to avoid timeout from …` — a correct instinct applied in one place. Apply the same reasoning to the remaining 5 `pumpAndSettle` calls in the file.
  **RESOLVED:** the remaining 5 settles in the file are bounded `pump()` + `pump(300ms)` pairs; all 40 tests green.
- [x] **[MED]** `bundle_import_page_test.dart` — 24 `pumpAndSettle` calls. The scan-flow tests go
  through multiple navigation steps and state transitions, each followed by `pumpAndSettle`. Audit
  each one: after a successful `tester.tap()` on a button that triggers an async provider update,
  `tester.pump()` is usually sufficient.
  **RESOLVED (stale):** the file has zero `pumpAndSettle` calls — already converted under the HIGH speed item.
- [x] **[MED]** `provisioned_status_page_test.dart` — 14 `pumpAndSettle` calls. Many occur after
  stream emissions (`matrixUnverifiedProvider` state changes). These are instant since
  `_FakeMatrixUnverifiedController.build()` returns immediately; `tester.pump()` suffices.
  **RESOLVED:** 11 of 14 settles are now bounded pumps; the three sites that pop routes / close modals (disconnect-confirm, close-button pop, auto-verification finally) keep `pumpAndSettle` — they drain genuine route transitions and fail with bounded pumps.
- [x] **[LOW]** `provisioned_sync_modal_test.dart` line 14: `class MockClient extends Mock implements Client {}` — check if `MockClient` exists in `test/mocks/mocks.dart`; if so, remove the inline declaration.
  **RESOLVED:** `test/mocks/mocks.dart` already defines `MockMatrixClient extends Mock implements Client` (the same seam, named `MockMatrixClient` and used by ~25 other sync tests). The inline `MockClient` in both `provisioned_sync_modal_test.dart` and `provisioned_status_page_test.dart` is deleted and replaced with the centralized `MockMatrixClient`; the now-unused `package:matrix/matrix.dart` import was dropped from the modal test (the status test still needs it for `DeviceKeys`).

---

## Generative (Glados) testing opportunities

- [x] **[MED]** `provisioned_config_page.dart` processes a `SyncProvisioningBundle` which contains
  homeServer, user, password, roomId strings. A Glados property over `(String, String, String, String)` can assert that any combination of well-formed strings does not cause the provisioning
  controller's state machine to enter an undefined state. Since this touches real controller logic
  (not just UI), scope to unit tests of `ProvisioningController`.
  **RESOLVED (stale):** the controller test's 'cross-field validation rejects exactly the corrupted field' Glados (numRuns 160) already drives generated homeServer/user/password/roomId strings through `decodeBundle` — valid combinations land in `bundleDecoded`, corrupted ones throw exactly the typed FormatException; no undefined state is reachable.
- [ ] **[LOW]** `provisioned_status_page.dart`: the display of unverified device lists is tested with
  fixed fixtures (0 devices, 1 device, 2 devices). A Glados property over `List<MockDeviceKeys>`
  of length 0–10 can assert the device list always renders `N` `DeviceCard` widgets and never
  overflows.
  **DEFERRED:** the actual list renderer is the `UnverifiedDevices` widget in
  `lib/features/sync/ui/unverified_devices_page.dart` (it does the `unverifiedDevices.map(... DeviceCard ...)`),
  whose one-test-per-source home is `test/features/sync/ui/unverified_devices_page_test.dart` — OUTSIDE this
  area dir. Driving the property through `ProvisionedStatusWidget` instead is unsound: that widget also mounts
  `_AutoVerificationLauncher`, which fires a real `showVerificationModalSheet` for any non-empty list (covering
  the page with a modal) and needs per-device `keyVerificationRunner`/`KeyVerification` stubs, so a "renders
  exactly N DeviceCards" assertion would not hold. The correct Glados target is out of scope; deferred to the
  unverified-devices/device-card test owner.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `provisioned_config_page_test.dart`: the `ProvisioningError` enum has multiple
  variants. Check that all error variants (e.g., `loginFailed`, `roomNotFound`, `invalidBundle`,
  network errors) map to distinct error strings displayed in the UI. The test at ~line 1284 covers
  some but the full enum space may not be exercised.
  **RESOLVED (stale):** the enum has exactly two variants (`loginFailed`, `configurationError`) and both are asserted against their distinct localized messages in this file (~lines 381–412); the other names the item guesses at do not exist.
- [x] **[MED]** `bundle_import_page_test.dart`: the cancel flow (user starts QR scan then cancels
  before a valid code is read) should be tested explicitly.
  **RESOLVED (stale):** the scan-toggle test covers it — scan button tap shows `MobileScanner`, tapping again before any barcode cancels and removes it.
- [x] **[LOW]** `provisioned_sync_modal_test.dart`: no test for the case where `MatrixService.login`
  returns `false` (login attempt fails silently vs. throws).
  **RESOLVED (non-actionable):** the modal (`provisioned_sync_modal.dart`) never calls `login()` — its
  `onTap` only reads `isLoggedIn()` + `syncRoomId` for smart page detection. `login(waitForLifecycle: false)`
  is invoked solely by `ProvisioningController` (`provisioning_controller.dart` lines 132/141), and
  `test/features/sync/state/provisioning_controller_test.dart` already stubs that call across 11 sites
  including the login-returns-false path. A login-false test does not belong in the modal test.

---

## Test execution speed opportunities

- [x] **[HIGH]** `bundle_import_page_test.dart` — 24 `pumpAndSettle` calls. Even if some are **RESOLVED:** all 18 settles replaced with bounded `pump()` + `pump(300ms)` pairs; file passes.
  necessary for async provider resolution, grouping by step (scan setup, validation, import) and
  using `tester.pump()` after each emission would remove the 10 s-timeout risk from ~20 of them.
  Estimated saving on a single CI shard: **several seconds** per test run.
- [x] **[MED]** `provisioned_status_page_test.dart` — 14 `pumpAndSettle` calls after
  `_FakeMatrixUnverifiedController` emissions that resolve synchronously. All 14 can become
  `tester.pump()`.
  **RESOLVED:** same change as the quality item above — 11 bounded pumps, 3 genuine route-transition settles kept.
- [x] **[MED]** `provisioned_config_page_test.dart` — 5 `pumpAndSettle` calls remaining after the
  inline comment correctly avoids one.
  **RESOLVED:** all 5 converted to bounded pumps (see the quality item above).
- [x] **[LOW]** `provisioned_sync_modal_test.dart` — 4 `pumpAndSettle` calls.
  **RESOLVED (kept, justified):** all 4 follow `tester.tap(ProvisionedSyncSettingsCard)`, which opens a
  Wolt multi-page modal sheet — a genuine route transition driven by the third-party package's internal
  animation timing (not asserting an unverified duration, per the correctness rule). One of them
  (status-page-when-logged-in) also awaits the real `provisioningControllerProvider.regenerateHandover()`
  before the QR image appears, which a fixed bounded pump cannot reliably cover. None of the opened pages run
  a looping animation (the only `CircularProgressIndicator`, in `_HandoverQrSection`, is transient), so there
  is no 10s-timeout hang risk. This mirrors the status-page resolution that kept route-transition settles.

---

## Summary

- 1 oversized test: `provisioned_config_page_test.dart` (1348 L); `bundle_import_page_test.dart`
  (850 L) is also significantly over-length for its impl.
- 3 duplicate inline mock-class clusters (`MockDeviceKeys`, `MockKeyVerificationRunner`,
  `MockKeyVerification`) repeated in config + status + widgets/matrix test files — should be
  centralized in `test/mocks/mocks.dart`.
- No strong Glados candidates in the UI layer; provisioning controller logic is a better target.
- Biggest speed win: 24 `pumpAndSettle` → `tester.pump()` conversions in
  `bundle_import_page_test.dart` and 14 in `provisioned_status_page_test.dart`.
