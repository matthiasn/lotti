# TEST_REVIEW — sync/ui/view_models

**Scope:** `lib/features/sync/ui/view_models/` and `test/features/sync/ui/view_models/`

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines (impl) | Lines (test) | Has test? | Top issue |
|------|-------------|--------------|-----------|-----------|
| `outbox_list_item_view_model.dart` | 195 | 852 | Yes | **[HIGH]** Test file 852 L: each payload-variant case pumps a full widget tree independently; high redundancy |
| `conflict_list_item_view_model.dart` | 109 | 193 | Yes | [MED] Glados opportunity on `conflictIdShort` truncation and `_entityLabel` mapping |

---

## File size / split opportunities

- [x] **[MED]** `test/features/sync/ui/view_models/outbox_list_item_view_model_test.dart` (852 lines)
  is nearly 4× the impl. The core issue is structural: each of the ~15 payload-kind variant
  tests pumps a fresh `makeTestableWidgetNoScroll` / `Builder` / `OutboxListItemViewModel.fromItem`
  scaffold. A shared helper (`pumpOutboxBundle` at line 446 covers only two cases). Extract a
  single `_pumpAndCapture(tester, OutboxItem)` helper and use it for all variant cases. This
  would halve the line count without reducing coverage.
  **RESOLVED:** done — the remaining per-variant bodies were data-driven: the 7 'remaining sync message variants' tests collapsed into a `variantCases` loop over the shared `pumpWithMessage` helper, the byte table became a pure `formatBytes` test, and the new retries/attachment tests share `makeItem`. The file now has one pump-helper per group and no duplicated scaffolds.

---

## Test quality improvements

- [x] **[HIGH]** `outbox_list_item_view_model_test.dart`: the payload-kind variant tests at lines **RESOLVED:** the 7 standalone payload-kind tests are generated from a `payloadKindCases` spec list with shared `pumpPayloadItem`/`payloadItem` helpers — one body, seven cases.
  115–773 are 14+ nearly-identical test bodies that each build a full widget tree to resolve a
  localization context. The only variation is the JSON payload and the expected
  `payloadKindLabel`. Collapse into a single parameterized loop (see `outboxBundleCases` pattern
  at lines 468–505 for an existing example that already does this correctly). Remaining
  individual tests should be refactored similarly.
- [x] **[MED]** `outbox_list_item_view_model_test.dart` — the `OutboxStatus.sending` group at line
  510 correctly uses one loop, but the surrounding `_payloadKindLabel remaining sync message
  variants` group at line 608 uses a shared helper `pumpWithMessage` yet still instantiates
  individual `testWidgets` per variant instead of one parameterized loop. Consolidate.
  **RESOLVED:** done — the group now declares a `variantCases` list of (name, message factory, expected-label selector) records and generates the seven tests from one loop body over `pumpWithMessage`.
- [x] **[MED]** `conflict_list_item_view_model_test.dart` line 165: the entity-label spot-check loop
  covers 5 types but the `_entityLabel` map in the impl has 13 entries
  (`conflict_list_item_view_model.dart` lines 87–101). The remaining 8 entry types
  (`JournalEvent`, `JournalImage`, `MeasurementEntry`, `SurveyEntry`, `WorkoutEntry` already
  covered by testWorkoutRunning, `HabitCompletionEntry`, `Checklist`, `ChecklistItem`,
  `AiResponse`) are not exercised. Extend the parameterized loop to cover all 13.
  **RESOLVED:** done — the loop now covers all 13 `_entityLabel` map entries (fixtures where available, minimal inline entities for JournalEvent/Checklist/ChecklistItem/AiResponse/SurveyEntry). Bonus: this exposed a real impl bug — the map key 'AiResponse' never matched the actual freezed runtime type `AiResponseEntry`, so AI-response conflicts always fell back to the raw type name; the key is fixed and the loop now pins the localized label.
- [ ] **[LOW]** `outbox_list_item_view_model_test.dart` `payloadSizeLabel` group (line 775): the
  `makeItem` factory builds an `OutboxItem` but pumps a full widget tree just to call a static
  formatting method. `_formatBytes` is a pure static method — the byte-range tests (B / KB / MB /
  GB thresholds) can be plain `test()` calls with no widget pump, saving ~4 widget-tree builds.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `outbox_list_item_view_model.dart` `_formatBytes` (lines 179–189) is a pure, purely **RESOLVED:** `_formatBytes` is exposed as `@visibleForTesting static formatBytes` and four Glados properties (120 runs each, `tags: 'glados'`) cover the B/KB/MB/GB threshold branches with round-trip-within-rounding assertions, plus the null→null case.
  deterministic function over `int? bytes` with three threshold branches (B / KB / MB / GB).
  A Glados property asserting:
  - `bytes == null` → `null` result
  - `bytes ∈ [0, 1023]` → result ends with `' B'`
  - `bytes ∈ [1024, 1048575]` → result ends with `' KB'`
  - `bytes ≥ 1048576` → result ends with `' MB'` or `' GB'`
  covers all branches without a widget tree. Tag `tags: 'glados'`.
- [x] **[MED]** `outbox_list_item_view_model.dart` `_buildRetriesLabel` (lines 128–139): pure function
  over `(int retryCount, String locale, AppLocalizations)`. A Glados property can assert that
  singular `retryCount == 1` always produces `'1 Retry'` (English) and any other non-negative
  count produces `'N Retries'`. Singular/plural invariant holds across all non-negative integers.
  Requires a widget pump to obtain `AppLocalizations`, but the core logic is testable with a
  direct call.
  **RESOLVED:** done (adapted) — exhaustive matrix beats Glados for the binary singular/plural rule: one pump builds view models for retries 0/1/2/42 from a single context and asserts `['0 Retries', '1 Retry', '2 Retries', '42 Retries']`.
- [x] **[MED]** `conflict_list_item_view_model.dart` `conflictIdShort` truncation rule (lines 52–54):
  pure string logic. A Glados property `(String id) → id.length <= 8 ? short == id : short.length == 8`
  covers the boundary exhaustively.
  **RESOLVED:** done — the truncation rule is now `@visibleForTesting static shortenConflictId` and a Glados property (numRuns 200) asserts the exact boundary: ≤8 chars pass through identically, longer ids yield the 8-char prefix.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `outbox_list_item_view_model_test.dart`: no test for the `attachmentValue` path when
  `filePath` is non-null and non-empty (the "has attachment" branch, impl line 90). Only the
  "no attachment" branch (`filePath == null`) is exercised through existing tests.
  **RESOLVED:** done — added the has-attachment branch test: a padded non-empty `filePath` surfaces trimmed as `attachmentValue` with `Icons.attachment_rounded`.
- [x] **[MED]** `outbox_list_item_view_model_test.dart`: `payloadSizeLabel` group at line 775 tests
  null + values in B, KB, MB ranges. The GB branch (`bytes >= 1073741824`) has no test.
  **RESOLVED:** stale — the GB branch is covered twice: the byte-threshold table includes 1073741824 → '1.00 GB' and 1610612736 → '1.50 GB', and a dedicated GiB-range Glados property asserts the ' GB' suffix with round-trip-within-rounding.
- [ ] **[LOW]** `conflict_list_item_view_model_test.dart`: unknown `runtimeType` (a type string not
  in the `_entityLabel` map) returns the raw type name — this fallback path at line 102 is not
  tested.

---

## Test execution speed opportunities

- [x] **[HIGH]** `outbox_list_item_view_model_test.dart` — 14+ `await tester.pumpWidget(...) / await **RESOLVED:** same change as the parameterization above — the 7 duplicated pumpWidget bodies collapsed into one loop body.
  tester.pump()` pairs, one per payload-variant test. Collapsing into a parameterized loop means
  one widget tree is reused per loop iteration (Flutter test runner reuses the `tester` across
  subtests in a loop), roughly halving widget-tree rebuild time for this group. Estimated saving:
  **10–12 widget-tree builds** per CI run.
- [x] **[MED]** `payloadSizeLabel` pure-function tests (4 tests × widget pump): convert to plain
  `test()` once `_formatBytes` is called directly, eliminating 4 widget-tree builds.
  **RESOLVED:** done — the byte-threshold table is now a plain `test()` calling `formatBytes` directly (zero widget builds); a single widget test remains to prove `payloadSize` is wired through to `payloadSizeLabel`.

---

## Summary

- No impl files oversized; test file `outbox_list_item_view_model_test.dart` (852 L) is the main
  over-length item due to per-variant copy-paste.
- 3 weak-quality patterns: 14 nearly-identical payload-variant tests, 8 entity-label types
  uncovered, no attachment-present path tested.
- 3 strong Glados candidates: `_formatBytes`, `_buildRetriesLabel` singular/plural, and
  `conflictIdShort` boundary — all pure functions that need no widget tree.
- Biggest speed win: collapsing the 14 payload-variant tests into one parameterized loop,
  eliminating ~12 redundant widget-tree builds.
