# TEST_REVIEW — sync/ui/widgets

**Scope:** `lib/features/sync/ui/widgets/` and `test/features/sync/ui/widgets/`

Generated by automated test-quality review (static analysis).

---

## File inventory

| File | Lines (impl) | Lines (test) | Has test? | Top issue |
|------|-------------|--------------|-----------|-----------|
| `sync_list_scaffold.dart` | 727 | 658 | Yes | [MED] Approaching size limit; 727 L impl |
| `sync_activity_indicator.dart` | 315 | 378 | Yes | [MED] 378 L test; need to check for `pumpAndSettle` overuse |
| `sync_feature_gate.dart` | 38 | 83 | Yes | OK |
| `conflicts/conflict_list_item.dart` | 195 | 206 | Yes (widget test at `widgets/conflict_list_item_test.dart`) | [HIGH] Misplaced test file |
| `conflicts/title_diff.dart` | 203 | 295 + 233 | Yes (2 files!) | **[HIGH]** Two test files cover same source; one is Glados-only, one is structural |
| `matrix/device_card.dart` | 108 | 299 | Yes | [MED] 13 `pumpAndSettle` calls; inline `MockDeviceKeys` |
| `matrix/diagnostic_info_button.dart` | 61 | 154 | Yes | [MED] 5 `pumpAndSettle` calls |
| `matrix/incoming_verification_modal.dart` | 288 | 631 | Yes | **[HIGH]** `Future.delayed` real-time usage in production code; inline mock classes |
| `matrix/status_indicator.dart` | 34 | 76 | Yes | OK |
| `matrix/sync_flow_section.dart` | 37 | 84 | Yes | OK |
| `matrix/verification_emojis_row.dart` | 49 | 107 | Yes | OK; Glados opportunity |
| `matrix/verification_modal.dart` | 296 | 598 | Yes | [MED] Inline mock classes; 1 `pumpAndSettle`; `tester.pump(Duration(seconds: 30))` |
| `matrix/verification_modal_sheet.dart` | 14 | 31 | Yes | OK |
| `outbox/outbox_list_item.dart` | 254 | 235 | Yes | [MED] Widget test at `widgets/outbox/outbox_list_item_test.dart` |
| `outbox/outbox_volume_chart.dart` | 85 | 136 | Yes | OK |
| **ORPHAN**: n/a | n/a | 295 (`title_diff_test.dart` at `widgets/` root) | No canonical impl at `widgets/` root | **[HIGH]** Split/duplicate violation |
| **ORPHAN**: n/a | n/a | 196 (`outbox_badge_test.dart`) | Tests `lib/features/settings/ui/pages/outbox/outbox_badge.dart` | [MED] Mislocated |
| **ORPHAN**: n/a | n/a | 582 (`outbox_monitor_test.dart`) | Tests `lib/features/sync/ui/pages/outbox/outbox_monitor_page.dart` | [MED] Should live in `pages/outbox/` |
| **ORPHAN**: n/a | n/a | 206 (`conflict_list_item_test.dart` at `widgets/` root) | Tests `widgets/conflicts/conflict_list_item.dart` | [HIGH] Mislocated |

---

## File size / split opportunities

- [x] **[MED]** `lib/features/sync/ui/widgets/sync_list_scaffold.dart` (727 lines) is well below the
  hard limit but growing. Candidate seam: the scaffold's header section is separable from the **RESOLVED:** Assessed, no change — 727 lines is under the hard limit; revisit when the file next grows (same conclusion as the LOW note in the parent sync/ui review).
  list-body section.
- [x] **[MED]** `test/features/sync/ui/widgets/matrix/incoming_verification_modal_test.dart` (631
  lines) — the test bench pattern is partially present but some tests still inline their own
  setup. Consider a `_IncomingVerificationBench` class similar to the `_Bench` in **RESOLVED:** Done — added a shared `pumpModalWithRunner(tester, runner)` helper (pump wrapper + stream emit + frame) and converted all 12 inline copies; the runner stubs stay per-test since they ARE the scenario.
  `conflict_detail_route_test.dart`.

---

## Test quality improvements

- [x] **[HIGH]** `test/features/sync/ui/widgets/title_diff_test.dart` (295 L) and
  `test/features/sync/ui/widgets/conflicts/title_diff_test.dart` (233 L) both test
  `lib/features/sync/ui/widgets/conflicts/title_diff.dart`. The canonical mirror path is
  `widgets/conflicts/title_diff_test.dart`. The root-level `widgets/title_diff_test.dart`
  contains the Glados property test (tagged `glados`) plus static examples. The
  `conflicts/title_diff_test.dart` tests the `TitleDiffSegment` value object.
  These should be merged into one file at `widgets/conflicts/title_diff_test.dart`.
- [x] **[HIGH]** `test/features/sync/ui/widgets/conflict_list_item_test.dart` (206 L) tests **RESOLVED:** relocated to `test/features/sync/ui/widgets/conflicts/conflict_list_item_test.dart` (imports adjusted).
  `lib/features/sync/ui/widgets/conflicts/conflict_list_item.dart` but lives at the wrong
  path. Mirror rule requires `widgets/conflicts/conflict_list_item_test.dart`. Relocate.
- [x] **[HIGH]** `test/features/sync/ui/widgets/outbox_monitor_test.dart` (582 L) tests **RESOLVED:** relocated to `test/features/sync/ui/pages/outbox/outbox_monitor_page_test.dart`, matching the impl path and the one-test-per-source naming.
  `lib/features/sync/ui/pages/outbox/outbox_monitor_page.dart` — this belongs under
  `test/features/sync/ui/pages/outbox/`.
- [x] **[MED]** `test/features/sync/ui/widgets/outbox_badge_test.dart` tests
  `lib/features/settings/ui/pages/outbox/outbox_badge.dart` — it belongs under **RESOLVED:** Done — the canonical mirror at `test/features/settings/ui/pages/outbox/outbox_badge_test.dart` already existed; the two unique scenarios from the misplaced file (loading and error connection-state -> bare icon) were transplanted into it (adapted to the provider-level harness) and the misplaced file deleted.
  `test/features/settings/ui/pages/outbox/`.
- [x] **[MED]** `verification_modal_test.dart` and `incoming_verification_modal_test.dart` both
  declare `MockKeyVerificationRunner` and `MockKeyVerification` inline. These should be added to **RESOLVED:** Stale + completed — `MockKeyVerificationRunner`/`MockKeyVerification`/`MockDeviceKeys` were already central; this pass also centralized the remaining duplicated `FakeKeyVerificationEmoji` into mocks.dart and removed both file-local copies.
  `test/mocks/mocks.dart` and imported from there. `incoming_verification_modal_test.dart` also
  declares a third `MockDeviceKeys` at line 624 (after the file-level `FakeKeyVerificationEmoji`)
  that duplicates the one defined at line 0 of `verification_modal_test.dart`.
- [x] **[MED]** `verification_modal_test.dart` line 187: `await tester.pump(const Duration(seconds: 30))`
  — advancing 30 real virtual seconds to handle an auto-dismiss timer. This is a real-timer **RESOLVED:** Assessed, no change — premise misreads the cost: `tester.pump(30s)` inside testWidgets advances FAKE time (testWidgets already runs under FakeAsync) and costs no wall-clock; 30s is the exact duration of the production auto-dismiss Timer, and pumping less would leave the timer pending at teardown.
  pattern; use `fakeAsync` or pump only the minimum duration needed.
- [x] **[MED]** `device_card_test.dart` — 13 `pumpAndSettle` calls on a card widget. Tap + settle **RESOLVED:** Stale — the file has zero `pumpAndSettle` calls left (the HIGH-phase fix replaced all 13 with bounded pumps).
  interactions for a `DeviceCard` should not require full animation settling; replace with
  `tester.pump()` after interactions.
- [x] **[LOW]** `incoming_verification_modal_test.dart` line 438: tests a `Future.delayed` path in
  production code. The comment acknowledges the real delay; if the production `IncomingVerificationModal`
  uses a real `Future.delayed`, that is a fake-time policy violation in the impl itself (not just
  the test). The impl should be refactored to accept a `Duration` or use a ticker so the test can
  drive time deterministically.
  **RESOLVED:** Assessed, no change — premise is mis-stated. The `Future.delayed` lives in the
  production `refreshUnverifiedDevices` retry loop (`incoming_verification_modal.dart` lines 71-78),
  and the test that drives it is at line 332 (not 438 — that's the lock-release test). Inside
  `testWidgets` the binding already runs under FakeAsync, so `Future.delayed` registers a timer on
  the test's fake clock and `tester.pump(500ms)` fires it deterministically with zero wall-clock
  cost — there is no fake-time policy violation (that policy targets real `Timer`/`Future.delayed`
  outside the test binding). The test already drives time deterministically and asserts the loop
  traversed the delay path (`callCount` > 2). Injecting a `Duration` into the impl is a production
  refactor with no test benefit here and is out of scope.

---

## Generative (Glados) testing opportunities

- [x] **[HIGH]** `verification_emojis_row.dart` renders `Iterable<KeyVerificationEmoji>?`. A Glados **RESOLVED:** the cell construction was extracted to a pure `@visibleForTesting static buildEmojiCells()` (the widget's `build` now delegates to it), and a `Glados<List<int>>` property (indices 0–63, 120 runs, `tags: 'glados'`) asserts exactly one cell per emoji carrying that entry's glyph and name, plus the null→empty case. The existing widget tests keep covering the Wrap wiring/alignment and the narrow-viewport wrap behavior.
  property over `List<int>` (emoji indices) can assert:
  - `null` input → `Wrap.children.isEmpty`
  - Non-null list of length N → `Wrap.children.length == N`
  - All `Wrap.alignment == WrapAlignment.center`
  This is already partially covered by hand-rolled tests in `verification_emojis_row_test.dart`;
  a Glados sweep removes the hand-rolled length cases. Tag `tags: 'glados'`.
- [x] **[MED]** `computeTitleDiff` in `title_diff.dart` already has a Glados property at
  `widgets/title_diff_test.dart:243`. Ensure the two test files are merged so the Glados test **RESOLVED:** Stale (verified) — the merged `conflicts/title_diff_test.dart` retains the full 'preserves generated token-side invariants' property (tags: glados) including generators and the adjacent-same-kind helper.
  remains (not inadvertently lost during consolidation).

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `sync_activity_indicator.dart` (315 lines) — no visible test for the error state
  or the "no data" placeholder (if any). Check if `sync_activity_indicator_test.dart` covers **RESOLVED:** Assessed (premise wrong) — the widget has no distinct error/no-data state: it reads `.value ?? 0` on both stream providers, so loading and error collapse to the zero-count rendering, which the existing empty/zero tests cover.
  all three states (loading, data, error).
- [x] **[MED]** `outbox_monitor_test.dart` (582 L) tests the full outbox page but lives in the wrong
  directory. Verify it fully covers `outbox_monitor_page.dart`'s filter tabs, sort actions, and **RESOLVED:** Stale (verified) — the relocated `pages/outbox/outbox_monitor_page_test.dart` covers filter tabs + switching, empty state, pre-fetch loader, fetch-failure toast, delete/retry success+failure toasts, refetch after delete, and the volume chart integration.
  empty-state paths.
- [x] **[LOW]** `verification_modal_sheet.dart` (14 lines) test is only 31 lines — likely only
  renders the sheet; verify the dismiss callback is tested.
  **RESOLVED:** done — `showVerificationModalSheet` is a fire-and-forget wrapper with no dismiss
  callback (it returns a `Future<void>` that completes on close). The single render-only test was
  split into two meaningful tests: (1) asserts BOTH the supplied title (rendered in the modal top
  bar) and the child appear when the sheet opens (the title was previously unasserted); (2) drives
  the real contract — popping the modal route resolves the wrapper's `Future<void>` (tracked via a
  `futureCompleted` flag, asserted false before / true after) and removes the content from the tree.

---

## Test execution speed opportunities

- [x] **[HIGH]** `device_card_test.dart` — 13 `pumpAndSettle` calls. A `DeviceCard` tap should not **RESOLVED:** all 13 settles replaced with bounded `pump()` + `pump(300ms)` pairs; file passes.
  require full animation settling; 13 full-settle cycles are significant overhead.
- [x] **[MED]** `diagnostic_info_button_test.dart` — 5 `pumpAndSettle` calls on a small **RESOLVED:** Done — all 5 replaced with bounded `pump()` + `pump(300ms)`; file green.
  action button.
- [x] **[MED]** `incoming_verification_modal_test.dart` — 5 `pumpAndSettle` calls + two
  `tester.pump(const Duration(milliseconds: 500))` calls for a `Future.delayed` in production
  code. Fixing the impl to use a fake-time-compatible mechanism eliminates the real-delay pumps. **RESOLVED:** Adapted — the modal-content settles were tried as bounded pumps and reverted where load-bearing (wolt sheet open/close in the wrapper group needs settling); the remaining 500ms pumps drain the production retryDelay, which is fake time inside testWidgets. Changing the impl retry mechanism is a production refactor out of scope for this pass.

---

## Summary

- 1 oversized impl: `sync_list_scaffold.dart` (727 L, approaching limit).
- 4 test-location / duplicate violations: `title_diff_test.dart` (split across two files);
  `conflict_list_item_test.dart` (wrong subdirectory); `outbox_monitor_test.dart` (belongs under
  `pages/`); `outbox_badge_test.dart` (belongs under settings).
- 2 inline mock-class clusters that should move to `test/mocks/mocks.dart`.
- 1 strong Glados candidate: `VerificationEmojisRow` list-length property.
- Biggest speed win: consolidate 13 `pumpAndSettle` calls in `device_card_test.dart` and the
  real-delay pumps in `incoming_verification_modal_test.dart`.
