# Test Review — `lib/features/tasks/ui/filtering/`

**Scope:** Implementation files in `lib/features/tasks/ui/filtering/` and their counterpart tests in `test/features/tasks/ui/filtering/`.

_Generated by automated test-quality review (static analysis)_

---

## File Summary Table

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `task_category_filter.dart` | 95 | Yes (533 lines) | Inline `getIt.registerSingleton` bypasses `setUpTestGetIt`; 18 `pumpAndSettle` calls |
| `task_filter_icon.dart` | 23 | Yes (193 lines) | `pumpAndSettle` on every open/close |
| `task_filter_modal.dart` | 313 | Yes (913 + 382 = 1295 lines split across 2 files) | **One-file-per-source rule violated** — two test files for one impl |
| `task_label_quick_filter.dart` | 117 | Yes (156 lines) | Inline `getIt.reset()`/`registerSingleton` bypasses `setUpTestGetIt` |
| `task_project_selection_modal.dart` | 223 | Yes (259 lines) | 8 `pumpAndSettle` calls; reasonable |
| `tasks_filter_sheet_state.dart` | 348 | Yes (730 lines) | Excellent — has Glados round-trip tests for all mappers |

---

## File size / split opportunities

- [ ] **[LOW]** `task_filter_modal_test.dart` (913 lines) + `task_filter_modal_save_flow_test.dart` (382 lines) together total 1295 test lines for a single 313-line source file. The split already violates the one-test-file-per-source-file rule (`AGENTS.md`). The save-flow tests should be merged back into the single `task_filter_modal_test.dart` file. Path: `test/features/tasks/ui/filtering/task_filter_modal_save_flow_test.dart`.

---

## Test quality improvements

- [x] **[HIGH]** `task_filter_modal_save_flow_test.dart` is a second test file for `task_filter_modal.dart`, violating the "one test file per source file" rule. Consolidate into `task_filter_modal_test.dart`. (`test/features/tasks/ui/filtering/task_filter_modal_save_flow_test.dart`)

- [x] **[HIGH]** `task_category_filter_test.dart:100` — `getIt.registerSingleton<EntitiesCacheService>(mockEntitiesCacheService)` in `setUp()`, cleaned via `getIt.reset` in `tearDown`, instead of using `setUpTestGetIt()` / `tearDownTestGetIt()`. The `WidgetTestBench` wrapper is used correctly but GetIt is managed ad hoc. **RESOLVED (stale):** the file already uses `setUpTestGetIt(additionalSetup: ...)` + `tearDown(tearDownTestGetIt)` (converted in an earlier pass).

- [x] **[HIGH]** `task_label_quick_filter_test.dart:55-62` — uses raw `getIt.reset()` / `getIt.registerSingleton` instead of the standard `setUpTestGetIt()` / `tearDownTestGetIt()` pair. This pattern risks cross-test contamination in the batched `very_good test` run. **RESOLVED:** converted to `setUpTestGetIt(additionalSetup: ...)` + `tearDown(tearDownTestGetIt)`.

- [x] **[MED]** `task_filter_modal_test.dart:538` — a lone `getIt.registerSingleton<DomainLogger>(mockDomainLogger)` call inside a single test without a matching teardown guard beyond the file-level `tearDown(getIt.reset)`. The order of teardown relative to other singletons registered in `setUp` is fragile. The logger should be registered in `setUp` so cleanup is symmetric.
  - **RESOLVED:** done — `mockDomainLogger` is now a file-level field registered in `setUp`'s `additionalSetup` (with a comment on the symmetry rationale); the in-test registration block is gone and the save-error test just verifies against the shared mock.

- [x] **[MED]** `task_category_filter_test.dart:121-145` — the first `testWidgets` ("renders correctly with categories") checks `findsNWidgets(5)` for chips and `findsOneWidget` for the "All" chip but the comment is the real spec ("2 favorites + unassigned + all + ellipsis = 5"). The test would be more robust asserting the named labels rather than a raw count. `test/features/tasks/ui/filtering/task_category_filter_test.dart:127`.
  - **RESOLVED:** done — the test now pins the exact chip label set: both favorites ('Work', 'Health'), the localized unassigned label, '...', plus a negative assertion that the non-favorite 'Personal' is absent; the count stays as a completeness check.

- [x] **[MED]** `task_filter_modal_test.dart` — `buildSubject()`, `buildWithSaveEnabled()`, and `buildWithProjects()` are three near-identical factory functions (all pump a `ProviderScope > WidgetTestBench > Scaffold > Builder > ElevatedButton`). They should be collapsed into a single `_buildSubject({bool enableProjects, ...})` helper with named parameters. `test/features/tasks/ui/filtering/task_filter_modal_test.dart:129-246`.
  - **RESOLVED:** (stale) — the three factories are already collapsed: `buildSubject` is the single host construction and `buildWithSaveEnabled`/`buildWithProjects` are thin documented parameter-forwarding wrappers (3–8 lines), exactly the named-parameter shape the item asks for.

- [ ] **[LOW]** `task_filter_icon_test.dart:138` — `expect(find.byType(TaskFilterIcon), findsOneWidget)` is a pure smoke assertion with no behavioral value; the subsequent assertions in the same test are meaningful. Remove the smoke line. `test/features/tasks/ui/filtering/task_filter_icon_test.dart:138`.

---

## Generative (Glados) testing opportunities

`tasks_filter_sheet_state_test.dart` already has Glados round-trip tests for `TaskSortOption`, `AgentAssignmentFilter`, `SearchMode`, and priority IDs — well covered. No new Glados candidates identified in this subdir beyond what exists.

- [ ] **[LOW]** `_prioritySetToDisplayIds` is a pure set transformation (filter + map). A Glados property could assert that the output set is always a subset of `{'p0','p1','p2','p3'}` for any subset of `{'P0','P1','P2','P3','UNKNOWN'}`. This is a small gap but genuinely generative logic. `lib/features/tasks/ui/filtering/tasks_filter_sheet_state.dart:343`.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `task_filter_modal_test.dart` — no test exercises the label field interaction path (opening the label multi-select modal and applying a selection). The category and status paths are tested but label selection is skipped.
  - **RESOLVED:** done — added `'opens label selection modal and applies the selection'`: opens the label field, toggles the cached label via the key-based selection option, applies the field selection (removable chip appears in the draft), applies the sheet, and asserts the batch update carried `label-1` into `setSelectedLabelIds`.

- [x] **[MED]** `task_category_filter_test.dart` — the "category chip with `null` categoryId" special sentinel (mapped to empty-string ID for "Unassigned") is tested via indirect tapping but the resulting `toggleSelectedCategoryIds` call with `''` is asserted only conditionally. A dedicated test for the unassigned sentinel ID would strengthen this. `test/features/tasks/ui/filtering/task_category_filter_test.dart:190-240`.
  - **RESOLVED:** done — the unassigned test now finds the chip deterministically by the localized `taskCategoryUnassignedLabel` (replacing the elimination-loop heuristic) and asserts the toggle list is exactly `['']` — the empty-string sentinel and nothing else.

- [ ] **[LOW]** `task_project_selection_modal_test.dart` — no test covers the "cancel without selecting" path when the project modal is dismissed via back navigation, only the success path. This is the mirror of the gap covered in the filter-modal tests.

---

## Test execution speed opportunities

- [x] **[HIGH]** `task_category_filter_test.dart` — 18 `pumpAndSettle` calls, many of which fire immediately after simple state changes that do not involve animations. Most can be replaced with `tester.pump()` or `tester.pump(const Duration(milliseconds: N))`. Each unnecessary `pumpAndSettle` carries a 10-second hang risk and adds ~50-200 ms in CI. **RESOLVED:** all 17 settles replaced with bounded `pump()` + `pump(300ms)` pairs; full file passes.

- [x] **[HIGH]** `task_filter_modal_test.dart` — 50+ `pumpAndSettle` calls, largely justified by the Wolt bottom-sheet animation (which genuinely needs settling). However, the `buildSubject()` chain duplicates full provider boilerplate three times; shared setup would reduce tree-rebuild overhead across tests. **RESOLVED:** the three near-identical builders are consolidated into one parameterized `buildSubject({savedTaskFiltersController, hasUnsavedClauses, mediaQueryData})`; `buildWithSaveEnabled`/`buildWithProjects` are now thin wrappers (the unused `savedFilters` parameter was dropped). The file-level setUp also moved to `setUpTestGetIt` (replacing `allowReassignment` + `tearDown(getIt.reset)`). The Wolt-animation settles stay, as the review notes they are justified.

- [x] **[MED]** `task_filter_icon_test.dart` — every test opens the modal with `pumpAndSettle` then immediately pumps again. Modal open/close patterns could use bounded `tester.pump(const Duration(milliseconds: 400))` if the animation is bounded, instead of open-ended `pumpAndSettle`.
  - **RESOLVED:** done — the four initial static-render `pumpAndSettle` calls replaced with a single `pump()`; the settles after taps remain because Wolt modal route transitions genuinely animate.

- [ ] **[LOW]** `task_project_selection_modal_test.dart:8` — 8 `pumpAndSettle` calls; acceptable given the modal animation, but consider bounded pumps for determinism.

---

## Summary

- 1 one-file-per-source split violation: `task_filter_modal` has two test files (`task_filter_modal_test.dart` + `task_filter_modal_save_flow_test.dart`)
- 2 files use ad-hoc GetIt setup bypassing `setUpTestGetIt()` (`task_category_filter_test.dart`, `task_label_quick_filter_test.dart`)
- ~70+ `pumpAndSettle` calls across the subdir; `task_category_filter_test.dart` (18) and `task_filter_modal_test.dart` (50+) are the largest contributors
- Glados coverage is strong in `tasks_filter_sheet_state_test.dart`; one small pure-logic gap in `_prioritySetToDisplayIds`
- Missing label-field interaction test in `task_filter_modal_test.dart`
