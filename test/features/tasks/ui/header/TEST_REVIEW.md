# Test Review — `lib/features/tasks/ui/header/`

**Scope:** Implementation files in `lib/features/tasks/ui/header/` and their counterpart tests in `test/features/tasks/ui/header/`.

_Generated by automated test-quality review (static analysis)_

---

## File Summary Table

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `desktop_task_header.dart` | **1127** | Yes (1095 lines) | Oversized impl — exceeds 1000-line threshold |
| `desktop_task_header_connector.dart` | 443 | Yes (1246 lines) | Oversized test file |
| `estimated_time_widget.dart` | 117 | Yes (119 lines) | Inline `getIt.registerSingleton` in `setUpTestGetIt` callback |
| `task_due_date_widget.dart` | 143 | Yes (375 lines) | Reasonable |
| `task_status_modal_content.dart` | 89 | Yes (340 lines) | Inline `MockTask` class bypasses centralized mocks |

---

## File size / split opportunities

- [ ] **[HIGH]** `desktop_task_header.dart` at 1127 lines exceeds the 1000-line hard threshold. Concrete split seams:
  - `_RenderTrailingAlignedWrap` custom render object (lines ~452–674) is a standalone, well-bounded layout engine with no UI dependencies beyond its parent widget. It could move to `desktop_task_header_render.dart`.
  - `_TitleEditor` + `_TitleReadOnly` (lines ~917–1127) are independent presentational widgets testable in isolation; they could move to `desktop_task_header_title.dart`.
  - `_MetaRow` + `_HeroCrumb` + their sub-widgets (lines ~261–442) could move to `desktop_task_header_meta.dart`.
  - The main `DesktopTaskHeader` StatefulWidget and its data model classes (lines ~1–260) would remain in `desktop_task_header.dart`.

- [ ] **[MED]** `desktop_task_header_connector_test.dart` at 1246 lines is the largest test file in this group. It covers a 443-line connector. If `desktop_task_header.dart` is split, the connector test should also be split to mirror the new source files.

---

## Test quality improvements

- [ ] **[HIGH]** `task_status_modal_content_test.dart:13-28` — defines `class MockTask extends Mock implements Task` inline, but the central `test/mocks/mocks.dart` does not include a `MockTask`. Per the rules, the mock should be added to `test/mocks/mocks.dart` first and imported here. This inline definition duplicates concern and silently diverges from any stubs added to centralized mocks later.

- [ ] **[HIGH]** `desktop_task_header_connector_test.dart:551-552` — a lone `getIt.registerSingleton<TimeService>(MockTimeService())` + `addTearDown(() => getIt.unregister<TimeService>())` inside a single test bypasses the project's `setUpTestGetIt`/`tearDownTestGetIt` contract and is fragile when the shared setUp already registers a `TimeService`.

- [ ] **[MED]** `desktop_task_header_test.dart` — the "status pill tinting" group (lines ~582–648) and "priority palette" group (lines ~651–666) iterate over all enum values with per-value `testWidgets` using a loop, which is good DRY practice. However, the assertions are `findsOneWidget` on the status/priority label text only. A more meaningful assertion would also verify the pill's background or foreground color token via `tester.widget<DecoratedBox>(...).decoration` or similar, since the whole purpose of these tests is the tinting logic.

- [ ] **[MED]** `task_status_modal_content_test.dart:103-108` — "renders a row for every status" loops over `allTaskStatuses` but asserts only `find.text(status)`. This proves the status string appears in the widget, which is better than nothing, but the raw status string (`'IN PROGRESS'`) is the internal enum label not the localized label. A test that finds the localized text would catch localization regressions. `test/features/tasks/ui/header/task_status_modal_content_test.dart:103`.

- [ ] **[LOW]** `desktop_task_header_connector_test.dart:518` — `expect(find.byType(DesktopTaskHeaderConnector), findsOneWidget)` is a smoke-only assertion in a test named 'renders DesktopTaskHeaderConnector'. The test body that follows does check real content, so this line is redundant noise. `test/features/tasks/ui/header/desktop_task_header_connector_test.dart:518`.

- [ ] **[LOW]** `desktop_task_header_connector_test.dart:537` — `expect(find.byType(LabelSelectionSliverContent), findsOneWidget)` is again a widget-type finder without further behavioral assertion. The meaningful check (that the modal opened in response to the tap) should verify the displayed label list content. `test/features/tasks/ui/header/desktop_task_header_connector_test.dart:537`.

---

## Generative (Glados) testing opportunities

- [ ] **[MED]** `_RenderTrailingAlignedWrap.performLayout` is a pure layout algorithm operating on a list of sizes and constraints. A Glados property test could assert: for any sequence of N non-negative child widths and any maxWidth, the trailing child's `dx` always equals `maxWidth - trailing.width`. The existing `desktop_task_header_test.dart` tests the render object only by pumping the full widget tree, not as a unit. A direct render-object test with generated sizes would be a genuine Glados opportunity. `lib/features/tasks/ui/header/desktop_task_header.dart:528`.

- [ ] **[LOW]** `_statusTint` is a pure switch over `TaskStatus` variants to `_StatusTint`. A Glados test with `any.choose(allTaskStatus values)` that asserts the returned background alpha is always between 0 and 1, and that `foreground` is never transparent, would catch regressions if a new status variant is added without updating the switch. `lib/features/tasks/ui/header/desktop_task_header.dart:803`.

---

## Coverage / missing-behavior gaps

- [ ] **[HIGH]** `desktop_task_header.dart` — the `_CrumbSegment` with `onTap == null` branch (lines ~370-373) renders a plain `Padding` instead of a tappable `InkWell`. No test verifies the non-tappable variant. `lib/features/tasks/ui/header/desktop_task_header.dart:370`.

- [ ] **[MED]** `desktop_task_header_test.dart` — `didUpdateWidget` is tested for the "title changes while not editing" path but not for the "title changes while editing" path (where the controller should NOT be updated). The guarded branch at line ~150 (`if (!_isEditing && ...)`) is only half-covered.

- [ ] **[MED]** `estimated_time_widget_test.dart` — only 119 lines for a 117-line widget; coverage appears narrow. No test exercises the zero-estimate edge (when estimate is 0), which would render "No estimate" (or similar placeholder).

- [ ] **[LOW]** `task_due_date_widget_test.dart` — 375 lines for a 143-line widget; coverage looks good. No obvious gaps noted.

---

## Test execution speed opportunities

- [ ] **[HIGH]** `desktop_task_header_connector_test.dart` — 96 `pumpAndSettle` calls in 1246 lines. Many of these follow simple state changes (category tap, label list load) that resolve in a single frame. Replacing `pumpAndSettle` with `tester.pump()` or `tester.pump(const Duration(milliseconds: N))` would remove the 10-second hang risk on each call and cut CI time.

- [ ] **[MED]** `task_status_modal_content_test.dart` — 0 `pumpAndSettle` calls (good); uses direct `pump()` throughout. This is the pattern to replicate.

- [ ] **[MED]** `desktop_task_header_test.dart` — 4 `pumpAndSettle` calls, all in dialog tests that genuinely need animation settling (AlertDialog open/close). These are appropriate usages.

- [ ] **[LOW]** `task_due_date_widget_test.dart` — 17 `pumpAndSettle` calls; worth auditing each for whether a bounded pump would suffice.

---

## Summary

- 1 oversized impl file: `desktop_task_header.dart` (1127 lines), with 3 concrete split seams
- 1 oversized test file: `desktop_task_header_connector_test.dart` (1246 lines)
- 2 test quality issues: inline `MockTask` in `task_status_modal_content_test.dart`, rogue `getIt` in connector test
- `pumpAndSettle` heavy: 96 calls in connector test alone, most replaceable
- 2 Glados candidates: `_RenderTrailingAlignedWrap` layout algorithm [MED], `_statusTint` switch [LOW]
- 2 coverage gaps: `_CrumbSegment` null-tap branch [HIGH], `didUpdateWidget` editing-guard branch [MED]
