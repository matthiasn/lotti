# Test Review — `lib/features/tasks/ui/labels/`

**Scope:** Implementation files in `lib/features/tasks/ui/labels/` and their counterpart tests in `test/features/tasks/ui/labels/`.

_Generated by automated test-quality review (static analysis)_

---

## File Summary Table

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `label_selection_modal_content.dart` | 291 | Yes (`label_selection_sliver_content_test.dart`, 530 lines) | Test file name does not mirror source file name |
| `label_ui_utils.dart` | 58 | **No** | Missing test file entirely |

---

## File size / split opportunities

- [ ] **[LOW]** Both source files are well within acceptable size bounds (291 and 58 lines). No splits needed.

---

## Test quality improvements

- [x] **[HIGH]** `label_selection_sliver_content_test.dart:16-19` — defines two inline mock classes (`_MockLabelsRepository` and `_MockEntitiesCacheService`) that almost certainly duplicate mocks already in `test/mocks/mocks.dart`. Per the rules, inline mock classes are forbidden when centralized equivalents exist. Verify `MockLabelsRepository` and `MockEntitiesCacheService` in `test/mocks/mocks.dart` and replace the inline definitions with imports. **RESOLVED:** the inline classes are deleted; the file now uses the central `MockLabelsRepository` and `MockEntitiesCacheService` from `test/mocks/mocks.dart`.

- [x] **[HIGH]** `label_selection_sliver_content_test.dart:56-79` — uses raw `getIt.reset()` / `getIt.registerSingleton<LabelsRepository>` / `getIt.registerSingleton<EntitiesCacheService>` instead of the standard `setUpTestGetIt()` / `tearDownTestGetIt()` pair from `test/widget_test_utils.dart`. This bypasses the project's shared GetIt lifecycle and risks cross-test contamination in the `very_good test` batched run. `test/features/tasks/ui/labels/label_selection_sliver_content_test.dart:56`. **RESOLVED:** converted to `setUpTestGetIt(additionalSetup: ...)` + `tearDown(tearDownTestGetIt)`.

- [x] **[HIGH]** Test file naming mismatch: the source file is `label_selection_modal_content.dart` but the test file is `label_selection_sliver_content_test.dart`. The rules require `lib/features/foo/bar.dart` → `test/features/foo/bar_test.dart`. Either rename the source to match the widget class `LabelSelectionSliverContent`, or rename the test to `label_selection_modal_content_test.dart`. The mismatch causes the mirroring rule to technically fail. **RESOLVED:** the test file is renamed to `label_selection_modal_content_test.dart`, mirroring the source path.

- [x] **[MED]** `label_selection_sliver_content_test.dart` — the `buildWidget` helper constructs a `ProviderScope > WidgetTestBench > Material > CustomScrollView > LabelSelectionSliverContent` wrapper. Multiple tests create ad-hoc `ProviderScope` without using `makeTestableWidget()`. The helper is defined in the file which is good, but it does not use the centralized `makeTestableWidget(overrides: [...])` pattern, making it harder to maintain if the test infrastructure evolves.
  - **RESOLVED:** done (adapted) — the file is now `label_selection_modal_content_test.dart`. `makeTestableWidget` cannot host a sliver (it wraps in a SingleChildScrollView), so the bench + CustomScrollView host stays; instead, `buildWidget` gained an `applyController` parameter and the four ad-hoc `ProviderScope` wrappers in the Apply/Category groups were consolidated onto it, leaving a single host construction in the file.

- [x] **[MED]** `label_selection_sliver_content_test.dart` — the "apply via controller" path is tested (lines ~306-370) but the `applyController.value` callback's return value (`Future<bool>`) is not asserted — the tests only check UI side effects. A test that confirms `applyController.value()` returns `true` on success and `false` when persistence fails would be a meaningful behavioral assertion.
  - **RESOLVED:** (stale) — `'calls repository.setLabels when apply is invoked'` asserts `await applyController.value!() == true` and `'returns false when repository.setLabels fails'` asserts the `false` outcome; both return-value paths are covered.

- [ ] **[LOW]** `label_selection_sliver_content_test.dart` — 17 `pumpAndSettle` calls; the label list loads synchronously from a provider override so most calls could be replaced with `tester.pump()`.

---

## Generative (Glados) testing opportunities

`label_ui_utils.dart` contains utility functions. Without seeing their exact signatures, color-parsing and CSS hex conversion logic are prime Glados targets if they are pure functions:

- [x] **[MED]** If `label_ui_utils.dart` contains pure color-parsing or label-display string functions, these are candidates for Glados round-trip tests (e.g., parse → format → parse is idempotent). Requires reading the file to confirm. `lib/features/tasks/ui/labels/label_ui_utils.dart`.
  - **RESOLVED:** done — `label_ui_utils.dart` holds pure list-building functions; added a Glados2 property over generated label specs × search terms (numRuns 150, `tags: 'glados'`): `availableIds` mirrors the available list, every produced item is in the union AND matches the search, no matching label is dropped (exact count), and the result is strictly A–Z ordered case-insensitively regardless of selection state.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `label_ui_utils.dart` (58 lines) has **no test file at all**. This is a complete coverage gap. A `test/features/tasks/ui/labels/label_ui_utils_test.dart` must be created. `lib/features/tasks/ui/labels/label_ui_utils.dart`. **RESOLVED:** new `label_ui_utils_test.dart` covers buildUnionLabels dedup/override semantics, all four buildLabelSubtitleText branches (incl. whitespace-only descriptions), and buildSelectorLabelList filtering, case-insensitive A–Z sorting, and availableIds scoping.

- [x] **[MED]** `label_selection_sliver_content_test.dart` — no test exercises the `categoryId` parameter being non-null (filtering labels by category). The `filterLabelsForCategory` mock is stubbed as a pass-through, but no test verifies that a specific `categoryId` actually constrains the displayed list. `test/features/tasks/ui/labels/label_selection_sliver_content_test.dart`.
  - **RESOLVED:** (stale) — the `'uses availableLabelsForCategoryProvider with category'` test passes `categoryId: 'work'` with a constrained provider override and asserts only the category's label renders.

- [x] **[MED]** No test covers the behavior when the `LabelsRepository` throws on save (the `apply` callback should return `false`). The error path in `_LabelSelectionSliverContentState.apply` is untested.
  - **RESOLVED:** done — `apply()` now catches repository throws and returns `false` (the repository already logs its own failures and returns false on its internal catch; this hardens the UI boundary), and a new `'returns false when repository.setLabels throws'` test pins it.

- [ ] **[LOW]** The "create label" flow is tested (opening the editor sheet) but not the result: after the editor sheet creates a new label, does the list refresh with the new label? The post-creation `labelsStreamProvider` update path is not asserted.

---

## Test execution speed opportunities

- [x] **[MED]** `label_selection_sliver_content_test.dart` — 17 `pumpAndSettle` calls. Since all data comes from synchronous provider overrides, most of these can be `tester.pump()`. The `pumpAndSettle` calls in the label creation modal tests (which open a `showWoltModalBottomSheet`) are more justified but should still use bounded pump sequences.
  - **RESOLVED:** done — all 17 `pumpAndSettle` calls replaced via the `pumpSliver` helper (pumpWidget + two bounded pumps for the synchronous overrides/stream value); the modal-open test already used bounded pumps.

- [ ] **[LOW]** The `buildWidget` helper runs a full `ProviderScope` + `CustomScrollView` setup for every test. A `setUpAll` with a shared widget build could reduce rebuild overhead for the read-only label-rendering tests.

---

## Summary

- 0 oversized impl files; 1 missing test file (`label_ui_utils_test.dart`) — high priority
- 2 inline mock classes bypassing `test/mocks/mocks.dart`; 1 ad-hoc GetIt lifecycle violation
- Test file name does not mirror source file name
- 1 Glados candidate in `label_ui_utils.dart` if it contains pure logic (confirm by reading)
- 17 `pumpAndSettle` calls; most replaceable with `tester.pump()`
