# Test Review — `lib/features/tasks/ui/linked_tasks/`

**Scope:** Implementation files in `lib/features/tasks/ui/linked_tasks/` and their counterpart tests in `test/features/tasks/ui/linked_tasks/`.

_Generated by automated test-quality review (static analysis)_

---

## File Summary Table

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `linked_tasks_widget.dart` | 473 | Yes (866 lines) | 45 `pumpAndSettle` calls; good assertions overall |
| `link_task_modal.dart` | 315 | Yes (998 lines) | 23 `pumpAndSettle` calls; test file 3× larger than impl |

---

## File size / split opportunities

- [ ] **[MED]** `linked_tasks_widget.dart` at 473 lines is close to the ~500 line guidance boundary. It contains five private widget classes (`_LinkedTasksHeader`, `_CountBadge`, `_LinkedTaskRow`, `_StatusGlyph`) plus the main `LinkedTasksWidget`. Splitting `_LinkedTaskRow` + `_StatusGlyph` into a `linked_task_row.dart` file would bring the main file to ~350 lines and allow unit-testing the row in isolation without the full provider stack. Not urgent, but a clean seam exists at line 330.

- [ ] **[LOW]** `link_task_modal.dart` at 315 lines is within bounds and well-organized.

---

## Test quality improvements

- [ ] **[MED]** `linked_tasks_widget_test.dart` — the `pumpWidget` helper at the top of the file is a well-designed shared fixture, which is good. However, several tests share `MediaQueryData` or `extraOverrides` parameters that differ only in one flag. Consider looping over `(manageMode: true/false)` cases instead of duplicating test bodies. `test/features/tasks/ui/linked_tasks/linked_tasks_widget_test.dart`.

- [ ] **[MED]** `link_task_modal_test.dart` — 998 lines for a 315-line source is a 3:1 ratio. Much of this is per-test fixture construction (creating `Task` objects inline in each test). Extracting a shared `_buildTask({required String id, String title = 'Task', TaskStatus? status})` factory used across all tests would substantially reduce duplication. A factory already exists in `test/helpers/entity_factories.dart` — use it. `test/features/tasks/ui/linked_tasks/link_task_modal_test.dart`.

- [ ] **[MED]** `link_task_modal_test.dart` — several tests set up identical provider overrides (linking controller, journal repository, nav service) in per-test closures. Extracting a shared `_pumpModal(tester, {...})` helper (like the one in `linked_tasks_widget_test.dart`) would reduce duplication.

- [ ] **[LOW]** `linked_tasks_widget_test.dart:184-186` — three `findsNothing` assertions immediately after pumping an empty-state widget. These are appropriate "nothing rendered" checks rather than smoke tests; they are justified here.

---

## Generative (Glados) testing opportunities

No pure-logic functions in `linked_tasks_widget.dart` or `link_task_modal.dart` that qualify for property-based testing. Both files are primarily UI widgets with provider-driven state. No Glados candidates.

---

## Coverage / missing-behavior gaps

- [ ] **[MED]** `linked_tasks_widget_test.dart` — the `_LinkedTaskRow` "manage mode" delete confirmation path (the two-tap delete) is tested, but the animation between armed and confirmed states is not verified. A test that taps once, checks the row is in "armed" visual state (if any indicator exists), then taps again to confirm would improve coverage of the state machine.

- [ ] **[MED]** `linked_tasks_widget_test.dart` — no test covers the "linked task navigated to" path: tapping a `_LinkedTaskRow` should call `NavService.beamToNamed`. The nav callback is mocked but it is not verified that it receives the correct task ID.

- [ ] **[MED]** `link_task_modal_test.dart` — the search debounce path is not tested: typing quickly and waiting for the debounce window to fire vs. typing slowly. Since the search triggers a provider watch, the debounce (if any) could mask results. At minimum, a test that types and immediately checks that results appear (no debounce) or that a pump is required would document the behavior.

- [ ] **[LOW]** `link_task_modal_test.dart:504` — the "no tasks available to link" empty state is tested with `findsOneWidget`. No test covers transitioning from non-empty to empty (e.g., search that eliminates all results).

---

## Test execution speed opportunities

- [ ] **[HIGH]** `linked_tasks_widget_test.dart` — 45 `pumpAndSettle` calls. Most follow a single provider state change (tasks loaded from the fake controller) that resolves synchronously. Replacing these with `tester.pump()` would remove the 10-second hang risk on every call. `test/features/tasks/ui/linked_tasks/linked_tasks_widget_test.dart`.

- [ ] **[MED]** `link_task_modal_test.dart` — 23 `pumpAndSettle` calls for a bottom-sheet modal. The `DraggableScrollableSheet` animation is bounded so `tester.pump(const Duration(milliseconds: 400))` would be a safer, faster substitute than unbounded settling.

- [ ] **[LOW]** Each test in `link_task_modal_test.dart` builds the full provider stack independently. A shared `setUp` / `setUpAll` with a pre-built `_pumpModal` helper would save ~100ms per test by reusing the baseline widget tree configuration.

---

## Summary

- 0 oversized impl files (473 is close to boundary; a clean split seam exists)
- `link_task_modal_test.dart` (998 lines) is 3× the impl size — high duplication from inline fixture construction
- 45 `pumpAndSettle` calls in `linked_tasks_widget_test.dart` — largest speed opportunity
- 2 missing behavior gaps: nav callback assertion, debounce path
- No Glados candidates (pure UI widgets)
