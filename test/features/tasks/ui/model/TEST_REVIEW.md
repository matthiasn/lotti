# Test Review — `lib/features/tasks/ui/model/`

**Scope:** Implementation files in `lib/features/tasks/ui/model/` and their counterpart tests in `test/features/tasks/ui/model/`.

_Generated by automated test-quality review (static analysis)_

---

## File Summary Table

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `task_browse_models.dart` | 179 | Yes (205 lines) | Good coverage; `dueOverdue` bucket untested |
| `task_browse_row_interactions.dart` | 63 | **No** | Missing test file entirely |
| `task_list_detail_models.dart` | 113 | **No** | Missing test file entirely |
| `task_list_detail_state.dart` | 195 | Yes (647 lines; has Glados) | Excellent — comprehensive Glados scenario test |

---

## File size / split opportunities

- [ ] **[LOW]** All implementation files are well within size bounds. No splits needed.

---

## Test quality improvements

- [ ] **[MED]** `task_browse_models_test.dart` — all tests use a hand-rolled fixture building pattern (`TestTaskFactory.create(...).copyWith(...)`). The `TestTaskFactory` from `test/helpers/entity_factories.dart` is already imported and used correctly. The chain `.copyWith(data: TestTaskDataFactory.create(...).copyWith(due: ...))` appears in 5 tests with near-identical structure. Extracting a `_taskWithDue(String id, DateTime dateFrom, DateTime due)` helper would reduce repetition. `test/features/tasks/ui/model/task_browse_models_test.dart`.

- [ ] **[LOW]** `task_list_detail_state_test.dart` — the `_record(...)` factory at line ~274 is well-designed for the deterministic sorting tests below. The factory is private to the test file, which is appropriate. No issues here.

---

## Generative (Glados) testing opportunities

- [ ] **[HIGH]** `task_browse_row_interactions.dart` — `buildTaskBrowseRowInteraction` and `taskRowInteractionPriority` are **pure functions** with a small input space (`taskId`, `selectedTaskId`, `hoveredTaskId` strings; `previousTaskIdInSection` / `nextTaskIdInSection` optional strings). A Glados property test is ideal:
  - Property 1 (idempotence): for any IDs, calling `buildTaskBrowseRowInteraction` twice with the same arguments returns equal values.
  - Property 2 (priority invariant): `taskRowInteractionPriority` returns 2 iff `taskId == selectedTaskId`, 1 iff `taskId == hoveredTaskId && taskId != selectedTaskId`, 0 otherwise.
  - Property 3: `overlap` values in `GroupedCardRowInteraction` are always in `{0, 1}` for any input combination.
  - This file has NO tests at all yet — a perfect Glados candidate. `lib/features/tasks/ui/model/task_browse_row_interactions.dart`.

- [ ] **[HIGH]** `task_list_detail_state_test.dart` already has an excellent Glados property test (`_GeneratedTaskListScenario` with `numRuns: 180`). This is the gold standard for the subdir.

- [ ] **[MED]** `task_browse_models.dart` — `buildTaskBrowseEntries` is a pure function grouping a list of tasks into `TaskBrowseEntry` records. A Glados property test could assert:
  - For any list of tasks with `sortOption: byDueDate`, the output entries maintain monotone section-key ordering (overdue < yesterday < today < tomorrow < future < noDueDate).
  - For any list, `sectionCount` on the first entry in a section equals the number of entries sharing that `sectionKey`, except when `hasNextPage` is true (where the trailing partial section is null).
  - `lib/features/tasks/ui/model/task_browse_models.dart`.

---

## Coverage / missing-behavior gaps

- [ ] **[HIGH]** `task_browse_row_interactions.dart` (63 lines) has **no test file**. `buildTaskBrowseRowInteraction` is pure logic with multiple branches (`hasUpperInteraction`, `hasLowerInteraction`, `topOverlap`, `bottomOverlap`, `showDividerBelow`). Create `test/features/tasks/ui/model/task_browse_row_interactions_test.dart`. `lib/features/tasks/ui/model/task_browse_row_interactions.dart`.

- [ ] **[HIGH]** `task_list_detail_models.dart` (113 lines) has **no test file**. While these are plain data classes (`TaskRecord`, `TaskListData`, etc.), their field contracts (e.g., `sectionDate` used as a sort key in `_compareExpectedTasks`) should have at least a smoke test that verifies construction and equality. Create `test/features/tasks/ui/model/task_list_detail_models_test.dart`. `lib/features/tasks/ui/model/task_list_detail_models.dart`.

- [ ] **[MED]** `task_browse_models_test.dart` — `TaskBrowseSectionKind.dueOverdue` is not exercised by any test. There is no fixture with a task whose due date is strictly before yesterday. `lib/features/tasks/ui/model/task_browse_models.dart`.

- [ ] **[MED]** `task_browse_models_test.dart` — the `dueThisWeek` and `dueLater` bucket kinds (if they exist in the enum) are not tested. Check `TaskBrowseSectionKind` enum values and ensure each kind is covered.

- [ ] **[LOW]** `task_list_detail_state_test.dart` — the Glados scenario handles priority sort and created-date sort but does not specifically exercise the "due date sort with tasks lacking a due date" path separately from the `byDate` sort. The deterministic test at the bottom partially covers this, but the Glados `scenario.sortId == dueDateSort` branch in `_compareExpectedTasks` with mixed null/non-null `due` values is only exercised when the generator happens to produce it.

---

## Test execution speed opportunities

- [ ] **[LOW]** `task_browse_models_test.dart` — 0 `pumpAndSettle` calls. All tests are pure unit tests. Excellent.
- [ ] **[LOW]** `task_list_detail_state_test.dart` — 0 `pumpAndSettle` calls. All tests are pure logic tests. Glados runs `numRuns: 180` which is at the upper bound of the recommended range; monitor if CI times grow.

---

## Summary

- 0 oversized impl files; **2 missing test files** (`task_browse_row_interactions_test.dart`, `task_list_detail_models_test.dart`) — highest priority
- `task_browse_models_test.dart` missing `dueOverdue` bucket coverage [MED]
- **2 strong Glados candidates**: `task_browse_row_interactions.dart` (pure function, no test at all) and `task_browse_models.dart` (section-ordering invariant)
- `task_list_detail_state_test.dart` Glados test is exemplary — should serve as a template
- 0 `pumpAndSettle` calls across all model tests — excellent speed profile
