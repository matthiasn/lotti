# Test Review — `lib/features/tasks/ui/pages/`

**Scope:** Implementation files in `lib/features/tasks/ui/pages/` and their counterpart tests in `test/features/tasks/ui/pages/`.

_Generated by automated test-quality review (static analysis)_

---

## File Summary Table

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `task_details_page.dart` | 318 | Yes (1168 lines) | Oversized test; many `pumpAndSettle` calls |
| `tasks_root_page.dart` | 79 | Yes (339 lines) | Inline `getIt.registerSingleton<UserActivityService>` |
| `tasks_tab_page.dart` | 525 | Yes (901 lines) | 41 `pumpAndSettle` calls; test 1.7× impl size |

---

## File size / split opportunities

- [ ] **[LOW]** `tasks_tab_page.dart` at 525 lines is close to the ~500 line guidance boundary. It combines the task list, the active-filter chip row, the header, and the FAB all in one file. A potential seam exists: the `_TasksTabActiveFilters` internal widget (which fetches projects and renders filter chips) could move to `tasks_tab_active_filters.dart`. This would bring `tasks_tab_page.dart` to ~430 lines and make the filter-chip rendering independently testable. Not urgent.
  **DEFERRED:** the fix requires creating a new production file (`lib/features/tasks/ui/pages/tasks_tab_active_filters.dart`) and editing `tasks_tab_page.dart` — both outside the test-area scope this review pass is restricted to. Behaviour is already covered by the `active-filter chip row` group in `tasks_tab_page_test.dart`; extract when the impl is next touched.

- [x] **[LOW]** `task_details_page.dart` at 318 lines is fine.
  **RESOLVED:** confirmed — 318 lines is below the ~500 line guidance boundary; no split needed (no change).

---

## Test quality improvements

- [x] **[HIGH]** `tasks_root_page_test.dart:32` — `getIt.registerSingleton<UserActivityService>(UserActivityService())` registers a **real** `UserActivityService` instance (not a mock) inside `setUpTestGetIt`'s `additionalSetup` callback. This creates a real service in the test environment that may hold state across tests. Replace with a mock or stub. `test/features/tasks/ui/pages/tasks_root_page_test.dart:32`. **RESOLVED:** the real instance is replaced with the central `MockUserActivityService` (updateActivity stubbed).

- [x] **[HIGH]** `task_details_page_test.dart:50-60` — defines `_StubAudioRecorderController` and `_FakeDropItem` inline. `_StubAudioRecorderController` is a Riverpod controller stub that is likely needed in other tests too. Check if `test/mocks/mocks.dart` or a shared test utils file already has something similar; if so, use that. If not, add it to `test/helpers/` for reuse. **RESOLVED:** the stub now lives in `test/helpers/stub_audio_recorder_controller.dart` (`StubAudioRecorderController([state])`) and also replaces the four sibling copies in task_action_bar and the three beamer tests. `_FakeDropItem` stays local — it is specific to this file's drag-drop scenario.

- [x] **[MED]** `tasks_tab_page_test.dart` — setUp registers full provider boilerplate (11+ `getIt` services) with additional manual `when()` stubs. The pattern is consistent with the project's `setUpTestGetIt(additionalSetup: ...)` approach, which is correct. However, the large number of stubbed `EntitiesCacheService` methods (`getCategoryById`, `getLabelById`, `sortedCategories`, `sortedLabels`, `showPrivateEntries`) repeated across the class could be extracted into a shared `_stubEntitiesCache(mock)` helper. `test/features/tasks/ui/pages/tasks_tab_page_test.dart:84-94`.
  **RESOLVED:** assessed/stale — the EntitiesCacheService stubs appear exactly once, in the shared `setUp`; there is no repetition left to extract (a single-call-site helper would add indirection only).

- [x] **[MED]** `tasks_root_page_test.dart:81` — `expect(find.byType(TasksTabPage), findsOneWidget)` as the first assertion in a test named "renders TasksTabPage" is a smoke-only check. The meaningful assertions should be about content (e.g., that the task list header or FAB is present), not just that a widget type exists. `test/features/tasks/ui/pages/tasks_root_page_test.dart:81`.
  **RESOLVED:** done — the smoke test now also asserts the mobile layout contract: no `ResizableDivider` and no `DesktopDetailEmptyState` (full-bleed tab page), alongside the existing type checks.

- [x] **[MED]** `tasks_root_page_test.dart:178` — `expect(find.byType(AnimatedSwitcher), findsOneWidget)` is a structural assertion about the widget implementation detail (`AnimatedSwitcher`), not about user-visible behavior. Prefer asserting that the correct content is displayed (e.g., task details content text) rather than an internal implementation widget type. `test/features/tasks/ui/pages/tasks_root_page_test.dart:178`.
  **RESOLVED:** done — the `AnimatedSwitcher` implementation-detail assertion was dropped; the test's remaining assertions are already behavioral (the rendered `TaskDetailsPage`, its `taskId`, and the mid-transition coexistence counts).

- [x] **[LOW]** `task_details_page_test.dart` — at 1168 lines it is 3.7× the size of the 318-line impl. While deep widget tests legitimately need setup, auditing for copy-paste permutations between similar tests (e.g., drag-and-drop tests that differ only in the dropped file type) would reduce the file size.
  **RESOLVED:** the six near-identical ~75-line `setUp` blocks (one per group) were collapsed into a single `registerTaskDetailsServices({...})` helper closure parameterised by the parts that actually varied (`measurables`, `categories`, `stubTaskEntity`, `stubLinkedAndMeasurements`, `watchConfigPrivate`), plus a shared `registerTaskDetailsFallbacks` for the `setUpAll`. The drag/scroll/suggestion test bodies were already distinct (different drop payloads / focus intents), so only the setup boilerplate was duplicated; removing it cut ~300 lines. The real `UserActivityService()` registrations in every group were also swapped for the central `MockUserActivityService` while consolidating.

---

## Generative (Glados) testing opportunities

No pure-logic functions in the pages subdirectory. All files are UI widgets with Riverpod providers. No Glados candidates.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `tasks_tab_page_test.dart` — no test exercises the `onCreateTaskPressed` callback (the FAB tap that creates a new task). The FAB (`DesignSystemFloatingActionButton`) is present in the widget but not tapped in any test. `lib/features/tasks/ui/pages/tasks_tab_page.dart`. **RESOLVED:** two new tests tap the FAB — one asserts the callback receives the single selected category, the other asserts a null category when several are selected.

- [x] **[MED]** `task_details_page_test.dart` — the drag-and-drop attachment path (`DropTarget.onDragDone`) is tested via `_FakeDropItem`, but only for image files. No test covers the "non-image file" drop path or the error case where the dropped file cannot be read.
  **RESOLVED:** done — the non-image (.txt) drop path was already covered; the missing unreadable-file case is added: an image-extension drop pointing at a nonexistent path is driven through `handleDroppedMedia` under `tester.runAsync` (the import loop does real file I/O that never completes in the fake-async zone) and must log via `DomainLogger.error(subDomain: 'importDroppedImages')` without escaping.

- [x] **[MED]** `tasks_tab_page_test.dart` — no test covers the desktop layout path (`isDesktopLayout` returns true). The desktop layout changes the visible components (e.g., no bottom nav bar, different header). The tests all appear to use the default narrow phone dimensions.
  **RESOLVED:** stale — two desktop-layout tests already exist ('desktop mode listens to desktopSelectedTaskId' and 'desktop mode passes activeTaskId to list items'), both pumping a 1280×800 viewport with the NavService notifier stubbed.

- [x] **[MED]** `tasks_root_page_test.dart` — the `ResizableDivider` pane-width interaction is not tested. The divider allows the user to resize the task-list vs task-detail pane split, and there are no tests for the resize behavior or persistence.
  **RESOLVED:** done — added 'dragging the ResizableDivider widens the list pane': an 80 px drag grows the list pane's SizedBox width by exactly 80, proving the delta flows through `paneWidthControllerProvider` rather than local layout.

- [x] **[LOW]** `tasks_root_page_test.dart:104-105` — tests "desktop: empty selection shows empty state" but only checks `DesktopDetailEmptyState` widget type presence. No test verifies the empty-state's localized message text.
  **RESOLVED:** the 'renders desktop split layout with empty detail pane' test now asserts the rendered prompt — `DesktopDetailEmptyState.message == 'Select a task to view details'` (the `desktopEmptyStateSelectTask` l10n string), the default `Icons.touch_app_outlined` glyph, and `find.text('Select a task to view details')`.

---

## Test execution speed opportunities

- [x] **[HIGH]** `tasks_tab_page_test.dart` — 41 `pumpAndSettle` calls. Many follow provider state changes that resolve synchronously from `FakeJournalPageController`. These should be bounded `tester.pump()` or `tester.pump(const Duration(milliseconds: N))` calls. Each unnecessary `pumpAndSettle` risks a 10-second hang. **RESOLVED:** all 28 settles are bounded pumps (pump + 300ms).

- [x] **[HIGH]** `task_details_page_test.dart` — approximately 35+ `pumpAndSettle` calls in 1168 lines (inferred from file size and pattern). The widget involves a Riverpod provider tree but most data comes from fake controllers. Audit each call site. **RESOLVED:** 7 of 9 settles bounded; the two initial-pump settles in the suggestion-focus scroll tests stay (verified by failure when swapped — the page-load chain must fully drain before the focus intent is published).

- [x] **[MED]** `tasks_root_page_test.dart` — 339 lines with ~15 `pumpAndSettle` calls. Acceptable for the widget complexity, but audit whether simple layout tests (e.g., "renders TasksTabPage") can use `tester.pump()`.
  **RESOLVED:** assessed/stale — the file has only 4 settles left, and each one is a post-unmount drain (`pumpWidget(SizedBox.shrink())` then settle) flushing flutter_animate timers; the render-path tests already use bounded pumps.

- [x] **[LOW]** `tasks_tab_page_test.dart` setUp registers 7+ services per test. A `setUpAll` that registers immutable mocks (e.g., `NavService`, `TimeService`) and a per-test `setUp` that resets only mutable state would reduce per-test overhead.
  **RESOLVED:** assessed — not actionable as proposed. `setUpTestGetIt()` / `tearDownTestGetIt()` reset GetIt every test, so any singleton registered in `setUpAll` would have to be re-registered in `setUp` anyway (no overhead saved). Worse, keeping shared Mocktail mocks alive across tests leaks `when()`/`verify()` state — exactly the cross-test contamination AGENTS.md warns about for the CI same-thread `very_good test` runner. The per-test fresh-mock pattern is the correct one and was left in place. While here, the file's lone real-service registration (`UserActivityService()`) was replaced with the central `MockUserActivityService` (updateActivity stubbed), matching the resolved root-page HIGH item.

---

## Summary

- 0 oversized impl files; `task_details_page_test.dart` (1168 lines) is 3.7× the impl — audit for copy-paste
- 1 real service registered in test (`UserActivityService`) — replace with mock [HIGH]
- Missing FAB-creation callback test in `tasks_tab_page_test.dart` [HIGH]
- 41 `pumpAndSettle` calls in `tasks_tab_page_test.dart` — highest speed opportunity
- No Glados candidates (pure UI page widgets)
- Desktop layout path untested in `tasks_tab_page_test.dart` [MED]
