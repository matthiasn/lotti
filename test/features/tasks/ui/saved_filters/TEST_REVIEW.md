# Test Review — `lib/features/tasks/ui/saved_filters/`

**Scope:** Implementation files in `lib/features/tasks/ui/saved_filters/` and their counterpart tests in `test/features/tasks/ui/saved_filters/`.

_Generated by automated test-quality review (static analysis)_

---

## File Summary Table

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `saved_task_filter_row.dart` | 478 | Yes (588 lines) | Inline `getIt.isRegistered`/`registerSingleton` bypasses `setUpTestGetIt` in one group |
| `saved_task_filters_section.dart` | 175 | Yes (387 lines) | Reasonable; 6 `pumpAndSettle` calls (justified for controller settle) |
| `saved_task_filter_toast.dart` | 39 | Yes (112 lines) | Good — all 3 toast variants tested |
| `tasks_saved_filters_tree.dart` | 46 | Yes (208 lines) | Good; `pumpAndSettle` appropriate |

---

## File size / split opportunities

- [x] **[MED]** `saved_task_filter_row.dart` at 478 lines is approaching the ~500 line guideline. The widget contains distinct behaviors: hover/delete confirmation state machine, inline rename with keyboard shortcuts, category-color dot rendering, and the row layout itself. A potential seam: the `_CategoryDotColumn` private widget (responsible for fetching and rendering category dots) could be extracted to `saved_task_filter_category_dots.dart`, making it independently testable and bringing `saved_task_filter_row.dart` to ~420 lines. Not urgent, but a clean seam.
  **RESOLVED:** assessed, no change — the file is 478 lines (under the limit) and the premise is stale: there is no `_CategoryDotColumn` private widget, the dots are ~30 inline lines in `build`. The seam stays documented here for if/when the file actually grows past 500.

---

## Test quality improvements

- [x] **[HIGH]** `saved_task_filter_row_test.dart:435-438` — the "category color dots" group's `setUp` contains `if (!getIt.isRegistered<EntitiesCacheService>()) { getIt.registerSingleton... }`. Using the conditional registration check is a smell: it means the outer `setUpTestGetIt()` / `tearDownTestGetIt()` may or may not have registered the service depending on execution order. The correct pattern is to rely solely on `setUpTestGetIt()` with an `additionalSetup` callback. `test/features/tasks/ui/saved_filters/saved_task_filter_row_test.dart:435`. **RESOLVED:** the conditional guard is removed — the file-level `setUpTestGetIt()` resets GetIt before every test, so the group setUp now registers the mock unconditionally (the guard was dead code that obscured that invariant).

- [x] **[MED]** `saved_task_filter_row_test.dart` — the "category color dots" group creates `CategoryDefinition` objects inline using a local `makeCategory` factory (lines 422-432). This is fine for isolation, but the factory duplicates the centralized `test/features/categories/test_utils.dart` factory. Consider importing from the central factory to reduce drift. `test/features/tasks/ui/saved_filters/saved_task_filter_row_test.dart:422`.
  **RESOLVED:** done (adapted) — colored categories now come from the central `CategoryTestUtils.createTestCategory`; the null-color edge keeps a direct construction because the central factory maps `color: null` to a default `'#0000FF'` (and freezed `copyWith` cannot reset a field to null), documented inline.

- [x] **[MED]** `saved_task_filters_section_test.dart:264-300` — the "onReorderItem newIndex > oldIndex branch" test uses the ReorderableListView semantic accessibility layer to trigger the reorder callback. This is a clever technique, but the comment explaining the guard (`adjusted == oldIndex → return early`) is important. The assertion that no `saveSettingsItem` call was made would be clearer if stated explicitly with `verifyNever(...)` rather than relying on the absence of a captured call.
  **RESOLVED:** stale — the no-op reorder test already ends with an explicit `verifyNever(saveSettingsItem(storageKey, any()))`, exactly what the item requested.

- [x] **[LOW]** `tasks_saved_filters_tree_test.dart:85-87` — `expect(find.byKey(...), findsOneWidget)` combined with `expect(find.byKey(SavedTaskFiltersSectionKeys.list), findsNothing)` are appropriate negative assertions for the empty state. These are meaningful structural assertions, not smoke tests.
  **RESOLVED:** confirmed, no change — the pair asserts the real empty-state contract (section root present as `SizedBox.shrink`, list absent). It is a structural distinction, not a build-didn't-crash smoke test, so it stands as written.

- [x] **[LOW]** `saved_task_filter_toast_test.dart` — all tests assert on both the text content and the `SnackBar` widget type. The final test also checks the leading icon (`Icons.check_circle_rounded`). This is a good, meaningful assertion pattern.
  **RESOLVED:** confirmed, no change — verified against the file: the saved/updated/deleted tests assert the localised label text (and SnackBar type), and the fourth test asserts the success glyph (`check_circle_rounded`) is a descendant of the SnackBar, proving the helper routes through the shared `context.showToast` path. Each test asserts content, not just existence.

---

## Generative (Glados) testing opportunities

- [x] **[MED]** `saved_task_filter_row.dart` — the delete confirmation state machine is a mini state machine with states `{idle, armed, confirmed}` and transitions `{hover-in, hover-out, first-tap, second-tap}`. A Glados property test could assert: for any sequence of hover/tap events, the `onDelete` callback is invoked exactly once per "arm + second-tap" sequence and never for any other sequence. This is a genuine behavioral invariant that a generative test covers better than the fixed-sequence widget tests. `lib/features/tasks/ui/saved_filters/saved_task_filter_row.dart`.
  **RESOLVED:** done (adapted) — Glados cannot drive a `WidgetTester`, so the property is a model-based sequence test with a fixed-seed `Random(42)`: 30 random sequences (≤8 events each) over {hover-in, hover-out, tap} run against a 3-state model oracle; after every event the actual `onDelete` count must equal the model's (arm + second tap fires exactly once; hover-out disarms; non-hover taps are swallowed by the IgnorePointer). A final assert proves the commit path was exercised.

- [x] **[LOW]** `TasksFilter` serialization (used in `saved_task_filter_row_test.dart` and `saved_task_filters_section_test.dart`) has a JSON round-trip test in `tasks_filter_sheet_state_test.dart`. A Glados round-trip test over the full `TasksFilter` fields (`selectedTaskStatuses`, `selectedCategoryIds`, `selectedLabelIds`, `selectedPriorities`, `agentAssignmentFilter`) would be stronger. This belongs in the `saved_filters/` state layer tests, not UI tests.
  **RESOLVED:** out of scope here, no change — the item itself scopes the proposed Glados round-trip to the `TasksFilter` state-serialization layer, not this UI test directory. Adding it would require editing state-layer test files outside `test/features/tasks/ui/saved_filters/`, which this pass is forbidden to touch. The UI tests here exercise `TasksFilter` only through rendered output (statuses/categories/priorities), which is already covered; the serialization property test should be added alongside the `saved_filters/` state tests in a future state-layer pass.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `saved_task_filter_row_test.dart` — no test covers the render path when both `count` is present and the row is in the `_editing` (rename) state simultaneously. The count badge is hidden during rename, but this path is not exercised.
  **RESOLVED:** done — added 'hides the count badge while renaming and restores it after commit': count visible → double-tap to rename → count gone while the rename field shows → commit via done action → field gone, count back.

- [x] **[MED]** `saved_task_filters_section_test.dart` — the reorder path (drag to reorder filters) is tested for the "no-op" branch (adjusted equals oldIndex) but not for a genuine reorder where the list order changes and the new order is persisted. `test/features/tasks/ui/saved_filters/saved_task_filters_section_test.dart`.
  **RESOLVED:** stale — 'onReorder moves item up via semantics — persists reordered list' already performs a genuine reorder and asserts the persisted JSON order (`sv-2` before `sv-1`).

- [x] **[MED]** `tasks_saved_filters_tree_test.dart` — no test exercises the `context.mounted` guard in `TasksSavedFiltersTree._build`'s `onDeleted` callback (line 41 of the source). Verifying that the toast is not shown when the context is unmounted would close this path.
  **RESOLVED:** done — added 'onDeleted is a no-op (no toast, no crash) when the tree context is already unmounted': captures the wired closure, unmounts the tree, invokes it, asserts no exception and no SnackBar.

- [x] **[LOW]** `saved_task_filter_row_test.dart` — the `dragHandle` slot when `active: true` is not tested (the test at line 107 checks that the drag handle is hidden on an active row when NOT hovering, but does not test hover-on-active). The hover-in behavior on an active row (which also shows the delete button) may have different visual styling.
  **RESOLVED:** done — added 'hover on an active row also reveals the delete affordance and drag handle': starts with `active: true` + a `dragHandle`, asserts neither grip nor delete glyph before hover, then after a mouse hover asserts both the delete button and drag handle surface (and the count is still present behind the delete glyph). This proves `active` only changes the surface tint, not the hover-driven controls — closing the previously-untested hover-on-active path.

---

## Test execution speed opportunities

- [x] **[MED]** `saved_task_filter_row_test.dart` uses `_afterDoubleTapTimeout = Duration(milliseconds: 350)` in multiple tests to pump past the GestureDetector's double-tap timer. This is the correct deterministic approach (no real sleeps). The pattern is well-implemented.
  **RESOLVED:** verified, no change — the item itself records the pattern as correct; the 350 ms pumps are fake-time (no wall-clock cost) and stay.

- [x] **[LOW]** `saved_task_filters_section_test.dart` — 6 `pumpAndSettle` calls. The section loads from `SavedTaskFiltersController` (async provider), so the first `pumpAndSettle` after `_pumpSection` is justified. Subsequent calls within a test (after a synchronous state change) could be `tester.pump()`. Net impact is small given only 6 calls total.
  **RESOLVED:** confirmed, no change. Traced each call: the one in `_pumpSection` settles the async provider load (justified). The remaining post-mutation settles wait on the controller's two-hop async chain (`final current = await future;` then `await _persistence.save(next)` in `SavedTaskFiltersController.rename/delete/reorder`) to flush before the `verify`/`verifyNever` reads `saveSettingsItem`. A bare `tester.pump()` advances only one frame and could fire `verify` before the async save runs, making the assertion flaky — so these are not gratuitous. There are no animations in the section (no toast is shown here), so each `pumpAndSettle` returns as soon as the tree is stable; the 10 s default is only a ceiling, not actual cost. Converting them would trade correctness for no measurable speed gain.

- [x] **[LOW]** `tasks_saved_filters_tree_test.dart` — 3 `pumpAndSettle` calls: one after initial pump (provider settle), one after a tap, one after provider mutation. All three are appropriate.
  **RESOLVED:** confirmed, no change — verified against the file: `_pumpTree`'s settle covers the async provider load; the settle after the row tap waits on the awaited `SavedTaskFilterActivator.activate` chain into the page controller; the settle after the direct controller `delete` mutation flushes the controller's async save before reading provider state. Each waits on a real async dependency, so all three are appropriate as the item states.

---

## Summary

- 0 oversized impl files; `saved_task_filter_row.dart` (478 lines) has a clean extraction seam for category-dot rendering
- 1 conditional inline `getIt` registration bypassing `setUpTestGetIt` contract [HIGH]
- 2 coverage gaps worth addressing: genuine reorder persistence path, mounted-guard path
- 1 Glados candidate: delete-confirmation state machine in `saved_task_filter_row.dart` [MED]
- Speed profile is generally good; `_afterDoubleTapTimeout` usage is the correct deterministic pattern
