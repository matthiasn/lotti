# Theming Feature — Test Quality Review

**Scope:** `lib/features/theming/**` + `test/features/theming/**`
Generated by automated test-quality review (static analysis)

---

## File inventory

| File | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/features/theming/model/theme_definitions.dart` | 49 | Yes (114 lines) | None |
| `lib/features/theming/state/theming_controller.dart` | 270 | Yes (731 lines) | Inline GetIt setup instead of `setUpTestGetIt`; real async timeout |

---

## File size / split opportunities

- [x] **[LOW]** Both impl files are well within size limits. No splits needed.
  **RESOLVED:** Confirmed — `theme_definitions.dart` (49 lines) and `theming_controller.dart` (272 lines) are both far under any split threshold. No change.
- [x] **[HIGH]** `test/features/theming/state/theming_controller_test.dart` at **731 lines** approaches the ceiling but stays under 1000. However, it has a natural split seam: **RESOLVED (assessed, no change):** splitting one source file's tests across multiple files contradicts AGENTS.md — "One test file per source file. Never split tests for one source file across multiple test files." The existing `group()` seams provide the internal organisation.
  - `theming_controller_test.dart` — keep `themingControllerProvider` + `ThemingState` groups (~470 lines).
  - `enable_tooltips_provider_test.dart` — extract the `enableTooltipsProvider` group (~70 lines).
  - `theming_controller_error_test.dart` — extract the `error handling` group (~300 lines).
  This would keep each file under ~500 lines and align with the one-concern-per-file principle.

---

## Test quality improvements

- [x] **[HIGH]** `theming_controller_test.dart:382–401` (`handles stream errors`) — **Real async timeout violation.** Uses a real `Completer<void>` with `.timeout(const Duration(milliseconds: 100))`. This waits for real wall-clock time. Rewrite using `fakeAsync` and a synchronous stream error emission:
  ```dart
  fakeAsync((async) {
    container.listen(enableTooltipsProvider, (_, _) {});
    tooltipController.addError(error);
    async.flushMicrotasks();
    final state = container.read(enableTooltipsProvider);
    expect(state.hasError, isTrue);
  });
  ```
- [x] **[MED]** `theming_controller_test.dart:53–108` — **Inline GetIt setup / teardown.** The `setUp` manually calls `GetIt.I.allowReassignment = true` and registers five singletons; `tearDown` calls `GetIt.I.reset()`. The project provides `setUpTestGetIt()` / `tearDownTestGetIt()` (from `test/widget_test_utils.dart`) for this purpose. Migrate to those helpers, passing the extra service registrations as additional setup after calling `setUpTestGetIt()`.
  - **RESOLVED:** done — `setUp` now calls `setUpTestGetIt(additionalSetup: ...)` (OutboxService registered there; the helper's real DomainLogger reassigned with the mock these tests verify against), reuses the returned settingsDb/journalDb/updateNotifications mocks with file-specific re-stubs, and `tearDown` uses `tearDownTestGetIt()`.
- [x] **[MED]** `theming_controller_test.dart:121–128` (`initial state has default Grey Law theme`) — includes `expect(state.darkTheme, isNotNull)` and `expect(state.lightTheme, isNotNull)`. These bare existence checks add no coverage beyond what the non-null typed field guarantees. Replace with structural assertions, e.g. `expect(state.darkTheme!.brightness, Brightness.dark)` and `expect(state.lightTheme!.brightness, Brightness.light)`.
  - **RESOLVED:** done — the bare `isNotNull` checks replaced with `state.darkTheme!.brightness == Brightness.dark` / `state.lightTheme!.brightness == Brightness.light`.
- [x] **[MED]** `theming_controller_test.dart:405–454` (`gracefully handles OutboxService not registered`) — the test body comment says "Test passes if no exception is thrown" and makes no `expect` call. This is a pure smoke test. Add at least one assertion, e.g. that the state's `lightThemeName` is unchanged after the call, or that `saveSettingsItem` was still called.
  - **RESOLVED:** done — the test now asserts the local state change (`lightThemeName == 'Indigo'`) and that `saveSettingsItem(lightSchemeNameKey, 'Indigo')` was still called; only the sync enqueue is skipped when OutboxService is missing.
- [x] **[LOW]** `theme_definitions_test.dart:44–48` (`has non-null FlexScheme value`) — `expect(themes[defaultThemeName], isNotNull)` is a bare existence check. The surrounding group already checks all themes for `isA<FlexScheme>()`, making this assertion redundant. Remove or replace with `expect(themes[defaultThemeName], FlexScheme.greyLaw)` to pin the specific value.
  **RESOLVED:** done — the bare `isNotNull` check was replaced with `expect(themes[defaultThemeName], equals(FlexScheme.greyLaw))`, pinning the concrete scheme so an accidental remap of the default entry is caught. Renamed the test to `maps to the expected FlexScheme value`.
- [x] **[LOW]** `theme_definitions_test.dart:51–60` (`contains expected number of standard themes`) — hardcodes `equals(21)`. This will silently fail when a new theme is added. Replace with a size check that auto-derives from the map, or add a comment noting that this test is intentionally a pinned count and must be updated when adding themes.
  **RESOLVED:** done — kept the pinned `equals(21)` (auto-deriving from `themes.length` would be tautological and lose the guard) and added a comment documenting that it is an intentional pinned count that must be updated deliberately when a theme is added/removed.

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** `isValidThemeName(String?)` is a pure function with a simple boolean output. A Glados property (`for any string not in `themes.keys`, isValidThemeName returns false`) could guard against accidental `containsKey`-true collisions. The input space is small and the function is trivial, so the benefit is marginal. Mention as a low-priority future hardening.
  **RESOLVED:** done — added a `Glados<String>(any.string)` property (`tags: 'glados'`) in the `isValidThemeName function` group asserting `isValidThemeName(input) == themes.containsKey(input)` for any string. This is stronger than the suggested "returns false for non-keys" form: it pins validity to exactly map membership, so it also catches false-negatives on real keys.
- [x] **[LOW]** No other meaningful Glados candidates. `ThemingController` has real I/O (SettingsDb, OutboxService) and is not generative. `_buildTheme` has no invariants beyond "does not throw", which is already covered implicitly.
  **RESOLVED:** Confirmed — `ThemingController` depends on GetIt-resolved `SettingsDb`/`OutboxService`/`UpdateNotifications` and is exercised via mocks, not generators. `_buildTheme` is private and brightness-routing is now covered by an example test. No additional Glados target. No change.

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `ThemingController.setDarkTheme` is tested, but there is no test that sets both light and dark themes and then verifies that `_buildTheme` is called with the correct `isDark` flag for each. The test only checks the name stored in state, not whether `FlexThemeData.dark` vs `FlexThemeData.light` was actually used.
  - **RESOLVED:** done — added `'setLightTheme and setDarkTheme rebuild each slot with the right brightness'`: sets both slots, asserts each rebuilt ThemeData's `brightness` (light slot → light, dark slot → dark) and that the two resolved schemes produce different primaries.
- [x] **[MED]** `_getEmojiFontFallback()` returns `null` on web (`kIsWeb`) and the fallback list otherwise. There is no test exercising the web branch. Since `kIsWeb` is compile-time, this is hard to test without a platform stub; document as a known untestable branch.
  - **RESOLVED:** (assessed, documented) — `kIsWeb` is a compile-time constant, so the web branch cannot be exercised from VM unit tests; a comment in `_getEmojiFontFallback` now records this known untested trivial branch.
- [x] **[LOW]** `ThemingState.copyWith` with all-null arguments is tested (`copyWith preserves all values`), but `copyWith` where only `themeMode` is updated (and themes are null in original state) is not. Low risk given the simple null-coalescing pattern.
  **RESOLVED:** done — added `copyWith updates only themeMode and leaves null fields null` in the `ThemingState` group: starts from `const ThemingState()` (all theme/name fields null), applies `copyWith(themeMode: ThemeMode.light)`, and asserts the mode changed while every theme/name field stays null.
- [x] **[LOW]** `onThemeSelectionChanged` with an empty set (`modes.first` would throw). This is a potential crash path if the `SegmentedButton` ever emits an empty set. There is no test or guard for `modes.isEmpty`. Consider adding a defensive check in the impl and a test for the empty-set path.
  **RESOLVED:** done (test only) — added `onThemeSelectionChanged with empty set throws and persists nothing` that asserts the current behavior (`throwsStateError` from `modes.first`) and verifies no `saveSettingsItem`/`enqueueMessage` occurs on the failed call. The suggested defensive guard in the impl was NOT added: this batch is restricted to `test/features/theming/` and may not edit `lib/`. The test documents the crash path and notes it should flip to a no-op expectation if a guard is later added to `ThemingController.onThemeSelectionChanged`.

---

## Test execution speed opportunities

- [x] **[HIGH]** `theming_controller_test.dart:382–401` — real 100 ms timeout via `completer.future.timeout(...)`. Even though 100 ms is short, it still involves real wall-clock time on every CI run. Switching to `fakeAsync` removes the real-time wait entirely. **Estimated impact:** −100 ms per run (low absolute impact but violates the fake-time policy).
- [x] **[MED]** Multiple tests call `async.elapse(const Duration(milliseconds: 100))` to wait for the initial `_loadSelectedSchemes` async call. This is correct under fakeAsync but adds virtual-time overhead. Consider extracting a `_waitForInit(async)` helper to centralize the `elapse(100ms) + flushMicrotasks()` pattern used across ~10 tests.
  - **RESOLVED:** done — extracted `waitForInit(async)` (elapse 100 ms + flushMicrotasks) and replaced all 18 repeated init-wait pairs.
- [x] **[LOW]** All other timer and debounce tests correctly use `fakeAsync` with appropriate `elapse` + `flushMicrotasks` sequences. No real-timer or `Future.delayed` usage found beyond the one Completer case above.
  **RESOLVED:** Confirmed — re-audited `theming_controller_test.dart`: the lone real-time `Completer.future.timeout` was already replaced with a deterministic `await completer.future` (the addError → provider error → listener completion chain settles on microtasks), and every other timer/debounce test uses `fakeAsync` + `elapse`/`flushMicrotasks`. No `Future.delayed`/`sleep`/real `Timer`. No change.

---

## Summary

- Test file at 731 lines is large; split into 3 focused files is recommended.
- One real-time violation: `completer.future.timeout(100ms)` in `handles stream errors`.
- Two weak assertions: bare `isNotNull` on theme objects; smoke-only `OutboxService not registered` test.
- Inline GetIt setup should migrate to `setUpTestGetIt()` / `tearDownTestGetIt()`.
- One potentially crashy uncovered path: `onThemeSelectionChanged` with an empty set.
