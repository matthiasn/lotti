# Test Review: test/flatpak/

Scope: `test/flatpak/flatpak_security_test.dart` (341 lines). Lib counterparts: `lib/services/portals/portal_service.dart` (220 lines), `lib/services/portals/screenshot_portal_service.dart` (236 lines).

Generated by automated test-quality review (static analysis).

## File / scope table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/services/portals/portal_service.dart` | 220 | Yes (partial, via flatpak_security_test) | Core business logic of `PortalService` not unit-tested; only constants exercised |
| `lib/services/portals/screenshot_portal_service.dart` | 236 | Minimal | Only `UnsupportedError` throw (outside Flatpak) is covered |
| `test/flatpak/flatpak_security_test.dart` | 341 | n/a | Many tests assert on hardcoded string literals and local variables, not actual runtime behavior |

---

## File size / split opportunities

- [x] **[LOW]** Files are within acceptable limits. No split needed.
  **RESOLVED:** Confirmed non-actionable. `flatpak_security_test.dart` is 392 lines after this review's additions — comfortably within limits; no split warranted.

---

## Test quality improvements

- [x] **[HIGH]** Large portions of `flatpak_security_test.dart` are documentation disguised as tests. Examples: **RESOLVED:** the five self-asserting tests now validate the real checked-in manifest (`flatpak/com.matthiasn.lotti.flatpak-flutter.yml`) via a `_manifestFinishArgs()` reader — exact filesystem grants (ro/app-scoped invariant), runtime pin (YY.08 cadence; the hardcoded 24.08 was already stale vs the manifest's 25.08), declared shares/sockets, and least-privilege checks (no host/home filesystem, device access limited to DRI).
  - `'uses Freedesktop runtime 24.08'` (lines 162-175) — only asserts on two `const` strings defined locally inside the test body. No app code is exercised.
  - `'network access is properly declared'` (lines 212-222), `'IPC is shared for display server communication'` (lines 224-233), `'audio access uses PulseAudio socket'` (lines 235-243) — each asserts `expect(true, isTrue)` on a `const bool = true` literal defined in the test body. These have zero value and should be removed.
  - `'principle of least privilege is followed'` (lines 289-319) — iterates a `const` map declared inside the test and validates its own keys. Nothing in the app is exercised.
  - `'validates restricted filesystem access paths'` (lines 47-69) — same pattern; a locally defined `const` map is iterated and validated against itself.
- [x] **[HIGH]** `'portal handle tokens are unique and secure'` (lines 141-158) uses `await Future<void>.delayed(const Duration(milliseconds: 2))` — a real timer delay inside a test. This violates the project fake-time policy. Since `PortalService.createHandleToken` uses `DateTime.now().millisecondsSinceEpoch`, the test relies on wall-clock progress to assert token uniqueness, which is inherently flaky at microsecond resolution. The correct approach: mock the timestamp source or assert on token prefix only, not timestamp suffix difference.
- [x] **[MED]** `'ensures app data is properly isolated'` (lines 72-84) and `'verifies system directories are not directly accessible'` (lines 86-104) — when outside Flatpak (the common test environment) the bodies are empty (`if (PortalService.isRunningInFlatpak)` gates them). These tests pass vacuously and provide no signal outside the sandbox. Document this limitation or convert to unconditional assertions on the constants being tested.
  **RESOLVED:** Both `if (PortalService.isRunningInFlatpak)`-gated tests were rewritten to assert unconditionally against the real checked-in manifest (`_readManifest()`/`_manifestFinishArgs()`). The isolation test now asserts the `app-id` is declared and that no filesystem grant reaches into `.var/app` (the per-app data sandbox of another app); the system-directory test now asserts no `--filesystem=` grant contains any sensitive host location (`/usr`, `/etc`, `/var/log`, `/root`, `host`, `home`). Verified against the manifest's three grants (`xdg-documents/Lotti:create`, `xdg-pictures:ro`, `xdg-download/Lotti:create`) — no false positives.

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** `PortalService.createHandleToken(String prefix)` performs string formatting. A Glados property that the returned token always starts with `prefix + '_'` and ends with a numeric suffix would cover the format contract over arbitrary prefix strings. Low priority given the simplicity.
  **RESOLVED:** Added two `tags: 'glados'` property tests (config omitted, per policy) in the `Portal Communication Security` group of `flatpak_security_test.dart`, using the built-in `glados.any.letterOrDigits` generator (no `_`, so `split('_')` yields exactly 3 parts). The first property asserts the `<prefix>_<timestamp>_<counter>` format contract (prefix preserved, positive integer timestamp, non-negative integer counter) over arbitrary prefixes; the second asserts successive calls are always unique with a strictly monotonic counter suffix.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/services/portals/portal_service.dart` and `screenshot_portal_service.dart` are each ~220+ lines but only their static constants and the non-Flatpak guard are exercised. The actual D-Bus communication, response-handling, cancellation, and timeout paths are untested (though these require real Linux D-Bus infrastructure and would be integration tests; document the gap). **RESOLVED:** the premise is stale — the portal D-Bus protocol (initialization, response handling, signals, errors) is unit-tested with mocked `DBusClient`s in `test/services/portals/` (portal_service_test + 29-test screenshot_portal_service_test). The remaining genuine gap (real `xdg-desktop-portal` round-trips needing Linux D-Bus infra) is now documented in the suite header as integration-test scope.
- [x] **[MED]** `ScreenshotPortalService.takeScreenshot()` — only the `!shouldUsePortal` branch is asserted to throw `UnsupportedError`. The happy path and timeout path are untested.
  - **RESOLVED:** (assessed — integration-scope gap, accepted) confirmed by reading the source: `ScreenshotPortalService` is a hard **singleton** (`factory => _instance`) that builds its own D-Bus connection internally via the inherited `createPortalObject()` — there is no constructor/`@visibleForTesting` seam to inject a mock `DBusClient`, and the existing `screenshot_portal_service_test.dart` only *creates* a `MockDBusClient` (never wired in) and early-returns its "happy path" cases on non-Flatpak CI hosts. Driving the real happy/timeout path would require **two** production seams (force `shouldUsePortal=true` via the private `_isRunningInFlatpakCache`, *and* inject a mock D-Bus object into the singleton) plus singleton-state reset between tests — a disproportionate refactor for a Linux/Flatpak-only D-Bus screenshot path. The D-Bus *protocol* layer (init/response/signal/error/cancel) is already unit-tested with mocked clients, and the pure helpers (`parseUriFromResults`, `persistScreenshot`) are covered; the residual real-`xdg-desktop-portal` round-trip is genuine integration-test territory and is documented as such rather than faked.
- [x] **[LOW]** `PortalService.isRunningInFlatpak` detection — the test for it is conditional on the actual runtime environment. A test that mocks `Platform.environment` would give deterministic coverage regardless of host OS.
  - **RESOLVED:** (assessed — host-dependent, accepted) the detection reads `Platform.environment[...]` directly (FLATPAK_ID / FLATPAK_DEST / container / `/app` + `/var/run/host` path probes) and memoizes into the private static `_isRunningInFlatpakCache`. Deterministically exercising each branch needs the environment to be injectable — a production refactor (route detection through an injectable env map) disproportionate to this single boolean on a Linux-only path. A `@visibleForTesting` cache setter would only let tests bypass detection, not exercise the branches the item targets, so it adds no real coverage. Accepted as host-dependent; the non-Flatpak default (the path every CI host and the app's non-Linux targets actually take) is exercised today.

---

## Test execution speed opportunities

- [x] **[HIGH]** `flatpak_security_test.dart:144` — `await Future<void>.delayed(const Duration(milliseconds: 2))` is a real wall-clock delay inside a unit test. Violates the fake-time policy. In a fast machine this is harmless, but it is non-deterministic and sets a bad precedent. Fix: either inject a clock into `createHandleToken`, or test only the token's structural properties (prefix presence, numeric suffix), not inter-token timestamp inequality.

---

## Summary

- Roughly half the tests in `flatpak_security_test.dart` assert on locally-defined `const` data with no app code involved — they are documentation, not tests, and should be removed.
- One test contains a real `Future.delayed` timer, violating the project fake-time policy.
- The two portal service implementation files (~456 lines combined) have minimal meaningful test coverage; D-Bus-dependent paths are untestable without infrastructure, but the non-D-Bus branches (error handling, token format, timeout constant) can and should be unit-tested.
- No Glados opportunities of substance.
