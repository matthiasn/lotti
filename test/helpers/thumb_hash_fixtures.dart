/// ThumbHash fixtures for widget tests: one hash that decodes, one that does
/// not.
library;

/// The penguin world's "control room cats" cover, as
/// `demoMediaThumbHashes` stores it.
const sampleThumbHash = 'EMYFFIIHmId8iIZwh4a1kF0J6A==';

/// What a corrupt store might hand a widget; parses as nothing.
const corruptThumbHash = 'not a hash!';
