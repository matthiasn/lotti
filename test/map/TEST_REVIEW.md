# Test Review: lib/map/ + test/map/

Scope: `lib/map/cached_tile_provider.dart` (15 lines), `test/map/cached_tile_provider_test.dart` (124 lines).

Generated by automated test-quality review (static analysis).

## File / scope table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/map/cached_tile_provider.dart` | 15 | Yes | Impl is 1 method override; test file is 8× larger and has 3 weak assertions |
| `test/map/cached_tile_provider_test.dart` | 124 | n/a | Constructor smoke tests and `isNotNull` asserts dominate |

---

## File size / split opportunities

- [x] **[LOW]** Both files are well within size limits. No split needed.
  - **RESOLVED:** (assessed, no change) — confirmed `cached_tile_provider_test.dart` is 131 lines (source likewise small), far under the 1000-line ceiling; no split warranted.

---

## Test quality improvements

- [x] **[HIGH]** `cached_tile_provider_test.dart:17-21` — `'constructor accepts headers parameter'` test asserts only `expect(provider, isNotNull)`. This is a constructor smoke test with zero behavioral value; the AGENTS.md rule "no constructor smoke tests" applies. Delete or replace with a meaningful assertion (e.g., that `getImage` returns a provider whose URL is derived from the headers or tile layer). **RESOLVED:** the smoke test is replaced by 'headers reach the image provider produced by getImage', asserting the constructor headers surface on the `CachedNetworkImageProvider`.
- [x] **[HIGH]** `cached_tile_provider_test.dart:110-113` — `'headers parameter is optional'` uses `expect(CachedTileProvider.new, returnsNormally)` which is another constructor smoke test. Same issue — delete or merge into a substantive test. **RESOLVED:** replaced with 'omitted headers yield a provider with empty headers' — drives `getImage` and asserts the produced `CachedNetworkImageProvider` carries no headers.
- [x] **[MED]** `cached_tile_provider_test.dart:42-49` — `'getImage passes headers to CachedNetworkImageProvider'` asserts `imageProvider.url, contains('tile.openstreetmap.org')` as a proxy for headers. The comment explicitly says "We can't directly access them" — but `CachedNetworkImageProvider.headers` *is* a public field. Assert the actual header map equality instead of URL presence.
  **RESOLVED:** the test now asserts `imageProvider.headers == headers` (two-entry map) and the misleading comment is gone.
- [x] **[MED]** `cached_tile_provider_test.dart:99-108` — `'multiple instances with different headers'` tests that three instances are not `equal` to each other using the default `Object` equality. Since `CachedTileProvider` does not override `==`, this always passes regardless of implementation. The test has no signal value; remove it.
  **RESOLVED:** removed — it only exercised `Object` identity.
- [x] **[LOW]** `cached_tile_provider_test.dart:55-60` — `'getImage without headers creates provider with null headers'` asserts only `isNotNull`. Should verify `imageProvider.headers` is null or empty.
  **RESOLVED:** removed as a redundant `isNotNull` duplicate of `'omitted headers yield a provider with empty headers'`, which already asserts the produced provider has empty headers.

---

## Generative (Glados) testing opportunities

- [x] **[LOW]** `getTileUrl` has pure formatting logic (string interpolation of `{z}/{x}/{y}` into a URL template). A Glados property that `getTileUrl(TileCoordinates(x, y, z), layer).contains(z.toString())` holds for any valid tile coordinate triple is a genuine gap — the current tests only exercise three hand-picked coordinate triples. Low priority given the simplicity, but it is the only pure function in the area.
  **RESOLVED:** added a `Glados3(any.int, any.int, any.int)` property (tagged `glados`) asserting `getTileUrl` interpolates to exactly `https://tile.openstreetmap.org/$z/$x/$y.png` for any coordinate triple. (`getTileUrl` itself is inherited from flutter_map's `TileProvider`; the property pins the substitution contract the wrapper relies on.)

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `CachedTileProvider.getImage` passes `headers` to `CachedNetworkImageProvider`. No test directly asserts `imageProvider.headers == headers` for a non-empty map (the test at line 33 only confirms the provider was created and checks the URL). This is the only real behavior the class has — it should be its own clear assertion.
  **RESOLVED:** two tests now assert `imageProvider.headers == headers` for non-empty maps (`'headers reach the image provider produced by getImage'` and the strengthened `'getImage passes headers to CachedNetworkImageProvider'`).
- [x] **[LOW]** No test for the case where the tile layer URL template has no `{z}/{x}/{y}` placeholders (degenerate/misconfigured layer). Behaviour falls through to `flutter_map`'s own substitution, so this may be out-of-scope for the thin wrapper.
  **RESOLVED (no change):** confirmed out of scope — `CachedTileProvider` adds no template logic of its own; substitution of a degenerate template is entirely `flutter_map`'s `TileProvider.getTileUrl` behavior, not a path this wrapper can affect or should re-test.

---

## Test execution speed opportunities

- [x] **[LOW]** No widget tests, no async, no timers. All tests are synchronous unit tests. Speed is fine.
  - **RESOLVED:** (assessed, no change) — confirmed no `Future.delayed`/`Timer`/`testWidgets`/`await tester` in the map tests; all synchronous, no speed concern.

---

## Summary

- Three of nine tests (`isNotNull`, `returnsNormally`, non-equality-based instance comparison) are constructor smoke tests with zero behavioral signal and should be deleted.
- The one real behavioral assertion (`getImage` returns a `CachedNetworkImageProvider` with the right URL) does not assert the `headers` field, which is the entire purpose of the class.
- One modest Glados candidate exists for `getTileUrl`'s URL-substitution logic.
- No speed concerns; all tests are synchronous.
