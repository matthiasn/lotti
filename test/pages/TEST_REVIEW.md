# Test Review: lib/pages/ + test/pages/

Scope: `lib/pages/create/complete_habit_dialog.dart` (353 lines), `lib/pages/create/create_measurement_dialog.dart` (262 lines), `lib/pages/empty_scaffold.dart` (21 lines), and their counterpart tests.

Generated by automated test-quality review (static analysis).

## File / scope table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/pages/create/complete_habit_dialog.dart` | 353 | Yes — `complete_habit_dialog_test.dart` (463 lines) | `getIt.registerSingleton` used directly, not `setUpTestGetIt` |
| `lib/pages/create/create_measurement_dialog.dart` | 262 | Yes — `create_measurement_dialog_test.dart` (597 lines) | Same getIt pattern; heavy `pumpAndSettle` use |
| `lib/pages/empty_scaffold.dart` | 21 | **No** | Trivial widget, no test file |

---

## File size / split opportunities

- [ ] **[MED]** `test/pages/create/create_measurement_dialog_test.dart` is 597 lines — oversized for a single widget. The `tapping suggestion chip saves measurement` test (lines 374–496) alone is 122 lines, with 60+ lines of mock-data construction repeated in the test body. Extract a `_buildMockMeasurements()` helper and a shared `_pumpDialog(tester, {BeamerDelegate? delegate})` helper that absorbs the `ConstrainedBox`/`makeTestableWidgetWithScaffold` boilerplate.
- [ ] **[LOW]** `test/pages/create/complete_habit_dialog_test.dart` is 463 lines but has a local `pumpHabitDialog` helper — reasonable. No split needed.

---

## Test quality improvements

- [ ] **[HIGH]** Both test files call `getIt.registerSingleton<...>(...)` directly in `setUp` and call `getIt.reset` in `tearDown`, bypassing the project-standard `setUpTestGetIt()` / `tearDownTestGetIt()` helpers from `test/widget_test_utils.dart`. This diverges from centralized infrastructure and risks state leakage if `getIt.reset` throws or is missed.
- [ ] **[MED]** `complete_habit_dialog_test.dart:99-102` — after tapping the save button the test only calls `pumpAndSettle()` and does not `verify` that `createHabitCompletionEntry` was called, making it a partial smoke test. The `Fail` and `Skip` button tests do capture and verify — the `Success` first test should do the same.
- [ ] **[MED]** `create_measurement_dialog_test.dart:374–496` — the suggestion-chip test registers `MockUpdateNotifications` inside the test body *after* `setUp` has run, creating a second path through `getIt.registerSingleton` that bypasses normal teardown scope; the registration is also not cleaned up between subtests.
- [ ] **[LOW]** `complete_habit_dialog_test.dart:181` — `expect(find.byType(SingleChildScrollView).evaluate().length, 2)` tests structure rather than behavior; this is a layout smoke test. Replace with an assertion that `DashboardWidget` is present.

---

## Generative (Glados) testing opportunities

- [ ] **[LOW]** No pure-logic functions in either dialog file warrant Glados. All logic is UI-state machines or I/O calls. Skip.

---

## Coverage / missing-behavior gaps

- [ ] **[HIGH]** `lib/pages/empty_scaffold.dart` (21 lines) has **no test**. Although trivial, the project's "one test file per source file" rule applies. A single widget test verifying the `AppBar` title and optional body render is sufficient.
- [ ] **[MED]** `_MeasurementDialogState.saveMeasurement` error path: if `_formKey.currentState` is null, validation is skipped silently (`create_measurement_dialog.dart:40-73`). There is no test for form-state-null guard.
- [ ] **[MED]** `HabitDialog` + `isDesktop` hotkey registration branches (`complete_habit_dialog.dart:91-97`, `103-105`) are untested. Platform-conditional paths not exercised.
- [ ] **[LOW]** `HabitDialog` date field `setDateTime` callback is tested only by direct callback invocation (`complete_habit_dialog_test.dart:294-306`). The tap-on-field → picker-open → pick-date full flow is not exercised.

---

## Test execution speed opportunities

- [ ] **[HIGH]** Both test files use `pumpAndSettle()` heavily (17 calls in `create_measurement_dialog_test.dart`, 11 in `complete_habit_dialog_test.dart`). For dialogs that render synchronously after `pump()`, most of these could be replaced with `tester.pump()` to cut run time and reduce the risk of hanging on an unsettled animation. Use `pumpAndSettle` only where animation completion is load-bearing.
- [ ] **[LOW]** No `Future.delayed` or real timers found. Hotkey registration in `initState` may not play well with `pumpAndSettle` in the desktop path — worth investigating if tests flake on CI.

---

## Summary

- `lib/pages/empty_scaffold.dart` is completely untested, violating the one-file-one-test rule.
- Both dialog test files bypass `setUpTestGetIt`/`tearDownTestGetIt`, creating getIt-management inconsistency.
- `create_measurement_dialog_test.dart` is 597 lines and has duplicated pump/setup boilerplate; a shared helper would shrink it significantly.
- Heavy `pumpAndSettle` use across both files is the main speed risk.
