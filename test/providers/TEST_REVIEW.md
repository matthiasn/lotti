# Test Review: lib/providers/ + test/providers/

Scope: `lib/providers/service_providers.dart` (70 lines), `test/providers/service_providers_test.dart` (35 lines).

Generated by automated test-quality review (static analysis).

## File / scope table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/providers/service_providers.dart` | 70 | Yes (partial) | Two providers untested: `syncDatabaseProvider` and `outboxLoginGateStreamProvider` |
| `test/providers/service_providers_test.dart` | 35 | n/a | Incomplete provider coverage; otherwise clean |

---

## File size / split opportunities

- [x] **[LOW]** Both files are small. No split needed.
  **RESOLVED:** Confirmed by reading both files — `service_providers.dart` is 70 lines of simple provider declarations and the test is a single loop plus one StreamProvider test. No split warranted.

---

## Test quality improvements

- [x] **[LOW]** The test uses a `for` loop over a `Map<String, ProviderListenable>` to generate test cases — a clean pattern. No issues with test quality or setup verbosity.
  **RESOLVED:** Verified — the `for` loop over the providers map is a DRY, parameterised pattern and each generated test asserts a meaningful error type (`ProviderException` wrapping `UnimplementedError`). No change needed.

---

## Generative (Glados) testing opportunities

- None applicable. All providers are simple `throw UnimplementedError` stubs or a single `StreamProvider` delegation. No pure logic to exercise generatively.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `syncDatabaseProvider` (`service_providers.dart:36-40`) is **not in the tested map** in `service_providers_test.dart:8-14`. It should throw `UnimplementedError` wrapped in `ProviderException` when read unoverridden, just like the others. Add it to the loop. **RESOLVED:** `syncDatabaseProvider` added to the tested map; it throws `ProviderException`-wrapped `UnimplementedError` like the others.
- [x] **[MED]** `outboxLoginGateStreamProvider` (`service_providers.dart:59-62`) is also absent from the test. It is a `StreamProvider` that calls `ref.watch(outboxServiceProvider).notLoggedInGateStream`; because `outboxServiceProvider` itself throws, reading `outboxLoginGateStreamProvider` in an unoverridden container should also surface an error. A test that reads it and asserts it enters an error state (or throws) would cover this delegation.
  **RESOLVED:** Added a dedicated test in `service_providers_test.dart` that reads `outboxLoginGateStreamProvider` in an unoverridden `ProviderContainer`. Unlike the plain throwing `Provider`s (which throw a `ProviderException` on read), a `StreamProvider` whose `create` throws captures the error into the returned `AsyncValue` (verified against Riverpod 3.1.0's own `stream_provider_test.dart`: "throwing inside 'create' result in an AsyncValue.error" with the raw thrown object as `.error`). The test therefore asserts `state.hasError == true`, `state.hasValue == false`, and `state.error` is the originating `UnimplementedError`. This exercises the delegation through `outboxServiceProvider`.

---

## Test execution speed opportunities

- [x] **[LOW]** Tests are purely synchronous. No speed issues.
  **RESOLVED:** Confirmed — all tests are synchronous `container.read` checks with no timers, delays, or `pumpAndSettle`. The added StreamProvider test is also synchronous (the error is captured at read time). No speed concerns.

---

## Summary

- `syncDatabaseProvider` and `outboxLoginGateStreamProvider` are both missing from the test coverage loop, leaving two providers without any test.
- The test file is otherwise well-structured; the fix is a 2-line addition to the providers map.
- No quality, Glados, or speed issues beyond the missing coverage.
