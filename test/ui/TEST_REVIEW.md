# Test Review: lib/ui/ + test/ui/

Scope: `lib/ui/app_fonts.dart` (39 lines), `test/ui/app_fonts_test.dart` (48 lines).

Generated by automated test-quality review (static analysis).

## File / scope table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/ui/app_fonts.dart` | 39 | Yes | One test is `isA<TextStyle>()` (weak) but coverage is otherwise adequate |
| `test/ui/app_fonts_test.dart` | 48 | n/a | Minor: one no-args defaults test adds little over the first test |

---

## File size / split opportunities

- [ ] **[LOW]** Both files are small and well within limits. No split needed.

---

## Test quality improvements

- [ ] **[MED]** `app_fonts_test.dart:40-45` — `'inconsolata returns a style with defaults when no args'` asserts only `isA<TextStyle>()` and `fontFamily == 'Inconsolata'`. The first assertion (`isA<TextStyle>()`) is a type-existence check with zero behavioral value — every `TextStyle` constructor returns a `TextStyle`. The useful assertion is `fontFamily`, which duplicates what the first test already checks when `allowRuntimeFetching` is false. Consider removing or folding this test into the first.
- [ ] **[LOW]** The `'delegates to google_fonts when runtime fetching allowed'` test (`app_fonts_test.dart:33-38`) asserts `fontFamily` matches the `GoogleFonts.inconsolata()` family — solid. However it does not assert that the numeric parameters (`fontSize`, `fontWeight`, etc.) are forwarded, unlike the fallback-path test. A parallel `fontSize` check would catch a future regression where the delegation path drops parameters.

---

## Generative (Glados) testing opportunities

- [ ] **[LOW]** `AppFonts.inconsolata` accepts four optional parameters (`fontSize`, `fontWeight`, `color`, `letterSpacing`). A Glados property that all provided values are present in the returned `TextStyle` — for both the runtime-fetching-off and on branches — would be a genuine property test. Moderate fit; each parameter is independently forwarded with no transformation, making shrinking trivially useful.

---

## Coverage / missing-behavior gaps

- [ ] **[LOW]** The `GoogleFonts.config.allowRuntimeFetching = true` branch is tested for `fontFamily` only. The remaining four constructor parameters are not verified for pass-through on the live path. In the fallback path they are all checked; symmetry would be valuable.

---

## Test execution speed opportunities

- [ ] **[LOW]** All tests are synchronous. No speed issues.

---

## Summary

- Test coverage is functionally adequate for this small file.
- One test (`isA<TextStyle>()` smoke assertion) can be removed; one test has an asymmetry vs the fallback path.
- A small Glados opportunity exists for round-trip parameter forwarding.
- No speed concerns.
