# Test Review: lib/widgetbook/ + test/widgetbook/

Scope: `lib/widgetbook/zoom_pan_wrapper.dart` (81 lines), `test/widgetbook/zoom_pan_wrapper_test.dart` (246 lines).

Generated by automated test-quality review (static analysis).

## File / scope table

| File | Lines | Has test? | Top issue |
|------|-------|-----------|-----------|
| `lib/widgetbook/zoom_pan_wrapper.dart` | 81 | Yes | Well-covered; minor gap on `KeyRepeatEvent` and `applyScale` below-min boundary |
| `test/widgetbook/zoom_pan_wrapper_test.dart` | 246 | n/a | Structurally sound; a few weak assertions; real platform check in a module-level value |

---

## File size / split opportunities

- [x] **[LOW]** Both files are well within size limits. No action needed.
  **RESOLVED:** Confirmed by reading both files — source is 81 lines, test is well under any split threshold. No action needed.

---

## Test quality improvements

- [x] **[MED]** `zoom_pan_wrapper_test.dart:41-49` — `'renders SizedBox.shrink when child is null'` asserts `find.byType(SizedBox), findsOneWidget` — but `SizedBox.shrink()` and `InteractiveViewer` can both match `SizedBox` in a widget tree. The test already finds an `InteractiveViewer` which is correct; the `SizedBox` assertion is too blunt. Replace with an assertion that the `InteractiveViewer.child` is a `SizedBox` with zero size, or simply remove the redundant `SizedBox` check since the real behavior (child is absent) is proven by `InteractiveViewer` being present.
  **RESOLVED:** Renamed to "uses an empty SizedBox.shrink child when child is null" and replaced the blunt `findsOneWidget` with concrete assertions: reads `InteractiveViewer.child`, asserts it `isA<SizedBox>()` with width/height of 0, and asserts the real content ('Content') is absent (`findsNothing`).
- [x] **[LOW]** `zoom_pan_wrapper_test.dart:10` — `final LogicalKeyboardKey _modifierKey = Platform.isMacOS ? ...` is a real platform check executed at module-load time. In the test environment this value is fixed at the CI runner's platform; tests relying on it will silently use the wrong modifier on the wrong OS. Consider parameterizing modifier-key tests or testing both via `handleKeyEvent` directly (as the `Cmd++` test already does) instead of via `tester.sendKeyDownEvent`.
  **RESOLVED:** Removed the `dart:io` `Platform.isMacOS` module-level check entirely. The keyboard tests now loop over a `const _modifierKeys = [metaLeft, controlLeft]` list, so both Command and Control paths are exercised on every platform. The shortcut key itself is delivered via `handleKeyEvent` directly while the modifier is held via `sendKeyDownEvent`.

---

## Generative (Glados) testing opportunities

- [x] **[MED]** `applyScale(double scale)` clamps its input to `[minScale, maxScale]`. A Glados property test over any `double` input asserting that the resulting `controller.value.getMaxScaleOnAxis()` is always within `[minScale, maxScale]` is a genuine pure-logic property. The current tests only cover three fixed values (2, 10, 0.01) and the default 1.
  **RESOLVED:** Added a Glados property test (`numRuns: 120`, `tags: 'glados'`) over a `scaleInput` generator that constructs a `ZoomPanWrapperState` directly, calls `applyScale(input)`, and asserts the stored x/y diagonal factors (`controller.value.storage[0]`/`[5]`) equal `input.clamp(minScale, maxScale)` (closeTo 1e-9) and lie in `[minScale, maxScale]`. Note: `getMaxScaleOnAxis()`/`currentScale` is NOT a valid observation here because the matrix z-axis stays 1.0, so it floors any sub-unit scale to 1.0 — the raw storage indices are the only faithful read. The controller is disposed in a `finally` per run.
- [x] **[LOW]** `handleKeyEvent` returns `KeyEventResult.ignored` for all non-`KeyDownEvent`/`KeyRepeatEvent` types. A Glados test over synthetic `KeyUpEvent` and `KeyRepeatEvent` inputs would cover the ignored/handled border more exhaustively.
  **RESOLVED:** Added a Glados property test (`numRuns: 100`, `tags: 'glados'`) generating a `KeyUpEvent` over a set of logical keys (add/minus/digit0/keyA/equal/space — both shortcut and non-shortcut keys) and asserting the result is always `KeyEventResult.ignored` and the scale never moves, because the type guard rejects key-up before any modifier/key inspection. (The `handled` KeyRepeatEvent border is covered by the new widget tests under item line 38, which need a live `HardwareKeyboard` modifier-pressed state.)

---

## Coverage / missing-behavior gaps

- [x] **[MED]** `KeyRepeatEvent` handling — `zoom_pan_wrapper.dart:36` accepts both `KeyDownEvent` and `KeyRepeatEvent` but the test suite only tests `KeyDownEvent` and `KeyUpEvent`. A `KeyRepeatEvent` with the modifier held + `+` key should also return `handled` and advance the scale; this path is unexercised.
  **RESOLVED:** Added a `'<modifier> + "+" on a KeyRepeatEvent also zooms in'` widget test (run for both Command and Control) that holds the modifier via `sendKeyDownEvent`, feeds a `KeyRepeatEvent` with `logicalKey: add` through `handleKeyEvent`, and asserts the result is `handled` and `currentScale == initial + zoomStep` (closeTo 0.01).
- [x] **[LOW]** `applyScale` below the minimum: `zoom_pan_wrapper_test.dart:100-111` tests the below-min case but reads `matrix.storage[0]` (a raw index into the underlying `Float64List`) instead of using `getMaxScaleOnAxis()`. While functionally equivalent, it's fragile and inconsistent with the rest of the test file.
  **RESOLVED (corrected):** the review's premise is wrong — they are NOT functionally equivalent. `getMaxScaleOnAxis()` floors at 1.0 for sub-unit scales (the matrix z-axis stays 1.0), so it reports 1.0 for a 0.25 clamp. The below-min test therefore keeps reading the stored x/y factors (`controller.value.storage[0]`/`[5]`), now asserting both equal `minScale`, with a comment explaining why `getMaxScaleOnAxis()` cannot observe a below-min clamp. (Storage indexing is necessary here, not fragile.)

---

## Test execution speed opportunities

- [x] **[LOW]** The `_pumpWrapper` helper uses `tester.pump()` (not `pumpAndSettle`) — correct. No speed issues found.
  **RESOLVED:** Confirmed — `_pumpWrapper` still uses `tester.pump()` only, and no `pumpAndSettle` or real delays were introduced. No action needed.

---

## Summary

- Coverage is solid for the main paths; `KeyRepeatEvent` and the `applyScale` continuous-clamp property are the two genuine gaps.
- `applyScale` is a good Glados candidate — pure arithmetic clamp over any double.
- Module-level `Platform.isMacOS` check in the test file is fragile across CI platforms.
- No speed concerns.
