# test/widgets — Group A (Part 1 of 2)

**Scope:** `lib/widgets/{app_bar,buttons,cards,charts,date_time,events,flags}/` and their mirror test directories.  
**Generated by automated test-quality review (static analysis only).**

---

## Per-file summary

| File (impl) | Lines | Has test? | Top issue |
|---|---|---|---|
| `lib/widgets/app_bar/glass_action_button.dart` | 40 | Yes | 8× ad-hoc `MaterialApp/Scaffold` pump; no `makeTestableWidget`; 1 stray `pumpAndSettle` |
| `lib/widgets/app_bar/glass_back_button.dart` | 43 | Yes | 12× ad-hoc pump; 2× `pumpAndSettle`; `findsOneWidget`-only assertion for nav path |
| `lib/widgets/app_bar/glass_icon_container.dart` | 41 | Yes | 9× ad-hoc pump; no `makeTestableWidget` |
| `lib/widgets/app_bar/journal_sliver_appbar.dart` | 261 | Yes (split across `journal_sliver_appbar_test` + `journal_filter_test` + `journal_filter_icon_test`) | 6× `pumpAndSettle` across 3 files; test files test impl symbols scattered across the appbar file; `JournalFilter` / `JournalFilterIcon` have no separate source files mirroring them |
| `lib/widgets/app_bar/settings_header_dimensions.dart` | 252 | Yes | Good pure-function coverage; Glados opportunity for `collapseProgress`/`horizontalPadding` |
| `lib/widgets/app_bar/settings_page_header.dart` | 317 | Yes (417-line test) | 14× `pumpAndSettle`; test file approaching 500 lines |
| `lib/widgets/app_bar/sliver_title_bar.dart` | 41 | **No** | Zero coverage; no `sliver_title_bar_test.dart` |
| `lib/widgets/app_bar/title_app_bar.dart` | 105 | **No** | Zero coverage (3 classes: `TitleAppBar`, `TitleWidgetAppBar`, `BackWidget`) |
| `lib/widgets/buttons/lotti_primary_button.dart` | 83 | **No** | Zero coverage for primary button |
| `lib/widgets/buttons/lotti_secondary_button.dart` | 83 | Yes | Only 4 tests; disabled-state tap verified but style/color/icon+label combos missing |
| `lib/widgets/buttons/lotti_tertiary_button.dart` | 73 | Yes (240 lines) | 5× `findsOneWidget`; mostly good but 3 near-identical color tests could be parameterized |
| `lib/widgets/cards/index.dart` | 5 | N/A | Barrel file; no test needed |
| `lib/widgets/cards/modern_base_card.dart` | 165 | Yes (402 lines) | 18× `pumpAndSettle` on a static card; inline `MockCallbacks` class not in central mocks |
| `lib/widgets/cards/modern_card_content.dart` | 120 | Yes (352 lines) | 14× `pumpAndSettle` on static card; 9× `isNotNull` |
| `lib/widgets/cards/modern_icon_container.dart` | 98 | Yes (270 lines) | 12× `pumpAndSettle`; 5× `isNotNull`; 12× `makeTestableWidget` OK but no shared pump helper |
| `lib/widgets/cards/modern_status_chip.dart` | 85 | Yes (392 lines) | 12× `pumpAndSettle`; 3× `isNotNull`; light/dark alpha tests are near-duplicate (could loop) |
| `lib/widgets/charts/dashboard_item_modal.dart` | 65 | **No** | Zero coverage; aggregation-type selection logic untested |
| `lib/widgets/charts/habits/dashboard_habits_chart.dart` | 34 | Yes (in `dashboard_habits_test`) | `findsOneWidget` is the only assertion beyond title text |
| `lib/widgets/charts/habits/dashboard_habits_data.dart` | 93 | Yes (`dashboard_habits_data_test`, 110 lines) | Good; Glados opportunity for `habitResultsByDay` deduplication |
| `lib/widgets/charts/habits/habit_completion_rate_chart.dart` | 302 | Yes (560 lines) | 9× `pumpAndSettle`; 14× `findsOneWidget`/`isNotNull`; large test; GetIt manual registration (not `setUpTestGetIt`) |
| `lib/widgets/charts/utils.dart` | 319 | Yes (491 lines; Glados present) | Good; Glados tagged correctly; `aggregateAvgByDay` has no Glados property test |
| `lib/widgets/date_time/datetime_bottom_sheet.dart` | 114 | Yes (within `datetime_field_test`) | `DateTime.now()` used live in "tap now button" test — nondeterministic |
| `lib/widgets/date_time/datetime_field.dart` | 92 | Yes (398 lines) | 12× `pumpAndSettle`; 18× `findsOneWidget`/`isNotNull`; 2× inline mock classes; modal-integration tests use `DateTime.now()` |
| `lib/widgets/events/event_form.dart` | 164 | Yes (2 files: `event_form_test.dart`, `event_form_labels_test.dart`) | 8× `pumpAndSettle`; 13× `isNotNull`; setup duplication between two test files (both replicate GetIt/provider bootstrap) |
| `lib/widgets/events/event_status.dart` | 24 | **No** | Zero coverage for `EventStatusWidget` |
| `lib/widgets/flags/language_flag.dart` | 42 | Yes (41 lines) | `tw`→`gh` override untested; `isNotNull` pattern on returned value |
| **Misplaced tests** | — | — | `dashboard_health_test`, `dashboard_health_data_test`, `dashboard_measurables_test`, `dashboard_workout_test` live in `test/widgets/charts/` but test `features/dashboards/**` widgets — violates one-test-file-per-source-file rule |

---

## File size / split opportunities

- [ ] **[LOW]** `lib/widgets/app_bar/settings_header_dimensions.dart` (252 lines) — pure data class with constants and 3 static math helpers; well within limit but could separate constants from helpers if it grows further. No immediate action needed.
- [ ] **[LOW]** `lib/widgets/app_bar/settings_page_header.dart` (317 lines) — borderline; the `FlexibleSpace` inner builder and the `collapseProgress` rendering logic are candidates for extraction if the file grows.
- [ ] **[LOW]** `lib/widgets/charts/habits/habit_completion_rate_chart.dart` (302 lines) — the `barData` pure function and `leftTitleWidgets`/`getTooltipItems` callbacks are already well-exercised via extracted tests; no split urgently needed.
- [x] **[MED]** `test/widgets/app_bar/settings_page_header_test.dart` (417 lines) — test file growing toward 500; the `pumpWithScale`, `pumpWithWidth`, `pumpWithTheme` helpers are already good extractions, but 10 repeated full-scroll tests could share a single `_pumpHeader(tester, {title, subtitle, scale, width})` top-level helper to cut ~100 lines. **RESOLVED:** done — a single `_pumpHeader(tester, {title, subtitle, scale, width, theme, pinned, bottom, ...})` helper replaced all inline scaffolding; the file shrank to ~260 lines.
- [x] **[MED]** `test/widgets/charts/habits/habit_completion_rate_chart_test.dart` (560 lines) — largest test file in the group; the 5 inline `_*Controller` subclasses and duplicate `ProviderScope/MaterialApp/Scaffold` scaffolding could be collapsed to a `_pumpChart(tester, {controller})` helper, removing ~80 lines. **RESOLVED:** done — the five controller subclasses collapsed into one `_FixedStateController(state)` plus a `pumpChart(tester, {state})` helper; the three bounds-check variants are now a parameterized loop.
- [ ] **[LOW]** `test/widgets/date_time/datetime_field_test.dart` (398 lines) — same `makeTestableWidgetWithScaffold` call repeated ~15 times; extracting a `_pumpField(tester, {dateTime, labelText, mode, clear})` helper would be a clean DRY improvement.

---

## Test quality improvements

- [x] **[HIGH]** `test/widgets/app_bar/glass_action_button_test.dart`, `glass_back_button_test.dart`, `glass_icon_container_test.dart` — all three use 8–12 hand-rolled `MaterialApp(home: Scaffold(body: ...))` pumpWidget calls instead of `makeTestableWidget()`. Extract a file-level `_pump(tester, widget)` helper using `makeTestableWidget`. These are the only Group A files that never call `makeTestableWidget`.

- [x] **[HIGH]** `test/widgets/cards/modern_base_card_test.dart` — inline `class MockCallbacks extends Mock implements TestCallbacks {}` (line 13). This is a callback mock that should either go into `test/mocks/mocks.dart` or be replaced by a plain Dart closure (no Mock needed here — `mockCallbacks.onTap` could simply be `() { callCount++ }`). Using `verify()` on a non-centralised mock is noise; the tap test at line 150 is the only one that actually calls `verify`, and it could use a simple bool flag instead.

- [x] **[HIGH]** `test/widgets/date_time/datetime_field_test.dart` — two inline mock classes: `MockCallback` (line 11) and `MockVoidCallback` (line 15). Neither exists in `test/mocks/mocks.dart`. The `MockVoidCallback` pattern in particular is reused 3 times in the same file (3× `setUp` allocation). These should be extracted to central mocks or replaced with closures.

- [x] **[HIGH]** `test/widgets/events/event_form_labels_test.dart` + `test/widgets/events/event_form_test.dart` — both test `EventForm` and replicate identical GetIt setup blocks (registering `EntitiesCacheService`, `EditorStateService`, `JournalDb`, `UpdateNotifications`) across two files. The AGENTS.md rule "one test file per source file" also applies; `event_form.dart` has two test files. The label-related tests could be merged back into `event_form_test.dart` behind a sub-group, or the setup should use `setUpTestGetIt()` where possible.

- [x] **[MED]** `test/widgets/cards/modern_status_chip_test.dart` (line 124 vs 154) — the `dark mode adjusts alpha values correctly` and `light mode adjusts alpha values correctly` tests are near-identical; only `theme: ThemeData.dark()` vs `.light()` and the expected alpha constant differ. Replace with a parameterized loop or helper. **RESOLVED:** done — single loop over `(name, theme, containerAlpha, textAlpha)` records.

- [x] **[MED]** `test/widgets/app_bar/glass_back_button_test.dart` (line 158) — the "calls Navigator.maybePop by default when tapped" test asserts only `findsOneWidget` after the tap. This verifies neither that `maybePop` was called nor any visible state change; the assertion is purely smoke-test level. **RESOLVED:** (stale) the test was already rewritten to push a second route and assert the pop actually returns to the first page.

- [x] **[MED]** `test/widgets/app_bar/journal_filter_icon_test.dart` (line 117) — "is wrapped in Padding with correct spacing" test asserts `findsWidgets` (plural, uncounted) on any `Padding` ancestor of `IconButton`. This is too loose to catch regressions; assert on the specific `EdgeInsets` value. **RESOLVED:** done — asserts `EdgeInsets.only(right: AppTheme.spacingSmall)` on the nearest Padding ancestor (test now lives in `journal_sliver_appbar_test.dart` after the mirror-rule merge below).

- [x] **[MED]** `test/widgets/charts/habits/habit_completion_rate_chart_test.dart` — uses manual GetIt registration (`getIt..registerSingleton<JournalDb>…`) in `setUp` instead of `setUpTestGetIt()` / `tearDownTestGetIt()` from `test/widget_test_utils.dart`. This is the only file in Group A doing raw GetIt manipulation for chart tests. **RESOLVED:** done — rewired onto `setUpTestGetIt(additionalSetup: ...)` / `tearDownTestGetIt`, stubbing the harness-provided `mocks.journalDb`.

- [x] **[MED]** `test/widgets/flags/language_flag_test.dart` — the `tw`→`gh` (Akan/Twi → Ghana) override in `lib/widgets/flags/language_flag.dart:14` has no test case. Add a `test('returns Ghana flag override for tw', ...)` analogous to the `zh` case. **RESOLVED:** added.

- [ ] **[LOW]** `test/widgets/cards/modern_card_content_test.dart` and `modern_icon_container_test.dart` — both have 9 and 5 `isNotNull` assertions respectively. While some are on nullable style properties (valid defensive checks), several (`expect(icon.color, isNotNull)` at line 31 of icon_container_test) do not verify the specific token-aligned value.

---

## Generative (Glados) testing opportunities

Per the README: Glados is for pure functions with structured input. Widget tests are explicitly excluded. Only pure helpers qualify.

- [x] **[MED]** `lib/widgets/app_bar/settings_header_dimensions.dart` — `SettingsHeaderDimensions.collapseProgress(expandedHeight, collapsedHeight, currentHeight)` is a pure, side-effect-free math function with invariants: result is always in `[0, 1]`; monotone-decreasing with `currentHeight`; symmetric under clamping. The current test exercises only 6 hand-picked values. A Glados property test (`result ∈ [0.0, 1.0]` for any triple of doubles) would be more robust. `horizontalPadding(width)` also qualifies: `result >= 20` always, and is non-decreasing with width. **RESOLVED:** done — Glados groups added: `collapseProgress` result always in [0,1] and non-increasing in currentHeight; `horizontalPadding` within [20,160] and non-decreasing with width.

- [x] **[MED]** `lib/widgets/charts/utils.dart` — `aggregateAvgByDay` is the one aggregate function not yet covered by a Glados test (`aggregateSumByDay` has one at line 381 of utils_test.dart, tagged correctly). The same scenario class `_SumByDayScenario` can be reused (or adapted) to add a property: "output length equals number of distinct days in range that have at least one measurement, values equal per-day arithmetic mean". **RESOLVED:** (stale) `aggregateAvgByDay — Glados properties` already exists in utils_test.dart asserting exactly these invariants via `any.avgByDayScenario`.

- [ ] **[LOW]** `lib/widgets/charts/habits/dashboard_habits_data.dart` — `habitResultsByDay` contains deduplication logic (latest-write wins for same day). A Glados test checking "output contains at most one entry per `(habitId, dayString)` pair for any shuffled input list" would guard against regressions more cheaply than maintaining exhaustive example cases.

- [ ] **[LOW]** `lib/widgets/flags/language_flag.dart` — `buildLanguageFlag` is a pure function; however, its input space is just `String languageCode` plus a few constants, and there are no algebraic invariants beyond "returns a `Widget`". Not a strong candidate.

---

## Coverage / missing-behavior gaps

- [x] **[HIGH]** `lib/widgets/app_bar/title_app_bar.dart` (105 lines) — **zero test coverage**. Contains three classes: `TitleAppBar`, `TitleWidgetAppBar`, and `BackWidget`. `BackWidget` uses GetIt to call `NavService.beamBack()` and wraps an animated `fadeIn`. None of the rendering, back-navigation callback, `onPressed` override path, or animation are tested.

- [x] **[HIGH]** `lib/widgets/app_bar/sliver_title_bar.dart` (41 lines) — **zero test coverage**. `SliverTitleBar` renders a `SliverAppBar` with configurable `pinned`, `showBackButton`, and `bottom`. No test exercises even basic rendering.

- [x] **[HIGH]** `lib/widgets/buttons/lotti_primary_button.dart` (83 lines) — **zero test coverage**. `LottiPrimaryButton` has `isDestructive` styling, `icon` rendering, `semanticsLabel`, and a custom `style` merge path. The secondary and tertiary buttons have tests; the primary does not.

- [x] **[HIGH]** `lib/widgets/events/event_status.dart` (24 lines) — **zero test coverage**. `EventStatusWidget` is a simple `Chip` with status-colored background; tests should verify label text, background alpha, and all `EventStatus` variants.

- [x] **[HIGH]** `lib/widgets/charts/dashboard_item_modal.dart` (65 lines) — **zero test coverage**. The aggregation-type selection and `Navigator.pop` trigger are completely untested.

- [x] **[MED]** `test/widgets/charts/` — `dashboard_health_test.dart`, `dashboard_health_data_test.dart`, `dashboard_measurables_test.dart`, `dashboard_workout_test.dart` all test widgets from `lib/features/dashboards/ui/widgets/charts/**`, not from `lib/widgets/charts/`. This violates the "one test file per source file, paths must mirror" rule. These four files should be moved/merged into `test/features/dashboards/ui/widgets/charts/`. **RESOLVED:** done — `dashboard_health_test`/`dashboard_health_data_test` deleted (fully covered by the feature mirrors incl. the health_data Glados suite); `dashboard_workout_test` deleted (valueType matrix covered by `state/workout_data_test.dart`, rendering by the mirror); `dashboard_measurables_test`'s unique bits (hourlySum label, description rendering) merged into the mirrors, then deleted.

- [x] **[MED]** `test/widgets/app_bar/journal_sliver_appbar_test.dart` — tests from `JournalFilter` and `JournalFilterIcon` (separate test files) verify behaviour already partially overlapping this file's `pumpAndSettle`-heavy checks. The `sliver_title_bar_test.dart` gap is the bigger priority. **RESOLVED:** done — `sliver_title_bar_test.dart` exists since the HIGH pass; the deeper issue was three test files for one source (`JournalSliverAppBar`, `JournalFilter`, `JournalFilterIcon` all live in journal_sliver_appbar.dart) — `journal_filter_test.dart` and `journal_filter_icon_test.dart` were merged into `journal_sliver_appbar_test.dart` as groups sharing one `buildBench` helper, with the three per-filter tap tests parameterized.

- [x] **[MED]** `lib/widgets/date_time/datetime_field.dart` line 77 — the `onNow` path calls `DateTime.now()` inside the widget. The test "complete flow: open modal, tap now button" (line 337 of `datetime_field_test.dart`) then asserts `capturedDate isA<DateTime>()` without verifying the value is recent or reasonable. This is effectively a smoke test for the live-clock path. **RESOLVED:** done — the tap is bracketed with wall-clock readings and the captured value asserted inside `[before, after]` (the live clock is the production contract here, so the deterministic-dates rule yields to a bounded sandwich).

- [ ] **[LOW]** `lib/widgets/buttons/lotti_secondary_button.dart` — the `style` parameter and its theming (border, text style) are not tested; only `fullWidth`, `enabled`, and `icon` presence are covered.

---

## Test execution speed opportunities

- [x] **[HIGH]** `test/widgets/date_time/datetime_field_test.dart` — **12× `pumpAndSettle`** across 12 test bodies, including 5 on tests that pump purely static `TextField` or `CupertinoDatePicker` widgets with no ongoing animations. Replace post-pump `pumpAndSettle()` with `tester.pump()` for the non-modal static cases (renders, format checks), keeping `pumpAndSettle` only for modal open/close transitions. Estimated: removes ~7 unnecessary settle cycles per run.

- [x] **[HIGH]** `test/widgets/cards/modern_base_card_test.dart` — **18× `pumpAndSettle`** (one per test, on a static decoration card with no animations). `ModernBaseCard` uses `Container`, not `AnimatedContainer`; `pumpAndSettle` is never needed. Replace all with `await tester.pump()`. Estimated: up to 18× 10s timeout exposure eliminated (each `pumpAndSettle` has a 10s default; in practice they complete fast, but in animation-heavy environments they can stall).

- [x] **[MED]** `test/widgets/cards/modern_card_content_test.dart` and `modern_status_chip_test.dart` — **14×** and **12×** `pumpAndSettle` respectively, on static layout cards. Same pattern as above: replace with `tester.pump()`. **RESOLVED:** done — all 26 replaced.

- [x] **[MED]** `test/widgets/cards/modern_icon_container_test.dart` — **12× `pumpAndSettle`** on a static icon widget. Replace with `tester.pump()`. **RESOLVED:** done — all 12 replaced.

- [x] **[MED]** `test/widgets/charts/habits/habit_completion_rate_chart_test.dart` — **9× `pumpAndSettle`** including several on widget variants that only differ by controller state. The `leftTitleWidgets` and `barData pure function` tests inside this file (lines 280–491) each pump their own `MaterialApp/Scaffold` from scratch rather than using a shared helper. Extracting a `_pumpTitleWidget(tester, value)` helper could batch these. **RESOLVED:** done — `pumpAndSettle` eliminated (the `pumpChart` helper uses two bounded pumps) and `pumpTitleWidget(tester, value)` now serves both leftTitleWidgets loops.

- [x] **[MED]** `test/widgets/app_bar/settings_page_header_test.dart` — **14× `pumpAndSettle`**, many inside nested `pumpWithScale`/`pumpWithWidth` loop bodies. For `SettingsPageHeader` layout-only checks (no animations expected), `tester.pump()` is sufficient. Keep `pumpAndSettle` only for scroll-gesture tests (lines 170–215 where real scroll animation occurs). **RESOLVED:** done — `_pumpHeader` uses bounded pumps (one frame + 1s for the BackWidget flutter_animate fade-in, which otherwise leaves a pending timer); `pumpAndSettle` remains only after drag gestures and the theme-transition assert.

- [ ] **[LOW]** `test/widgets/app_bar/glass_action_button_test.dart` — 1× `pumpAndSettle` after tap (line 40). An `InkWell` ink-splash animation is the only candidate; `tester.pump(const Duration(milliseconds: 50))` would settle it without the 10s default.

- [ ] **[LOW]** `test/widgets/app_bar/glass_back_button_test.dart` — 2× `pumpAndSettle` (lines 153, 171). Same splash-animation concern.

---

## Summary

- **5 untested impl files**: `sliver_title_bar.dart`, `title_app_bar.dart`, `lotti_primary_button.dart`, `event_status.dart`, `dashboard_item_modal.dart` — highest priority coverage gaps.
- **4 misplaced test files** in `test/widgets/charts/` that actually test `features/dashboards` widgets, violating the mirror-path rule (`dashboard_health_test`, `dashboard_health_data_test`, `dashboard_measurables_test`, `dashboard_workout_test`).
- **3 weak-assertion clusters**: glass button tests (structural `findsOneWidget` only for nav path); card tests (18+14+12+12× `pumpAndSettle` on static widgets; `isNotNull` checks that don't verify token-aligned values); `habit_completion_rate_chart_test` (`findsOneWidget` dominating widget render assertions).
- **3 Glados candidates** (pure-function helpers): `SettingsHeaderDimensions.collapseProgress/horizontalPadding`; `aggregateAvgByDay` in `charts/utils.dart`; `habitResultsByDay` in `dashboard_habits_data.dart`.
- **~80+ `pumpAndSettle` calls** across Group A test files; roughly 50 of these are on static widgets (cards group = ~56, date_time = 12, settings_page_header = 14) and can be replaced with `tester.pump()` — eliminating exposure to the 10s `pumpAndSettle` timeout on every animation-free pump.
- **Biggest single opportunity**: add `title_app_bar_test.dart` and `lotti_primary_button_test.dart` — two untested widget files with non-trivial rendering and callback paths, plus `sliver_title_bar_test.dart` and `event_status_test.dart` as quick wins; together they close ~303 uncovered lines.
